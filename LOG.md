## 2026-06-21 Memo Nexus 保存安定化・ZIP修正

### 変更内容

* IndexedDB の putNote/deleteNote を transaction 完了待ちに変更
* flushSave() を追加し、ZIPバックアップ前に遅延保存を強制実行
* pagehide / visibilitychange でiPhone終了時の保存漏れ対策を追加
* ZIPヘッダーに正しいDOS日時を書き込むよう修正
* ZIP内ファイル名の重複を避ける uniqueZipFileNames() を追加
* README.md にiPhone保存安定化とZIPバックアップ改善の仕様を追記

### 修正理由

* iPhoneでは遅延保存や非同期IndexedDB保存が、ページ終了・画面ロック・アプリ切替で完了しない可能性があるため
* ZIPの日付が u16(0), u16(0) になっており、iPhone側で1979/11/30のような不正な日付として表示される可能性があるため
* 同名タイトルのメモがあると、ZIP内で同じ .md ファイル名が複数生成される可能性があるため

### 確認方法

* PCブラウザでメモを作成・編集し、再読み込み後に残ること
* iPhone Safariまたはホーム画面追加版でメモを作成・編集し、アプリを閉じて再起動しても残ること
* ZIPバックアップを作成し、ZIP内ファイルの日付が1979/11/30のような不正日付にならないこと
* 同じタイトルのメモが複数あっても、ZIP内ファイル名が -2, -3 付きで一意になること
* 既存のメモ作成、編集、削除、検索、Markdownプレビュー、Wikiリンクが壊れていないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* IndexedDBモックで putNote/deleteNote がtransaction完了前には解決せず、完了後に解決することを確認
* 同名ファイルが `新規メモ.md`、`新規メモ-2.md`、`新規メモ-3.md` になることを確認
* 指定日時がDOS日時へ変換され、ZIPのローカルヘッダーとセントラルディレクトリヘッダーの両方へ同じ値が書き込まれることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-06-21 iPhone保存対策: 編集中メモのlocalStorageドラフト退避

### 変更内容

* localStorageに全件バックアップする案は採用せず、容量を考慮して現在編集中の1件のみをドラフト退避する実装を追加
* saveCurrentDraftMirror() を追加
* scheduleSave() の先頭でドラフト退避するよう変更
* restoreCurrentDraftMirror() を追加
* init() で ensureStartupNotes() の前にドラフト復元を試すよう変更
* pagehide / visibilitychange 時にもドラフト退避するよう変更
* 古すぎるドラフトを復元しない判定を追加
* 削除したメモがlocalStorageドラフトから復活しないよう、一致するドラフトだけを削除
* 復元時・保存時の診断ログを追加
* README.md に編集中文書1件のドラフト退避・復元仕様を追記

### 修正理由

* iPhone Safari / PWA では、入力直後にアプリを閉じると遅延保存やIndexedDB保存が完了しない可能性があるため
* localStorageは容量が小さいため、全メモではなく現在編集中の1件だけを退避する方針にした
* IndexedDBを本保存として維持しつつ、直前の入力内容だけを救済できるようにするため

### 確認方法

* PCで既存のメモ作成・編集・削除・検索・Markdownプレビュー・Wikiリンク・ZIP出力が壊れていないことを確認
* iPhoneでメモを作成または編集する
* 入力直後にホーム画面へ戻る、Safariを閉じる、またはPWAを終了する
* 再起動後、直前に編集中だったメモが復元されることを確認
* console.log に Draft mirror saved / Draft mirror restored が出ることを確認
* ZIPの日付異常修正が維持されていることを確認

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* localStorageに現在編集中の1件だけが保存され、タイトル・本文・各日時が含まれることを確認
* IndexedDBより新しいドラフト、およびIndexedDBから消失したIDのドラフトが復元されることを確認
* IndexedDB側の方が新しい場合はドラフトを復元しない条件を確認
* 30日以上古いドラフトが復元されず、localStorageから削除されることを確認
* 削除したメモと同じIDのドラフトが削除され、次回起動時に復活しないことを確認
* ZIP内ファイル名の重複回避とZIPヘッダーのDOS日時が維持されていることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* iPhone Safari/PWAでの実機確認は未実施

## v0.3.1 "Draft Mirror" - 2026-06-21

### 変更内容

* アプリバージョン APP_VERSION / APP_LABEL / APP_BUILD を追加
* 画面上に v0.3.1 "Draft Mirror" を表示
* 起動時にバージョン、URL、IndexedDBメモ件数を console.log に出力
* localStorage が利用可能か確認する checkLocalStorageAvailable() を追加
* プライベートブラウズ、別タブグループ、Safari版とホーム画面PWA版の保存領域違いに関する注意表示を追加
* 保存リスクがある場合、storageWarning に警告を表示
* README.md にv0.3.1のバージョン表示と保存領域警告を追記

### 修正理由

* iPhone Safariでは通常ブラウズ、プライベートブラウズ、タブグループ、ホーム画面PWAで保存領域が分かれる場合があるため
* メモが消えたように見えても、別の保存領域を見ているだけの場合があるため
* 今後、ユーザーがプライベートブラウズや別領域で誤って使うことを避けるため
* iPhoneで古いコードや別URLを開いているかを確認しやすくするため

### 確認方法

* PCブラウザでアプリを開き、画面上に v0.3.1 "Draft Mirror" が表示されること
* console.log にバージョン、URL、IndexedDBメモ件数が表示されること
* iPhone Safari通常ブラウズで開き、既存メモが残っていること
* iPhone Safariプライベートブラウズ、または保存が不安定な環境では注意表示が出ること
* ZIPバックアップの日付修正が維持されていること
* 既存のメモ作成、編集、削除、検索、Markdownプレビュー、Wikiリンクが壊れていないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* APP_VERSION / APP_LABEL / APP_BUILD と画面表示用要素が正しく設定されていることを確認
* localStorage試し書きの成功・失敗と、失敗時の警告表示を確認
* IndexedDBメモ件数が0～1件の場合は控えめな保存領域確認を表示し、通常件数では非表示になることを確認
* DB_NAME / STORE_NAME / DB_VERSION が従来値のまま変更されていないことを確認
* ZIPヘッダーのDOS日時処理が維持されていることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* iPhone Safari/PWAでの実機確認は未実施

## 2026-06-21 v0.3.1 バージョン表示修正

### 変更内容

* バージョン表示をメモ一覧の末尾からツールバー直下へ移動
* `style.css` と `app.js` の読み込みURLに `v=0.3.1` を追加
* バージョン表示へ区切り線を追加し、既存UI内で小さく常時確認できるよう調整
* README.md の表示位置説明とキャッシュ対策を更新

### 修正理由

* バージョン表示がメモ一覧の下へ押し出され、特にiPhoneで見えない場合があったため
* Safari/PWAが古いCSSやJavaScriptをキャッシュしている場合でも、v0.3.1のファイルを再取得しやすくするため

### 確認方法

* 画面左側のツールバー直下に `v0.3.1 "Draft Mirror"` が表示されること
* `index.html` が `style.css?v=0.3.1` と `app.js?v=0.3.1` を読み込むこと
* 既存のメモ一覧、検索、保存、ZIP出力が影響を受けていないこと

### 確認結果

* `appVersion` 要素が1件だけ存在し、ツールバー直下に配置されていることを確認
* JavaScript構文と差分の空白エラーがないことを確認

## 2026-06-21 v0.3.1 PC・スマホ別バージョン表示

### 変更内容

* PCではバージョンを関連メモ見出しの直下に表示
* スマホでは従来どおり左側ツールバー直下に表示
* 複数のバージョン表示要素を共通クラスで取得し、同じバージョン文字列を設定
* 更新したCSSとJavaScriptを再取得させるため、読み込みURLの識別子を `v=0.3.1-2` に更新
* README.md の表示位置説明をPC・スマホ別の内容へ更新

### 修正理由

* PCでは左サイドバーの上部が横幅不足で、バージョン表示を視認しにくかったため
* 関連メモの見出し直下であれば、PCの3カラム表示でも自然に確認できるため
* スマホでは関連メモ欄が画面下部へ移動するため、上部のツールバー直下表示を維持するため

### 確認方法

* 画面幅が980pxを超える場合、関連メモの見出し直下に `v0.3.1 "Draft Mirror"` が表示されること
* 画面幅が980px以下の場合、左側ツールバー直下に `v0.3.1 "Draft Mirror"` が表示されること
* PC用とスマホ用の両要素に同じバージョン文字列が設定されること

### 確認結果

* PC用・スマホ用のバージョン表示要素が各1件存在することを確認
* 両方の表示要素に `v0.3.1 "Draft Mirror"` が設定されることを確認
* CSSでPC時は関連メモ側、980px以下ではツールバー側だけが表示されることを確認
* JavaScript構文と差分の空白エラーがないことを確認

## 2026-06-21 保存領域リスク警告の文言改善

### 変更内容

* 「メモ件数が少ない状態です」という開発者向け表現を削除
* プライベートブラウズの可能性と、通常ブラウズ側とは別保存になる場合があることを明記
* 「いつもと同じ開き方で開き直してください」とユーザーが取るべき行動を明記
* 警告文の3行構成が画面上でも維持されるよう表示スタイルを調整
* 更新した警告文とスタイルを再取得させるため、静的ファイル識別子を `v=0.3.1-3` に更新

### 修正理由

* 保存領域リスクの原因と対応方法を、ユーザーが短い文面で理解できるようにするため

### 確認方法

* 保存領域リスクの注意表示が指定された3行の文言になること
* 保存条件やIndexedDB・localStorageの処理が変更されていないこと

### 確認結果

* 警告文以外の判定ロジックと保存処理に差分がないことを確認
* 警告領域で改行が維持される設定を確認

## 2026-06-24 設定UI整理

### 変更内容

* 上部ツールバーに設定ボタンを追加
* 設定モーダルを追加
* 設定モーダルに保存状態、取り込み、バックアップ、表示、データ管理の各セクションを追加
* 保存状態に IndexedDB の保存方式、DB名、ストア名、メモ件数、使用容量、上限目安、使用率、アプリバージョン、現在のURLを表示
* AIニュースファイル取り込みボタンを左カラムから設定モーダル内へ移動
* JSON貼り付け取り込みボタンを左カラムから設定モーダル内へ移動
* Markdown ZIP書き出しボタンを設定モーダル内にも追加
* 表示設定、JSON全体書き出し、全データ削除は Coming soon または未実装表示にした
* 左カラムから知識レベルパネルを撤去
* `renderAll()` と入力時処理から `renderLevel()` の呼び出しを削除

### 修正理由

* 左カラムを検索とメモ一覧中心に整理し、将来のタグ・エクスプローラ表示に使いやすくするため
* AIニュースファイル取り込み、JSON取り込み、保存状態などの管理系機能を設定モーダルへ集約するため
* 知識レベルの表示を、タグ・知識マップ再設計までいったん撤去するため
* IndexedDBの使用容量や保存状態をアプリ内から確認できるようにするため
* 今回はUI整理に範囲を絞り、タグ機能・知識マップ・レトロモードなどの大きな機能追加を避けるため

### 確認方法

* `node --check app.js` でJavaScript構文エラーがないことを確認する
* `git diff --check` で差分の空白エラーがないことを確認する
* `index.html` に設定ボタンと設定モーダルが追加され、左カラムから AI Import、JSON取り込み、知識レベルが消えていることを確認する
* `app.js` で既存の `importAiNewsFile()`、`openJsonImportDialog()`、`downloadMarkdownZip()` が設定モーダル内ボタンから呼び出されることを確認する
* `DB_NAME`、`STORE_NAME`、`DB_VERSION` が変更されていないことを確認する
* Edgeでアプリを開き、設定ボタンから設定モーダルが開くことを確認する

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `git diff --check` で差分の空白エラーがないことを確認
* `index.html` 上で設定ボタン、設定モーダル、取り込みボタン、バックアップボタンが追加されていることを確認
* `index.html` 上で左カラムの AI Import、JSON取り込み、知識レベル要素が撤去されていることを確認
* `app.js` 上で `navigator.storage.estimate()` を使った保存容量表示処理が追加されていることを確認
* `app.js` 上で既存の `importAiNewsFile()`、`openJsonImportDialog()`、`downloadMarkdownZip()` が再利用されていることを確認
* `DB_NAME`、`STORE_NAME`、`DB_VERSION` が従来値のまま変更されていないことを確認
* Edgeでの実ブラウザ確認は未実施
* 設定モーダル内からのAIニュースファイル取り込み、JSON取り込み、Markdown ZIP書き出しの実操作確認は未実施
* 既存のメモ保存、検索、語句統計、Graph、関連メモの実操作確認は未実施

## 2026-06-24 ダークモード追加

### 変更内容

* 設定モーダルの表示セクションにテーマ切替を追加
* ライト / ダークを切り替えられるように変更
* body に dark クラスを付け外しする方式でダークモードを実装
* CSS変数で背景色、文字色、パネル色、枠線色などを切り替えるよう調整
* 選択したテーマを localStorage の memo-nexus-theme に保存
* 起動時に保存済みテーマを復元
* 更新したCSS/JavaScriptを読み込ませるため、静的ファイル識別子を `v=0.3.1-5` に更新

### 修正理由

* 夜間や暗い環境でメモを読み書きしやすくするため
* 表示設定の中で最初に実用性が高いテーマ切替を先行して実装するため
* JavaScriptで各要素の色を直接変更せず、CSS側で管理できるようにするため

### 確認方法

* 設定モーダルを開き、テーマをライト / ダークで切り替えられること
* ダークを選ぶと画面全体が暗色テーマになること
* ライトを選ぶと元の明色テーマに戻ること
* 再読み込み後も選択したテーマが維持されること
* 既存のメモ保存、検索、ZIP出力、JSON取り込み、語句統計、Graph、関連メモが壊れていないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* テーマ切替UIが設定モーダルに表示されることを確認
* localStorage に memo-nexus-theme が保存されることを確認
* 再読み込み後に保存済みテーマが復元されることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-06-25 JSON貼り付け取り込みの1メモ化

### 変更内容

* JSON貼り付け取り込みで items ごとの個別メモを作成しないように変更
* JSON貼り付け取り込みで createNote() を1回だけ呼び、1件のメモにまとめるように変更
* items の各ニュースを、作成した1件のメモ本文内に見出し付きで展開するように変更
* heading / title / headline / 見出し を見出し候補として扱うように変更
* date / publishedAt / day / 日付、trend_summary / trendSummary / summary / overview / 全体傾向 の揺れを扱えるように変更
* period、era、importance、urgency、tags、sourceUrl などの項目を本文へ反映できるように変更
* 取り込み成功メッセージを1件作成に合う文言へ変更
* 更新したJavaScriptを読み込ませるため、静的ファイル識別子を `v=0.3.1-6` に更新

### 修正理由

* ニュースJSONは日付ごとのまとまりとして1メモで保存した方が読み返しやすいため
* items ごとに個別メモを作成すると、ニュース取り込み時にメモが増えすぎるため
* 複数メモへの分割取り込みは、将来の外部メモ取り込み機能として別途設計するため
* heading 形式のJSONで title 空エラーが出ないようにするため

### 確認方法

* JSON貼り付け取り込みダイアログに、items が複数あるJSONを貼り付ける
* 取り込み後に作成されたメモが1件だけであることを確認する
* 作成されたメモ本文に、items の各ニュースが見出し付きで展開されていることを確認する
* heading しかないitemでもエラーにならないことを確認する
* period、importance、tags が本文に表示されることを確認する
* 既存のAIニュースファイル取り込みが従来どおり動くことを確認する
* 新規メモ作成、今日メモ、検索、自動保存、語句統計、Graph、関連メモが壊れていないことを確認する

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `importPastedItNewsJson()` 内の `createNote()` 呼び出しが1回だけになっていることを確認
* items が複数あるJSONから作るメモデータが1件分だけであることをNode上の関数検証で確認
* heading 形式のJSONで `items[0] の title が空です。` が出ないことをNode上の関数検証で確認
* 作成される本文に複数ニュースの見出しが展開されることをNode上の関数検証で確認
* period、importance、tags が本文に反映されることをNode上の関数検証で確認
* 既存のAIニュースファイル取り込み、新規メモ作成、今日メモ、検索、自動保存、語句統計、Graph、関連メモの実操作確認は未実施
* `git diff --check` で差分の空白エラーがないことを確認
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-06-25 JSON貼り付け取り込みのAI朝刊体裁合わせ

### 変更内容

* JSON貼り付け取り込みで作成するメモタイトルを `タイトル 日付` の形式に変更
* JSON貼り付け取り込みの本文をAIニュース取り込みに近い体裁へ変更
* items の見出しを `1. 見出し` の形式で出すように変更
* カテゴリ、時代、重要度、緊急度、タグ、要約、重要性、情報源を箇条書き中心で表示するように変更
* 全体傾向をAIニュース取り込みと同じく個別ニュースの後ろに表示するように変更
* 更新したJavaScriptを読み込ませるため、静的ファイル識別子を `v=0.3.1-7` に更新
* READMEのJSON貼り付け取り込み説明を、1メモ化とAI朝刊体裁に合わせて更新

### 修正理由

* JSON貼り付け取り込みのメモが、AIニュースファイル取り込みで作成したメモと見比べやすくなるようにするため
* メモ一覧やタイトル欄で日付がすぐ分かるようにするため

### 確認方法

* JSON貼り付け取り込みで作るメモタイトルに日付が含まれることを確認する
* 作成される本文が `# タイトル 日付`、`日付: ...`、`1. 見出し`、箇条書き、`## 全体傾向` の並びになることを確認する
* JSON貼り付け取り込みが引き続き1メモだけ作成することを確認する
* `node --check app.js` でJavaScript構文エラーがないことを確認する
* `git diff --check` で差分の空白エラーがないことを確認する

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* Node上の関数検証で、メモタイトルが `AI朝刊 2026-05-31` 形式になることを確認
* Node上の関数検証で、本文がAIニュース取り込みに近い `1. 見出し` と箇条書き形式になることを確認
* Node上の関数検証で、全体傾向が個別ニュースの後ろに表示されることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* ブラウザ上でのJSON貼り付け取り込み実操作確認は未実施
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-07-01 プレビューのfenced code block / Mermaid対応

### 変更内容

* 右側プレビューで Markdown風の fenced code block を扱う renderPreviewHtml() を追加
* 本文を通常テキストとコードブロックへ分割する splitFencedBlocks() を追加
* 通常テキスト用の renderTextBlock() を追加し、既存の renderRichText() を通常文章用として維持
* コード表示用の renderCodeBlock() を追加し、```js / ```html / ```css / ```json などを `<pre><code>` で表示
* Mermaid表示用の renderMermaidBlock() / renderMermaidDiagrams() を追加
* renderPreview() から renderPreviewHtml(body) を呼ぶ構造に整理
* Mermaid.js を index.html で読み込むよう追加
* コードブロックと Mermaid 図用の最小スタイルを style.css に追加

### 修正理由

* メモ本文にコードや Mermaid 図をそのまま書き、右側プレビューで読みやすく確認できるようにするため
* コードブロック内の `[[...]]` が知識リンクとして誤ってボタン化されないようにするため
* IndexedDB の保存形式や saveCurrentNote() を変更せず、表示処理だけで対応するため

### 確認方法

* ```js の中身がコードブロックとして表示されること
* ```mermaid の中身が Mermaid.js により図として表示されること
* Mermaid がないメモでも従来どおりプレビュー表示されること
* 普通の文章中の `[[リンク]]` が従来どおりクリックできること
* コードブロック内の `[[リンク]]` がクリックボタン化されないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `saveCurrentNote()` と IndexedDB 保存形式に差分がないことを確認
* `git diff -- app.js index.html style.css` で変更内容を確認
* ブラウザ上での Mermaid 描画実機確認は未実施

## 2026-07-01 スマホ向けUndoボタン追加

### 追加修正

* `index.html` の `style.css` / `app.js` リンクに `v=0.3.1-8` を追加し、古いキャッシュ読み込みを防止
* `#undoBtn` と `.undo-button` に PC でも表示されるよう `display: inline-flex` を明示
* `:disabled` 状態でも非表示にせず `opacity: 0.48` で薄く表示するよう調整
* `app.js` では既存の `Ctrl+Z` を奪う処理は存在せず、ブラウザ標準動作を妨げないことを確認

### 確認内容

* PC 幅・スマホ幅の両方で Undo ボタンが表示されること
* Undo 履歴なしではボタンが薄く表示されること
* Undo 履歴ありではボタンが押せること
* PC の `Ctrl+Z` が標準ブラウザ Undo を妨げないこと

### 実装した内容

* `index.html` の上部ツールバーに `undoBtn` を追加
* `style.css` でツールバーボタンの最小高さを調整し、スマホでもUndoを押しやすくした
* `app.js` に `undoStack`、`UNDO_LIMIT`、`lastUndoSnapshotAt` を追加
* `titleInput` と `editor` の `beforeinput` で変更前のタイトル・本文をUndo履歴へ保存
* 削除、切り取り、貼り付け、ドロップは間隔に関係なくUndo履歴を保存
* 通常入力は800ms間隔で履歴を保存し、連続する同一内容は保存しないようにした
* Undoボタンで現在メモIDに対応する直近履歴を復元し、保存予約・プレビュー・関連メモ・保存状態を更新

### 変更したファイル

* `index.html`
* `style.css`
* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* Undo履歴が最大50件に制限される実装であることを確認
* `keydown` で Ctrl+Z を奪っていないことを確認
* IndexedDBの保存形式と既存メモデータ構造を変更していないことを確認

### 未対応事項

* Redoは未実装
* ブラウザ上でのスマホ実機操作確認は未実施
* Excelのような完全な操作履歴は未実装

## 2026-07-01 新規メモの仮タイトル自動付与

### 実装した内容

* `createNote()` に `options` を追加し、必要な場合だけ重複回避を無効にできるようにした
* タイトルが空の場合、現在読み込んでいる `notes.length + 1` から `memo301` のような仮タイトルを作る `temporaryMemoTitle()` を追加
* 新規メモボタンでは空タイトルで `createNote()` を呼び、`uniqueTitle()` による重複回避を無効にした
* 今日メモ、AI取り込み、JSON取り込み、`[[リンク]]` からの新規作成では従来どおり重複回避を維持

### 変更したファイル

* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `git diff --check -- app.js` で差分の空白エラーがないことを確認
* `uniqueTitle()` を削除していないことを確認
* 新規メモボタン以外の `createNote()` 呼び出しは既存の重複回避を使うことを確認

### 未対応事項

* ブラウザ上での新規メモ作成実操作確認は未実施

## 2026-07-01 Markdown風プレビュー強化・削除Undo追加

### 今回変更した概要

* `renderTextBlock()` を行単位のMarkdown風表示へ拡張
* `#` / `##` / `###` 見出し、`**太字**`、インラインコード、`-` 箇条書き、`>` 引用をプレビュー表示に追加
* 通常文章中の `[[リンク]]` は従来どおりクリック可能なボタンとして維持
* fenced code block と Mermaid block 内では `[[リンク]]` をボタン化しない既存挙動を維持
* Mermaid描画失敗時に、該当ブロックへ「Mermaid構文エラー」と元コードを表示するよう改善
* メモ削除直前の直近1件を `deletedNoteSnapshot` として保持し、削除後に「元に戻す」から復元できる削除Undoを追加
* 静的ファイルの読み込み識別子を `v=0.3.1-9` に更新

### 変更したファイル

* `app.js`
* `index.html`
* `style.css`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* Mermaid CDN 読み込みが既存の1件だけで重複していないことを確認
* IndexedDB保存形式、本文データ形式、本文編集Undoの構造を変更していないことを確認
* `keydown` で Ctrl+Z を奪う処理を追加していないことを確認

### 未対応・今後の検討事項

* ブラウザ上でのMarkdown風表示、Mermaid構文エラー表示、削除Undoの実操作確認は未実施
* 本格的なMarkdownパーサー、シンタックスハイライト、Redo、削除Undoの永続化は未実装

## 2026-07-01 削除Undo通知の表示位置調整

### 変更内容

* 削除Undo通知をタイトル欄右側から左カラムの検索欄直前へ移動
* 通知用要素を `deleteUndoNotice` とし、通知がないときは `hidden` で高さを取らないようにした
* 通知表示時だけ検索欄とメモ一覧が下に押し下げられるよう、コンパクトな通知スタイルを追加
* 通知内に「元に戻す」と閉じる `×` ボタンを表示
* 既存の削除Undo復元処理は維持し、本文編集Undoとは分離したままにした

### 変更したファイル

* `index.html`
* `style.css`
* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* 旧 `deleteUndoMessage` / `.delete-undo-message` 参照が残っていないことを確認
* 通知が `searchInput` の直前に配置されていることを確認

### 未対応・今後の検討事項

* ブラウザ上での削除、復元、閉じる操作の実操作確認は未実施

## 2026-07-03 JSON貼り付け取り込みのLangBench Live対応

### 変更内容

* JSON貼り付け取り込みの入口でJSON形式判定を追加
* `items` 配列を持つ既存ニュースJSONは、従来のニュース取り込み処理へ渡す形を維持
* `type: "langbench_result"` と `samples` 配列を持つLangBench Live結果JSONに対応
* LangBench結果JSONを1件のMarkdownメモに変換する処理を追加
* LangBench結果本文に `[[LangBench Live]]`、実験名、言語名のWikiリンク、各sample/run/summaryの測定値、注意書きを含めるようにした

### 変更したファイル

* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* ニュースJSONは `items` 配列判定後に既存の `buildItNewsNotes()` へ渡す構造を確認
* Python / JavaScript のLangBench結果JSONを想定し、言語表示が `Python` / `JavaScript` になることを確認
* 不正なJSON構文と未知形式JSONで、それぞれ分かりやすいエラーメッセージを出す分岐を確認

### 未対応・今後の検討事項

* ブラウザ上での貼り付け取り込み実操作確認は未実施

## 2026-07-04 LangBench Live新結果JSON対応

### 変更内容

* LangBench結果メモのタイトルに `runner_label` を含めるよう拡張
* 本文冒頭のWikiリンクに `runner_label` を追加し、Wikiリンク内の `/` はスペースへ置換するようにした
* LangBench結果本文に `type`、実行条件、ランタイム、実行環境セクションを追加
* `memory_total_bytes` は bytes と概算MB/GBを併記するようにした
* sample表示で `input` / `input_file` / `input_file_size_bytes` / sample直下の `line_count` / `average_ms` / `median_ms` に対応
* run表示で `run.line_count` と `run.metrics.line_count` の両方に対応
* 注意文に、コード、ランタイム、実行条件、PC環境、入力CSV条件へ依存する旨を追記

### 変更したファイル

* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* ニュースJSONは従来どおり `items` 配列判定後に既存処理へ渡す構造のまま変更していないことを確認
* LangBench結果JSON判定は `payload.type === "langbench_result"` と `payload.samples` 配列の条件を維持していることを確認

### 未対応・今後の検討事項

* ブラウザ上での `javascript_result.json` / `python_result.json` 貼り付け取り込み実操作確認は未実施
* 既存ニュースJSONのブラウザ上での貼り付け取り込み実操作確認は未実施

## 2026-07-07 LangBench Result JSON取り込みのresults展開対応

### 変更内容

* LangBench Result JSON の取り込み対応を更新
* `type === "langbench_result"` による判定へ変更
* Markdown形式でのメモ生成を追加
* summary の表示を追加
* results の各 iteration を Markdown 表として表示
* results が多い場合は先頭100件と末尾100件に省略
* results から簡易 Observations を生成
* checksum mismatch 件数の表示を追加
* Raw JSON をコードブロックとして保持
* 既存JSON取り込み処理を壊さないよう分岐追加

### 確認した動作

* `type === "langbench_result"` の場合だけ LangBench Result として扱う構造を確認
* ニュースJSONは従来どおり `items` 配列判定後に既存処理へ渡す構造を維持
* results がない場合でも本文に「results がありません」と表示する構造を確認

### 未対応・今後の検討事項

* ブラウザ上での貼り付け取り込み実操作確認は未実施
* ファイル取り込みでの LangBench JSON 実操作確認は未実施

## 2026-07-12 コードブロックのシンタックスハイライト対応

### highlight.jsの導入方法

* `package.json` のない静的構成と、既存MermaidのCDN読み込み方式に合わせ、jsDelivrの `@highlightjs/cdn-assets` から highlight.js 11.11.1 のブラウザ用ビルドを読み込むようにした
* 対象言語をまとめて利用できるブラウザ用ビルドを採用し、新しいnpm構成やビルド処理は追加していない

### 言語指定あり・なしの処理

* 言語指定ありは `js`、`ts`、`html`、`py`、`sh`、`shell`、`ps`、`md`、`yml` をhighlight.jsの言語名へ変換し、対応言語だけ明示指定で色付けする
* 言語指定なしは `highlightAuto()` を利用し、言語が取得でき、relevanceが2以上の場合だけ色付けする
* Mermaidコードブロックは従来どおり図の専用処理へ渡し、highlight.jsの対象外とした
* IndexedDBやMarkdown本文には判定結果を書き戻さず、プレビューDOMだけを変更する

### 自動判定とフォールバック

* 明示言語がhighlight.jsで未対応の場合は自動判定せず、元のコードをそのまま表示する
* 空コード、自動判定の言語なし、低relevance、ライブラリ未読込、処理例外ではプレーンテキスト表示を維持する
* 1件のハイライト失敗でメモ全体のプレビューを停止しない

### CSSテーマの変更

* Memo NexusのCSS変数へ控えめなコード色を追加し、ライトモードとダークモードで色を切り替えるようにした
* 既存の等幅フォント、改行・空白保持、横スクロール、角丸レイアウトを維持した
* 明示指定されたコードブロックだけ、上部に入力された言語名を表示する

### セキュリティ上の確認事項

* highlight.jsへ渡す前のコードは従来どおり `escapeHtml()` してDOMへ配置し、DOMからは `textContent` で元コードを取得する
* highlight.jsはコードを解析して装飾用spanを返すだけで、コード内のHTML、script、イベント属性、JavaScriptを実行しない
* 例外時は `textContent` へ戻すため、危険なHTMLとして挿入しない
* ブラウザ標準コピーでは装飾spanではなく表示テキストがコピーされ、元のプレーンテキストコードを維持する

### 動作確認したコード

* JavaScript、Python、HTML、CSS、JSONの明示指定
* 言語指定なしのJavaScript風コード
* コメント、文字列、数値を含むJavaScript
* `<script>alert("test");</script>` を含むHTMLコード
* 無効な言語名を持つコード
* Mermaidコードブロックの既存分岐

### 未対応・今後の候補

* コードブロック専用のコピーボタンは追加していない（ブラウザ標準コピーはプレーンテキストで利用可能）
* CDNが利用できないオフライン環境では安全なプレーンテキスト表示へフォールバックする
* 実ブラウザでの端末別の色味調整や、自動判定の閾値調整は今後の候補

## 2026-07-13 コレクション・エクスプローラ

### 実装した機能

* 初期コレクション「歴史」「プログラミング」「IT技術」「技術史」「雑学」「未分類」と、最下部の「ゴミ箱」を追加
* 最大5階層のツリー、子孫を含む通常メモ件数、直下メモのタイトルと「作成 YYYY/MM/DD」表示を追加
* コレクションの作成、インライン名前変更、親変更、同一階層の並び替え、子を含む安全な削除を追加
* 自分自身・子孫への移動、5階層を超える移動、未分類・ゴミ箱配下への移動を拒否
* 選択中コレクションへの新規メモ作成、メニューとドラッグによるメモ移動を追加
* Ctrlクリック、Shiftクリック、Ctrl+A、選択バーからの複数メモ移動・削除を追加
* PCのドラッグ＆ドロップで、行の上端25%を前、下端25%を後、中央50%を子への移動として判定
* メモを論理削除するゴミ箱、元コレクションへの同一ID復元、完全削除を追加
* 全件ZIPとコレクション単位ZIPへコレクション階層、禁止文字置換、同名連番を追加
* 1201px以上は左サイドパネル、1200px以下はオーバーレイドロワーとして表示し、外側クリック・閉じるボタン・Escapeへ対応
* ライト・ダークテーマの既存CSS変数を再利用し、選択・ドロップ先を背景、枠、下線で表示

### 変更ファイル

* `app.js`: データ構造、migration、CRUD、ツリー、移動、複数選択、ゴミ箱、ZIP処理
* `index.html`: 開閉ボタン、エクスプローラ、操作メニュー、選択バー、移動ダイアログ
* `style.css`: サイドパネル／ドロワー、ツリー、選択・ドラッグ状態、テーマ対応
* `LOG.md`: 本項目

### IndexedDBと既存メモの移行

* DB名 `memo-nexus` と既存 `notes` object storeは維持し、DB versionを1から2へ更新
* `collections` object store（keyPath `id`）と `parentId` / `sortOrder` indexだけを追加し、既存storeを削除・再作成していない
* 起動時に `collectionId` がない、存在しない、または `deletedAt` が未定義の既存メモだけをトランザクションで補完
* 既存メモのID、タイトル、本文、日時は変更せず、`collectionId: "system-unclassified"` と `deletedAt: null` を追加

### データ構造とゴミ箱

* Collectionは `{id,name,parentId,sortOrder,isSystem,createdAt,updatedAt}` を保存
* Memoは既存構造へ `{collectionId,deletedAt}` を追加し、名前変更時にメモ側を書き換えないID参照とした
* ゴミ箱移動は元の `collectionId` を保持して `deletedAt` のみ設定し、復元時に元コレクションがなければ未分類へ戻す
* 完全削除時だけ `notes` レコードを削除し、新規IDは従来どおり `crypto.randomUUID()` で生成

### エクスポート仕様

* 全件エクスポートはゴミ箱を除外し、各メモをコレクション階層内のMarkdownとして出力
* コレクション単位では選択コレクションと全子孫を対象とし、`Memo-Nexus_コレクション名_YYYY-MM-DD.zip` を生成
* 既存実装に添付ファイル保存機構がないため、今回もMarkdownのみを対象とした

### 確認結果

* `node --check app.js` と `git diff --check` が成功
* 実ブラウザでversion 1の既存DBを開き、既存3件が保持され未分類へ表示されることを確認
* 初期6コレクションとゴミ箱、件数、作成日、インライン作成・名前変更を確認
* 選択中の新規コレクションへ新規メモが入り、件数が1になることを確認
* メモの「コレクションへ移動」ダイアログから未分類へ移動し、完了通知が出ることを確認
* 起動時と上記操作時にブラウザコンソールのerror / warningがないことを確認
* 既存の編集、Markdownプレビュー、Mermaid、highlight.js、関連メモが同じ画面で描画されることを確認

### 未実施・既知の制約

* HTML5ドラッグ＆ドロップ、Ctrl/Shift複数選択、ゴミ箱復元、完全削除、ZIPダウンロードの端末別実操作はコード経路と構文のみ確認し、全組み合わせの手動操作は未実施
* 閉じたコレクションへのドラッグ中の自動展開は、初期実装の安定性を優先して未実装
* スマートフォンの複数選択・複数ドラッグは未実装。単体メニュー移動は利用可能
* 空のコレクションはZIP内に空フォルダとしては出力されない

## 2026-07-13 v0.4.0 "Waypoint" README更新

### 変更内容

* アプリのバージョン名を `Collection Explorer` から `Waypoint` へ変更
* バージョン番号は `v0.4.0` を維持
* README冒頭へ現在バージョンとMemo Nexusの概要を追加
* コレクションの基本操作、最大階層、複数選択、ゴミ箱、階層ZIP、レスポンシブ表示をREADMEへ記載
* 静的ファイル識別子を `v0.4.0-2` へ更新

### 変更ファイル

* `app.js`
* `index.html`
* `README.md`
* `LOG.md`

## 2026-07-14 コレクション内メモの作成日順並べ替え

### 変更内容

* コレクション・エクスプローラ上部に「作成日：新しい順／古い順」の小型選択UIを追加
* 初期値を「新しい順」とし、コレクション直下のメモだけを `createdAt` で並べ替えるようにした
* 表示用コピーを作成して並べ替え、元のメモ配列やIndexedDB内の保存順を変更しない構造にした
* 作成日時が同じメモは既存のメモIDを第2比較条件として、再描画時の順序を安定させた
* 作成日がない、空、または不正なメモは、並べ替え方向にかかわらず末尾へ表示するようにした
* 選択した並び順をアプリ全体共通のUI設定として `localStorage` の `memo-nexus-collection-sort` に保存
* 保存値が未設定または不正な場合は「新しい順」へ戻すようにした
* 未分類は既存のコレクション描画処理を共有しているため同じ並べ替えを適用し、ゴミ箱は既存順を維持
* 全メモ一覧、検索結果、関連メモ、エクスポートなど、コレクション内メモ一覧以外の処理は変更していない
* 更新したCSSとJavaScriptを再取得させるため、静的ファイル識別子を `v=0.4.0-3` へ更新

### 変更ファイル

* `app.js`: 設定の復元・保存、作成日の比較、コレクション直下メモの表示用並べ替え
* `index.html`: 並べ替え選択UI、静的ファイル識別子
* `style.css`: 選択UIのライト・ダークテーマおよび狭い画面向け表示
* `LOG.md`: 本項目

### 確認結果

* 初期状態で作成日の新しいメモが上に表示されることを実ブラウザで確認
* 「古い順」へ変更すると表示順が反転することを実ブラウザで確認
* 再読み込み後も選択した「古い順」が維持されることを確認し、検証後は「新しい順」へ戻した
* 合成データで非破壊ソート、作成日欠損・不正値の末尾配置、同一作成日時のIDによる安定化を確認
* PC表示と狭い画面で選択UIが収まり、狭い画面で横方向のオーバーフローが発生しないことを確認
* ダークモードで選択UIの背景、文字、枠が既存テーマ色を使用し、読めることを確認
* ブラウザコンソールにerrorがないことを確認
* `node --check app.js` と `git diff --check` が成功

### 未実施・既知の制約

* 新規メモ作成、編集、削除、コレクション移動を伴う保存データ変更操作は、既存データを変更しないため実ブラウザでは未実施
* 実データへ作成日欠損メモを追加する確認は行わず、合成データによる比較関数の確認に留めた

## 2026-07-15 メモ単体・コレクションローカルエクスポート

### 実装内容

* 編集画面の三点ボタンから、現在のメモを元のMarkdown本文のまま `.md` ダウンロードできる機能を追加
* File System Access API対応環境では、メモ単体を選択したローカルフォルダへ直接保存できる機能を追加
* コレクションの既存エクスポートメニューへ、従来のZIPダウンロードとローカルフォルダ書き出しを選ぶダイアログを追加
* 選択コレクションをルートとし、子孫コレクションを実フォルダ、通常メモをMarkdownファイルとして書き出す処理を追加
* 空の子孫コレクションもローカルフォルダとして作成し、ゴミ箱内のメモは対象外とした
* 保存先に同名項目が存在する場合はルートフォルダを作成せず、ダイアログ内でローカル保存名を変更できるようにした
* 書き込み権限の確認、フォルダ選択キャンセル、部分的なファイル／フォルダ作成失敗の結果表示を追加
* File System Access API非対応環境ではローカル保存ボタンを非表示にし、通常ダウンロードを維持
* 既存の全メモZIPは従来どおりZIPダウンロードのみとし、コレクションZIPの生成処理も維持

### ファイル名とデータの扱い

* Windows禁止文字、制御文字、末尾のピリオド／空白、`CON`、`PRN`、`AUX`、`NUL`、`COM1`〜`COM9`、`LPT1`〜`LPT9`を安全化
* 空タイトルのメモは `無題のメモ.md`、空のローカル保存名は入力エラーとした
* 同一フォルダ内で同名または大文字・小文字だけが異なるメモは `メモ.md`、`メモ (2).md`、`メモ (3).md` の形式で上書きを回避
* 異なるフォルダ内の同名メモは連番を付けず、それぞれのパスへ出力
* 同じ `memo_id` のデータが異なるコレクションパスに現れた場合も重複排除せず、各パスへ独立したファイルとして出力
* 元のMarkdown本文へ見出しやYAMLフロントマターを追加せず、`memo_id`、所属情報、IndexedDBレコードを変更しない
* 未保存の実編集がある場合だけ既存保存処理を完了し、エクスポートだけでは更新日時を変更しないよう `flushSave()` の不要な再保存を抑止

### 仕様上の判断

* ローカル書き出しは同期や差分更新ではなく、実行時点のスナップショット
* 既存の同名ルートフォルダへの上書き、統合、自動連番、削除、差分更新は行わない
* ローカル保存名の変更は書き出し先だけに適用し、Memo Nexus内のコレクション名は変更しない
* メモ単体を既存ファイルと同名で保存する場合は、既存ファイルを上書きせず同一フォルダ内で括弧付き連番を付ける
* 保存途中で失敗しても、書き込み済みファイルは削除せず、成功件数と失敗パスを表示する

### 将来ヘルプへ記載する内容

* ローカル保存は同期ではなく、ローカルで編集してもMemo Nexusへ反映されない
* 同じメモが複数の保存先パスに現れる場合、複数の独立したファイルとして書き出される
* 全メモ一括はZIPダウンロードのみ
* コレクションはZIPダウンロードとローカルフォルダ書き出しに対応する
* メモ単体は通常ダウンロードと、対応環境でのローカル保存に対応する
* File System Access API非対応環境では通常ダウンロードを利用する
* 同名のコレクションフォルダが保存先にある場合は上書きせず書き込みを中止する
* ローカル保存名を変更してもMemo Nexus内のコレクション名は変わらない

### テスト結果

* `node --check app.js` が成功
* `node --check export-utils.js` が成功
* `node --test --test-isolation=none export-utils.test.js` が6件成功
* 自動テストでWindows向け名前安全化、予約名、大小文字を含む同一フォルダ内連番、異なるフォルダの同名許容、階層パス、ゴミ箱除外、同一IDの別パス出力、同名ルート衝突、File System Access API機能判定を確認
* `git diff --check` が成功
* 実ブラウザでメモ単体ダイアログ、コレクションダイアログ、保存方法の表示、ローカル保存名だけを編集するUI、空白名の拒否を確認
* メモ単体のダウンロード操作がエラーなく完了してダイアログが閉じることを確認。ただし自動操作環境では生成ファイルの実体確認は未実施
* 狭い表示幅でページとエクスポートダイアログが横方向へあふれず、ブラウザコンソールにerror / warningがないことを確認

### 未実施・既知の制約

* ネイティブのフォルダ選択画面を伴うローカル実ファイル書き込み、権限拒否、途中の権限喪失、実ファイルシステム上の同名ルート衝突は自動操作環境では未実施
* File System Access API非対応時の判定は自動テストとコード経路で確認し、Safari／iPhone実機確認は未実施
* 全メモZIPとコレクションZIPの端末別ダウンロード、通常の編集・削除・移動操作の回帰確認は構文、差分、既存コード経路の確認に留めた
* 全メモのローカルフォルダ書き出し、自動／差分／双方向同期、ローカル編集の再取り込み、既存フォルダ統合、ヘルプ画面は実装していない

## 2026-07-15 画像・PDF添付機能

### ブランチ

* `feature/image-pdf-attachments`

### 実装概要

* 画像とPDFをメモ本文から分離し、メモカード下部の添付エリアで追加、閲覧、削除できる機能を追加
* JPEG/JPG、PNG、WebP、PDFだけをMIMEタイプと拡張子の両方で許可し、SVGや偽装された未対応形式を拒否
* ファイル選択、複数選択、PCのドラッグ＆ドロップ、画像を含むクリップボード貼り付けに対応し、通常のテキスト貼り付けは既存動作を維持
* 複数ファイルは全件の検証・画像処理・容量計算が成功してから1トランザクションで保存し、途中失敗や上限超過時の一部保存を回避

### IndexedDBの変更

* DB名 `memo-nexus` と既存の `notes` / `collections` object storeを維持し、DB versionを2から3へ更新
* `attachments` object store（keyPath `id`）と `memoId` / `createdAt` indexを追加し、既存storeは削除・再作成していない
* 添付レコードは安定したUUIDで識別し、`memoId`、`kind`、`fileName`、`mimeType`、`size`、`blob`、画像寸法、作成日時を保存
* ゴミ箱移動と復元では添付を保持し、完全削除時だけメモと関連添付を同じIndexedDBトランザクションで削除

### 画像圧縮とPDF

* 画像は追加時にCanvasで縦横比を維持して長辺最大1800pxへ縮小
* JPEG/WebPは品質0.86、PNGはPNG形式と透明度を維持し、縮小不要で変換後の方が大きい場合は元画像Blobを採用
* `createImageBitmap`では画像向きを考慮し、利用できない環境ではImage要素へフォールバック。圧縮・デコード失敗時は保存しない
* PDFはMIMEタイプ、拡張子、先頭付近の `%PDF-` シグネチャを確認し、再圧縮せず元Blobを保存
* PDF閲覧時はユーザー操作内でObject URLを作りブラウザ標準ビューアを開く。同一PDFの古いURLを解放し、未使用URLはタイマーまたはページ終了時に解放

### 容量上限とUI

* 1メモあたりの添付合計上限を `20 * 1024 * 1024` bytesとして定数管理
* 画像は圧縮後、PDFは元データの容量を既存添付と合算し、20MBちょうどを許可して超過時は現在量、追加量、合計、上限、超過量を表示
* 添付エリアに件数、使用容量、20MB上限、追加ボタン、ドロップ領域、画像サムネイル、PDFカード、ファイル名、容量、開く、削除を追加
* 長いファイル名は省略しつつtitle属性で全文を確認可能とし、ボタンのラベル、フォーカス表示、画像モーダルのEsc終了、スマートフォン幅の折り返しに対応
* 添付削除時は確認後にIndexedDBレコードとObject URLを削除し、一覧、件数、使用容量を再計算

### エクスポート変更

* 添付付きメモは `メモ名/メモ名.md` と `メモ名/attachments/ファイル` の構成で出力し、添付なしメモの従来パスと本文を維持
* メモ単体の通常ダウンロードは添付なしなら従来のMarkdown、添付ありならZIPとし、ローカル保存も同じ構成へ対応
* コレクションZIP、全件ZIP、コレクションのローカルフォルダ書き出しへ画像・PDF Blobを含めるように変更
* 出力Markdownにだけ画像・PDFの相対参照を追加し、アプリ内の元Markdown本文は変更しない
* Windows向けのファイル名安全化を維持し、同名添付には `_2`、`_3` の連番を付けてMarkdown参照と実ファイル名を一致

### テスト内容と結果

* `node --check app.js`、`node --check export-utils.js`、`node --check attachment-utils.js` が成功
* `node --test --test-isolation=none export-utils.test.js attachment-utils.test.js` が12件成功
* JPEG/PNG/WebP/PDFの形式判定、MIME・拡張子不一致とSVG拒否、20MBちょうどと1byte超過、複数追加容量、同名添付連番、添付付き出力パスとMarkdown参照、Blob保持、添付なしの既存出力を自動テストで確認
* `git diff --check` が成功
* 実ブラウザでPNGをクリップボードから添付し、本文へ画像データが入らないこと、件数・圧縮後容量・サムネイルが表示されることを確認
* 通常テキストの貼り付けでは本文だけが更新され添付件数が増えないこと、ページ再読み込み後も画像添付が復元されることを確認
* 画像サムネイルの拡大ダイアログとEsc終了、添付付きメモのZIP生成経路、設定画面の `notes / collections / attachments` 表示を確認
* 1000pxの縦長表示と390pxのスマートフォン相当幅で横方向のオーバーフローがなく、サムネイルの `object-fit: contain`、操作ボタン高38pxを確認
* ダークテーマで添付セクション、添付カード、ファイル名が既存テーマ色へ切り替わり、ブラウザコンソールにerror / warningがないことを確認

### 既知の制限・今後の課題

* Memo Nexusの添付付きZIPを復元するインポートは未実装。既存取り込みはAI朝刊向けJSON/Markdown/テキストのままとし、Base64をJSONへ埋め込む方式は採用していない
* Markdown本文中への画像ブロック挿入、PDF.js、PDFテキスト抽出、OCR、画像編集、カード全体のPDF保存は未実装
* PDF表示はブラウザ標準ビューアに依存する。Safari/iPhone実機、端末固有のストレージ上限・メモリ不足、大容量画像の処理時間は継続確認が必要
* Chrome拡張のローカルファイルアクセス権限が無効だったため、PDF/JPEG/WebPの実ファイル選択、PDF標準ビューアの起動、ドラッグ＆ドロップ、複数ファイル同時選択、実ブラウザでの添付削除・完全削除はコード経路と自動テスト確認に留めた
* 添付付きZIPは生成処理完了とダイアログ終了まで確認したが、自動操作環境ではダウンロードファイルの実体展開確認は未実施

## 2026-07-15 本文内画像参照の修正

### 原因と修正方針

* 従来の画像貼り付けは画像Blobを添付ストアへ保存するだけで、本文への永続参照挿入とカード本文内での画像描画がなかった
* 画像ファイルを取得できない貼り付けや本文へのファイルドロップではブラウザ既定処理が残り、クリップボード由来の一時的な `blob:` URLが文字列として本文へ入る経路があった
* IndexedDBの既存 `attachments` store、画像圧縮、20MB容量判定を維持し、本文には `![代替テキスト](attachment://画像ID)` だけを保存する方式へ修正

### 実装内容

* 画像貼り付け時のカーソル位置を保存前に取得し、画像の圧縮・形式検証・容量判定・添付保存が成功した後に永続ID参照を挿入
* 本文へのファイルドロップも既存添付処理へ統合し、画像だけ本文参照を挿入。PDFは従来どおり添付として保存し、本文内表示は追加していない
* 一時的な `blob:` 文字列だけが貼り付けられた場合は本文への挿入を中止し、画像データをコピーし直す案内を表示
* 参照記法の前後へ必要な改行を補い、直後のMarkdown見出し・箇条書きなどを壊さないようにした
* 既存の画像参照記法内部へカーソルがある場合は参照の直後へ挿入位置を補正し、複数画像の貼り付けで参照文字列が分断されないようにした
* Markdownインライン解析へ `attachment://` 専用トークンを追加し、許可した永続IDだけを安全なプレースホルダーとして生成
* カード描画時に現在のメモの画像添付だけをID照合し、Blobから作ったObject URLを本文画像と添付サムネイルで再利用
* Object URLはメモ切り替え、添付削除、ページ終了時に既存処理で解放し、同じ画像の複数箇所参照にも対応
* 本文参照中の画像を添付欄から削除する場合は警告し、画像が存在しない参照には「画像を表示できません」を表示
* エクスポート時に本文内の `attachment://画像ID` を実際に出力する `attachments/ファイル名` へ変換し、本文位置と代替テキストを維持

### テスト結果

* `node --check app.js`、`node --check export-utils.js`、`node --check attachment-utils.js` が成功
* 自動テストで永続ID参照、blob URL非使用、先頭・中間・末尾・複数挿入、既存参照内部への挿入防御、参照ID抽出、エクスポート相対パス変換を確認
* 実ブラウザでPNGを本文の先頭・中間・末尾へ貼り付け、3件の異なる永続ID参照と3画像が記述順に表示されることを確認
* 画像の前後に通常文とMarkdown見出しを置き、見出し・本文・画像がカードで維持されることを確認
* 再読み込み後に参照3件と画像3件が復元され、別メモへ切り替えて戻った後も画像が再表示されることを確認
* 本文保存値の `blob:` が0件であること、一時blob URL貼り付けが拒否されること、通常テキスト貼り付けが維持されることを確認
* 存在しない画像IDでは他の画像やカード全体を壊さず「画像を表示できません」と表示されることを確認
* 本文画像の `max-width: 100%` と `height: auto`、ブラウザコンソールにerror / warningがないことを確認

### 既知の制限

* textarea内のドロップ座標から文字位置を安全に算出する共通APIがないため、本文への画像ドロップは現在のカーソル位置へ参照を挿入する
* PDFの本文内埋め込み、プレビュー、画像化は対象外で、従来の添付欄とブラウザ標準PDFビューアを維持

## 2026-07-15 添付容量判定の並行実行対策

* 同一メモへの添付追加をメモID単位のキューで直列化し、容量取得・20MB上限判定・IndexedDB保存が並行しないように修正
* ファイル選択、クリップボード貼り付け、添付欄へのドロップ、本文へのドロップが共有する `handleAttachmentFiles` 全体を直列化
* 待機中を含む処理件数をメモ単位で管理し、処理中の追加ボタン・ファイル入力・ドロップ領域を無効化。メモ切り替え後は表示中メモの処理状態だけを反映
* 同一メモへ12MBと9MBを連続追加した場合、1回目だけを許可し、合計20MBを超える2回目を拒否する自動テストを追加
* JavaScript構文確認、全18件の自動テスト、`git diff --check` が成功

## 2026-07-16 コードブロックの右端折り返し対応

* Markdownプレビューのコードブロックを `white-space: pre-wrap` とし、実改行、空白、タブ、インデントを維持したまま表示領域の右端で折り返すように変更
* 長いURL、ファイルパス、JSON文字列、空白を含まない識別子も親要素を突き抜けないよう `overflow-wrap: anywhere` を使用し、コードブロックの横スクロールを抑止
* 折り返しはCSSによる表示上の変更だけで、Markdown本文、IndexedDBの保存値、DOMのテキスト内容、コピーされるコードには改行や空白を追加しない
* highlight.jsのDOMと色分け処理は変更せず、言語指定、自動判定、素のコード表示を維持
* PC通常幅、画面半分程度の縦長表示、スマートフォン幅で長いJavaScript、URL、パス、JSON、識別子、日本語コメント、タブ・スペースを確認。コードブロックは親幅とビューポート幅の小さい側へ制限し、コード追加前よりページ全体の横スクロール幅が増えないようにした
* JavaScript構文確認、コードブロック折り返しのCSS契約テストを含む全21件の自動テスト、`git diff --check` が成功
* 折り返し行だけの字下げは、CSS標準だけでは自動折り返し位置と実改行を安全に区別できず、文字幅測定や行分割・DOM再構築が必要になるため見送り
* 折り返し記号は今回実装していない。将来追加する場合もコピー内容と保存データを変えない表示専用方式の検討が必要
* ブラウザのモバイル幅上書きでは、コード表示前から既存ツールバーの最小幅によるページ横スクロールが残ることを確認。今回のコードブロックは横幅を追加しておらず、既存ツールバーのレスポンシブ改善は別課題
* 設定画面の「表示」へ「コードブロックの折り返し」を `Coming soon` 項目として追加。設定UIや保存値は追加せず、現在の常時折り返し仕様を維持

## 2026-07-16 関連メモの右サイドパネル化

* 本文・カードの表示幅を圧迫していた関連メモの固定カラムを廃止し、画面右端から重ねて表示する幅340px（モバイルは最大90vw）の右ドロワーへ変更
* 初期状態とメモ切り替え時は閉じた状態とし、右端の件数付きボタン、パネル内の閉じるボタン、外側タップ、Escキーで開閉できるようにした
* 閉じた後の開閉ボタンへのフォーカス復帰、`aria-expanded`・`aria-controls`・`aria-hidden`・`inert`、44px以上の操作領域を追加
* パネル表示中は背景スクロールを抑止し、関連メモと新発見の領域だけをスクロール可能にした。200msのスライドと `prefers-reduced-motion` 時のアニメーション抑止にも対応
* 関連メモの抽出、関連度、並び順、最大8件の表示は変更せず、件数0の場合は実際の生成ルールに沿った案内を表示
* `node --check` 4件、関連ドロワーの契約テストを含む全25件の自動テスト、`git diff --check` が成功
* PC通常幅、狭いPC、390pxスマートフォン、375px iPhone相当で、オーバーレイ表示、件数、全閉じ方、関連メモ選択後の自動クローズ、本文幅が変わらないこと、内部スクロール、フォーカス復帰を確認
* iPhone Safariは実機ではなくChromeのレスポンシブ表示による相当確認のため、実機固有のタッチ・スクロール挙動は今後の確認事項

## 2026-07-16 関連メモの表示件数案内

* 右端ボタンの全候補件数とドロワー内の最大8件表示の違いを明確にするため、候補が表示件数を超える場合だけ一覧直前へ「全件数中、表示件数表示」を追加
* 文言は `allRelated.length` と `related.length` から生成し、例として候補9件では「9件中、8件表示」と表示。最大表示件数8件、`findRelated`、関連度、並び順、抽出条件は変更していない
* 候補が8件以下では案内を空にして非表示とし、0件時は従来の「関連メモはありません。」案内を維持
* 補助文字色 `var(--muted)`、12px、折り返しなしの控えめな表示とし、PCで候補9件・表示8件・右端ボタン9件を確認
* JavaScript構文確認、関連ドロワーテストを含む全25件の自動テスト、`git diff --check` を実行

## 2026-07-16 本文内画像ブロックの表示拡張

### 実装概要

* 設定画面の「表示」へ「カード内画像サイズ」を追加し、`small`（小）・`medium`（標準）・`large`（大）をアプリ全体へ適用
* 初期値と不正値のフォールバックを `medium` とし、`memo-nexus-image-block-size` のキーで既存テーマ設定と同じくlocalStorageへ保存
* 小は最大高200px、標準は350px、大はカード内かつ最大 `min(620px, 70vh)` とし、すべて `max-width: 100%`、`height: auto`、`object-fit: contain` で縦横比と画像全体を維持
* 1枚＋説明文では小を35:65、標準を50:50の横並び、大を上下配置とし、画像ブロック幅560px以下ではすべて上下配置へ変更
* 2枚ではサイズに関係なく画像を2列、共通説明文を下へ配置し、画像ブロック幅560px以下では画像1・画像2・説明文の縦並びへ変更
* 画像ブロックの操作へ2枚目追加、左右入れ替え、各画像参照を外す操作、共通説明文の追加・編集を追加。2枚時は追加操作を非表示とし、2枚から1枚へ戻しても説明文を維持
* 本文画像から既存の画像プレビューダイアログを開けるようにし、添付欄、PDF、画像圧縮、容量上限、Blob、画像ID、添付IDは変更していない

### 説明文と保存形式

* 説明文は改行、太字、斜体、インラインコード、HTTP/HTTPS・メール・ページ内リンク、箇条書きだけを安全なHTMLへ変換
* HTML、JavaScript、別画像、PDF、Mermaid、コードブロック、表、見出しは解釈せず、エスケープした通常テキストとして表示
* 保存形式は標準Markdownの画像参照と説明文を残し、画像ブロックの開始・説明文境界・終了だけを `<!-- memo-nexus:image-block -->`、`<!-- memo-nexus:image-caption -->`、`<!-- /memo-nexus:image-block -->` で示す方式を採用
* HTMLコメントは一般的なMarkdownビューアでは非表示となり、画像参照・順番・説明文自体は通常のMarkdownとして確認できる。コードフェンス内の同じ文字列とは衝突しない
* 既存の単独 `![alt](attachment://ID)` は変換せず、説明文なしの1枚ブロックとして遅延解釈。編集、2枚目追加、入れ替え時だけ新形式へ保存
* 新規に3枚以上を同時挿入した場合は、最大2枚の画像ブロックへ順番どおり分割

### 変更した主なファイル

* `app.js`: 設定保存、画像ブロック描画、編集操作、既存添付追加・画像プレビューとの接続
* `attachment-utils.js`: サイズ正規化、画像ブロック解析・直列化・置換、安全な説明文Markdown描画
* `index.html`: サイズ選択UI、説明、2枚目追加用画像入力、キャッシュ更新
* `style.css`: サイズ別、1枚・2枚、説明文、コンテナクエリ、編集UIのスタイル
* `attachment-utils.test.js`, `image-block-layout.test.js`: 保存形式、後方互換、安全性、操作、CSS・設定契約のテスト

### 実行したテスト

* `node --check app.js`、`node --check attachment-utils.js`、`node --check export-utils.js`、各テストファイルの構文確認
* `node --test --test-isolation=none export-utils.test.js attachment-utils.test.js code-block-wrap.test.js related-drawer.test.js image-block-layout.test.js`
* サイズの初期値・不正値フォールバック、画像1枚・2枚、最大2枚分割、説明文あり・なし、順番入れ替え、2枚から1枚、既存形式、コードフェンスとの非衝突、安全なMarkdown、既存エクスポート参照変換、添付・PDF関連の既存テストを確認
* `git diff --check`

### 手動確認

* Chromeで小200px、標準350px、大の最大高、画像の自然な縦横比、1枚＋説明文の横並び・上下配置を確認
* 2枚目追加後に2列表示、追加ボタン非表示、共通説明文の下配置、左右入れ替え、保存・再読み込み後の順番維持を確認
* 2枚から1枚へ戻した後も説明文が残り、添付件数と元データが維持されることを確認
* 太字、リンク、箇条書き、複数行と `<script>` 入力を確認し、スクリプト要素が生成・実行されず通常テキストになることを確認
* 390px相当の狭幅で画像2枚が縦並びとなり、画像・説明文・ページに横方向のはみ出しがないことを確認
* 本文画像クリックで既存画像プレビューダイアログが開き、クリーンな再読み込み後のコンソールにerror / warningがないことを確認

### 今回実装しなかった事項・残した課題

* トリミング、回転、色調補正、フィルター、画像への文字入れ、個別サイズ・個別説明文、3枚以上の1ブロック配置、自由配置、ドラッグ操作は実装していない
* 添付欄の表示、PDF本文埋め込み、画像圧縮・解像度変更、添付の自動削除方針、通常本文の回り込みは変更していない
* エクスポートMarkdownでは従来どおり `attachment://ID` を `attachments/ファイル名` へ変換し、画像と説明文は外部ビューアで確認できる。一方、既存の取り込み機能は添付付きZIPの復元に未対応のため、エクスポートZIPからBlobと内部IDを再構築する往復インポートは今後の課題
* Safari／iPhone実機でのコンテナクエリ、ファイル選択、タッチ操作は未確認

## 2026-07-16 PR #15 画像ブロック操作の追補修正

* 左右入れ替え、画像参照を外す、説明文保存、2枚目追加では、本文とUndo履歴を更新した直後にプレビューを再描画し、同じ操作の保存予約では重複描画しないようにした
* 通常時は画像と説明文だけを表示し、追加・入れ替え・説明文編集・画像参照を外す操作を小さな「…」メニューへ収納。PCのhover／focus、タップ、Enter／Space、Escに対応した
* 説明文エディタ表示中は通常操作メニューを非表示にして操作領域を差し替え、保存後は再描画、キャンセル後は状態復元により通常操作へ確実に戻すようにした
* 2枚目追加前と添付保存後に対象ブロックを再確認し、保存後の追加失敗では今回作成した添付IDだけをIndexedDBと表示中添付一覧からロールバックするようにした。既存添付は削除しない
* 既存形式の画像参照が2行連続していても自動ペア化せず、それぞれ独立した1枚画像ブロックとして解釈するようにした。2枚ブロックは明示的な `memo-nexus:image-block` 形式に限定した
* 状態変化を検証するテストとして、即時再描画の実行順、説明文編集時の操作領域差し替えとキャンセル復元、連続する既存画像の分離、保存後失敗時の新規添付だけのロールバックを追加した
* Chromeで1枚・2枚、説明文あり、2枚目追加、左右入れ替え、画像参照削除、説明文の保存・キャンセル、キーボードとタップによるメニュー操作、再読み込み後の保持を確認した
* Chromeの375px相当で2枚画像が縦並びになり、文書幅・画像ブロック・操作メニューが横方向へあふれないことを確認した
* 手動確認中に「…」ボタンと既存の関連メモ開閉ボタンが重なる配置を検出したため、操作入口を画像ブロック左上へ配置して衝突を解消した
* 変更前に対象ブロックが失われたケースでは新しい添付を保存しないこと、保存後の対象変更は自動テストで新規添付だけを削除することを確認した
* 画像Blob、既存添付ID、圧縮、PDF、添付欄、エクスポート処理は変更していない
* `node --check`、既存・新規を含む全40件の自動テスト、`git diff --check` が成功した

## 2026-07-17 レスポンシブ編集レイアウト

* `html` をレイアウト幅の基準となるCSSコンテナにし、実用最小幅（メモ一覧300px、本文320px、カード280pxと余白）からWide 1040px以上、Compact 720〜1039px、Mobile 719px以下の3モードを採用した。小数CSSピクセルでもJSとCSSの境界がずれないよう、Container Query側は719.98px／1039.98pxに余裕を持たせた
* Wideでは従来のメモ一覧・本文・カードの3ペインを維持し、主要操作と現在のコレクション表示を上部へまとめた。メモ、本文、カード、カード内部のレンダリングは複製していない
* Compactではメモ一覧を左オーバーレイドロワーへ収納し、本文とカードをデフォルト表示した。カード収納時はカードを非表示にして本文へ空き幅を割り当て、再表示で2ペインへ戻す一時状態を追加した
* Mobileでは本文を主領域とし、メモ一覧を左、カードを右のオーバーレイドロワーへ配置した。背景タップ、Esc、閉じるボタン、左右同時オープン防止、背景操作抑止、フォーカス移動に対応した
* メモ一覧は選択中コレクションのメモに絞り込み、通常のメモ選択後はドロワーを自動的に閉じる。コレクション選択、設定、保存状態、主要操作は全モードで上部に維持した
* レイアウト状態はメモ一覧、Mobileカード、Compactカードを分離したメモリ上の状態だけで管理し、モード変更時はWide=全ペイン、Compact=本文＋カード、Mobile=本文のみへ戻す。IndexedDBスキーマ、メモ・添付データ、カード描画、関連メモは変更していない
* ドロワーは160msのスライドとし、`prefers-reduced-motion` ではアニメーションを無効化した。本文・カード内部だけがスクロールし、320〜1440pxで文書全体の横スクロールが発生しないことを確認した
* 自動確認は `node --check app.js`、既存5ファイルと新規 `responsive-layout.test.js` の全44件、`git diff --check` が成功した。一括 `node --test` は実行環境の子プロセス起動制限（EPERM）のため、各テストファイルを単一プロセスで実行した
* Chromeで320、375、390、430、719、720、900、1039、1040、1440pxを確認した。Compactのカード収納で本文幅が496pxから876pxへ拡張し、再表示で元へ戻ること、Mobileの背景タップ・Esc・排他開閉、境界前後のモード一致、ライト／ダークの背景・境界・文字・オーバーレイ、コンソールにerror／warningがないことを確認した
* iPhone／iPadなどの実機Safari、ソフトウェアキーボード表示中の追従、実機固有のタッチスクロールは未確認事項として残した

## 2026-07-17 PR #18 レビュー指摘対応

* 上部操作バーに置いていた「すべてのメモ」／コレクション名表示を削除し、メモ一覧の見出し内へ表示範囲ラベルを移した。Compact／Mobileの上部にはメモ一覧・カードの開閉操作だけを残し、長いコレクション名由来の省略記号が上部へ単独表示されないようにした
* `main` の既存実装を確認し、左メモ一覧はコレクション選択では絞り込まず、未削除の全メモを表示する仕様へ戻した。コレクションごとの直接所属メモと子孫を含む件数は、従来どおりコレクション・エクスプローラ内だけで扱う
* ゴミ箱選択時だけ左メモ一覧を削除済みメモへ切り替え、表示範囲ラベルも「ゴミ箱」として内容と一致させた。通常、通常コレクション、未分類では「すべてのメモ」と未削除全件を維持する
* 一覧判定を `memo-list-utils.js` の副作用がない関数へ分離し、通常、通常コレクション、未分類、ゴミ箱、長いコレクション名についてラベルとメモIDの一致をデータテストで確認した
* レスポンシブ契約テストへ、表示範囲ラベルがサイドバー内にあり上部ヘッダーには存在しないこと、719px=Mobile、720px=Compact、1039px=Compact、1040px=Wideとなる実関数の境界テストを追加した
* `node --check` 2件、既存テストを含む全50件、`git diff --check` が成功した。ブラウザのWide表示でラベルがメモ一覧見出し内にあり、主要操作バーに「すべてのメモ」が残らず、コンソールにerror／warningがないことを確認した

## 2026-07-17 PR #18 表示整理の追補

* メモ一覧見出し内の小さな「すべてのメモ」表示と `memoListScopeLabel`、専用描画処理、CSSを削除し、通常時は見出し「メモ一覧」だけを表示する構成へ戻した
* ゴミ箱選択時は同じ見出しを「ゴミ箱」へ切り替え、サイドバーの `aria-label` も同期した。削除済みメモだけを左一覧へ出す処理は維持した
* Wide／Compactで右寄せになっていた主要操作ツールバーを、ペイン操作ボタンの後ろから左寄せで並ぶよう変更した。＋、コレクション、Today、Undo、ZIP、語句、Graph、設定、Delの順序と、狭幅時のツールバー内部横スクロールは維持した
* データテストで通常時「メモ一覧」＋未削除メモ、ゴミ箱時「ゴミ箱」＋削除済みメモの一致を確認し、構造テストで不要ラベルの不在、主要操作の順序、左寄せ、内部スクロールを確認した
* Chromeで390px Mobile、900px Compact、1040px Wideを目視確認し、719px=Mobile、720px=Compact、1039px=Compact、1040px=Wide、文書全体の横スクロールなしを再読み込み後に確認した。ゴミ箱選択操作でも見出しと0件の一覧が一致し、コンソールにerror／warningはなかった
