## 2026-06-21 Memo Nexus 保存安定化・ZIP修正

### 変更内容

* IndexedDB の putNote/deleteNote を transaction 完了待ちに変更
* flushSave() を追加し、ZIPバックアップ前に遅延保存を強制実行
* pagehide / visibilitychange でiPhone終了時の保存漏れ対策を追加
* ZIPヘッダーに正しいDOS日時を書き込むよう修正
* ZIP内ファイル名の重複を避ける uniqueZipFileNames() を追加
* README.md にiPhone保存安定化とZIPバックアップ改善の仕様を追記

### 修正理由

* iPhoneでは遅延保存や非同期IndexedDB保存が、ページ終了・画面ロック・アプリ切替で完了しない可能性があるため
* ZIPの日付が u16(0), u16(0) になっており、iPhone側で1979/11/30のような不正な日付として表示される可能性があるため
* 同名タイトルのメモがあると、ZIP内で同じ .md ファイル名が複数生成される可能性があるため

### 確認方法

* PCブラウザでメモを作成・編集し、再読み込み後に残ること
* iPhone Safariまたはホーム画面追加版でメモを作成・編集し、アプリを閉じて再起動しても残ること
* ZIPバックアップを作成し、ZIP内ファイルの日付が1979/11/30のような不正日付にならないこと
* 同じタイトルのメモが複数あっても、ZIP内ファイル名が -2, -3 付きで一意になること
* 既存のメモ作成、編集、削除、検索、Markdownプレビュー、Wikiリンクが壊れていないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* IndexedDBモックで putNote/deleteNote がtransaction完了前には解決せず、完了後に解決することを確認
* 同名ファイルが `新規メモ.md`、`新規メモ-2.md`、`新規メモ-3.md` になることを確認
* 指定日時がDOS日時へ変換され、ZIPのローカルヘッダーとセントラルディレクトリヘッダーの両方へ同じ値が書き込まれることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-06-21 iPhone保存対策: 編集中メモのlocalStorageドラフト退避

### 変更内容

* localStorageに全件バックアップする案は採用せず、容量を考慮して現在編集中の1件のみをドラフト退避する実装を追加
* saveCurrentDraftMirror() を追加
* scheduleSave() の先頭でドラフト退避するよう変更
* restoreCurrentDraftMirror() を追加
* init() で ensureStartupNotes() の前にドラフト復元を試すよう変更
* pagehide / visibilitychange 時にもドラフト退避するよう変更
* 古すぎるドラフトを復元しない判定を追加
* 削除したメモがlocalStorageドラフトから復活しないよう、一致するドラフトだけを削除
* 復元時・保存時の診断ログを追加
* README.md に編集中文書1件のドラフト退避・復元仕様を追記

### 修正理由

* iPhone Safari / PWA では、入力直後にアプリを閉じると遅延保存やIndexedDB保存が完了しない可能性があるため
* localStorageは容量が小さいため、全メモではなく現在編集中の1件だけを退避する方針にした
* IndexedDBを本保存として維持しつつ、直前の入力内容だけを救済できるようにするため

### 確認方法

* PCで既存のメモ作成・編集・削除・検索・Markdownプレビュー・Wikiリンク・ZIP出力が壊れていないことを確認
* iPhoneでメモを作成または編集する
* 入力直後にホーム画面へ戻る、Safariを閉じる、またはPWAを終了する
* 再起動後、直前に編集中だったメモが復元されることを確認
* console.log に Draft mirror saved / Draft mirror restored が出ることを確認
* ZIPの日付異常修正が維持されていることを確認

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* localStorageに現在編集中の1件だけが保存され、タイトル・本文・各日時が含まれることを確認
* IndexedDBより新しいドラフト、およびIndexedDBから消失したIDのドラフトが復元されることを確認
* IndexedDB側の方が新しい場合はドラフトを復元しない条件を確認
* 30日以上古いドラフトが復元されず、localStorageから削除されることを確認
* 削除したメモと同じIDのドラフトが削除され、次回起動時に復活しないことを確認
* ZIP内ファイル名の重複回避とZIPヘッダーのDOS日時が維持されていることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* iPhone Safari/PWAでの実機確認は未実施

## v0.3.1 "Draft Mirror" - 2026-06-21

### 変更内容

* アプリバージョン APP_VERSION / APP_LABEL / APP_BUILD を追加
* 画面上に v0.3.1 "Draft Mirror" を表示
* 起動時にバージョン、URL、IndexedDBメモ件数を console.log に出力
* localStorage が利用可能か確認する checkLocalStorageAvailable() を追加
* プライベートブラウズ、別タブグループ、Safari版とホーム画面PWA版の保存領域違いに関する注意表示を追加
* 保存リスクがある場合、storageWarning に警告を表示
* README.md にv0.3.1のバージョン表示と保存領域警告を追記

### 修正理由

* iPhone Safariでは通常ブラウズ、プライベートブラウズ、タブグループ、ホーム画面PWAで保存領域が分かれる場合があるため
* メモが消えたように見えても、別の保存領域を見ているだけの場合があるため
* 今後、ユーザーがプライベートブラウズや別領域で誤って使うことを避けるため
* iPhoneで古いコードや別URLを開いているかを確認しやすくするため

### 確認方法

* PCブラウザでアプリを開き、画面上に v0.3.1 "Draft Mirror" が表示されること
* console.log にバージョン、URL、IndexedDBメモ件数が表示されること
* iPhone Safari通常ブラウズで開き、既存メモが残っていること
* iPhone Safariプライベートブラウズ、または保存が不安定な環境では注意表示が出ること
* ZIPバックアップの日付修正が維持されていること
* 既存のメモ作成、編集、削除、検索、Markdownプレビュー、Wikiリンクが壊れていないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* APP_VERSION / APP_LABEL / APP_BUILD と画面表示用要素が正しく設定されていることを確認
* localStorage試し書きの成功・失敗と、失敗時の警告表示を確認
* IndexedDBメモ件数が0～1件の場合は控えめな保存領域確認を表示し、通常件数では非表示になることを確認
* DB_NAME / STORE_NAME / DB_VERSION が従来値のまま変更されていないことを確認
* ZIPヘッダーのDOS日時処理が維持されていることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* iPhone Safari/PWAでの実機確認は未実施

## 2026-06-21 v0.3.1 バージョン表示修正

### 変更内容

* バージョン表示をメモ一覧の末尾からツールバー直下へ移動
* `style.css` と `app.js` の読み込みURLに `v=0.3.1` を追加
* バージョン表示へ区切り線を追加し、既存UI内で小さく常時確認できるよう調整
* README.md の表示位置説明とキャッシュ対策を更新

### 修正理由

* バージョン表示がメモ一覧の下へ押し出され、特にiPhoneで見えない場合があったため
* Safari/PWAが古いCSSやJavaScriptをキャッシュしている場合でも、v0.3.1のファイルを再取得しやすくするため

### 確認方法

* 画面左側のツールバー直下に `v0.3.1 "Draft Mirror"` が表示されること
* `index.html` が `style.css?v=0.3.1` と `app.js?v=0.3.1` を読み込むこと
* 既存のメモ一覧、検索、保存、ZIP出力が影響を受けていないこと

### 確認結果

* `appVersion` 要素が1件だけ存在し、ツールバー直下に配置されていることを確認
* JavaScript構文と差分の空白エラーがないことを確認

## 2026-06-21 v0.3.1 PC・スマホ別バージョン表示

### 変更内容

* PCではバージョンを関連メモ見出しの直下に表示
* スマホでは従来どおり左側ツールバー直下に表示
* 複数のバージョン表示要素を共通クラスで取得し、同じバージョン文字列を設定
* 更新したCSSとJavaScriptを再取得させるため、読み込みURLの識別子を `v=0.3.1-2` に更新
* README.md の表示位置説明をPC・スマホ別の内容へ更新

### 修正理由

* PCでは左サイドバーの上部が横幅不足で、バージョン表示を視認しにくかったため
* 関連メモの見出し直下であれば、PCの3カラム表示でも自然に確認できるため
* スマホでは関連メモ欄が画面下部へ移動するため、上部のツールバー直下表示を維持するため

### 確認方法

* 画面幅が980pxを超える場合、関連メモの見出し直下に `v0.3.1 "Draft Mirror"` が表示されること
* 画面幅が980px以下の場合、左側ツールバー直下に `v0.3.1 "Draft Mirror"` が表示されること
* PC用とスマホ用の両要素に同じバージョン文字列が設定されること

### 確認結果

* PC用・スマホ用のバージョン表示要素が各1件存在することを確認
* 両方の表示要素に `v0.3.1 "Draft Mirror"` が設定されることを確認
* CSSでPC時は関連メモ側、980px以下ではツールバー側だけが表示されることを確認
* JavaScript構文と差分の空白エラーがないことを確認

## 2026-06-21 保存領域リスク警告の文言改善

### 変更内容

* 「メモ件数が少ない状態です」という開発者向け表現を削除
* プライベートブラウズの可能性と、通常ブラウズ側とは別保存になる場合があることを明記
* 「いつもと同じ開き方で開き直してください」とユーザーが取るべき行動を明記
* 警告文の3行構成が画面上でも維持されるよう表示スタイルを調整
* 更新した警告文とスタイルを再取得させるため、静的ファイル識別子を `v=0.3.1-3` に更新

### 修正理由

* 保存領域リスクの原因と対応方法を、ユーザーが短い文面で理解できるようにするため

### 確認方法

* 保存領域リスクの注意表示が指定された3行の文言になること
* 保存条件やIndexedDB・localStorageの処理が変更されていないこと

### 確認結果

* 警告文以外の判定ロジックと保存処理に差分がないことを確認
* 警告領域で改行が維持される設定を確認

## 2026-06-24 設定UI整理

### 変更内容

* 上部ツールバーに設定ボタンを追加
* 設定モーダルを追加
* 設定モーダルに保存状態、取り込み、バックアップ、表示、データ管理の各セクションを追加
* 保存状態に IndexedDB の保存方式、DB名、ストア名、メモ件数、使用容量、上限目安、使用率、アプリバージョン、現在のURLを表示
* AIニュースファイル取り込みボタンを左カラムから設定モーダル内へ移動
* JSON貼り付け取り込みボタンを左カラムから設定モーダル内へ移動
* Markdown ZIP書き出しボタンを設定モーダル内にも追加
* 表示設定、JSON全体書き出し、全データ削除は Coming soon または未実装表示にした
* 左カラムから知識レベルパネルを撤去
* `renderAll()` と入力時処理から `renderLevel()` の呼び出しを削除

### 修正理由

* 左カラムを検索とメモ一覧中心に整理し、将来のタグ・エクスプローラ表示に使いやすくするため
* AIニュースファイル取り込み、JSON取り込み、保存状態などの管理系機能を設定モーダルへ集約するため
* 知識レベルの表示を、タグ・知識マップ再設計までいったん撤去するため
* IndexedDBの使用容量や保存状態をアプリ内から確認できるようにするため
* 今回はUI整理に範囲を絞り、タグ機能・知識マップ・レトロモードなどの大きな機能追加を避けるため

### 確認方法

* `node --check app.js` でJavaScript構文エラーがないことを確認する
* `git diff --check` で差分の空白エラーがないことを確認する
* `index.html` に設定ボタンと設定モーダルが追加され、左カラムから AI Import、JSON取り込み、知識レベルが消えていることを確認する
* `app.js` で既存の `importAiNewsFile()`、`openJsonImportDialog()`、`downloadMarkdownZip()` が設定モーダル内ボタンから呼び出されることを確認する
* `DB_NAME`、`STORE_NAME`、`DB_VERSION` が変更されていないことを確認する
* Edgeでアプリを開き、設定ボタンから設定モーダルが開くことを確認する

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `git diff --check` で差分の空白エラーがないことを確認
* `index.html` 上で設定ボタン、設定モーダル、取り込みボタン、バックアップボタンが追加されていることを確認
* `index.html` 上で左カラムの AI Import、JSON取り込み、知識レベル要素が撤去されていることを確認
* `app.js` 上で `navigator.storage.estimate()` を使った保存容量表示処理が追加されていることを確認
* `app.js` 上で既存の `importAiNewsFile()`、`openJsonImportDialog()`、`downloadMarkdownZip()` が再利用されていることを確認
* `DB_NAME`、`STORE_NAME`、`DB_VERSION` が従来値のまま変更されていないことを確認
* Edgeでの実ブラウザ確認は未実施
* 設定モーダル内からのAIニュースファイル取り込み、JSON取り込み、Markdown ZIP書き出しの実操作確認は未実施
* 既存のメモ保存、検索、語句統計、Graph、関連メモの実操作確認は未実施

## 2026-06-24 ダークモード追加

### 変更内容

* 設定モーダルの表示セクションにテーマ切替を追加
* ライト / ダークを切り替えられるように変更
* body に dark クラスを付け外しする方式でダークモードを実装
* CSS変数で背景色、文字色、パネル色、枠線色などを切り替えるよう調整
* 選択したテーマを localStorage の memo-nexus-theme に保存
* 起動時に保存済みテーマを復元
* 更新したCSS/JavaScriptを読み込ませるため、静的ファイル識別子を `v=0.3.1-5` に更新

### 修正理由

* 夜間や暗い環境でメモを読み書きしやすくするため
* 表示設定の中で最初に実用性が高いテーマ切替を先行して実装するため
* JavaScriptで各要素の色を直接変更せず、CSS側で管理できるようにするため

### 確認方法

* 設定モーダルを開き、テーマをライト / ダークで切り替えられること
* ダークを選ぶと画面全体が暗色テーマになること
* ライトを選ぶと元の明色テーマに戻ること
* 再読み込み後も選択したテーマが維持されること
* 既存のメモ保存、検索、ZIP出力、JSON取り込み、語句統計、Graph、関連メモが壊れていないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* テーマ切替UIが設定モーダルに表示されることを確認
* localStorage に memo-nexus-theme が保存されることを確認
* 再読み込み後に保存済みテーマが復元されることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-06-25 JSON貼り付け取り込みの1メモ化

### 変更内容

* JSON貼り付け取り込みで items ごとの個別メモを作成しないように変更
* JSON貼り付け取り込みで createNote() を1回だけ呼び、1件のメモにまとめるように変更
* items の各ニュースを、作成した1件のメモ本文内に見出し付きで展開するように変更
* heading / title / headline / 見出し を見出し候補として扱うように変更
* date / publishedAt / day / 日付、trend_summary / trendSummary / summary / overview / 全体傾向 の揺れを扱えるように変更
* period、era、importance、urgency、tags、sourceUrl などの項目を本文へ反映できるように変更
* 取り込み成功メッセージを1件作成に合う文言へ変更
* 更新したJavaScriptを読み込ませるため、静的ファイル識別子を `v=0.3.1-6` に更新

### 修正理由

* ニュースJSONは日付ごとのまとまりとして1メモで保存した方が読み返しやすいため
* items ごとに個別メモを作成すると、ニュース取り込み時にメモが増えすぎるため
* 複数メモへの分割取り込みは、将来の外部メモ取り込み機能として別途設計するため
* heading 形式のJSONで title 空エラーが出ないようにするため

### 確認方法

* JSON貼り付け取り込みダイアログに、items が複数あるJSONを貼り付ける
* 取り込み後に作成されたメモが1件だけであることを確認する
* 作成されたメモ本文に、items の各ニュースが見出し付きで展開されていることを確認する
* heading しかないitemでもエラーにならないことを確認する
* period、importance、tags が本文に表示されることを確認する
* 既存のAIニュースファイル取り込みが従来どおり動くことを確認する
* 新規メモ作成、今日メモ、検索、自動保存、語句統計、Graph、関連メモが壊れていないことを確認する

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `importPastedItNewsJson()` 内の `createNote()` 呼び出しが1回だけになっていることを確認
* items が複数あるJSONから作るメモデータが1件分だけであることをNode上の関数検証で確認
* heading 形式のJSONで `items[0] の title が空です。` が出ないことをNode上の関数検証で確認
* 作成される本文に複数ニュースの見出しが展開されることをNode上の関数検証で確認
* period、importance、tags が本文に反映されることをNode上の関数検証で確認
* 既存のAIニュースファイル取り込み、新規メモ作成、今日メモ、検索、自動保存、語句統計、Graph、関連メモの実操作確認は未実施
* `git diff --check` で差分の空白エラーがないことを確認
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-06-25 JSON貼り付け取り込みのAI朝刊体裁合わせ

### 変更内容

* JSON貼り付け取り込みで作成するメモタイトルを `タイトル 日付` の形式に変更
* JSON貼り付け取り込みの本文をAIニュース取り込みに近い体裁へ変更
* items の見出しを `1. 見出し` の形式で出すように変更
* カテゴリ、時代、重要度、緊急度、タグ、要約、重要性、情報源を箇条書き中心で表示するように変更
* 全体傾向をAIニュース取り込みと同じく個別ニュースの後ろに表示するように変更
* 更新したJavaScriptを読み込ませるため、静的ファイル識別子を `v=0.3.1-7` に更新
* READMEのJSON貼り付け取り込み説明を、1メモ化とAI朝刊体裁に合わせて更新

### 修正理由

* JSON貼り付け取り込みのメモが、AIニュースファイル取り込みで作成したメモと見比べやすくなるようにするため
* メモ一覧やタイトル欄で日付がすぐ分かるようにするため

### 確認方法

* JSON貼り付け取り込みで作るメモタイトルに日付が含まれることを確認する
* 作成される本文が `# タイトル 日付`、`日付: ...`、`1. 見出し`、箇条書き、`## 全体傾向` の並びになることを確認する
* JSON貼り付け取り込みが引き続き1メモだけ作成することを確認する
* `node --check app.js` でJavaScript構文エラーがないことを確認する
* `git diff --check` で差分の空白エラーがないことを確認する

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* Node上の関数検証で、メモタイトルが `AI朝刊 2026-05-31` 形式になることを確認
* Node上の関数検証で、本文がAIニュース取り込みに近い `1. 見出し` と箇条書き形式になることを確認
* Node上の関数検証で、全体傾向が個別ニュースの後ろに表示されることを確認
* `git diff --check` で差分の空白エラーがないことを確認
* ブラウザ上でのJSON貼り付け取り込み実操作確認は未実施
* PCブラウザおよびiPhone Safari/PWAでの実機確認は未実施

## 2026-07-01 プレビューのfenced code block / Mermaid対応

### 変更内容

* 右側プレビューで Markdown風の fenced code block を扱う renderPreviewHtml() を追加
* 本文を通常テキストとコードブロックへ分割する splitFencedBlocks() を追加
* 通常テキスト用の renderTextBlock() を追加し、既存の renderRichText() を通常文章用として維持
* コード表示用の renderCodeBlock() を追加し、```js / ```html / ```css / ```json などを `<pre><code>` で表示
* Mermaid表示用の renderMermaidBlock() / renderMermaidDiagrams() を追加
* renderPreview() から renderPreviewHtml(body) を呼ぶ構造に整理
* Mermaid.js を index.html で読み込むよう追加
* コードブロックと Mermaid 図用の最小スタイルを style.css に追加

### 修正理由

* メモ本文にコードや Mermaid 図をそのまま書き、右側プレビューで読みやすく確認できるようにするため
* コードブロック内の `[[...]]` が知識リンクとして誤ってボタン化されないようにするため
* IndexedDB の保存形式や saveCurrentNote() を変更せず、表示処理だけで対応するため

### 確認方法

* ```js の中身がコードブロックとして表示されること
* ```mermaid の中身が Mermaid.js により図として表示されること
* Mermaid がないメモでも従来どおりプレビュー表示されること
* 普通の文章中の `[[リンク]]` が従来どおりクリックできること
* コードブロック内の `[[リンク]]` がクリックボタン化されないこと

### 確認結果

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `saveCurrentNote()` と IndexedDB 保存形式に差分がないことを確認
* `git diff -- app.js index.html style.css` で変更内容を確認
* ブラウザ上での Mermaid 描画実機確認は未実施

## 2026-07-01 スマホ向けUndoボタン追加

### 追加修正

* `index.html` の `style.css` / `app.js` リンクに `v=0.3.1-8` を追加し、古いキャッシュ読み込みを防止
* `#undoBtn` と `.undo-button` に PC でも表示されるよう `display: inline-flex` を明示
* `:disabled` 状態でも非表示にせず `opacity: 0.48` で薄く表示するよう調整
* `app.js` では既存の `Ctrl+Z` を奪う処理は存在せず、ブラウザ標準動作を妨げないことを確認

### 確認内容

* PC 幅・スマホ幅の両方で Undo ボタンが表示されること
* Undo 履歴なしではボタンが薄く表示されること
* Undo 履歴ありではボタンが押せること
* PC の `Ctrl+Z` が標準ブラウザ Undo を妨げないこと

### 実装した内容

* `index.html` の上部ツールバーに `undoBtn` を追加
* `style.css` でツールバーボタンの最小高さを調整し、スマホでもUndoを押しやすくした
* `app.js` に `undoStack`、`UNDO_LIMIT`、`lastUndoSnapshotAt` を追加
* `titleInput` と `editor` の `beforeinput` で変更前のタイトル・本文をUndo履歴へ保存
* 削除、切り取り、貼り付け、ドロップは間隔に関係なくUndo履歴を保存
* 通常入力は800ms間隔で履歴を保存し、連続する同一内容は保存しないようにした
* Undoボタンで現在メモIDに対応する直近履歴を復元し、保存予約・プレビュー・関連メモ・保存状態を更新

### 変更したファイル

* `index.html`
* `style.css`
* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* Undo履歴が最大50件に制限される実装であることを確認
* `keydown` で Ctrl+Z を奪っていないことを確認
* IndexedDBの保存形式と既存メモデータ構造を変更していないことを確認

### 未対応事項

* Redoは未実装
* ブラウザ上でのスマホ実機操作確認は未実施
* Excelのような完全な操作履歴は未実装

## 2026-07-01 新規メモの仮タイトル自動付与

### 実装した内容

* `createNote()` に `options` を追加し、必要な場合だけ重複回避を無効にできるようにした
* タイトルが空の場合、現在読み込んでいる `notes.length + 1` から `memo301` のような仮タイトルを作る `temporaryMemoTitle()` を追加
* 新規メモボタンでは空タイトルで `createNote()` を呼び、`uniqueTitle()` による重複回避を無効にした
* 今日メモ、AI取り込み、JSON取り込み、`[[リンク]]` からの新規作成では従来どおり重複回避を維持

### 変更したファイル

* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* `git diff --check -- app.js` で差分の空白エラーがないことを確認
* `uniqueTitle()` を削除していないことを確認
* 新規メモボタン以外の `createNote()` 呼び出しは既存の重複回避を使うことを確認

### 未対応事項

* ブラウザ上での新規メモ作成実操作確認は未実施

## 2026-07-01 Markdown風プレビュー強化・削除Undo追加

### 今回変更した概要

* `renderTextBlock()` を行単位のMarkdown風表示へ拡張
* `#` / `##` / `###` 見出し、`**太字**`、インラインコード、`-` 箇条書き、`>` 引用をプレビュー表示に追加
* 通常文章中の `[[リンク]]` は従来どおりクリック可能なボタンとして維持
* fenced code block と Mermaid block 内では `[[リンク]]` をボタン化しない既存挙動を維持
* Mermaid描画失敗時に、該当ブロックへ「Mermaid構文エラー」と元コードを表示するよう改善
* メモ削除直前の直近1件を `deletedNoteSnapshot` として保持し、削除後に「元に戻す」から復元できる削除Undoを追加
* 静的ファイルの読み込み識別子を `v=0.3.1-9` に更新

### 変更したファイル

* `app.js`
* `index.html`
* `style.css`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* Mermaid CDN 読み込みが既存の1件だけで重複していないことを確認
* IndexedDB保存形式、本文データ形式、本文編集Undoの構造を変更していないことを確認
* `keydown` で Ctrl+Z を奪う処理を追加していないことを確認

### 未対応・今後の検討事項

* ブラウザ上でのMarkdown風表示、Mermaid構文エラー表示、削除Undoの実操作確認は未実施
* 本格的なMarkdownパーサー、シンタックスハイライト、Redo、削除Undoの永続化は未実装

## 2026-07-01 削除Undo通知の表示位置調整

### 変更内容

* 削除Undo通知をタイトル欄右側から左カラムの検索欄直前へ移動
* 通知用要素を `deleteUndoNotice` とし、通知がないときは `hidden` で高さを取らないようにした
* 通知表示時だけ検索欄とメモ一覧が下に押し下げられるよう、コンパクトな通知スタイルを追加
* 通知内に「元に戻す」と閉じる `×` ボタンを表示
* 既存の削除Undo復元処理は維持し、本文編集Undoとは分離したままにした

### 変更したファイル

* `index.html`
* `style.css`
* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* 旧 `deleteUndoMessage` / `.delete-undo-message` 参照が残っていないことを確認
* 通知が `searchInput` の直前に配置されていることを確認

### 未対応・今後の検討事項

* ブラウザ上での削除、復元、閉じる操作の実操作確認は未実施

## 2026-07-03 JSON貼り付け取り込みのLangBench Live対応

### 変更内容

* JSON貼り付け取り込みの入口でJSON形式判定を追加
* `items` 配列を持つ既存ニュースJSONは、従来のニュース取り込み処理へ渡す形を維持
* `type: "langbench_result"` と `samples` 配列を持つLangBench Live結果JSONに対応
* LangBench結果JSONを1件のMarkdownメモに変換する処理を追加
* LangBench結果本文に `[[LangBench Live]]`、実験名、言語名のWikiリンク、各sample/run/summaryの測定値、注意書きを含めるようにした

### 変更したファイル

* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* ニュースJSONは `items` 配列判定後に既存の `buildItNewsNotes()` へ渡す構造を確認
* Python / JavaScript のLangBench結果JSONを想定し、言語表示が `Python` / `JavaScript` になることを確認
* 不正なJSON構文と未知形式JSONで、それぞれ分かりやすいエラーメッセージを出す分岐を確認

### 未対応・今後の検討事項

* ブラウザ上での貼り付け取り込み実操作確認は未実施

## 2026-07-04 LangBench Live新結果JSON対応

### 変更内容

* LangBench結果メモのタイトルに `runner_label` を含めるよう拡張
* 本文冒頭のWikiリンクに `runner_label` を追加し、Wikiリンク内の `/` はスペースへ置換するようにした
* LangBench結果本文に `type`、実行条件、ランタイム、実行環境セクションを追加
* `memory_total_bytes` は bytes と概算MB/GBを併記するようにした
* sample表示で `input` / `input_file` / `input_file_size_bytes` / sample直下の `line_count` / `average_ms` / `median_ms` に対応
* run表示で `run.line_count` と `run.metrics.line_count` の両方に対応
* 注意文に、コード、ランタイム、実行条件、PC環境、入力CSV条件へ依存する旨を追記

### 変更したファイル

* `app.js`
* `LOG.md`

### 確認した動作

* `node --check app.js` でJavaScript構文エラーがないことを確認
* ニュースJSONは従来どおり `items` 配列判定後に既存処理へ渡す構造のまま変更していないことを確認
* LangBench結果JSON判定は `payload.type === "langbench_result"` と `payload.samples` 配列の条件を維持していることを確認

### 未対応・今後の検討事項

* ブラウザ上での `javascript_result.json` / `python_result.json` 貼り付け取り込み実操作確認は未実施
* 既存ニュースJSONのブラウザ上での貼り付け取り込み実操作確認は未実施

## 2026-07-07 LangBench Result JSON取り込みのresults展開対応

### 変更内容

* LangBench Result JSON の取り込み対応を更新
* `type === "langbench_result"` による判定へ変更
* Markdown形式でのメモ生成を追加
* summary の表示を追加
* results の各 iteration を Markdown 表として表示
* results が多い場合は先頭100件と末尾100件に省略
* results から簡易 Observations を生成
* checksum mismatch 件数の表示を追加
* Raw JSON をコードブロックとして保持
* 既存JSON取り込み処理を壊さないよう分岐追加

### 確認した動作

* `type === "langbench_result"` の場合だけ LangBench Result として扱う構造を確認
* ニュースJSONは従来どおり `items` 配列判定後に既存処理へ渡す構造を維持
* results がない場合でも本文に「results がありません」と表示する構造を確認

### 未対応・今後の検討事項

* ブラウザ上での貼り付け取り込み実操作確認は未実施
* ファイル取り込みでの LangBench JSON 実操作確認は未実施

## 2026-07-12 コードブロックのシンタックスハイライト対応

### highlight.jsの導入方法

* `package.json` のない静的構成と、既存MermaidのCDN読み込み方式に合わせ、jsDelivrの `@highlightjs/cdn-assets` から highlight.js 11.11.1 のブラウザ用ビルドを読み込むようにした
* 対象言語をまとめて利用できるブラウザ用ビルドを採用し、新しいnpm構成やビルド処理は追加していない

### 言語指定あり・なしの処理

* 言語指定ありは `js`、`ts`、`html`、`py`、`sh`、`shell`、`ps`、`md`、`yml` をhighlight.jsの言語名へ変換し、対応言語だけ明示指定で色付けする
* 言語指定なしは `highlightAuto()` を利用し、言語が取得でき、relevanceが2以上の場合だけ色付けする
* Mermaidコードブロックは従来どおり図の専用処理へ渡し、highlight.jsの対象外とした
* IndexedDBやMarkdown本文には判定結果を書き戻さず、プレビューDOMだけを変更する

### 自動判定とフォールバック

* 明示言語がhighlight.jsで未対応の場合は自動判定せず、元のコードをそのまま表示する
* 空コード、自動判定の言語なし、低relevance、ライブラリ未読込、処理例外ではプレーンテキスト表示を維持する
* 1件のハイライト失敗でメモ全体のプレビューを停止しない

### CSSテーマの変更

* Memo NexusのCSS変数へ控えめなコード色を追加し、ライトモードとダークモードで色を切り替えるようにした
* 既存の等幅フォント、改行・空白保持、横スクロール、角丸レイアウトを維持した
* 明示指定されたコードブロックだけ、上部に入力された言語名を表示する

### セキュリティ上の確認事項

* highlight.jsへ渡す前のコードは従来どおり `escapeHtml()` してDOMへ配置し、DOMからは `textContent` で元コードを取得する
* highlight.jsはコードを解析して装飾用spanを返すだけで、コード内のHTML、script、イベント属性、JavaScriptを実行しない
* 例外時は `textContent` へ戻すため、危険なHTMLとして挿入しない
* ブラウザ標準コピーでは装飾spanではなく表示テキストがコピーされ、元のプレーンテキストコードを維持する

### 動作確認したコード

* JavaScript、Python、HTML、CSS、JSONの明示指定
* 言語指定なしのJavaScript風コード
* コメント、文字列、数値を含むJavaScript
* `<script>alert("test");</script>` を含むHTMLコード
* 無効な言語名を持つコード
* Mermaidコードブロックの既存分岐

### 未対応・今後の候補

* コードブロック専用のコピーボタンは追加していない（ブラウザ標準コピーはプレーンテキストで利用可能）
* CDNが利用できないオフライン環境では安全なプレーンテキスト表示へフォールバックする
* 実ブラウザでの端末別の色味調整や、自動判定の閾値調整は今後の候補

## 2026-07-13 コレクション・エクスプローラ

### 実装した機能

* 初期コレクション「歴史」「プログラミング」「IT技術」「技術史」「雑学」「未分類」と、最下部の「ゴミ箱」を追加
* 最大5階層のツリー、子孫を含む通常メモ件数、直下メモのタイトルと「作成 YYYY/MM/DD」表示を追加
* コレクションの作成、インライン名前変更、親変更、同一階層の並び替え、子を含む安全な削除を追加
* 自分自身・子孫への移動、5階層を超える移動、未分類・ゴミ箱配下への移動を拒否
* 選択中コレクションへの新規メモ作成、メニューとドラッグによるメモ移動を追加
* Ctrlクリック、Shiftクリック、Ctrl+A、選択バーからの複数メモ移動・削除を追加
* PCのドラッグ＆ドロップで、行の上端25%を前、下端25%を後、中央50%を子への移動として判定
* メモを論理削除するゴミ箱、元コレクションへの同一ID復元、完全削除を追加
* 全件ZIPとコレクション単位ZIPへコレクション階層、禁止文字置換、同名連番を追加
* 1201px以上は左サイドパネル、1200px以下はオーバーレイドロワーとして表示し、外側クリック・閉じるボタン・Escapeへ対応
* ライト・ダークテーマの既存CSS変数を再利用し、選択・ドロップ先を背景、枠、下線で表示

### 変更ファイル

* `app.js`: データ構造、migration、CRUD、ツリー、移動、複数選択、ゴミ箱、ZIP処理
* `index.html`: 開閉ボタン、エクスプローラ、操作メニュー、選択バー、移動ダイアログ
* `style.css`: サイドパネル／ドロワー、ツリー、選択・ドラッグ状態、テーマ対応
* `LOG.md`: 本項目

### IndexedDBと既存メモの移行

* DB名 `memo-nexus` と既存 `notes` object storeは維持し、DB versionを1から2へ更新
* `collections` object store（keyPath `id`）と `parentId` / `sortOrder` indexだけを追加し、既存storeを削除・再作成していない
* 起動時に `collectionId` がない、存在しない、または `deletedAt` が未定義の既存メモだけをトランザクションで補完
* 既存メモのID、タイトル、本文、日時は変更せず、`collectionId: "system-unclassified"` と `deletedAt: null` を追加

### データ構造とゴミ箱

* Collectionは `{id,name,parentId,sortOrder,isSystem,createdAt,updatedAt}` を保存
* Memoは既存構造へ `{collectionId,deletedAt}` を追加し、名前変更時にメモ側を書き換えないID参照とした
* ゴミ箱移動は元の `collectionId` を保持して `deletedAt` のみ設定し、復元時に元コレクションがなければ未分類へ戻す
* 完全削除時だけ `notes` レコードを削除し、新規IDは従来どおり `crypto.randomUUID()` で生成

### エクスポート仕様

* 全件エクスポートはゴミ箱を除外し、各メモをコレクション階層内のMarkdownとして出力
* コレクション単位では選択コレクションと全子孫を対象とし、`Memo-Nexus_コレクション名_YYYY-MM-DD.zip` を生成
* 既存実装に添付ファイル保存機構がないため、今回もMarkdownのみを対象とした

### 確認結果

* `node --check app.js` と `git diff --check` が成功
* 実ブラウザでversion 1の既存DBを開き、既存3件が保持され未分類へ表示されることを確認
* 初期6コレクションとゴミ箱、件数、作成日、インライン作成・名前変更を確認
* 選択中の新規コレクションへ新規メモが入り、件数が1になることを確認
* メモの「コレクションへ移動」ダイアログから未分類へ移動し、完了通知が出ることを確認
* 起動時と上記操作時にブラウザコンソールのerror / warningがないことを確認
* 既存の編集、Markdownプレビュー、Mermaid、highlight.js、関連メモが同じ画面で描画されることを確認

### 未実施・既知の制約

* HTML5ドラッグ＆ドロップ、Ctrl/Shift複数選択、ゴミ箱復元、完全削除、ZIPダウンロードの端末別実操作はコード経路と構文のみ確認し、全組み合わせの手動操作は未実施
* 閉じたコレクションへのドラッグ中の自動展開は、初期実装の安定性を優先して未実装
* スマートフォンの複数選択・複数ドラッグは未実装。単体メニュー移動は利用可能
* 空のコレクションはZIP内に空フォルダとしては出力されない

## 2026-07-13 v0.4.0 "Waypoint" README更新

### 変更内容

* アプリのバージョン名を `Collection Explorer` から `Waypoint` へ変更
* バージョン番号は `v0.4.0` を維持
* README冒頭へ現在バージョンとMemo Nexusの概要を追加
* コレクションの基本操作、最大階層、複数選択、ゴミ箱、階層ZIP、レスポンシブ表示をREADMEへ記載
* 静的ファイル識別子を `v0.4.0-2` へ更新

### 変更ファイル

* `app.js`
* `index.html`
* `README.md`
* `LOG.md`

## 2026-07-14 コレクション内メモの作成日順並べ替え

### 変更内容

* コレクション・エクスプローラ上部に「作成日：新しい順／古い順」の小型選択UIを追加
* 初期値を「新しい順」とし、コレクション直下のメモだけを `createdAt` で並べ替えるようにした
* 表示用コピーを作成して並べ替え、元のメモ配列やIndexedDB内の保存順を変更しない構造にした
* 作成日時が同じメモは既存のメモIDを第2比較条件として、再描画時の順序を安定させた
* 作成日がない、空、または不正なメモは、並べ替え方向にかかわらず末尾へ表示するようにした
* 選択した並び順をアプリ全体共通のUI設定として `localStorage` の `memo-nexus-collection-sort` に保存
* 保存値が未設定または不正な場合は「新しい順」へ戻すようにした
* 未分類は既存のコレクション描画処理を共有しているため同じ並べ替えを適用し、ゴミ箱は既存順を維持
* 全メモ一覧、検索結果、関連メモ、エクスポートなど、コレクション内メモ一覧以外の処理は変更していない
* 更新したCSSとJavaScriptを再取得させるため、静的ファイル識別子を `v=0.4.0-3` へ更新

### 変更ファイル

* `app.js`: 設定の復元・保存、作成日の比較、コレクション直下メモの表示用並べ替え
* `index.html`: 並べ替え選択UI、静的ファイル識別子
* `style.css`: 選択UIのライト・ダークテーマおよび狭い画面向け表示
* `LOG.md`: 本項目

### 確認結果

* 初期状態で作成日の新しいメモが上に表示されることを実ブラウザで確認
* 「古い順」へ変更すると表示順が反転することを実ブラウザで確認
* 再読み込み後も選択した「古い順」が維持されることを確認し、検証後は「新しい順」へ戻した
* 合成データで非破壊ソート、作成日欠損・不正値の末尾配置、同一作成日時のIDによる安定化を確認
* PC表示と狭い画面で選択UIが収まり、狭い画面で横方向のオーバーフローが発生しないことを確認
* ダークモードで選択UIの背景、文字、枠が既存テーマ色を使用し、読めることを確認
* ブラウザコンソールにerrorがないことを確認
* `node --check app.js` と `git diff --check` が成功

### 未実施・既知の制約

* 新規メモ作成、編集、削除、コレクション移動を伴う保存データ変更操作は、既存データを変更しないため実ブラウザでは未実施
* 実データへ作成日欠損メモを追加する確認は行わず、合成データによる比較関数の確認に留めた

## 2026-07-15 メモ単体・コレクションローカルエクスポート

### 実装内容

* 編集画面の三点ボタンから、現在のメモを元のMarkdown本文のまま `.md` ダウンロードできる機能を追加
* File System Access API対応環境では、メモ単体を選択したローカルフォルダへ直接保存できる機能を追加
* コレクションの既存エクスポートメニューへ、従来のZIPダウンロードとローカルフォルダ書き出しを選ぶダイアログを追加
* 選択コレクションをルートとし、子孫コレクションを実フォルダ、通常メモをMarkdownファイルとして書き出す処理を追加
* 空の子孫コレクションもローカルフォルダとして作成し、ゴミ箱内のメモは対象外とした
* 保存先に同名項目が存在する場合はルートフォルダを作成せず、ダイアログ内でローカル保存名を変更できるようにした
* 書き込み権限の確認、フォルダ選択キャンセル、部分的なファイル／フォルダ作成失敗の結果表示を追加
* File System Access API非対応環境ではローカル保存ボタンを非表示にし、通常ダウンロードを維持
* 既存の全メモZIPは従来どおりZIPダウンロードのみとし、コレクションZIPの生成処理も維持

### ファイル名とデータの扱い

* Windows禁止文字、制御文字、末尾のピリオド／空白、`CON`、`PRN`、`AUX`、`NUL`、`COM1`〜`COM9`、`LPT1`〜`LPT9`を安全化
* 空タイトルのメモは `無題のメモ.md`、空のローカル保存名は入力エラーとした
* 同一フォルダ内で同名または大文字・小文字だけが異なるメモは `メモ.md`、`メモ (2).md`、`メモ (3).md` の形式で上書きを回避
* 異なるフォルダ内の同名メモは連番を付けず、それぞれのパスへ出力
* 同じ `memo_id` のデータが異なるコレクションパスに現れた場合も重複排除せず、各パスへ独立したファイルとして出力
* 元のMarkdown本文へ見出しやYAMLフロントマターを追加せず、`memo_id`、所属情報、IndexedDBレコードを変更しない
* 未保存の実編集がある場合だけ既存保存処理を完了し、エクスポートだけでは更新日時を変更しないよう `flushSave()` の不要な再保存を抑止

### 仕様上の判断

* ローカル書き出しは同期や差分更新ではなく、実行時点のスナップショット
* 既存の同名ルートフォルダへの上書き、統合、自動連番、削除、差分更新は行わない
* ローカル保存名の変更は書き出し先だけに適用し、Memo Nexus内のコレクション名は変更しない
* メモ単体を既存ファイルと同名で保存する場合は、既存ファイルを上書きせず同一フォルダ内で括弧付き連番を付ける
* 保存途中で失敗しても、書き込み済みファイルは削除せず、成功件数と失敗パスを表示する

### 将来ヘルプへ記載する内容

* ローカル保存は同期ではなく、ローカルで編集してもMemo Nexusへ反映されない
* 同じメモが複数の保存先パスに現れる場合、複数の独立したファイルとして書き出される
* 全メモ一括はZIPダウンロードのみ
* コレクションはZIPダウンロードとローカルフォルダ書き出しに対応する
* メモ単体は通常ダウンロードと、対応環境でのローカル保存に対応する
* File System Access API非対応環境では通常ダウンロードを利用する
* 同名のコレクションフォルダが保存先にある場合は上書きせず書き込みを中止する
* ローカル保存名を変更してもMemo Nexus内のコレクション名は変わらない

### テスト結果

* `node --check app.js` が成功
* `node --check export-utils.js` が成功
* `node --test --test-isolation=none export-utils.test.js` が6件成功
* 自動テストでWindows向け名前安全化、予約名、大小文字を含む同一フォルダ内連番、異なるフォルダの同名許容、階層パス、ゴミ箱除外、同一IDの別パス出力、同名ルート衝突、File System Access API機能判定を確認
* `git diff --check` が成功
* 実ブラウザでメモ単体ダイアログ、コレクションダイアログ、保存方法の表示、ローカル保存名だけを編集するUI、空白名の拒否を確認
* メモ単体のダウンロード操作がエラーなく完了してダイアログが閉じることを確認。ただし自動操作環境では生成ファイルの実体確認は未実施
* 狭い表示幅でページとエクスポートダイアログが横方向へあふれず、ブラウザコンソールにerror / warningがないことを確認

### 未実施・既知の制約

* ネイティブのフォルダ選択画面を伴うローカル実ファイル書き込み、権限拒否、途中の権限喪失、実ファイルシステム上の同名ルート衝突は自動操作環境では未実施
* File System Access API非対応時の判定は自動テストとコード経路で確認し、Safari／iPhone実機確認は未実施
* 全メモZIPとコレクションZIPの端末別ダウンロード、通常の編集・削除・移動操作の回帰確認は構文、差分、既存コード経路の確認に留めた
* 全メモのローカルフォルダ書き出し、自動／差分／双方向同期、ローカル編集の再取り込み、既存フォルダ統合、ヘルプ画面は実装していない

## 2026-07-15 画像・PDF添付機能

### ブランチ

* `feature/image-pdf-attachments`

### 実装概要

* 画像とPDFをメモ本文から分離し、メモカード下部の添付エリアで追加、閲覧、削除できる機能を追加
* JPEG/JPG、PNG、WebP、PDFだけをMIMEタイプと拡張子の両方で許可し、SVGや偽装された未対応形式を拒否
* ファイル選択、複数選択、PCのドラッグ＆ドロップ、画像を含むクリップボード貼り付けに対応し、通常のテキスト貼り付けは既存動作を維持
* 複数ファイルは全件の検証・画像処理・容量計算が成功してから1トランザクションで保存し、途中失敗や上限超過時の一部保存を回避

### IndexedDBの変更

* DB名 `memo-nexus` と既存の `notes` / `collections` object storeを維持し、DB versionを2から3へ更新
* `attachments` object store（keyPath `id`）と `memoId` / `createdAt` indexを追加し、既存storeは削除・再作成していない
* 添付レコードは安定したUUIDで識別し、`memoId`、`kind`、`fileName`、`mimeType`、`size`、`blob`、画像寸法、作成日時を保存
* ゴミ箱移動と復元では添付を保持し、完全削除時だけメモと関連添付を同じIndexedDBトランザクションで削除

### 画像圧縮とPDF

* 画像は追加時にCanvasで縦横比を維持して長辺最大1800pxへ縮小
* JPEG/WebPは品質0.86、PNGはPNG形式と透明度を維持し、縮小不要で変換後の方が大きい場合は元画像Blobを採用
* `createImageBitmap`では画像向きを考慮し、利用できない環境ではImage要素へフォールバック。圧縮・デコード失敗時は保存しない
* PDFはMIMEタイプ、拡張子、先頭付近の `%PDF-` シグネチャを確認し、再圧縮せず元Blobを保存
* PDF閲覧時はユーザー操作内でObject URLを作りブラウザ標準ビューアを開く。同一PDFの古いURLを解放し、未使用URLはタイマーまたはページ終了時に解放

### 容量上限とUI

* 1メモあたりの添付合計上限を `20 * 1024 * 1024` bytesとして定数管理
* 画像は圧縮後、PDFは元データの容量を既存添付と合算し、20MBちょうどを許可して超過時は現在量、追加量、合計、上限、超過量を表示
* 添付エリアに件数、使用容量、20MB上限、追加ボタン、ドロップ領域、画像サムネイル、PDFカード、ファイル名、容量、開く、削除を追加
* 長いファイル名は省略しつつtitle属性で全文を確認可能とし、ボタンのラベル、フォーカス表示、画像モーダルのEsc終了、スマートフォン幅の折り返しに対応
* 添付削除時は確認後にIndexedDBレコードとObject URLを削除し、一覧、件数、使用容量を再計算

### エクスポート変更

* 添付付きメモは `メモ名/メモ名.md` と `メモ名/attachments/ファイル` の構成で出力し、添付なしメモの従来パスと本文を維持
* メモ単体の通常ダウンロードは添付なしなら従来のMarkdown、添付ありならZIPとし、ローカル保存も同じ構成へ対応
* コレクションZIP、全件ZIP、コレクションのローカルフォルダ書き出しへ画像・PDF Blobを含めるように変更
* 出力Markdownにだけ画像・PDFの相対参照を追加し、アプリ内の元Markdown本文は変更しない
* Windows向けのファイル名安全化を維持し、同名添付には `_2`、`_3` の連番を付けてMarkdown参照と実ファイル名を一致

### テスト内容と結果

* `node --check app.js`、`node --check export-utils.js`、`node --check attachment-utils.js` が成功
* `node --test --test-isolation=none export-utils.test.js attachment-utils.test.js` が12件成功
* JPEG/PNG/WebP/PDFの形式判定、MIME・拡張子不一致とSVG拒否、20MBちょうどと1byte超過、複数追加容量、同名添付連番、添付付き出力パスとMarkdown参照、Blob保持、添付なしの既存出力を自動テストで確認
* `git diff --check` が成功
* 実ブラウザでPNGをクリップボードから添付し、本文へ画像データが入らないこと、件数・圧縮後容量・サムネイルが表示されることを確認
* 通常テキストの貼り付けでは本文だけが更新され添付件数が増えないこと、ページ再読み込み後も画像添付が復元されることを確認
* 画像サムネイルの拡大ダイアログとEsc終了、添付付きメモのZIP生成経路、設定画面の `notes / collections / attachments` 表示を確認
* 1000pxの縦長表示と390pxのスマートフォン相当幅で横方向のオーバーフローがなく、サムネイルの `object-fit: contain`、操作ボタン高38pxを確認
* ダークテーマで添付セクション、添付カード、ファイル名が既存テーマ色へ切り替わり、ブラウザコンソールにerror / warningがないことを確認

### 既知の制限・今後の課題

* Memo Nexusの添付付きZIPを復元するインポートは未実装。既存取り込みはAI朝刊向けJSON/Markdown/テキストのままとし、Base64をJSONへ埋め込む方式は採用していない
* Markdown本文中への画像ブロック挿入、PDF.js、PDFテキスト抽出、OCR、画像編集、カード全体のPDF保存は未実装
* PDF表示はブラウザ標準ビューアに依存する。Safari/iPhone実機、端末固有のストレージ上限・メモリ不足、大容量画像の処理時間は継続確認が必要
* Chrome拡張のローカルファイルアクセス権限が無効だったため、PDF/JPEG/WebPの実ファイル選択、PDF標準ビューアの起動、ドラッグ＆ドロップ、複数ファイル同時選択、実ブラウザでの添付削除・完全削除はコード経路と自動テスト確認に留めた
* 添付付きZIPは生成処理完了とダイアログ終了まで確認したが、自動操作環境ではダウンロードファイルの実体展開確認は未実施

## 2026-07-15 本文内画像参照の修正

### 原因と修正方針

* 従来の画像貼り付けは画像Blobを添付ストアへ保存するだけで、本文への永続参照挿入とカード本文内での画像描画がなかった
* 画像ファイルを取得できない貼り付けや本文へのファイルドロップではブラウザ既定処理が残り、クリップボード由来の一時的な `blob:` URLが文字列として本文へ入る経路があった
* IndexedDBの既存 `attachments` store、画像圧縮、20MB容量判定を維持し、本文には `![代替テキスト](attachment://画像ID)` だけを保存する方式へ修正

### 実装内容

* 画像貼り付け時のカーソル位置を保存前に取得し、画像の圧縮・形式検証・容量判定・添付保存が成功した後に永続ID参照を挿入
* 本文へのファイルドロップも既存添付処理へ統合し、画像だけ本文参照を挿入。PDFは従来どおり添付として保存し、本文内表示は追加していない
* 一時的な `blob:` 文字列だけが貼り付けられた場合は本文への挿入を中止し、画像データをコピーし直す案内を表示
* 参照記法の前後へ必要な改行を補い、直後のMarkdown見出し・箇条書きなどを壊さないようにした
* 既存の画像参照記法内部へカーソルがある場合は参照の直後へ挿入位置を補正し、複数画像の貼り付けで参照文字列が分断されないようにした
* Markdownインライン解析へ `attachment://` 専用トークンを追加し、許可した永続IDだけを安全なプレースホルダーとして生成
* カード描画時に現在のメモの画像添付だけをID照合し、Blobから作ったObject URLを本文画像と添付サムネイルで再利用
* Object URLはメモ切り替え、添付削除、ページ終了時に既存処理で解放し、同じ画像の複数箇所参照にも対応
* 本文参照中の画像を添付欄から削除する場合は警告し、画像が存在しない参照には「画像を表示できません」を表示
* エクスポート時に本文内の `attachment://画像ID` を実際に出力する `attachments/ファイル名` へ変換し、本文位置と代替テキストを維持

### テスト結果

* `node --check app.js`、`node --check export-utils.js`、`node --check attachment-utils.js` が成功
* 自動テストで永続ID参照、blob URL非使用、先頭・中間・末尾・複数挿入、既存参照内部への挿入防御、参照ID抽出、エクスポート相対パス変換を確認
* 実ブラウザでPNGを本文の先頭・中間・末尾へ貼り付け、3件の異なる永続ID参照と3画像が記述順に表示されることを確認
* 画像の前後に通常文とMarkdown見出しを置き、見出し・本文・画像がカードで維持されることを確認
* 再読み込み後に参照3件と画像3件が復元され、別メモへ切り替えて戻った後も画像が再表示されることを確認
* 本文保存値の `blob:` が0件であること、一時blob URL貼り付けが拒否されること、通常テキスト貼り付けが維持されることを確認
* 存在しない画像IDでは他の画像やカード全体を壊さず「画像を表示できません」と表示されることを確認
* 本文画像の `max-width: 100%` と `height: auto`、ブラウザコンソールにerror / warningがないことを確認

### 既知の制限

* textarea内のドロップ座標から文字位置を安全に算出する共通APIがないため、本文への画像ドロップは現在のカーソル位置へ参照を挿入する
* PDFの本文内埋め込み、プレビュー、画像化は対象外で、従来の添付欄とブラウザ標準PDFビューアを維持

## 2026-07-15 添付容量判定の並行実行対策

* 同一メモへの添付追加をメモID単位のキューで直列化し、容量取得・20MB上限判定・IndexedDB保存が並行しないように修正
* ファイル選択、クリップボード貼り付け、添付欄へのドロップ、本文へのドロップが共有する `handleAttachmentFiles` 全体を直列化
* 待機中を含む処理件数をメモ単位で管理し、処理中の追加ボタン・ファイル入力・ドロップ領域を無効化。メモ切り替え後は表示中メモの処理状態だけを反映
* 同一メモへ12MBと9MBを連続追加した場合、1回目だけを許可し、合計20MBを超える2回目を拒否する自動テストを追加
* JavaScript構文確認、全18件の自動テスト、`git diff --check` が成功

## 2026-07-16 コードブロックの右端折り返し対応

* Markdownプレビューのコードブロックを `white-space: pre-wrap` とし、実改行、空白、タブ、インデントを維持したまま表示領域の右端で折り返すように変更
* 長いURL、ファイルパス、JSON文字列、空白を含まない識別子も親要素を突き抜けないよう `overflow-wrap: anywhere` を使用し、コードブロックの横スクロールを抑止
* 折り返しはCSSによる表示上の変更だけで、Markdown本文、IndexedDBの保存値、DOMのテキスト内容、コピーされるコードには改行や空白を追加しない
* highlight.jsのDOMと色分け処理は変更せず、言語指定、自動判定、素のコード表示を維持
* PC通常幅、画面半分程度の縦長表示、スマートフォン幅で長いJavaScript、URL、パス、JSON、識別子、日本語コメント、タブ・スペースを確認。コードブロックは親幅とビューポート幅の小さい側へ制限し、コード追加前よりページ全体の横スクロール幅が増えないようにした
* JavaScript構文確認、コードブロック折り返しのCSS契約テストを含む全21件の自動テスト、`git diff --check` が成功
* 折り返し行だけの字下げは、CSS標準だけでは自動折り返し位置と実改行を安全に区別できず、文字幅測定や行分割・DOM再構築が必要になるため見送り
* 折り返し記号は今回実装していない。将来追加する場合もコピー内容と保存データを変えない表示専用方式の検討が必要
* ブラウザのモバイル幅上書きでは、コード表示前から既存ツールバーの最小幅によるページ横スクロールが残ることを確認。今回のコードブロックは横幅を追加しておらず、既存ツールバーのレスポンシブ改善は別課題
* 設定画面の「表示」へ「コードブロックの折り返し」を `Coming soon` 項目として追加。設定UIや保存値は追加せず、現在の常時折り返し仕様を維持

## 2026-07-16 関連メモの右サイドパネル化

* 本文・カードの表示幅を圧迫していた関連メモの固定カラムを廃止し、画面右端から重ねて表示する幅340px（モバイルは最大90vw）の右ドロワーへ変更
* 初期状態とメモ切り替え時は閉じた状態とし、右端の件数付きボタン、パネル内の閉じるボタン、外側タップ、Escキーで開閉できるようにした
* 閉じた後の開閉ボタンへのフォーカス復帰、`aria-expanded`・`aria-controls`・`aria-hidden`・`inert`、44px以上の操作領域を追加
* パネル表示中は背景スクロールを抑止し、関連メモと新発見の領域だけをスクロール可能にした。200msのスライドと `prefers-reduced-motion` 時のアニメーション抑止にも対応
* 関連メモの抽出、関連度、並び順、最大8件の表示は変更せず、件数0の場合は実際の生成ルールに沿った案内を表示
* `node --check` 4件、関連ドロワーの契約テストを含む全25件の自動テスト、`git diff --check` が成功
* PC通常幅、狭いPC、390pxスマートフォン、375px iPhone相当で、オーバーレイ表示、件数、全閉じ方、関連メモ選択後の自動クローズ、本文幅が変わらないこと、内部スクロール、フォーカス復帰を確認
* iPhone Safariは実機ではなくChromeのレスポンシブ表示による相当確認のため、実機固有のタッチ・スクロール挙動は今後の確認事項

## 2026-07-16 関連メモの表示件数案内

* 右端ボタンの全候補件数とドロワー内の最大8件表示の違いを明確にするため、候補が表示件数を超える場合だけ一覧直前へ「全件数中、表示件数表示」を追加
* 文言は `allRelated.length` と `related.length` から生成し、例として候補9件では「9件中、8件表示」と表示。最大表示件数8件、`findRelated`、関連度、並び順、抽出条件は変更していない
* 候補が8件以下では案内を空にして非表示とし、0件時は従来の「関連メモはありません。」案内を維持
* 補助文字色 `var(--muted)`、12px、折り返しなしの控えめな表示とし、PCで候補9件・表示8件・右端ボタン9件を確認
* JavaScript構文確認、関連ドロワーテストを含む全25件の自動テスト、`git diff --check` を実行

## 2026-07-16 本文内画像ブロックの表示拡張

### 実装概要

* 設定画面の「表示」へ「カード内画像サイズ」を追加し、`small`（小）・`medium`（標準）・`large`（大）をアプリ全体へ適用
* 初期値と不正値のフォールバックを `medium` とし、`memo-nexus-image-block-size` のキーで既存テーマ設定と同じくlocalStorageへ保存
* 小は最大高200px、標準は350px、大はカード内かつ最大 `min(620px, 70vh)` とし、すべて `max-width: 100%`、`height: auto`、`object-fit: contain` で縦横比と画像全体を維持
* 1枚＋説明文では小を35:65、標準を50:50の横並び、大を上下配置とし、画像ブロック幅560px以下ではすべて上下配置へ変更
* 2枚ではサイズに関係なく画像を2列、共通説明文を下へ配置し、画像ブロック幅560px以下では画像1・画像2・説明文の縦並びへ変更
* 画像ブロックの操作へ2枚目追加、左右入れ替え、各画像参照を外す操作、共通説明文の追加・編集を追加。2枚時は追加操作を非表示とし、2枚から1枚へ戻しても説明文を維持
* 本文画像から既存の画像プレビューダイアログを開けるようにし、添付欄、PDF、画像圧縮、容量上限、Blob、画像ID、添付IDは変更していない

### 説明文と保存形式

* 説明文は改行、太字、斜体、インラインコード、HTTP/HTTPS・メール・ページ内リンク、箇条書きだけを安全なHTMLへ変換
* HTML、JavaScript、別画像、PDF、Mermaid、コードブロック、表、見出しは解釈せず、エスケープした通常テキストとして表示
* 保存形式は標準Markdownの画像参照と説明文を残し、画像ブロックの開始・説明文境界・終了だけを `<!-- memo-nexus:image-block -->`、`<!-- memo-nexus:image-caption -->`、`<!-- /memo-nexus:image-block -->` で示す方式を採用
* HTMLコメントは一般的なMarkdownビューアでは非表示となり、画像参照・順番・説明文自体は通常のMarkdownとして確認できる。コードフェンス内の同じ文字列とは衝突しない
* 既存の単独 `![alt](attachment://ID)` は変換せず、説明文なしの1枚ブロックとして遅延解釈。編集、2枚目追加、入れ替え時だけ新形式へ保存
* 新規に3枚以上を同時挿入した場合は、最大2枚の画像ブロックへ順番どおり分割

### 変更した主なファイル

* `app.js`: 設定保存、画像ブロック描画、編集操作、既存添付追加・画像プレビューとの接続
* `attachment-utils.js`: サイズ正規化、画像ブロック解析・直列化・置換、安全な説明文Markdown描画
* `index.html`: サイズ選択UI、説明、2枚目追加用画像入力、キャッシュ更新
* `style.css`: サイズ別、1枚・2枚、説明文、コンテナクエリ、編集UIのスタイル
* `attachment-utils.test.js`, `image-block-layout.test.js`: 保存形式、後方互換、安全性、操作、CSS・設定契約のテスト

### 実行したテスト

* `node --check app.js`、`node --check attachment-utils.js`、`node --check export-utils.js`、各テストファイルの構文確認
* `node --test --test-isolation=none export-utils.test.js attachment-utils.test.js code-block-wrap.test.js related-drawer.test.js image-block-layout.test.js`
* サイズの初期値・不正値フォールバック、画像1枚・2枚、最大2枚分割、説明文あり・なし、順番入れ替え、2枚から1枚、既存形式、コードフェンスとの非衝突、安全なMarkdown、既存エクスポート参照変換、添付・PDF関連の既存テストを確認
* `git diff --check`

### 手動確認

* Chromeで小200px、標準350px、大の最大高、画像の自然な縦横比、1枚＋説明文の横並び・上下配置を確認
* 2枚目追加後に2列表示、追加ボタン非表示、共通説明文の下配置、左右入れ替え、保存・再読み込み後の順番維持を確認
* 2枚から1枚へ戻した後も説明文が残り、添付件数と元データが維持されることを確認
* 太字、リンク、箇条書き、複数行と `<script>` 入力を確認し、スクリプト要素が生成・実行されず通常テキストになることを確認
* 390px相当の狭幅で画像2枚が縦並びとなり、画像・説明文・ページに横方向のはみ出しがないことを確認
* 本文画像クリックで既存画像プレビューダイアログが開き、クリーンな再読み込み後のコンソールにerror / warningがないことを確認

### 今回実装しなかった事項・残した課題

* トリミング、回転、色調補正、フィルター、画像への文字入れ、個別サイズ・個別説明文、3枚以上の1ブロック配置、自由配置、ドラッグ操作は実装していない
* 添付欄の表示、PDF本文埋め込み、画像圧縮・解像度変更、添付の自動削除方針、通常本文の回り込みは変更していない
* エクスポートMarkdownでは従来どおり `attachment://ID` を `attachments/ファイル名` へ変換し、画像と説明文は外部ビューアで確認できる。一方、既存の取り込み機能は添付付きZIPの復元に未対応のため、エクスポートZIPからBlobと内部IDを再構築する往復インポートは今後の課題
* Safari／iPhone実機でのコンテナクエリ、ファイル選択、タッチ操作は未確認

## 2026-07-16 PR #15 画像ブロック操作の追補修正

* 左右入れ替え、画像参照を外す、説明文保存、2枚目追加では、本文とUndo履歴を更新した直後にプレビューを再描画し、同じ操作の保存予約では重複描画しないようにした
* 通常時は画像と説明文だけを表示し、追加・入れ替え・説明文編集・画像参照を外す操作を小さな「…」メニューへ収納。PCのhover／focus、タップ、Enter／Space、Escに対応した
* 説明文エディタ表示中は通常操作メニューを非表示にして操作領域を差し替え、保存後は再描画、キャンセル後は状態復元により通常操作へ確実に戻すようにした
* 2枚目追加前と添付保存後に対象ブロックを再確認し、保存後の追加失敗では今回作成した添付IDだけをIndexedDBと表示中添付一覧からロールバックするようにした。既存添付は削除しない
* 既存形式の画像参照が2行連続していても自動ペア化せず、それぞれ独立した1枚画像ブロックとして解釈するようにした。2枚ブロックは明示的な `memo-nexus:image-block` 形式に限定した
* 状態変化を検証するテストとして、即時再描画の実行順、説明文編集時の操作領域差し替えとキャンセル復元、連続する既存画像の分離、保存後失敗時の新規添付だけのロールバックを追加した
* Chromeで1枚・2枚、説明文あり、2枚目追加、左右入れ替え、画像参照削除、説明文の保存・キャンセル、キーボードとタップによるメニュー操作、再読み込み後の保持を確認した
* Chromeの375px相当で2枚画像が縦並びになり、文書幅・画像ブロック・操作メニューが横方向へあふれないことを確認した
* 手動確認中に「…」ボタンと既存の関連メモ開閉ボタンが重なる配置を検出したため、操作入口を画像ブロック左上へ配置して衝突を解消した
* 変更前に対象ブロックが失われたケースでは新しい添付を保存しないこと、保存後の対象変更は自動テストで新規添付だけを削除することを確認した
* 画像Blob、既存添付ID、圧縮、PDF、添付欄、エクスポート処理は変更していない
* `node --check`、既存・新規を含む全40件の自動テスト、`git diff --check` が成功した

## 2026-07-17 レスポンシブ編集レイアウト

* `html` をレイアウト幅の基準となるCSSコンテナにし、実用最小幅（メモ一覧300px、本文320px、カード280pxと余白）からWide 1040px以上、Compact 720〜1039px、Mobile 719px以下の3モードを採用した。小数CSSピクセルでもJSとCSSの境界がずれないよう、Container Query側は719.98px／1039.98pxに余裕を持たせた
* Wideでは従来のメモ一覧・本文・カードの3ペインを維持し、主要操作と現在のコレクション表示を上部へまとめた。メモ、本文、カード、カード内部のレンダリングは複製していない
* Compactではメモ一覧を左オーバーレイドロワーへ収納し、本文とカードをデフォルト表示した。カード収納時はカードを非表示にして本文へ空き幅を割り当て、再表示で2ペインへ戻す一時状態を追加した
* Mobileでは本文を主領域とし、メモ一覧を左、カードを右のオーバーレイドロワーへ配置した。背景タップ、Esc、閉じるボタン、左右同時オープン防止、背景操作抑止、フォーカス移動に対応した
* メモ一覧は選択中コレクションのメモに絞り込み、通常のメモ選択後はドロワーを自動的に閉じる。コレクション選択、設定、保存状態、主要操作は全モードで上部に維持した
* レイアウト状態はメモ一覧、Mobileカード、Compactカードを分離したメモリ上の状態だけで管理し、モード変更時はWide=全ペイン、Compact=本文＋カード、Mobile=本文のみへ戻す。IndexedDBスキーマ、メモ・添付データ、カード描画、関連メモは変更していない
* ドロワーは160msのスライドとし、`prefers-reduced-motion` ではアニメーションを無効化した。本文・カード内部だけがスクロールし、320〜1440pxで文書全体の横スクロールが発生しないことを確認した
* 自動確認は `node --check app.js`、既存5ファイルと新規 `responsive-layout.test.js` の全44件、`git diff --check` が成功した。一括 `node --test` は実行環境の子プロセス起動制限（EPERM）のため、各テストファイルを単一プロセスで実行した
* Chromeで320、375、390、430、719、720、900、1039、1040、1440pxを確認した。Compactのカード収納で本文幅が496pxから876pxへ拡張し、再表示で元へ戻ること、Mobileの背景タップ・Esc・排他開閉、境界前後のモード一致、ライト／ダークの背景・境界・文字・オーバーレイ、コンソールにerror／warningがないことを確認した
* iPhone／iPadなどの実機Safari、ソフトウェアキーボード表示中の追従、実機固有のタッチスクロールは未確認事項として残した

## 2026-07-17 PR #18 レビュー指摘対応

* 上部操作バーに置いていた「すべてのメモ」／コレクション名表示を削除し、メモ一覧の見出し内へ表示範囲ラベルを移した。Compact／Mobileの上部にはメモ一覧・カードの開閉操作だけを残し、長いコレクション名由来の省略記号が上部へ単独表示されないようにした
* `main` の既存実装を確認し、左メモ一覧はコレクション選択では絞り込まず、未削除の全メモを表示する仕様へ戻した。コレクションごとの直接所属メモと子孫を含む件数は、従来どおりコレクション・エクスプローラ内だけで扱う
* ゴミ箱選択時だけ左メモ一覧を削除済みメモへ切り替え、表示範囲ラベルも「ゴミ箱」として内容と一致させた。通常、通常コレクション、未分類では「すべてのメモ」と未削除全件を維持する
* 一覧判定を `memo-list-utils.js` の副作用がない関数へ分離し、通常、通常コレクション、未分類、ゴミ箱、長いコレクション名についてラベルとメモIDの一致をデータテストで確認した
* レスポンシブ契約テストへ、表示範囲ラベルがサイドバー内にあり上部ヘッダーには存在しないこと、719px=Mobile、720px=Compact、1039px=Compact、1040px=Wideとなる実関数の境界テストを追加した
* `node --check` 2件、既存テストを含む全50件、`git diff --check` が成功した。ブラウザのWide表示でラベルがメモ一覧見出し内にあり、主要操作バーに「すべてのメモ」が残らず、コンソールにerror／warningがないことを確認した

## 2026-07-17 PR #18 表示整理の追補

* メモ一覧見出し内の小さな「すべてのメモ」表示と `memoListScopeLabel`、専用描画処理、CSSを削除し、通常時は見出し「メモ一覧」だけを表示する構成へ戻した
* ゴミ箱選択時は同じ見出しを「ゴミ箱」へ切り替え、サイドバーの `aria-label` も同期した。削除済みメモだけを左一覧へ出す処理は維持した
* Wide／Compactで右寄せになっていた主要操作ツールバーを、ペイン操作ボタンの後ろから左寄せで並ぶよう変更した。＋、コレクション、Today、Undo、ZIP、語句、Graph、設定、Delの順序と、狭幅時のツールバー内部横スクロールは維持した
* データテストで通常時「メモ一覧」＋未削除メモ、ゴミ箱時「ゴミ箱」＋削除済みメモの一致を確認し、構造テストで不要ラベルの不在、主要操作の順序、左寄せ、内部スクロールを確認した
* Chromeで390px Mobile、900px Compact、1040px Wideを目視確認し、719px=Mobile、720px=Compact、1039px=Compact、1040px=Wide、文書全体の横スクロールなしを再読み込み後に確認した。ゴミ箱選択操作でも見出しと0件の一覧が一致し、コンソールにerror／warningはなかった

## 2026-07-18 JSON貼り付け取り込み成功後の入力欄クリア

* `importPastedItNewsJson()` の既存取り込み処理、再描画、作成メモ表示、ダイアログ終了、成功通知がすべて正常に完了した後だけ、JSON貼り付け入力欄を空にするようにした
* 空欄、不正なJSON、メモ保存失敗では入力内容を維持し、既存の案内・エラー表示から修正して再実行できる動作を保った
* JSONファイル取り込み、保存形式、IndexedDBのデータ構造、成功通知、ダイアログを開いた際の入力欄フォーカスは変更していない
* `json-import-input.test.js` を追加し、成功時の入力欄クリアと通知、空欄・構文エラー・保存失敗時の入力保持を確認した
* `node --check app.js`、`node --check json-import-input.test.js`、既存テストを含む全54件、`git diff --check` が成功した

## 2026-07-20 記法ガイド

* メモタイトルと本文入力欄の間へ細いエディタ操作列を追加し、上部の主要操作を増やさずに「記法ガイド」を開けるようにした
* 専用の `dialog` にMarkdown、コードブロック、Mermaidの3区分を追加し、閉じるボタン、Esc、dialog標準の閉じる動作と `aria-expanded` の同期に対応した
* Markdownは現在のプレビューが対応する見出し1〜3、太字、インラインコード、箇条書き、引用、Wikiリンクだけを掲載した。未対応の斜体、取り消し線、番号付きリスト、チェックボックス、通常リンク、水平線、表は掲載していない
* JavaScriptコードブロックの完成例、言語名を省略した場合のhighlight.js自動判定、プレーン表示、非実行の説明と、既存の9種類の言語短縮名を掲載した
* Mermaidは `mermaid` 指定を含むフローチャート、シーケンス図、状態遷移図、クラス図の完成例と、主要な方向・矢印・分岐ラベル・関係記号の説明を掲載した
* ガイドデータを `category`、`name`、`syntax`、`description`、`notes` を持つ配列へ分離し、安全なDOM APIで表示するようにした
* 各サンプルのコピーはClipboard APIを優先し、利用不可・失敗時は一時textareaへフォールバックする。成功時はボタンを1.8秒間「コピーしました」に変更し、両方式が失敗した場合はライブ領域へ案内を出す
* コピー対象はコードブロックとMermaidの開始・終了バッククォートを含む完成形とし、本文への挿入、本文変更、保存予約、Undo履歴更新は行わない
* 既存CSS変数を使い、PC・中間幅は2列、719.98px以下は1列とした。ダイアログを画面内へ収め、本文だけを縦スクロールし、狭幅ではサンプルを折り返して閉じる／コピーボタンを40px以上にした
* `syntax-guide.test.js` を追加し、入口・dialog構造、掲載記法、非掲載記法、コード・Mermaidの完全なコピー文字列、短縮名、コピー成功・失敗処理、開閉ARIA、本文・保存・Undo処理からの独立、テーマ変数、レスポンシブ契約を確認した
* `node --check app.js`、`node --check syntax-guide.test.js`、新規8件と既存54件の自動テスト、`git diff --check` が成功した
* ChromeでPC幅、中間幅、390×844pxを確認し、画面全体の横はみ出しなし、ダイアログの上下収まり、本文内部スクロール、携帯1列化、40pxの閉じる／コピーボタンを確認した
* ライト／ダークの背景・文字・コード背景を目視し、テスト後は元のライトテーマへ戻した。閉じるボタンとEscで閉じた後に `aria-expanded=false` となり、本文、選択範囲、本文スクロール位置が開く前と一致し、コンソールにerror／warningがないことを確認した
* 実ブラウザではコードブロックのコピーボタンが「コピーしました」から「コピー」へ戻ることを確認した。ブラウザ操作環境からOSクリップボード内容を直接取得できなかったため、コピー文字列そのものは自動テストで開始・終了フェンスを含めて確認した
* 今回は本文への自動挿入、Markdownレンダリング、highlight.js、Mermaid初期化、IndexedDB、添付、Wikiリンク、保存、Undo、エクスポート、取り込みの各処理を変更していない
* PRレビュー対応として、モーダル表示中のフォールバック用textareaを記法ガイドdialog内へ追加し、コピー後は必ず削除して元のフォーカスへ戻すよう修正した。dialog外では従来どおりbodyを安全な追加先にする
* Clipboard API成功・拒否、`execCommand("copy")` の成功・失敗・例外、textareaのcleanup、フォーカス復元、本文状態の維持を実関数テストで確認した。ブラウザではPermissions-PolicyによるClipboard拒否時もコピー成功表示、表示復帰、継続操作を確認し、実機iPhone／iPad Safariは未確認

## 2026-07-21 PR #23 Mermaid複数描画の再修正

* PR #23ではMermaid処理をキュー化したが、`mermaid-diagram-${noteId}-${index}` 相当のDOM IDがプレビュー再生成後も同一で、実行中の旧世代と新世代をDOM IDで隔離できていなかった。実ブラウザで再描画前後の4 IDがすべて同一になることを確認した
* PR #23で `app.js` を変更した際に `index.html` の `app.js?v=0.4.0-16` を更新しておらず、配信済み画面が修正前の並列 `mermaid.run()` をキャッシュから継続利用できる状態だった。クエリを `0.4.0-17` へ更新した
* MermaidブロックのDOM IDへ `renderGeneration` を含め、同じメモ・同じブロック番号でも世代間で重複しないようにした。Mermaid内部へ渡すSVG IDにも世代、ブロック番号、通し番号を含めた
* 画面上の `.mermaid-diagram` を `mermaid.run()` の直接描画先にする処理を廃止した。各ソースを画面外の一意なホストへ `mermaid.render()` で1個ずつ直列描画し、完了後も現行世代である場合だけ対応ブロックへSVGを反映する。旧世代は処理中の1件が戻った時点で残りを打ち切り、ホストは成功・失敗・失効のすべてで削除する
* 各ブロックへ反映するSVGが厳密に1個であることを検査し、構文エラーはそのブロックだけ既存の「Mermaid構文エラー」と元ソース表示へ切り替える。CSSの高さ、余白、overflowは変更していない
* `mermaid-rendering.test.js` を非同期Mermaid APIテストダブルとDOMダブルによる競合テストへ変更した。旧描画中の世代交代、4ブロック・各SVG1個、sequence／flowchartの内容非混在、空ブロックなし、DOM ID非重複、構文エラー後の残り3図、空行あり・なしを結果DOMから確認する
* `node --check`、既存テストを含む全72件、`git diff --check` が成功した。Chromeでは空行あり・なし、重い旧描画中の連続入力、保存、メモ切り替え、再読み込みを確認し、常に4ブロック・各SVG1個、空ブロック0、sequence／flowchart相互混入なし、旧テキスト混入なし、画面外ホスト残存0となった。構文エラー時はSVG数 `[1,0,1,1]`、エラーと元ソースは該当ブロックだけだった

## 2026-07-22 Mermaidテンプレート詳細

* 記法ガイドのMermaid欄を9種類（フローチャート、シーケンス図、状態遷移図、クラス図、ER図、ガントチャート、Gitグラフ、タイムライン、マインドマップ）の「詳細」一覧へ変更した
* 各種類へ5件ずつ、合計45件の完成テンプレートをデータとして追加した。各項目は分類、Mermaid宣言名、テンプレート名、用途説明、コードフェンスを含む構文、補足を保持する
* 選択した種類だけを表示する詳細ダイアログを追加し、閉じるボタン、Esc、元の詳細ボタンへのフォーカス復元、詳細本文だけの縦スクロール、コードの横スクロール、ライト／ダーク、390px幅に対応した
* コピーは既存のClipboard APIと一時textareaフォールバックを再利用し、詳細ダイアログ表示中はフォールバック要素を最前面のダイアログ内で作成・削除する。コピー文字列には開始・終了のMermaidコードフェンスを含める
* PR #24のプレビュー世代管理、世代別DOM ID、画面外ホストでの `mermaid.render()`、直列描画、旧世代失効は変更していない。`app.js`のキャッシュ番号だけを `0.4.0-18` へ更新した
* `syntax-guide.test.js` を19件へ拡張し、9種類・各5件、必須データ、宣言名、コードフェンス、詳細UI、コピー、フォーカス復元、テーマ変数、レスポンシブ、PR #24描画経路の維持を確認した
* `node --check app.js`、`node --check syntax-guide.test.js`、指定の `node --test --test-isolation=none syntax-guide.test.js` 19件、既存テストを含む全77件が成功した
* Chromeで9種類すべての詳細に5件ずつ表示されること、コピー成功表示、Escで詳細だけが閉じて元の「詳細」へフォーカスが戻ることを確認した
* 45件すべてを1つの本文へ貼り付け、初回描画と保存後の再読み込みの両方で45個のMermaidブロックにSVGが1個ずつ表示され、構文エラー0件、空ブロック0件、画面外ホスト残存0件となることを確認した
* 390×844pxで詳細ダイアログが画面内に収まり、詳細本文だけがスクロールし、ページ全体の横はみ出しがないことを確認した。ライト／ダークでは既存テーマ変数に応じてダイアログ、カード、コード、文字色が切り替わった

## 2026-07-22 Mermaidダークテーマ対応

* 原因は、Mermaid初期化が `startOnLoad: false` と `securityLevel: "strict"` だけで、アプリのライト／ダーク状態を渡していなかったことと、テーマ切替がアプリのCSSクラスだけを変更して生成済みSVGを再描画していなかったことだった
* 既存の `body.dark` を唯一のテーマ判定として再利用し、ライトではMermaid標準の `theme: "default"`／`darkMode: false`、ダークでは `theme: "dark"`／`darkMode: true` を設定するようにした。同じテーマでは再初期化せず、テーマが変わった時だけ設定を更新する
* テーマ切替後は既存の `renderPreview()` を呼び、IndexedDBの本文を書き換えず、現在のメモ本文からプレビュー世代とMermaidブロックを作り直す。各ブロックの非表示 `.mermaid-source` と元Markdownを維持し、生成済みSVGを入力として再利用しない
* PR #24のプレビュー世代管理、世代別DOM ID、画面外ホストでの `mermaid.render()`、直列描画、旧世代失効を維持した。Mermaid内部の汎用IDと `url(#...)` などの参照もSVGごとの描画IDで名前空間化し、シーケンス図とタイムライン等の内部ID衝突を防止した
* Mermaid標準テーマだけで8種類の線・矢印・枠・文字が判読できたため、独自 `themeVariables` とCSS色補正は追加していない。`app.js`のキャッシュ番号を `0.4.0-19` へ更新した
* 自動テストへライト／ダーク設定、テーマ変更時だけの再描画、初期化回数、元本文保持、SVG内部IDと参照の名前空間化を追加した。`node --check` 3件、既存テストを含む全81件、対象差分の `git diff --check` が成功した
* ブラウザでフローチャート、シーケンス図、クラス図、ER図、ガントチャート、Gitグラフ、タイムライン、マインドマップをライト／ダーク双方で目視した。ライト→ダーク→ライト→ダーク、待たずにライト→ダーク、別メモへの切替と復帰を行い、常に8ブロック・各SVG1個、空ブロック0、構文エラー0、画面外ホスト残存0、DOM重複ID 0、元Markdown一致を確認した
* Mermaid標準のガント図は狭いカード幅では日付ラベルが密集するが、今回のテーマ切替前からあるレイアウト特性であり、線や文字の消失とは別件のためCSS固定幅などの補正は行っていない

## 2026-07-22 Mermaidクラス図の長い文字切れ修正

* Mermaid設定にはフォント指定がなく、アプリ本文は `"Yu Gothic UI", "Hiragino Sans", Meiryo, system-ui, sans-serif`、生成SVGはMermaid既定の `"trebuchet ms", verdana, arial, sans-serif` を使用していた。Webフォント、Mermaid文字へのCSS上書き、ライト／ダーク間のフォント差はなかったため、環境依存のフォールバックによって文字幅計測と最終表示がずれる余地を原因と判断した
* `getMermaidConfig()` のトップレベル `fontFamily` と `themeVariables.fontFamily` にアプリと同じフォントスタックを設定し、`themeVariables.fontSize` を `16px` で明示した。ライト／ダークで同じ値を使用し、個別クラス名、CSSの文字縮小、`overflow: visible`、余白による隠蔽は追加していない
* テーマ切替時の再初期化、元Markdownからの再描画、プレビュー世代管理、画面外ホスト、直列 `mermaid.render()`、旧世代失効、SVG内部IDの名前空間化は維持した。`app.js`のキャッシュ番号を `0.4.0-20` へ更新した
* 自動テストでライト／ダーク双方のトップレベルとテーマ変数のフォント一致、16px、既存のテーマ・安全性・複数描画契約を確認した。`node --check` 3件、既存テストを含む全81件、対象差分の `git diff --check` が成功した
* ブラウザでは `IndexedDbRepository`、`ExtremelyLongRepositoryImplementationClassName`、長い属性名・メソッド名について、各文字要素の左右端がクラス枠内にあることをDOM座標で確認した。ライト／ダーク、1487px、900px、390×844px、連続テーマ切替後のすべてで枠内に収まった
* 同じ本文のフローチャート、シーケンス図、ER図、ガント図を含む5ブロックは各SVG1個、空ブロック0、重複ID 0を維持し、クラス間の継承矢印も表示された。ブラウザコンソールのerror／warningは0件だった

## 2026-07-22 表ブロック

* エディタ操作列へ「表」を追加し、本文のカーソル位置へ2列×2行、先頭行見出し、空の説明・補足を持つ表ブロックを挿入できるようにした。既存表マーカー内部へカーソルがある場合も保存データを分断せず、その表の後ろへ安全に挿入する
* 表は `type`、固有 `id`、`caption`、`note`、`hasHeader`、`version`、二次元 `rows` を持つ構造化JSONを安全な専用コメントへ直列化して本文内へ保存する。空セル、不揃いな列、将来バージョンと未知メタデータを正規化・維持し、コードフェンスと画像ブロック内の同形文字列は表として解釈しない
* データ生成・解析・本文差し替え・行列操作・Tab移動を `table-block-utils.js` へ分離した。本文文字列を保存する既存IndexedDB、遅延自動保存、Undo、メモ複製、Markdownエクスポートの経路をそのまま利用し、表専用のHTML保存やDBスキーマ変更は行っていない
* 編集画面には本文内の表ごとに、上部説明、直接編集できるプレーンテキストセル、下部補足、右上の操作メニューを表示する。行・列の追加／削除、見出し行オン／オフ、表全体削除に対応し、1行×1列を下限とした。表全体削除だけ確認を表示する
* セルのTabで右隣、Shift+Tabで左隣へ移動し、最後のセルのTabでは新しい行を追加して先頭セルへフォーカスする。イベントは表編集領域へ委譲し、セル入力のたびに編集UI全体を作り直さず、既存の遅延保存へ接続した
* カード側は編集ボタンや入力欄を含めず、説明、`table`、`thead`、`tbody`、`th`、`td`、補足だけを出力する。すべての値をHTMLエスケープし、空の説明と補足は要素自体を表示しない。狭幅ではページ全体ではなく表領域だけを横スクロールする
* 記法ガイドへ「表ブロック」をデータ項目として追加し、挿入、説明・補足、セル編集、行列・見出し操作、Tab／Shift+Tab、削除、横スクロール、プレーンテキスト限定、数式・計算・結合・色・並べ替え・絞り込みの非対応と将来拡張を掲載した
* `table-block-utils.test.js` を追加し、挿入、編集、保存復元、複数表、コード・画像との非衝突、行列操作、1×1下限、見出し、Tab、削除、将来メタデータ、エクスポート、HTML無害化、意味的DOMを確認した。`node --check` 4件、既存テストを含む全114件、対象差分の `git diff --check` が成功した
* Chromeで挿入、説明・補足・セル編集、最終セルTabによる行追加、見出し切替、2表の独立ID、再読み込み復元、記法ガイドとEscを確認した。`<img onerror>` 風のセル値は画像要素を生成せず文字列として表示され、カード内入力UIは0件だった
* PC幅と390×844px、ライト／ダークを確認し、390pxでは文書幅390pxのまま表領域だけが322pxから349pxへ横スクロールした。ライト／ダークとも表、罫線、文字、説明、補足を判読でき、確認中のコンソールerror／warningは0件だった

## 2026-07-22 表ブロックの行・列選択と削除確認

* 編集用の表へ、左端の行番号と上端のExcel形式の列記号をbutton見出しとして追加した。列記号はA〜Zの後もAA、AB…を生成し、`aria-pressed`と「2行目を選択」「B列を選択」のラベルを持つ。見出しはUIだけに生成し、既存の表ブロック保存データ、カード表示、コピー・エクスポートへ混入させない
* 選択状態を表IDごとに管理し、行と列の排他切替、別見出しへの切替、同じ見出しの再押下による解除に対応した。通常セルの編集とは分離し、選択・解除後も同じ見出しへフォーカスを戻す
* 未選択時の末尾行・末尾列への暗黙フォールバックを削除し、「削除する行／列を選択してください」と各表のライブ領域で案内する。選択時は既存テーマに対応した専用dialogで「2行目／B列を削除しますか？」と表示し、日本語の「キャンセル」「削除」で確認する
* 削除後は削除位置に新しく来た行・列、末尾削除時はひとつ前を選択する。1行または1列しかない場合はデータを変更せず、最低1行・1列が必要な理由を表示する。追加は同じ種類の選択があれば直後、未選択なら末尾へ行い、追加した行・列を選択する
* 記法ガイドへ行番号・列記号による選択、選択位置への追加、削除確認、1行・1列の下限を追記した。キャッシュ番号を `style.css?v=0.4.0-18`、`table-block-utils.js?v=0.4.0-2`、`app.js?v=0.4.0-22` へ更新し、PR #24のMermaid描画経路を維持した
* 自動テストへA／Z／AA／ABの列記号、見出しDOMとARIA、表ごとの排他選択、未選択削除ガード、選択位置と末尾への追加、確認dialog、近接選択、1×1下限を追加した。`node --check` 4件、既存テストを含む全119件、対象差分の `git diff --check` が成功した
* Chromeで2表を使い、2行目とB列の選択、キャンセル、確定、削除対象と残存データの一致、削除後の近接選択、1行・1列保護、選択直後への追加、表間の選択独立、セル編集との区別、Spaceキーによる選択解除とフォーカス維持、保存後の再読み込みをDOMから確認した。本文の保存マーカーには行番号・列記号のUIラベルが含まれなかった
* ライト／ダーク双方で選択背景と文字の計算済み色を確認した。iPhone相当の狭幅では文書全体の横はみ出しがなく、約40pxの行番号欄を保ったまま表編集領域だけが横スクロールした。記法ガイドの追記とEsc閉じを確認し、コンソールerror／warningは0件だった

## 2026-07-22 外部表データの貼り付け

* 本文のpaste処理へ、HTML表、Markdown表、タブ区切りの優先順で表候補を検出する処理を追加した。確実に表と判断できない文字列、カンマ区切りの文章、コードフェンス内の貼り付けは既存の通常貼り付けへ戻し、画像のクリップボード貼り付けも従来経路を先に維持する
* タブ区切りはタブを必須として1行の表にも対応し、Markdown表は見出し区切り行、外側のパイプ省略、エスケープ済みパイプ、左・中央・右配置を解析する。HTML表はDOMParserで画面へ挿入せず解析し、script等を除去してプレーンテキストだけを取得する。`br`は改行、rowspan／colspanは左上へ値を置いて残りを空セルへ変換する
* 不揃いな行は最大列数へ補完し、末尾の完全な空行・空列だけを除去する。途中の空行・空列、`00123`、`1-2`、真偽値や数式らしい文字列は型変換せず維持する。上限は100行・30列・3000セルとし、超過時は切り捨てずテキスト貼り付けまたはキャンセルだけを表示する
* 確認dialogへ行列数、検出形式、見出し行チェック、表／テキスト／キャンセルを表示した。貼り付け時のメモID、本文、選択範囲を保持して誤挿入を防ぎ、表貼り付けは既存の表ブロック構造、挿入処理、Undo、自動保存を再利用する。貼り付け後は行列選択を残さず左上セルへフォーカスする
* Markdownの列配置は表ブロックの任意メタデータとして保存し、カードと編集セルへ反映する。列追加・削除時も配置配列を同期する。既存表の必須データ形式やIndexedDBスキーマは変更していない
* 確認dialogはライト／ダークの既存変数、モーダル背景、フォーカス、本文内部スクロール、セーフエリア、狭幅での縦並び44pxボタンに対応した。記法ガイドへ3形式、表／テキスト選択、見出し、上限、CSV非対応、結合セルの制限を追記した
* `node --check` 5件、既存テストを含む全140件、対象差分の `git diff --check` が成功した。解析テストでは空セル、不揃い行、誤判定防止、配置、HTML無害化、フォールバック、正規化、上限を確認し、UI契約テストでは挿入位置、Undo、保存、選択解除、Escape／Enter、テーマ、モバイルを確認した
* この作業セッションではブラウザ制御インターフェースを利用できなかったため、実画面の自動操作と実機モバイル確認は未実施。HTMLはDOM API互換テストダブルで検証しており、実ブラウザでのExcel／Googleスプレッドシート／Webページ由来クリップボード、保存後再読み込み、Undo、ライト／ダーク、狭幅はDraft PRの確認事項として残す
* 初期実装ではHTML内の最初のtableだけを対象とし、結合セルの見た目そのものは再現しない。CSVは自動判定しない

## 2026-07-22 表ブロック追加後の選択解除とセルフォーカス

* 原因は、選択中の行・列から追加位置を決めた後、新しい行・列を再び軸選択として登録し、その見出しへフォーカスしていたことだった。追加位置の決定には従来どおり現在の選択を使い、追加完了後は対象表の軸選択だけを解除するようにした
* 行追加後は新しい行の先頭セル、列追加後は新しい列の先頭セルへフォーカスする。未選択時の末尾追加も同じ挙動とし、フォーカスのために表データは変更しない。削除後に近い行・列を選択する既存仕様と、日本語の削除確認は変更していない
* 再描画後のセルは `requestAnimationFrame` で、表ID・行座標・列座標を組み合わせて対象表の編集領域内から一意に特定する。通常の `focus()` により、狭い画面で追加セルが表編集領域の外にある場合も、そのセルが見える位置まで表内をスクロールする
* 記法ガイドを選択時の直後追加、未選択時の末尾追加、追加後の選択解除と先頭セルへのカーソル移動に合わせて更新し、`app.js` のキャッシュ番号を `0.4.0-23` へ更新した
* `node --check` 4件、既存テストを含む全120件、対象差分の `git diff --check` が成功した。自動テストでは追加位置、選択解除、表IDと座標によるフォーカス、既存の削除確認と近接選択を確認した
* Chromeで選択時と未選択時の行・列追加、追加セルへの即時入力、追加後の見出し再選択、削除確認のキャンセル、2表間の選択とフォーカスの分離、保存後の再読み込みを確認した。ライト／ダーク双方でフォーカス枠と文字を判別でき、iPhone相当の狭幅では文書全体をはみ出さず、追加セルが見える位置まで表編集領域だけがスクロールした。コンソールerror／warningは0件だった

## 2026-07-22 表ブロックの外部コピー

* 各表ブロックの操作メニューへ「表をコピー」と「Markdown表としてコピー」を追加した。コピー直前に対象表の編集DOMから最新セル値だけを複製し、説明文、補足文、行番号、列記号、内部IDを除いた表全体を対象とする。行・列選択、本文、Undo履歴、自動保存は変更しない
* 通常コピーはセルをタブ区切り、行を改行にした `text/plain` と、見出し・本文・列配置を表す安全な `text/html` を1回の `ClipboardItem` に格納する。HTMLはセル文字列をエスケープし、改行だけを `br` へ変換するため、scriptやイベント属性を実行可能な形で出力しない
* 複数MIME形式に対応しない環境では `navigator.clipboard.writeText()` のタブ区切りへ移り、さらに利用できない場合は既存の一時textareaと `execCommand("copy")` を使う。成功・失敗は各表のライブ領域へ短時間表示する
* Markdownコピーは見出し、左・中央・右配置、パイプ、バックスラッシュ、セル内改行を安全に変換する。見出しなしの表はA、B、C…を仮見出しにし、元の先頭行をデータとして残す。Markdown解析側もエスケープ済みバックスラッシュを復元できるようにした
* 記法ガイドへExcel／Googleスプレッドシート向けコピー、HTML対応アプリ、Markdownコピー、説明・補足の除外、選択中でも表全体が対象になることを追記した。配信キャッシュを `style.css?v=0.4.0-20`、`table-block-utils.js?v=0.4.0-4`、`app.js?v=0.4.0-25` へ更新した
* `node --check`、変換・Clipboard・UI・記法ガイド・既存回帰を含む全151件、今回の対象差分の `git diff --check` が成功した
* ChromeでTSVとHTMLの同時コピー、Markdownコピー、成功表示、行選択とUndo状態の維持、`<script>`のHTMLエスケープ、Markdown表の貼り戻しを確認した。ライト／ダーク双方で成功表示を判別でき、コンソールerror／warningは0件だった
* この作業環境からExcel、Googleスプレッドシート、Wordへ直接貼り付ける確認は実施できていない。複数MIME非対応時はTSVだけになること、外部アプリ側のクリップボード形式選択によってはプレーンテキストとして貼り付けられることを既知の制限とする。モバイル幅はブラウザの表示領域切替が安定せず、既存の狭幅CSSと自動テストによる確認に留めた

## 2026-07-23 表ブロック位置とMermaidフローチャート文字切れ修正

* 編集カードのDOM順を、タイトル・操作列、本文textarea、`#tableBlockEditors`の順へ変更した。表編集領域のIDとイベント委譲、本文内の構造化マーカー、IndexedDB保存経路は変更せず、既存表も本文下へ描画する。本文との境界は10pxの余白と上罫線で示し、従来のテーマ変数と表内スクロールを維持した
* Mermaidフローチャートの原因は、HTMLラベルを固定高の`foreignObject`で生成した後、カード側の行高と折り返しが適用されることで、生成時のノード寸法と最終ラベル寸法がずれる点にあった。外部CSSやSVG全体の拡大ではなく、フローチャートだけ`htmlLabels: false`としてSVGの`text`／`tspan`をMermaid自身に計測させるようにした
* Mermaid 10の自動折り返しはMarkdownラベルが対象のため、`prepareMermaidSource()`を追加し、flowchart／graphの20文字以上の角括弧・波括弧ラベルだけを描画時にMarkdownラベルへ変換する。短いラベル、`br`による明示改行、既存Markdownラベル、保存中の元本文、シーケンス図、クラス図、ER図、ガント図など他図種は変更しない
* 回帰テストへ本文textareaより後ろに表編集領域があるDOM契約、境界スタイル、ライト／ダーク共通のMermaid設定、長い日本語・英数字混在ラベルの描画時変換、非フローチャートの無変更、配信キャッシュ更新を追加した。`node --check`、全154件、`git diff --check`が成功した。静的HTMLアプリでビルドスクリプトはないため、別のビルド工程はない
* Chromeで表の追加、説明・セル編集、選択位置への行・列追加、列削除確認、保存後再読み込みを確認した。再読み込み後も本文下の表、3行×2列、説明と既存セル値が復元され、カード側の表も一致した。表全体削除、Tab移動、見出し切替、コピー等は既存の自動テストを含む全件成功で回帰確認した
* Chromeで日本語・英数字混在の手動3行ラベルと長文8行ラベルを計測し、両方とも文字領域の上下左右の枠外超過0だった。フローチャート、シーケンス図、クラス図、ER図、ガント図の5ブロックは各SVG 1個、構文エラー0、重複ID 0、画面外ホスト残存0を維持した
* wide／compact／mobile相当の3幅で文書全体の横はみ出しがなく、mobileでは表編集領域だけが横スクロールした。ライト→ダーク→ライトの再描画後も5図すべてSVG 1個、長文ノードの枠外超過0、コンソールerror／warning 0だった

## 2026-07-23 KaTeX数式表示と単独式の計算ブロック

* カード表示へKaTeXによるインライン数式`$...$`とブロック数式`$$...$$`を追加した。IndexedDBへ保存するMarkdown本文は変更せず、表示時だけ数式要素を個別描画する。不正な数式はその箇所だけ簡潔なエラーと元記法を表示し、詳細を開発者コンソールへ出して他の本文・数式・ブロックの描画を継続する
* fenced codeとMermaidを先に分離し、通常テキスト内でブロック数式と`:::calc`を分離した後、インラインコードを優先してインライン数式を検出する順序へ整理した。コードブロック、インラインコード、Mermaid、計算ブロック内の数式記法はKaTeXで処理しない
* `calculation-engine.js`へ入力取得後の正規化、検証、単項パーセント展開、安全な評価関数への受け渡し、結果整形、エラー分類、表示用演算子の生成を分離した。Calculator Memoのcurrent mainと同じmathjs 15.2.0を使用し、四則演算、括弧、小数、負数、`%`、全角演算子と`x`の正規化に対応した。`10%`は`0.1`、`200 + 10%`は`200.1`、`200 * 10%`は`20`となる
* 計算ブロックは1ブロックにつき非空の1行・1式だけを扱う。累乗、平方根、数学関数、変数、複数式、複数行計算、ブロック間参照、履歴参照、単位計算、グラフには対応しない。`eval`、Functionコンストラクタ、動的スクリプト挿入は使わず、許可文字と括弧を検証してからmathjsへ渡す。極端に長い入力は評価前に拒否する
* 計算カードは小さな「計算」ラベル、控えめな入力式、罫線で区切った強めの結果を表示する。`*`と`/`は表示時だけ`×`と`÷`へ変え、計算には元の式を使用する。不正な式は「計算できません」とエスケープした元入力を表示し、他のブロックを維持する
* 記法ガイドへ「数式」と「計算ブロック」を追加し、KaTeXのインライン・ブロック・分数・平方根・累乗・添字・記号・総和・積分・極限・行列・複数行例と、Calculator Memo準拠の四則演算・括弧・小数・パーセント例、表示用と計算用の違い、非対応範囲を掲載した。READMEにも役割分担と今回Calculator Memoを変更していないことを追記した
* `calculation-engine.test.js`と`math-calculation-blocks.test.js`を追加し、Calculator Memoと共通する四則演算・括弧・小数・負数・%・正規化・表示整形・エラー条件、長大入力、JavaScript／HTML無害化、複数ブロック、コード・Mermaid・インラインコードとの分離、テーマ変数、内部スクロールを確認した。既存回帰を含む全171件、`node --check`、`git diff --check`が成功した。静的HTMLアプリのため別のビルド工程はない
* Chromeでインライン数式2件、ブロック数式2件、計算ブロック2件、Mermaid 1件を同じメモに置き、KaTeX描画、行列、Calculator Memo準拠の`(10000 + 5000) * 1.1 = 16,500`と`200 * 10% = 20`、コード内記法の未変換を確認した。ライト／ダーク双方で数式、分数線、記号、計算式、結果を判読でき、MermaidもSVG 1件を維持した
* PC、狭いPC、スマートフォン相当幅で文書全体とプレビューの横はみ出しがないことを確認した。幅1397px相当の長い数式は345pxの数式領域だけが横スクロールし、計算カードはカード幅内に収まった。不正なKaTeXと不正計算を各1件置いても、正常な数式・計算・前後本文は継続表示され、コンソールerrorは要件どおり各エラーの詳細2件だけだった

## 2026-07-25 Calculator Memoへの一方向連携

* 本文エディタの操作列へ「計算」ボタンを追加し、選択中の文字列を前後trimしたうえで、Calculator Memoの`expr`パラメータへ安全に設定して新しいタブで開くようにした。未選択時はパラメータなしで開く。
* 公開URL、2,000文字上限、URL生成、新規タブ起動を`calculator-link.js`へ分離し、`noopener,noreferrer`を指定した。本文、保存状態、Undo、IndexedDBの保存形式は変更していない。
* 記法ガイドとREADMEへ一方向連携の説明を追加した。
* `node --test`は既存回帰を含む全177件、`node --check app.js`、`node --check calculator-link.js`、`git diff --check`が成功した。
* ブラウザで`2 * (5 + 3)`の選択時に公開URLの`expr`付き新規タブ、未選択時に`expr`なし新規タブが開くこと、押下前後で本文が不変であること、ダークテーマでボタンが表示されること、コンソールerror 0件を確認した。既存CSSと自動テストでは狭幅ルールを維持しているが、接続ブラウザの幅変更が反映されず実画面での3幅比較は未実施。

## 2026-07-26 フォント設定とFont Comparison連携

* 設定画面へ全体およびメモ個別の本文・見出し・コード用フォントと文字サイズを追加した。全体設定は既存のlocalStorage、個別設定は既存メモオブジェクト内へ任意項目として保存し、古いメモとIndexedDBスキーマを維持する。
* 専用CSS変数を本文エディタ、カード本文、見出し、インライン／ブロックコードだけへ適用した。操作UI、KaTeX、Mermaidへはフォント設定を波及させない。
* Font Comparison公開URLへ用途、適用範囲、現在のフォント、戻り先、メモID、最大700文字の比較文章をURLSearchParamsで渡す。任意文章が空なら現在の本文、本文も空なら標準の日本語・英数字・記号サンプルを使い、コード比較にはコード例を追加する。
* Font Comparisonからの戻り値は送信元、用途、適用範囲、メモID、登録済みフォントIDとfont-familyの一致を検証し、処理後のパラメータをhistory.replaceStateで除去する。受信直後は保存せず、受取確認、プレビュー、明示保存の順にした。
* 初期設定へ戻す操作は確認後にフォント設定だけを対象とし、テーマ、本文、添付、コレクションなどの既存設定を変更しない。
* `font-settings.test.js`を追加し、URL生成、文章の優先順と上限、全体／個別設定、戻り値検証、URL清掃、明示保存UI、CSS適用範囲、本文不変を確認対象にした。
* `node --check app.js`、`node --check font-settings.js`、既存回帰を含む全185件、`git diff --check`が成功した。既存のKaTeX、Mermaid、表、画像、計算ブロックのテストも全件維持した。
* ブラウザで設定UI、全体／個別フィールド、プレビューを確認し、実際の「フォントを比較して選ぶ」から公開Font Comparison URLが`mode=memo-nexus`、`target=body`、現在値、戻り先、指定サンプル、`noopener,noreferrer`付きで新規タブに開くことを確認した。
* ローカルの両アプリを使い、Meiryo選択、Memo Nexusへの戻り、URLパラメータ除去、受取確認、保存前プレビュー、明示保存、本文不変、本文へのMeiryoフォールバック指定反映まで一連で確認した。確認中のコンソールerrorとエラーオーバーレイは0件だった。
* 接続ブラウザの表示領域を変更する機能を利用できなかったため、スマートフォン実幅とMemo Nexusのダークテーマは自動テストとCSS契約で確認し、実ブラウザの目視確認はDraft PRの確認事項として残す。

## 2026-07-26 PR #42 レビュー対応

### 変更内容

* 全体設定とメモ個別設定へ「題名フォント」「題名サイズ」を追加し、個別設定を優先して編集画面上部の`.title-input`だけへ適用した。未設定・既存メモは従来の26pxを維持し、メモ一覧の題名へは適用しない。
* 狭幅画面では題名サイズを従来の21px以内に収め、題名入力欄へ`min-width: 0`を設定して既存レイアウト内に収まるようにした。
* フォント設定保存時は、正規化後のメモ個別`fontSettings`に実質的な差分がある場合だけメモを保存し、`updatedAt`を更新するようにした。全体設定だけの保存では更新日時と更新日順へ影響しない。
* 配信キャッシュを`style.css?v=0.4.0-24`、`font-settings.js?v=0.4.0-2`、`app.js?v=0.4.0-30`へ更新した。

### 確認結果

* `node --test font-settings.test.js`: 11件成功。
* `node --test`: 188件成功、失敗0件。
* 通常幅のブラウザで題名設定UIとプレビュー反映を確認し、編集題名欄とメモ一覧題名の既定表示が変わらないこと、エラー画面がないことを確認した。
* 狭幅時の21px上限、`.title-input`だけへの適用、個別設定の優先、既存設定の補完、個別設定に差分がある場合だけ`updatedAt`を更新する条件を自動テストで確認した。

## 2026-08-03 OllamaローカルAIアダプタと右側AIアシスタント

* ブランチ `feature/ollama-ai-adapter` で、`ai-provider.js`（共通設定・状態・モデル正規化・NDJSON解析・利用者向けエラー）、`ollama-adapter.js`（接続確認・モデル取得・ストリーミング生成・タイムアウト・Abort）、`ai-prompts.js`（要約・整理・翻訳・自由質問の版付き内蔵プロンプト）を分離した。UIはOllama固有のURLやレスポンス形式を直接扱わず、将来のクラウドAIアダプタ追加と外部プロンプト管理アプリへの差し替え余地を残した。
* ローカルAIは初期状態で無効、標準接続先は `http://127.0.0.1:11434` とした。設定へ有効化、接続先、接続確認、モデル再取得、使用モデル、タイムアウト、system指示を追加した。Qwen・Phi・Granite系を大文字小文字を区別せず確認済み系統として分類し、その他の導入済みモデルも動作未確認表示で選択できる。外部URLには送信先警告を表示し、APIキー欄は設けていない。
* AIパネルへ現在のメモ全体／選択範囲、要約／整理／日本語・英語翻訳／自由質問、追加指示、送信、停止、テキスト回答、コピー、確認付き末尾追記、新規メモ保存を実装した。回答はストリーミングで表示し、パネルを閉じても生成を継続する。生成開始時のメモIDを保持し、別メモへ切り替えた回答は現在のメモへ追記できない。回答をHTMLとして挿入せず、本文の自動上書き・自動保存・リンク自動起動は行わない。
* 既存の単一 `#memoList` と `#memoSidebar` を新規作成せず画面最右列へ移した。PCでは同じ右領域をメモ一覧とAIパネルで切り替え、中間幅は右ドロワー、モバイルは全画面AIパネルとした。右端から約半分見えるオリジナルCSSロボットをアクセシブルなbuttonとして追加し、無効・未接続・待機・生成中・停止・完了・エラーを同じ状態管理元から表示する。クリック、Enter、Escape、フォーカス表示、ダークテーマ、セーフエリア、`prefers-reduced-motion`へ対応した。
* 変更ファイルは `ai-provider.js`、`ollama-adapter.js`、`ai-prompts.js`、各単体テスト、`ai-ui.test.js`、`index.html`、`app.js`、`style.css`、`README.md`、`LOG.md`、および配信キャッシュ期待値を持つ既存4テスト。`IMPORT`と`ai-news`関連ファイルは対象外として編集・stash・コミットしていない。
* `node --check` 4件、既存回帰を含む `node --test` 全215件、対象差分の `git diff --check` が成功した。静的HTMLアプリで `package.json` とtypecheck・lint・buildスクリプトは存在しないため、推測したnpmコマンドや別ビルドは実行していない。
* 実ブラウザではPC・中間幅・モバイル相当で右端配置、AIパネル切替、クリック・Enter・Escape、本文保持、送信／停止／回答操作の表示、翻訳先の用途連動、コンソールerror／warning 0件を確認した。ダークテーマと軽減モーションは既存テーマ変数・静止ルールと自動テストで確認した。
* ローカルOllamaで `qwen3.5:latest` の要約と英訳、`phi4-mini:latest` の自由質問、`granite4.1:8b` の整理を合成テキストで確認した。実OllamaへのAbortは `aborted` として分類された。ストリーム分割、受信済み文章保持、接続失敗、モデルなし、選択モデル削除、不正応答、タイムアウトはモック単体テストで確認した。利用者の実メモは手動モデル確認へ送信していない。
* 未実装はOllama／モデルの自動導入、クラウドAI、APIキー管理、複数メモ横断、RAG／ベクトル検索、自動タグ付け、会話履歴永続化、本文自動上書き・回答自動保存、携帯端末上のローカルAI実行、外部プロンプト管理アプリ本体。Qwen・Phi・Granite以外のモデルは選択可能だが未検証とする。

## 2026-08-04 PR #48: 右側メモ一覧の表示安定化

### 変更内容

* `app.js` の配信キャッシュ番号を `0.4.0-33` から `0.4.0-34` へ更新し、右側コンテキストパネルの修正後に古いJavaScriptが残らないようにした
* タブ切り替え時にAIパネルの開閉状態を同期し、AIからメモ一覧へ戻った後に旧 `body.ai-open .sidebar` スタイルで一覧が不可視のまま残る問題を解消した
* 旧モバイル用の `memoPaneOpen` 状態と関連CSSを除去し、メモ一覧の開閉はコンテキストパネル状態だけで管理するようにした

### 確認結果

* 実ブラウザで、複数メモの選択、コレクション・AI・メモ一覧のタブ往復、検索の入力と解除、パネルの閉じ・再表示を確認した
* 広い画面ではメモ選択後もパネルを維持し、狭幅では選択後にドロワーを閉じても再表示時に一覧が残ることを確認した

## 2026-08-04 PR #44 レビュー対応

* 保存済み `aiSettings` と設定画面の未保存ドラフトを分離し、接続確認・モデル更新はドラフトだけを更新するようにした。明示的な設定保存時のみAIパネルの実行設定とlocalStorageへ反映する。
* Ollamaの接続確認・モデル取得・ストリーミング生成を無通信タイムアウトへ変更し、チャンク受信ごとにタイマーをリセットする。外部Abort、reader、タイマー、イベントリスナーの後始末を追加した。
* 接続確認はリクエストIDとAbortControllerで最新リクエストを優先し、古い応答がモデル一覧や接続状態を上書きしないようにした。
* 変更ファイルは `app.js`、`ollama-adapter.js`、`ollama-adapter.test.js`、`ai-ui.test.js`、`README.md`、`LOG.md`。IMPORT配下は変更していない。
* `node --check app.js ai-provider.js ollama-adapter.js ai-prompts.js`、`node --test`、`git diff --check` を実行し、全件成功を確認した。
* 未実装事項は従来どおり、Ollama／モデルの自動導入、クラウドAI、会話履歴永続化など。設定ドラフトの視覚的な差分表示は今後の調整候補とする。

## 2026-08-04 PR #44 追加レビュー対応

* 原因は、保存時に接続確認済みのURL・モデル一覧・状態を判定する前に、保存済みURLとの差分を理由として破棄していたこと。
* `resolveSavedAiState` を追加し、保存するドラフトURLと確認済みURLが一致し、接続成功・モデル一覧あり・AI有効である場合は、モデル一覧とCONNECTED状態を保存後も維持するようにした。選択モデルが一覧にない場合はMODEL_REQUIREDへ留める。
* 未確認、失敗、URL変更、空モデル一覧、AI無効の場合は確認結果を破棄し、DISCONNECTEDまたはDISABLEDへ遷移する。
* 新しいURLでの確認後保存、モデル一覧・選択モデル維持、URL再変更、未選択・無効・不正モデルの状態遷移を純粋関数テストへ追加した。
* `node --check app.js ai-provider.js ollama-adapter.js ai-prompts.js`、`node --test`、`git diff --check` を実行した。
* 未実装事項は従来どおり、Ollama／モデル自動導入、クラウドAI、会話履歴永続化など。

## 2026-08-04 複数メモ・コレクション参照の追加

* 参照範囲へ `すべてのメモ` と `選択したコレクション` を追加した。前者は未分類を含む `activeNotes()` の全件、後者は選択コレクションと子孫コレクションの有効メモを対象にし、ゴミ箱・削除済みメモを除外する。
* `ai-reference-selection.js` に有効メモ抽出、コレクション子孫IDの再帰取得、コレクション配下メモ抽出を分離した。UIやOllamaアダプタにはコレクション処理を持ち込んでいない。
* `ai-context.js` の共通参照型を複数メモへ拡張し、メモID、タイトル、所属コレクションID／名、本文をメモごとの区切りでsystemメッセージへ渡す。資料中の命令文は利用者の命令ではなく参照資料として扱うことを明示する。
* 参照選択時にメモ一覧と本文をスナップショット化し、チャット履歴には本文を重複保存せず、モード、コレクション名、件数、合計文字数、メモID一覧だけを保持する。参照解除時は複数メモの本文・ID・タイトル・コレクション情報を破棄する。
* 複数メモの本文合計上限を `AI_REFERENCE_MAX_CHARS = 100000` に集約した。送信前に件数／文字数を表示し、対象0件または上限超過時は送信を停止する。黙った切り捨て、分割送信、自動要約は行わない。
* テストへ、全有効メモ、未分類、削除除外、直下／子／孫コレクション、別コレクション除外、対象0件、構造化区切り、解除、スナップショット、上限契約を追加した。既存の現在メモ・選択文章・指定メモ・整理・翻訳は維持する。
* 未実装は複数メモの分割要約、検索型参照、コレクションツリーからの専用ドラッグ選択、チャット履歴永続化。今回のコレクション選択は検索可能な階層リストで行う。

## 2026-08-04 AI自由チャットと参照範囲切替

* 通常のロボット起動を必ず `参照なし` にし、同じAIパネル内で `現在のメモ`、`選択した文章`、`指定したメモ` へ切り替えられるようにした。参照中のタイトル／文字数と解除ボタンを常時表示する。
* `ai-context.js` を追加し、参照モード、参照本文、送信時スナップショット、表示ラベル、Ollamaへ渡す参照メッセージをUIとプロバイダから分離した。`参照なし` は正規化時にメモID・タイトル・本文を破棄し、送信メッセージを生成しない。
* 現在のメモ、選択文章、指定メモはいずれも選択時の内容を固定する。指定メモ検索は未削除の `activeNotes()` だけを候補にし、送信前に削除されたメモはエラーとする。
* 自由質問と回答をタブ内のチャット履歴として表示し、各質問へ送信時の参照モード・メモタイトル・選択文字数を保持する。履歴は永続化せず、参照本文全体を重複保存しない。
* エディタへ `このメモを要約` と `選択範囲をAIに送る` を追加し、単一AI画面へ起動パラメータを渡す。回答はコピー、現在のメモのカーソル位置／本文末尾への確認付き挿入、新規メモ保存ができ、参照なしの回答でも同じ操作を利用できる。
* システム指示、参照コンテキスト、過去の会話、今回の質問を別メッセージとして構築する。Ollamaアダプタの通信処理は変更せず、将来の複数メモ、コレクション、添付、Wiki関連メモは共通参照型を拡張して追加できる。
* `node --check app.js ai-context.js ai-prompts.js` と `node --test --test-isolation=none *.test.js` を実行し、全232件成功。特に参照なしで秘密のメモタイトル・本文が送信データへ含まれないこと、4モード、選択内容固定、送信時参照ラベルを確認した。
* 実ブラウザで通常起動の参照なし、現在のメモ／指定メモ切替、指定メモ一覧、モバイル相当幅の横オーバーフローなし、ダークテーマの参照ラベル／入力欄の配色、コンソールエラー0件を確認した。AIは無効設定のまま確認し、実Ollamaへの送信は行っていない。
* 未実装は複数メモ、コレクション全体、添付、Wiki関連メモの参照、チャット履歴の永続化、カーソル位置挿入用の専用プレビューダイアログ。現段階の挿入位置確認はブラウザの選択プロンプトを使用する。

## 2026-08-04 AI用途選択の復元

* AIパネルで `自由質問`、`要約`、`整理`、`翻訳` の用途選択を再表示し、参照範囲とは独立して組み合わせられる状態へ戻した。
* 用途定義と参照必須判定は既存の `AI_PROMPTS` と `buildAiMessages()` を利用し、要約・整理・翻訳では参照なし送信を拒否する。自由質問は参照なしを許可し、質問・指示の入力は従来どおり必須とする。
* 翻訳先欄は翻訳選択時だけ表示する。「このメモを要約」「選択範囲をAIに送る」のショートカットは、それぞれ現在のメモ＋要約、選択文章＋自由質問を自動設定する。
* UI契約テストと用途×参照範囲の組み合わせテストを追加した。
* `node --check` と `node --test --test-isolation=none *.test.js` を実行し、全245件成功、失敗0件を確認した。

## 2026-08-04 PR #46 レビュー対応

* 通常のロボット起動とショートカット起動を区別し、通常起動では参照モード、参照コンテキスト、指定メモ／コレクション、選択文章、履歴、回答、エラー、用途、入力欄を新しい自由チャット用に初期化するようにした。ショートカットは起動パラメータを優先する。
* 自由質問だけ質問・指示を必須とし、空欄時は `自由質問を入力してください。` とする。要約・整理・翻訳は参照があれば追加指示なしでも実行でき、追加指示がある場合だけ `[利用者の追加指示]` を送信する。
* 通常起動の履歴リセット、ショートカット保持、用途別入力条件、追加指示のメッセージ境界をテストへ追加した。

## 2026-08-04 右側コンテキストパネル統合

* コレクション、AIアシスタント、新規メモ一覧を同じ右側コンテキストパネルへ統合し、単一のタブ状態（`collection`／`ai`／`new-memos`）で排他的に切り替えるようにした。起動時は必ずコレクションを表示する。
* デスクトップでは右側の固定列を維持し、AIや新規メモへの切り替えでメモ一覧・本文の位置を押し縮めない。長い内容はパネル内だけでスクロールする。
* 狭い画面では同じパネルを右側ドロワーとして表示し、閉じるボタン、コレクション、AI、新規メモの切り替えを利用できる。新規メモが0件でもタブと空状態を表示する。
* 既存のコレクション操作、AIショートカット、保存・設定系ツールバーは変更せず、UI契約テストへ右側パネルの排他表示と初期状態を追加した。

## 2026-08-04 PR #48 レビュー対応：メモ一覧を右パネルへ一本化

* 左列に残っていたメモ一覧を右側コンテキストパネルへ移動し、検索欄・描画・選択・現在メモの強調表示を同じ `memoSidebar` の処理で再利用する構成へ修正した。重複していた `newMemosPanel` は廃止し、タブ名を `メモ一覧` へ変更した。
* PCは本文＋右パネルの2列を基本とし、パネルを閉じたときは `body.context-panel-closed` で本文1列へ再構成する。固定幅の空白列は残さない。狭幅では従来どおり同じ右パネルをドロワーとして使用する。
* メモ一覧はゴミ箱以外を作成日時の新しい順に表示し、件数バッジは全対象件数を示す。`toggleCollectionExplorer(false)` はタブ選択を経由せず、実際にパネルを閉じるよう責務を分離した。
* 重複表示、閉じる後のグリッド幅、単一一覧の検索・選択、初期タブ、タブ切り替えをUI契約テストへ追加した。

## 2026-08-04 PR #48 最新レビュー対応：メモ選択後の開閉責務

* メモ一覧の選択後に旧 `setMemoPaneOpen(false)` がPCの右パネルまで閉じていたため、メモ選択時の自動クローズを `layoutMode !== "wide"` の場合だけへ限定した。PCでは本文を更新しつつ右パネルと選択状態を維持する。
* 狭幅での自動クローズは `explicit: false` として記録し、PC幅へ戻ったときは右パネルを再表示する。利用者が×ボタンで閉じた場合は `contextPanelUserClosed` で閉じた状態を尊重する。
* メモ選択後のwide／compact／mobile分岐、幅変更時の再表示、フォーカスとARIA状態の契約テストを追加した。

## 2026-08-05 PR #48: 狭幅コンテキストパネルの開閉同期

### 変更内容

* 狭幅で開いた右側コンテキストパネルをオーバーレイドロワーとして扱い、背景、背後領域の `inert`、背景クリック、`Escape` を同じ開閉経路へ接続した
* 右上の×でパネル全体を閉じる際、AI内部の開閉状態、`body.ai-open`、ロボットのARIA状態も同期して閉じるようにした
* AIタブを×で閉じた後は、会話や入力内容を破棄せず、ロボットボタン1回でAIタブを再開するようにした
* `app.js` の配信キャッシュ番号を `0.4.0-34` から `0.4.0-36` へ更新した

## 2026-08-05 Markdown拡張・注意書き・解説カード

* `app.js` の既存行単位／インラインMarkdownレンダラへ、斜体、打ち消し線、番号付きリスト、チェックリスト、水平線、通常リンクを追加した。コードスパン・フェンスは既存の先行分離を維持し、外部リンクは http/https のみ許可してHTMLエスケープと `noopener noreferrer` を適用する。
* 注意書きは互換性を優先してGitHub形式 `> [!NOTE]`／`TIP`／`IMPORTANT`／`WARNING` を本文へ保存する。編集ツールバーで種類を選んで挿入でき、通常引用とは別のカード表示にする。
* 解説カードは本文へ混ぜず、既存のメモレコードの任意プロパティ `explanations` 配列に保存する。カードID、種別、対象本文、前後文脈、開始・終了位置、本文、折りたたみ状態を保持し、再読み込み・IndexedDB保存で後方互換に扱う。対象不一致時もカードを自動削除しない。
* `markdown-enhancements.test.js` と記法ガイドテストを追加・更新した。既存のMarkdown ZIPは本文をそのまま書き出すため、解説カードのMarkdown単体エクスポートは現時点では含まれない。既存のJSON貼り付け取り込みはAI朝刊向けであり、解説カードを含むMemo Nexus全データJSONの入出力は別途必要である。

## 2026-08-05 PR #50 解説カード位置対応の追加修正

* Markdownソースの単純な出現回数をプレビューDOMへ流用せず、リンク／画像URLやMarkdown記号を除いた表示テキストのソース範囲から、対象文字列の表示上の出現順を求めるようにした。リンク表示名、画像代替文、インラインコード、太字・斜体・打ち消し線、見出し・リスト・引用・コードフェンスの本文を対象にする。
* 既存位置が不一致の場合は、対象が1件だけ、または前後文脈が一意に一致する場合だけ再特定する。同じ対象が複数あり特定不能なら孤立状態とし、先頭一致へ勝手に移動しない。
* 解説カードの初期 `details.open` 設定では保存せず、クリックまたはキーボードで利用者が開閉した場合かつ状態が変わった場合だけ `putNote()` を呼ぶ。再描画による保存ループと更新日時の意図しない変更を防ぐ。
* リンクURL・画像URL、インラインコード、曖昧な再特定、初期描画保存防止の回帰テストを追加した。

## 2026-08-05 PR #50 解説カード描画の追加修正

* Markdownソース上の対象文字列とプレビュー上の表示文字列を分離し、`**保存**` のように記号を含めて選択した場合も、解決済みソース範囲から表示文字列 `保存` と表示上の出現順を求めてマーカーを挿入する。安全に1つの表示範囲へ対応付けられない場合は、誤った位置へ付けず孤立状態にする。
* 解説カードの折りたたみを利用者操作として記録するイベントを `details` 全体から `summary` のみに限定し、編集・削除などカード内の通常操作を折りたたみ操作として扱わないようにした。
* 実際にテキストノードを分割してマーカーDOMを挿入する回帰テストを追加し、重複語句の出現順、リンク／インラインコード相当の表示ノード、解説カードと既存マーカーの除外を確認する。配信キャッシュを `markdown-enhancements-utils.js?v=0.4.0-3`、`app.js?v=0.4.0-40` へ更新した。

## 2026-08-05 PR #50 解説カード結合テストの補強

* リンクとインラインコードについて、ソース範囲の解決から実DOMのリンク／コード要素または末尾本文へのマーカー挿入、カードの非孤立判定までを結合テストにした。対応不能な入れ子Markdownは誤挿入せず、本文・メタデータを保持した孤立カードとして描画し、正常カードへ影響しないことも確認した。
* 解説カードのDOM生成、開閉操作、保存をテスト可能な小関数へ分離した。`summary` のclick／Enter／Spaceだけが開閉を行い、保存処理はメモID・カードIDを固定して直列実行する。初期描画、再描画、通常キー、編集・削除、本文・リンク操作、プログラム由来のtoggleでは保存しない。
* 正規表現ベースの表示位置対応は単一階層を対象とし、装飾の入れ子や複数表示ノードにまたがる対象は安全側に孤立させる制限をREADMEへ明記した。配信キャッシュを `markdown-enhancements-utils.js?v=0.4.0-4`、`app.js?v=0.4.0-41` へ更新した。

## 2026-08-05 PR #52 モバイルの関連メモ重なり順修正

* モバイル用コンテキストパネルの`z-index: 80`が関連メモUIの旧値を上回り、ボタンとドロワーを背後へ隠す可能性があった。`--z-context-panel`、`--z-related-backdrop`、`--z-related-drawer`、`--z-related-trigger`を一元管理し、ドロワー > 背景 > コンテキストパネル、ボタン > ドロワーの順序を全幅で保証した。
* モバイル専用の重複z-index指定を削除し、関連ドロワーの背景が開いている間は背面パネルを操作できないオーバーレイ構造を維持した。配信キャッシュを`style.css?v=0.4.0-29`へ更新した。

## 2026-08-05 右端固定アクションボタンの上下配置

* 関連メモボタンを`top: 40vh; right: 0`へ固定し、ドロワー表示中も右端に残るようにした。AIロボットボタンは`right: 0`とセーフエリア込みの下端へ固定し、狭幅用の負のright指定を削除した。
* `--z-ai-trigger`を関連メモボタンより前面に追加し、関連ドロワー表示中もロボットを操作できる階層にした。配置、負のright指定の不在、キャッシュ番号を回帰テストで確認した。配信キャッシュを`style.css?v=0.4.0-30`へ更新した。

## 2026-08-05 関連メモドロワーの復活

* 右側コンテキストパネル統合後、関連メモのボタンとドロワーが旧z-indexのまま残り、コンテキストパネルの背後へ隠れて操作できなくなっていた。関連メモを独立した右端中央のオーバーレイとして維持し、背景・ドロワー・ボタンをコンテキストパネルより前面へ整理した。
* 既存のリンク／逆リンク／共通リンク／共通語句によるスコアリングと最大8件表示は再利用した。メモ選択時は広い画面でドロワーを維持し、狭幅だけ自動的に閉じる。選択中メモがない場合の案内も追加した。
* `related-drawer.test.js` に重なり順、幅に応じたメモ選択後の開閉、未選択状態の案内を追加し、配信キャッシュを `style.css?v=0.4.0-28`、`app.js?v=0.4.0-42` へ更新した。

## 2026-08-06 Gemini APIアダプタ

* AI設定へ `Ollama（ローカル）` と `Gemini API（クラウド）` のプロバイダ選択を追加した。旧AI設定はOllama設定として移行し、各プロバイダのモデル・接続先・Gemini APIキーを独立して保持する。
* `gemini-adapter.js` はGemini APIのモデル取得、`streamGenerateContent` のSSE逐次受信、AbortControllerによる停止、認証・権限・モデル不存在・429・割り当て上限・5xxなどの共通エラー分類を担当する。APIキーはHTTPヘッダーにのみ使用し、ソース、ログ、テスト、表示用データへ出力しない。
* `ai-provider.test.js`、`gemini-adapter.test.js`、既存Ollamaテストで設定移行、プロバイダ切替、モデル正規化、SSE分割受信、中断、エラー分類、OllamaのNDJSON回帰を確認する。
* 配信キャッシュを `ai-provider.js?v=0.4.0-3`、`gemini-adapter.js?v=0.4.0-1`、`app.js?v=0.4.0-44` へ更新した。
* GitHub Pagesへ`config.js`やフロントエンド環境変数としてAPIキーを配信しない。Gemini APIキーは設定画面から端末ごとのlocalStorageへ保存する。PCとiPhoneなど別端末では共有されず、Safariのプライベートブラウズ、CORS、SSE、停止、再読み込み、ホーム画面追加は実機未確認として扱う。
* Gemini選択時は、質問・指示、選択したメモ本文・文章、AI用参照コンテキスト、会話履歴がGoogle Gemini APIへ送信されることを設定画面とREADMEで明示した。SDKは追加せず、公式REST APIと`fetch`を使用する。料金や無料枠は固定記載しない。

## 2026-08-06 Gemini生成・モデル検証の修正

* 症状は、モデル一覧取得だけを接続成功としていたため選択モデルの生成可否を検証せず、SSEの`Accept: text/event-stream`と初回応答・無通信・全体のタイムアウト設計も不足していたことだった。
* 接続確認でテキスト生成対応モデルを取得し、選択モデルに短い`generateContent`生成を行う。モデル変更後は再確認を必須にし、未検証モデルの送信を無効化する。
* `streamGenerateContent?alt=sse`はSSE専用Acceptヘッダー、CRLF/LF、コメント、複数data行、UTF-8 flush、最終イベント、usageのみ・finishReasonのみのイベントを扱う。初回応答、チャンク間無通信、全体上限を別タイマーで管理する。
* Geminiモデルは`generateContent`を明示するテキストモデルだけを残し、表示名から`models/`を除く。Ollama用の「未確認」は表示せず、安定版／Preview／Experimentalへ分ける。
* 利用者環境で、Gemini APIキーによる接続確認、選択モデルの最小生成、通常質問の回答表示まで確認された。CodexはAPIキーを扱わず、Safari実機、生成停止、再読み込み後の復元は未確認として扱う。診断エラーはprovider、code、HTTP status、model、phaseを内部情報として保持し、APIキー・本文・回答は記録しない。
* APIキー入力が変わった場合も、進行中の接続確認を中断して接続先識別子、選択モデルの生成確認、接続状態を必ず破棄する。保存後・送信前には新しいAPIキーで再度接続確認が必要になる。
* 配信キャッシュを`app.js?v=0.4.0-46`へ更新した。

## 2026-08-09 カード表示の番号付きリスト開始番号修正

* カード表示用Markdownレンダラが分割された番号付きリストごとに`<ol>`を開始番号なしで出力していたため、`2.`や`3.`がすべて`1.`として表示されていた。先頭項目の番号を読み取り、2以上の場合は`<ol start="…">`を出力するよう修正した。
* 本文のMarkdownは変更せず、連続リストと空行・通常文・箇条書きで分割されたリストの開始番号を回帰テストで確認する。配信キャッシュを`app.js?v=0.4.0-52`へ更新した。

## 2026-08-09 登録済み語句リンクの自動関連表示

* 本文の`[[語句]]`を明示語句として集約し、少なくとも1件で明示されている語句だけを登録済み語句として扱う。登録済み語句を本文に含む他メモには、保存済みメモ一覧の再描画時に自動語句として付与する。本文・IndexedDBスキーマは変更しない。
* 英数字のみの語句は前後を英数字・アンダースコア以外で区切る完全語判定にし、`AI`が`FAIL`へ誤一致することを防ぐ。日本語などを含む語句は文字列そのものの一致とする。
* 色は保存せず、英字の先頭文字グループ、数字、かなの行、漢字などは安定ハッシュから固定パレットを導出する。メモ一覧カードには最大4色の細いアクセントと同数の語句チップを表示し、残りは`+N`にまとめる。配信キャッシュを`term-link-utils.js?v=0.4.0-1`、`app.js?v=0.4.0-53`へ更新した。

## 2026-08-09 登録済み語句リンクの更新・色分類修正

* 語句関連のキャッシュを`notes`配列の参照比較から、明示的な無効化方式へ変更した。本文保存、メモ作成、削除、ゴミ箱移動・復元・完全削除、別ウィンドウからの同期読み込み後に無効化し、次回一覧描画時に再構築する。入力のたびに全メモは走査しない。

## 2026-08-09 本文プレビューの登録済み語句リンク

* 登録済み語句が通常本文に現れた場合、保存値を変えずにプレビューだけを既存のWikiリンクボタンとして装飾するようにした。明示リンク・自動リンクとも語句由来の同じ固定色を使い、既存のリンク遷移処理を共用する。
* 自動装飾はコード、Markdownリンクのラベル、画像代替文を除外する。短い`C`は`C言語`、区切り、`Cについて`・助詞に続く文脈だけを対象とし、`CSS`、`ABC`、`Cドライブ`、`Cクラス`、`C++`、`C#`を除外する。配信キャッシュを`term-link-utils.js?v=0.4.0-3`、`app.js?v=0.4.0-55`へ更新した。

## 2026-08-09 語句ランキングの共通一致判定

* 語句ランキングの出現回数を単純な部分一致ではなく、本文プレビュー・関連語句カードと同じ`findAutomaticTermMatches()`で数えるようにした。これにより`[[C]]`登録時も`C言語`や独立した`C`だけを数え、`Complete`、`CSS`、`CSV`、`ABC`、`C++`、`C#`、`Cドライブ`を除外する。対象メモ数も同じ一致件数から算出する。配信キャッシュを`app.js?v=0.4.0-56`へ更新した。

## 2026-08-09 重複登録語句の表示・集計分離

* 重複した登録語句は、本文プレビューとカードのautomatic語句チップでは既存の最長・非重複一致を使い、二重リンク・二重チップを防ぐようにした。明示`[[語句]]`は従来どおりautomatic表示より優先する。
* 語句ランキングには集計用matcherを追加し、登録済みの長い語句に含まれる短い語句も個別に数える。単純部分一致には戻さず、長い語句と`C`の専用境界判定を再利用する。配信キャッシュを`term-link-utils.js?v=0.4.0-4`、`app.js?v=0.4.0-57`へ更新した。
* 英字はA〜G、H〜N、O〜T、U〜Zの4固定グループへ変更した。かなは濁音・半濁音・小書き文字とカタカナを対応する五十音行へ揃え、`ヴ`はあ行として扱う。漢字その他は従来どおり安定ハッシュへフォールバックする。READMEに利用者向け仕様を追記し、配信キャッシュを`term-link-utils.js?v=0.4.0-2`、`app.js?v=0.4.0-54`へ更新した。

## 2026-08-09 ロゴアニメーション

* ヘッダーへ固定寸法の操作可能な`Memo Nexus`ロゴを追加し、タイプライター、Nexus接続、光の走査を一度だけ再生できるようにした。装飾は`aria-hidden`かつポインターイベントを受けず、終了時は再生クラスを解除する。
* 設定はlocalStorageへ保存し、日替わりはローカル日付から決定論的に選ぶ。オフと`prefers-reduced-motion: reduce`では静的表示にする。配信キャッシュを`style.css?v=0.4.0-35`、`logo-animation-utils.js?v=0.4.0-1`、`app.js?v=0.4.0-58`へ更新した。

## 2026-08-09 ロゴアニメーション循環とNexus接続の改良

* ロゴ操作は同じ演出の再生ではなく、タイプライター → Nexus接続 → 光の走査を循環する一時選択へ変更した。設定値は変更せず、再読み込み時は既存の固定設定または日替わり設定へ戻る。Reduced Motionでは種類だけを切り替え、アニメーションは再生しない。
* ロゴの文字を18px・176×44pxへ統一し、Nexus接続は左上の点、逐次表示する文字、左・上辺、右・下辺、右下の点、短いアクセントグローの順で完成する枠線演出へ更新した。配信キャッシュを`style.css?v=0.4.0-36`、`logo-animation-utils.js?v=0.4.0-2`、`app.js?v=0.4.0-59`へ更新した。

## 2026-08-09 エディタのアイドル時カーソル演出

* 標準textareaのキャレットを常時置換せず、デスクトップの安全な座標取得時だけ疑似キャレットを600ms一回転させる演出を追加した。IME、選択、入力・貼り付け・移動・スクロール、非表示、モーダル、AI、Reduced Motion、ポップアウトでは待機または即時中止する。
* `設定` → `表示` にON/OFF、4秒、Reduced Motion尊重を追加し、端末内の既存localStorage方式で保存する。配信キャッシュを`style.css?v=0.4.0-37`、`editor-caret-animation-utils.js?v=0.4.0-1`、`app.js?v=0.4.0-60`へ更新した。
* 初回の待機時間後に一度演出したら、操作がない限り回転完了から固定20秒で次回を1件だけ予約するように変更した。操作・中断条件では実行中の演出と予約を破棄し、初回待機時間から再開する。

## 2026-08-10 Web Clipper 第1段階

* `設定` → `データ管理`へWebクリップの入口を追加し、タイトル、URL、サイト名／ホスト名、選択本文、取得日時、保存先コレクションを保存前に確認・編集できるダイアログを追加した。拡張を使わず直接開いた場合も、同じ画面から手動入力して通常メモとして保存できる。
* 保存は既存の`createNote()`経路を利用し、本文は引用Markdown、出典、URL、取得日時で作成する。既存メモ・Importを壊さない省略可能な`source`メタデータへも、Webクリップ種別・出典情報を保存する。
* `http:`・`https:`以外のURLは保存を拒否し、既存の外部リンク表示と同じ安全なリンク形式だけを本文へ出力する。
* `extensions/web-clipper`へChrome／Edge共通のManifest V3拡張を追加した。ユーザーが拡張アイコンを押したタブだけの`activeTab`権限で選択文、タイトル、URL、ホスト名、取得日時を取得し、本文をURLパラメータに含めず、受取画面の準備完了後に`window.postMessage`で送信する。選択なしではURLクリップ候補として送信できる。
* 本体側は`web-clipper-config.js`で設定した拡張originだけを受信許可する。拡張の開発URL・本番URL・host permission・拡張ID設定、Chrome／Edgeへの開発版インストール手順は拡張READMEへ記載した。
* 画像クリップ、本文全体抽出、ダウンロード、AI要約、iPhone／iPad Safari拡張は未対応とする。`node --test`は348件成功し、構文確認、Manifest JSON確認、`git diff --check`も成功した。実ブラウザでの拡張インストールを伴う手動確認は、この作業環境にブラウザ自動操作CLIがないため未実施とする。

## 2026-08-10 Web ClipperのChrome／Edge許可origin設定

* `web-clipper-config.js`の許可originへ、Edge版`opejammnohhbjflpbhmmdlknhjkhfhdp`とChrome版`mhfbofiokmppgdliakminbgdgcmbhbac`を追加した。既存の`chrome-extension://<32文字ID>`形式による厳格なorigin検証、URL制限、受信処理は変更していない。
* 設定ファイルに両IDだけが登録され、プレースホルダーを残さず、受信側のorigin形式検証が維持されていることを回帰テストで確認する。

## 2026-08-10 Web Clipperフラグメント受け渡し修正

* 拡張ポップアップが本体の受信準備を待つ`postMessage`経路だけに依存していたため、ポップアップの終了タイミングによって本体が空欄で開く問題があった。選択した接続先URLに`?web-clip=1#clip=<base64url>`を付加し、タイトル、URL、ホスト名、選択本文、取得日時をUTF-8のbase64url payloadとして渡すよう変更した。
* 本体は初回表示でフラグメントをデコード・検証して確認画面へ反映し、成功・失敗を問わず`history.replaceState()`でクリップpayloadをURLと履歴から除去する。不正payload、必須情報の欠落、本文上限超過はクラッシュさせず、再実行を案内する。選択なしでは本文だけ空として扱う。
* 拡張のスクリプト注入不可ページは技術的な例外を表示せず、通常のWebページで試すよう案内する。URL長にはブラウザ／OSごとの上限が残るため、長すぎる選択範囲は短くする必要がある。

## 2026-08-11 Web Clipper ページ全文抽出の自己完結化

* `executeScript`へ渡す本文抽出関数の除外セレクタとスコアリングを関数内へ移し、対象ページ上で外部の`score`や`EXCLUDE`を参照しないよう修正した。本文候補がない場合は空文字を返し、利用者には既存のフォールバック案内を表示する。
* localhost／127.0.0.1の開発サーバー向けhost permissionを追加し、注入失敗、本文候補なし、Markdown変換後の空を利用者向けに区別して表示するようにした。
* ページ全文はポップアップ寿命に依存せず、`storage.local`の一時データをMemo-Nexus限定Content Scriptが転送し、ack後に削除する方式へ変更した。
* Content ScriptはACKまで400ms間隔・最大約12秒で再送し、初期化順序による一度きりの通知取りこぼしを回避する。タイムアウト時は利用者向けエラーへ切り替える。

## 2026-08-11 Web Clipper 第2段階

* 選択部分、ページ全文、リンクのみ、メモ付きの4方式を追加した。ページ全文は主要コンテンツを抽出して安全なMarkdownへ変換し、巨大なURLフラグメントには載せず、originを限定した`postMessage`で本体へ渡す。
* `source.type = "web-clip"`を維持し、任意の`clipMode`と`userMemo`を追加した。Chrome/Edgeの許可origin、http/https制限、短文のフラグメント経路は維持する。

## 2026-08-11 Web Clipperのエラー案内と開発接続先修正

* 選択本文が既存上限を超えた場合は、技術的な`clip too large`を表示せず「選択範囲が長すぎてクリップできません。範囲を短くして再度お試しください。」と案内するようにした。予期しない失敗の詳細はconsoleへ残し、画面には簡潔な案内だけを表示する。
* 拡張側で生成したUTF-8 base64url payloadを本体側で実際に復元する統合テストを追加し、日本語、URL、改行、絵文字、選択なしを確認した。壊れたpayloadの安全な処理も維持する。
* 開発環境の接続先とManifest許可URLを`http://127.0.0.1:5500/`へ統一し、接続先表示を「開発環境（127.0.0.1:5500）」へ変更した。本番URLは変更していない。`localhost:5500`へ戻らないことを回帰テストで確認し、開発本体はHTTP 200で応答することを確認した。

## 2026-08-11 Web Clipper payloadエラー改善の検証記録

* コミット`517db1e fix: clarify web clipper payload errors`で、長文選択時は指定の利用者向け文言を表示し、予期しないエラーはconsoleへ記録して画面には安全な案内のみを表示するようにした。
* 拡張側payloadを本体側で実際に復元する統合テストを追加し、日本語、URL、改行、絵文字、選択本文なしを確認した。既存の壊れたpayloadを安全に扱うテストも維持する。
* `node --test`は357件成功し、構文確認と`git diff --check`も成功した。`package.json`がないためlint設定はない。実機拡張確認は、この環境でブラウザ自動操作ツールを利用できず未実施とし、PR本文へ記載した。

## 2026-08-11 Web Clipper Chrome拡張ID修正

* 本体が受信を許可するChrome拡張originを、誤登録の`chrome-extension://mhfbofiokmppgdliakminbgdgcmbhbac`からMemo-Nexus用の`chrome-extension://aelacnladkiohkhbjhfbmeknbfgpcmlh`へ置き換えた。Edge版`chrome-extension://opejammnohhbjflpbhmmdlknhjkhfhdp`は維持した。
* 許可originが上記2件だけであり、誤ID・プレースホルダー・ワイルドカードが含まれず、`chrome-extension://<32文字ID>`形式の検証を維持するテストを追加した。

## 2026-08-12 PR #78 Web Clipper 実ブラウザE2E

* Playwright 1.56.1付属Chromium 141.0.7390.37をpersistent contextで起動し、`--disable-extensions-except`と`--load-extension`で`extensions/web-clipper`を実際に読み込んだ。Computer Use、Google Chrome、Edgeは使用していない。
* 拡張Service Workerの実URLからID `aelacnladkiohkhbjhfbmekpbfgpcmlh` を取得した。このIDを取得できるよう、転送処理を変更しない最小のbackground Service Worker定義を追加した。
* `http://127.0.0.1:5500/`のMemo-Nexusと、見出し・複数段落・リスト・リンク・固有文字列・約16万文字の長文を含むローカル取得元ページで、実際の拡張ポップアップ画面と確認dialogを操作した。
* ページ全文、選択部分、リンクのみ、メモ付き、タイトル・URL・本文反映、受信待ち文言の解消、ACKなし再送、ACK前の`storage.local`保持、ACK後削除、重複受信、タイムアウト、長文記事をすべて確認した。`node --test`は361件成功、`node --check web-clipper.e2e.js`と`git diff --check`も成功した。
* E2Eは`web-clipper.e2e.js`として追加し、ページ本文受信後とACKなしタイムアウト受信先のスクリーンショットを`e2e-artifacts/`へ保存した。コミット`934e074 test: add web clipper browser e2e`でPR #78へ反映し、実施結果と証跡をPRコメントへ追記した。未確認事項はない。

## 2026-08-12 Web Clipper Chrome拡張ID追加

* Chrome版Web Clipper `aelacnladkiohkhbjhfbmekpbfgpcmlh` のoriginを許可一覧へ追加した。既存のEdge版およびChrome版originは維持し、厳格な`chrome-extension://<32文字ID>`検証は変更していない。

## 2026-08-12 Web Clipper設定画面の整理

* 設定画面へWeb Clipperの専用欄を追加し、`allowedExtensionOrigins`から参照した許可済みOrigin全体と拡張ID、現在のMemo-NexusアプリURLをコピー操作付きで表示するようにした。Origin文字列からChrome／Edgeを判定する表示は行わない。
* 最後に受信したWeb ClipperメッセージのOriginと受信日時をlocalStorageへ記録し、現在の許可一覧に照らした許可済み／未許可／未受信を表示する。メモ保存、受信ダイアログ、保存先の既存挙動は変更していない。
* 検証では`node --check app.js`、`node --check web-clipper-config.js`、`node --test`（363件成功）、`git diff --check`を実行した。型チェック・lintは設定ファイルまたは実行スクリプトがないため実行対象なし。Playwright E2Eはこの環境に`playwright`パッケージがなく起動できなかった。

## 2026-08-12 メモフラグ

* 重要度の本文記法とは別に、あとで見返す・対応を残す・一時的に注目するメモを横断して示すboolean属性`isFlagged`を追加した。新規メモは`false`で、古いデータに属性がない場合も`false`として扱う。
* 編集画面のタイトル操作部にキーボード操作できるフラグ切替を追加し、押下時は既存の保存・別ウィンドウ同期経路で即時保存する。フラグ付きメモは一覧カードのタイトル前に共通のアクセントアイコンを表示し、既存の並び順は変更しない。
* ドラフト復元とMarkdownエクスポート／ファイル取り込みに`isFlagged`を含める。エクスポートは本文を変えず先頭のMemo Nexusメタデータコメントに保存し、既存形式のデータには影響しない。
* 新規値、付与・解除、旧データ互換、Markdown往復を自動テストで確認する。ライト／ダークテーマ、モバイル表示、ポップアウト同期はブラウザでの手動確認対象とする。将来候補の「フラグのみ表示」「フラグを上部へ固定」は今回実装しない。

## 2026-08-12 メモフラグの表示色と操作演出

* フラグ付きメモの旗はテーマのアクセントカラーではなく、ライト／ダークそれぞれで視認できるフラグ専用の赤系CSS変数`--note-flag`を使用するようにした。エラー用の`--danger`とは用途を分け、一覧アイコンと編集画面の切替ボタンで共用する。
* 切替ボタンはクリックごとに、付与時は下から持ち上がって軽く弾む260ms、解除時は控えめに戻る220msの演出を再生する。再生クラスは都度付け直し、`prefers-reduced-motion: reduce`では無効化する。保存、Markdown export/import、ポップアウト同期の経路は変更していない。

## 2026-08-12 メモフラグのタイトル行・連続アニメーション修正

* フラグ切替中に保存状態表示が縮み、操作ボタンの位置が動く不具合を修正した。タイトル操作部を固定グリッドにし、フラグ、ポップアウト、エクスポート、保存状態を独立した領域へ配置する。モバイルでも各操作のタップ領域と保存状態の幅を固定する。
* 旗の演出はボタンやタイトル行ではなく、内側の旗アイコンだけに限定した。クリックごとにアイコンだけを安全に差し替え、再生トークンと次フレームでのクラス付与により、連続した付与・解除でも毎回再生できるようにした。`animationend`後はアイコンの演出クラスを外し、Reduced Motionでは差し替え・演出を行わない。保存、Markdown export/import、ポップアウト同期の経路は変更していない。

## 2026-08-12 メモフラグのタイトル入力フォーカス修正

* タイトル入力を専用ラッパーへ分離し、フォーカスのborderとinset box-shadowをそのラッパーだけに適用するよう修正した。タイトル側は`min-width: 0`、入力は`box-sizing: border-box`として、長い題名でもフラグ・保存状態の固定操作領域を押し広げない。
* 保存状態はタイトル行の固定操作領域内に収め、狭幅では時刻を省いた「保存済み」表記にする。旗のアニメーションは引き続き内側アイコンだけを変形し、保存、Markdown export/import、ポップアウト同期は変更していない。

## 2026-08-12 メモフラグのタイトル操作部実寸修正

* PC操作部は`box-sizing: border-box`の254pxから右padding 16pxを除いた238pxへ、ボタン3個（各30px）、gap 3個（各7px）、残り127pxの保存状態列を配置するよう修正した。列とgapの合計237pxがcontent box内に収まり、保存状態は省略表示を含めて領域外へ出ない。
* タイトル入力ラッパーには`overflow: hidden`を追加し、フォーカス装飾は`title-input-wrap:focus-within`だけに維持する。ブラウザ実測では、PCでタイトル入力の右端が操作部の左端で止まり、保存表示が操作グリッド内に収まることを確認した。PCで5回連続した付与・解除では、各回の旗アニメーションと保存表示・ボタン・タイトル幅の不変を確認した。390px幅では、フラグの40pxタップ領域、短縮「保存済み」の72px領域、タイトルと操作部の境界を確認した。

## 2026-08-12 メモフラグのタイトルエリア二段構成

* タイトルエリアを、左に別ウィンドウ・フラグ・エクスポートの操作群、右に保存状態を置く上段と、全幅のタイトル入力を置く下段へ分離した。保存完了時は幅による短縮を廃止し、常に`保存済み HH:mm:ss`を表示する。保存状態列は104pxの固定領域として、保存中・保存済みの文言切替でも位置を変えない。
* フォーカス装飾は引き続き`title-input-wrap:focus-within`だけに限定し、旗のアニメーションは内側アイコンだけに適用する。実ブラウザで、PCの秒付き保存表示と5回連続切替時の配置不変・毎回の演出、390px幅の40px操作ボタン・上段内の保存表示・全幅タイトル入力を確認した。フラグ保存、Markdown export/import、ポップアウト同期の保存経路は変更していない。

## 2026-08-12 メモフラグの表示密度調整

* フラグボタンは40pxの押下領域を維持しつつ、フラグ済みの赤い塗り背景だけを32pxの内側要素へ縮小し、旗アイコンも15pxへ調整した。アニメーションは従来どおり内側の旗だけに適用し、操作ボタンや上段レイアウトを動かさない。
* 作成・更新日時をタイトル上段から外し、エディタカード末尾の控えめなメタ情報領域へ移動した。実ブラウザでPC・390px幅・ポップアウトを確認し、上段が操作群と保存状態だけであること、本文末尾の日時が編集領域と重ならないこと、40pxの押下領域と32pxの赤背景を確認した。

## 2026-08-12 メモフラグの同期表示と下部帯調整

* 本体・別ウィンドウ間の同期判定と保留状態は維持したまま、タイトル上段の「別ウィンドウで更新があります」補足表示を削除した。上段は左の操作アイコン群と右の保存状態だけとし、保存状態は従来どおり`保存済み HH:mm:ss`、保存中、保存エラーを表示する。
* エディタ末尾の日時を、左に将来の文字数詳細ボタン用40px領域、右に`作成: YYYY/MM/DD HH:mm　更新: YYYY/MM/DD HH:mm`を置く薄い帯へ整理した。実ブラウザでPC・390px幅・ポップアウトを確認し、帯が本文直後に収まりスクロールや入力を妨げず、フラグ切替中も上段の配置と旗アイコンだけの演出が維持されることを確認した。

## 2026-08-12 文字数・文章統計

* 本文エディタ末尾の既存メタ情報帯の左側へ、空白を除く文字数を表示する操作可能なチップと詳細ポップオーバーを追加した。詳細では空白込み文字数、空行で区切る段落数、推定読了時間、漢字・ひらがな・カタカナ・アルファベット・数字・記号・空白・その他（絵文字など）を表示する。チップはキーボードで開け、Esc、閉じるボタン、外側クリックで閉じる。
* 集計は副作用のない`text-stats-utils.js`へ分離し、`Intl.Segmenter`を利用できる環境では書記素単位で集計する。文字種は重複計上せず、空白なし文字数はUnicode空白を除外する。読了時間は日本語向けの毎分500文字を定数として採用し、500文字未満は`1分未満`、それ以上は切り上げた`約N分`で表示する。
* 本文入力ごとにチップと開いている詳細を更新するが、保存・Markdown export/import・ポップアウト同期の既存経路は変更していない。実ブラウザでPCの入力連動、内訳、Escによる閉じる操作、ライト／ダーク両テーマの可読性、390px幅で画面内に収まる詳細表示と閉じる操作、同じメモのポップアウト起動を確認した。Unicode集計、段落、読了時間、アクセシブルなUI・モバイル幅制約は自動テストで確認し、`node --test`は375件成功した。

## 2026-08-12 文字数・文章統計のCSS配信キャッシュ修正

* 文字数チップと文章統計ポップオーバー用の追加CSSが既存キャッシュで残らないよう、`style.css`の読み込みURLを`v=0.4.0-45`へ更新した。CSS読み込みURLを確認するレスポンシブ表示・表貼り付けの既存テストも同じ番号へ更新し、機能仕様は変更していない。

## 2026-08-12 Web Clipper 第3段階（記事画像のローカル保存）

* ページ全文と取得可能な選択範囲では、既存の本文抽出範囲にある画像から`srcset`、遅延読み込み属性、`src`を解決し、広告・ロゴ・ナビゲーション・プロフィール・追跡用小画像などを除外する。本文Markdownには位置マーカーを残し、確認画面で選択した画像を元の位置へ挿入する。リンクのみ・メモ付きでは画像を取得しない。
* 確認画面へ画像プレビュー、個別選択、全選択・全解除、alt、キャプション、元URL、形式、容量、画像ごとの失敗理由を追加した。取得失敗・対応外形式・容量超過が混在しても本文保存を継続し、該当位置には理由付きの「画像を保存できませんでした」注記を残す。
* 保存画像はWeb Clipper専用方式を作らず、既存の添付DB、画像圧縮、`attachment://`参照、画像ブロック直列化、プレビュー、削除、Markdown ZIPエクスポートを再利用する。クリップ由来、元画像URL、alt、キャプション、取得日時、位置トークンは添付とメモの任意メタデータに保持し、将来の同一URL更新でクリップ部分だけを識別できる構造にした。既存の非圧縮Markdown ZIPを画像参照ごと再取り込みする入口も追加した。
* 上限は既存のメモ添付合計20MBに合わせ、1画像5MB、1クリップ20画像、クリップ画像合計20MBを定数化した。JPEG、PNG、WebPを保存対象とし、既存画像ブロック未対応のSVG・GIFは変換せず除外する。認証必須、CORS拒否、期限切れURL、ページ側から取得できない画像は既知の取得制約として画像単位で案内する。
* 自動テストでは本文中の順序・位置、1枚除外、取得不能画像との混在、リンクのみ、形式・容量上限、既存画像ブロック統合、Markdown ZIP往復、モバイル1列表示を含む389件が成功した。`node --check`と`git diff --check`も成功し、lint・型チェックは実行設定がないため対象外とした。実ブラウザではPC幅で個別除外・失敗注記・ローカル画像ブロック・キャプション・拡大表示・添付一覧を、390px幅で1列のプレビューとチェック操作を確認した。

## 2026-08-12 Web Clipper画像取得のService Worker移行

* 記事画像を`chrome.scripting.executeScript()`で記事ページ内から取得していたため、別ドメイン/CDNではページのCORS制約を受け、取得結果が確認画面へ確定しない問題を修正した。画像本体の取得をManifest V3 Service Workerへ移し、ポップアップとのメッセージはMIME形式、容量、ファイル名、元URL、Base64本体を含むJSONとして返す。候補は成功・取得失敗・対応外形式・容量超過・権限不足・タイムアウトのいずれかへ必ず確定する。
* 任意記事のCDN画像を取得するため、host permissionは`http://*/*`と`https://*/*`だけを必須指定した。`<all_urls>`、`file:`、ブラウザ内部ページは許可しない。Chrome／Edgeでは、この権限を追加した版のインストール・更新・開発版再読み込み時にサイトアクセス権限の受諾が必要になる場合がある。
* 形式判定はURL拡張子ではなくHTTP `Content-Type`とマジックバイトを使い、`application/octet-stream`でもJPEG、PNG、WebPの実体なら保存可能にした。AVIF、SVG、GIFは変換せず「対応外形式」として確定する。保存可能画像は初期選択し、候補総数・保存可能数・選択数を分けて表示する。
* 選択画像のデコードまたはIndexedDB保存に失敗した場合はメモを作成せず確認画面を維持し、画像ごとの理由を表示して再試行できるようにした。利用者が明示的に「本文のみ保存」を選んだ場合だけ画像なしで保存し、添付保存後のメモ作成失敗では添付をロールバックする。
* Service Worker取得、同一ドメインJPEG、別ドメインのoctet-stream PNG、混在失敗、権限不足、タイムアウト、本文位置の`attachment://`置換を実処理テストへ追加し、`node --test`は393件成功した。実拡張E2EにはIndexedDB保存、再読み込み表示、全解除、保存失敗時のダイアログ維持を追加したが、検証用Playwrightの一時取得がネットワーク待ちで完了せず今回は未実行。Chrome／Edgeの実ブラウザでは本体確認画面の形式・容量・選択、保存・再読み込み表示を確認し、Chromeでは保存失敗・再試行・本文のみ保存も確認した。

## 2026-08-12 Web Clipper画像取得確定・GIF／SVG／AVIF対応

* ポップアップの画像取得を、リクエストID付きの画像単位メッセージと最大3件の並行処理へ変更した。`chrome.runtime.lastError`、応答ID不一致、受信先なし、権限、HTTP、ネットワーク、MIME、デコード、容量、変換、タイムアウトをエラーコード付きの確定状態へ変え、Service Worker停止・再起動後も再取得できるようにした。拡張バージョンをポップアップへ表示し、状態欄は省略せず折り返しと`title`で全文を確認できるようにした。
* 候補抽出はページへ`page-extractor.js`をファイル注入し、補助関数を含む抽出APIを実行する。`article`／`main`、`figure`／`figcaption`、実寸を評価し、ロゴ・広告・SNS・プロフィール・追跡用小画像などを除外してURL重複排除後に20件上限を適用する。抽出順と本文中の位置マーカーは維持する。
* GIFを既存画像添付へ追加し、署名と縦横サイズを検証してCanvas圧縮を通さず元バイト列のまま保存・表示・Markdown ZIP往復できるようにした。SVGとAVIFは追加した`offscreen`権限で必要時だけOffscreen Documentを作り、SVGのスクリプト、イベント、`foreignObject`、アニメーション要素、外部参照を除去してからWebP（利用不可時はPNG）へ変換する。AVIFもデコード可能な環境では同じ変換経路を使い、元MIMEと変換有無を添付メタデータへ保持する。
* Playwright 1.56.1付属Chromium 141のpersistent contextへ実際のManifest V3拡張を読み込み、2オリジン間のリダイレクトJPEG、octet-stream PNG、クエリ付きWebP、複数フレームGIF、安全化SVG、AVIF、HTTP 404、15秒タイムアウト、装飾SVG 25件、Service Worker停止・再起動を検証した。6画像のIndexedDB保存、`attachment://`置換、再読み込み後の6画像表示、GIF元データ一致、SVG外部通信0件、保存失敗時のダイアログ維持、本文のみ保存の明示操作まで成功した。
* `node --test`は398件成功し、全JavaScriptの`node --check`、Manifest JSON解析、`git diff --check`も成功した。`package.json`がないためlint・typecheck・buildは実行対象なし。Google Chrome／Edgeへの開発版手動インストールと日文研の実ページ確認は未実施で、READMEの手順に従う手動確認対象として残す。

## 2026-08-12 Web Clipper起動URL消費・開発／本番更新管理

* `?web-clip=1`を確認画面の一回限りの起動目印として扱い、起動判定を保持して画面を開いた後、受信成功・失敗のどちらでも`history.replaceState()`により`web-clip`だけを全件削除する。他のquery、popout、クリップ転送のhashは維持し、閉じる・保存後の再読み込みで空の確認画面を再表示しない。
* 拡張版を`0.3.1`へ更新し、manifestを正として拡張バージョン、Manifestバージョン、ブラウザ種別、development／production接続先をすべての受信データへ付ける。本体はSemVerで最低互換版`0.3.0`と比較し、欠落または古い版には更新警告を表示する。設定画面にも最後に受信した診断情報を残すが、旧版の本文クリップ自体は拒否しない。
* development選択時だけ、ポップアップ起動直後に開発サーバーのmanifestを`no-store`で確認する。新しい版では保留中の転送がない時だけ一度reloadし、対象版と試行時刻を保存する。同じ対象版のままなら別フォルダ読込の手動確認へ止め、新しい対象版なら再び一度試せる。接続先は拡張ストレージへ保存し、サーバー停止時は通常のクリップ処理を継続する。
* production選択時はlocalhostのmanifest取得、独自の更新確認、無条件reloadを行わず、将来のEdgeアドオン（Hidden公開可）の標準更新を前提とする。公開更新はmanifest版を上げてPartner Centerへ再提出する手順をREADMEへ記録し、未登録の現状ではストア更新を確認済みとしていない。自動更新機能を持たない従来のローカル開発版から導入する時だけ、利用者による最後の一度の手動再読み込みが必要になる。
* 利用者の実ブラウザ確認ではRenderの記事で画像候補14件、保存可能13件、`UNSUPPORTED_FORMAT` 1件へ確定し、13件の保存、メモ再表示、ブラウザ再読み込み後の維持まで成功した。画像取得、IndexedDB、`attachment://`、再表示の主要経路が動作することを確認済みの前提として、今回の変更ではその保存経路を変更していない。
* 自動テストは`node --test` 410件が成功した。Playwright付属Chromiumへ実際のManifest V3拡張`0.3.1`を読み込み、起動URLの成功・失敗時消費と再読込時の非再表示、他query維持、開発manifestの503時もクリップ継続、development／production選択の永続化、production再起動時の開発manifestアクセス0件、既存の6画像保存・IndexedDB・`attachment://`・再読込表示を確認した。Edgeアドオンの登録・ストア配信と、Google Chrome／Microsoft Edge実機での今回の更新検出は未確認とする。

## 2026-08-12 Web Clipper転送清掃・配布方式分離

* 拡張版を`0.3.2`へ上げ、転送レコードの10分TTL、キー形式、clip形式の検証を共通ユーティリティへ集約した。更新確認では全転送キーを調べ、期限切れ、`createdAt`欠落、不正clipだけを削除し、10分以内の正常な転送が1件以上ある場合だけreloadを延期する。
* 転送用`window.open()`が失敗した場合とACKなしで再送がタイムアウトした場合は、今回作成した転送キーだけを削除してからエラーを通知する。ACK成功時の削除は維持し、同時に存在する別の正常な転送は削除しない。
* 接続先と配布方式を分離し、現在の設定を`unpacked-development`（ローカル開発版、初期接続先development）と明示した。productionへ接続してもローカル版表示を維持し、localhost更新確認は行わない。将来の`edge-store`は初期接続先production、Edge標準更新とする設定位置を用意したが、ストアパッケージ作成・登録・自動更新は未確認とする。画像取得・保存と`?web-clip`消費処理は変更していない。
* `node --test`は416件成功した。Playwright付属Chromiumの実Manifest V3 E2Eでは、期限切れ転送だけの清掃、正常転送の保持、`window.open()`失敗後の残存0件、ACKタイムアウト後の該当キー削除と別正常キー保持、ローカル版の本番接続表示、production時の開発manifestアクセス0件を確認した。従来のJPEG／PNG／WebP／GIF／SVG／AVIF、6画像のIndexedDB保存、`attachment://`置換、再読み込み表示も成功した。

## 2026-08-13 Web Clipper Edge設定復旧

* 設定の「データ管理」内へWeb Clipper欄を配置し、許可済み拡張機能IDをOriginとともに一覧表示するよう明確化した。各IDの既存コピー操作を維持し、Edgeで拡張機能が見つからない場合に、拡張機能一覧を開く、有効化または「展開して読み込み」で再登録する、表示IDと許可IDを照合する、という復旧手順を追加した。
* Memo-Nexus Web Clipper用の許可originを追加した。既存の許可originは削除せず、テストで同IDが1回だけ登録されることを確認する。既存の厳格な`chrome-extension://<32文字ID>`検証とChrome／Edge共通の受信経路は変更していない。
* `node --check app.js`、`node --check web-clipper-config.js`、`node --test`（416件成功）、`git diff --check`を実行した。Web Clipperの受信ペイロード復元、起動URL消費、転送保持の既存テストは成功した。実Manifest V3を読み込む`node web-clipper.e2e.js`は、この環境に`playwright`がなく起動できず、一時導入もネットワーク待ちで完了しなかったため未実施とする。

## 2026-08-13 Memo-Nexus Web Clipper ID訂正

* Memo-Nexus Web Clipperの正しいEdge拡張ID`lecpajkpnjnagbeokicilagdonkcimbo`を許可originへ登録した。誤って登録されていた別拡張機能のIDは、許可設定、テストデータ、LOGの説明から除去し、既存の他の許可originは維持した。設定画面の許可ID一覧とIDコピー操作は既存のまま利用できる。
* `node --check web-clipper-config.js`、`node --test web-clipper-config.test.js web-clip-receive.test.js`（12件成功）、`git diff --check`を実行した。実機Edgeでの確認画面表示・保存確認は、Windows操作が現在のブラウザURLを安全に判定できず停止したため未実施とする。

## 2026-08-13 Web Clipper ポップアップ取得フロー修正

* ポップアップの方式名を「クリップ方式」とし、選択なしでも暗黙にリンクのみに切り替えず、選択部分は送信不可としてページ上の選択を促すようにした。ページ全文では、確認画面を開く前に本文抽出、本文内画像のService Worker取得、取得件数の表示を完了し、本文・画像・URL・タイトル・取得日時を揃えてから転送する。リンクのみは本文と画像を渡さない。
* 拡張から受信した確認画面ではクリップ方式を固定し、別方式の未取得本文・画像を捏造しない。再取得が必要な場合は拡張機能へ戻る案内を表示する。アプリ内で手動作成するWebクリップでは従来どおり方式を編集できる。
* 拡張manifestを`0.3.3`へ更新し、拡張コード・HTML・CSS・manifest・設定を変更するPRでは同じPRでversionとREADMEの更新案内も上げる運用を記載した。`node --test`は417件成功、関連テストは13件成功、関連JavaScriptの`node --check`と`git diff --check`も成功した。実拡張E2EとWikipediaを使うEdge実機確認は、環境に`playwright`がなく起動できず、Windows操作もブラウザURLを安全に判定できないため未実施とする。

## 2026-08-13 Web Clipper 展開読み込み版の判別・更新案内

* ポップアップ上部の表示を「拡張機能バージョン: <version>」へ明確化し、実行中の`chrome.runtime.getManifest().version`を表示する。利用できるクリップ方式として、選択部分、本文・画像を取得してから確認画面を開くページ全文、リンクのみ、メモ付きを常時表示する。
* manifestを`0.3.4`へ更新し、READMEの現在版表記と同期した。展開読み込み版はGitHubマージだけで更新されないこと、PC内リポジトリをpullしてから`edge://extensions`で版数を確認し、`0.3.4`未満なら再読み込みする手順を記載した。再読み込み後も更新されない場合は、古い拡張を削除して現在の`extensions/web-clipper`を再登録し、Memo-Nexus設定画面で許可済みIDを確認・登録するよう案内する。
* `node --test`は418件成功、関連テストは14件成功、`node --check extensions/web-clipper/popup.js`、`node --check web-clip-receive.test.js`、`git diff --check`も成功した。本文・画像取得、許可済みID、保存済みデータの経路は変更していない。

## 2026-08-14 Web Clipper 再クリップと失敗画像再試行

* 同一URLのWebクリップは、fragmentを除き末尾スラッシュだけを正規化して検出する。確認画面で既存メモを既定で更新するか、新規保存するかを選べ、複数一致時は対象を明示選択する。更新時はメモID、コレクション、フラグ、クリップと無関係のメタデータを保持し、ノートと旧・新Webクリップ画像を同一IndexedDBトランザクションで置換する。
* 画像取得失敗はログイン要求、URL期限切れ、アクセス拒否、権限不足、タイムアウト、容量、非対応形式を判別して表示する。確認画面の「失敗した画像だけ再試行」は、Memo-Nexusページ上の拡張Content Scriptを経由して既存Service Worker取得経路へ再依頼し、成功済み画像は再取得しない。画像なし保存の意味が明確になるようボタン表記を変更した。
* 拡張manifestとREADMEの現在版を`0.3.5`へそろえ、再クリップ・再試行の挙動と制限をREADMEへ追記した。構文確認、関連テスト34件、`git diff --check`は成功した。実MV3 Chromium E2Eはこの環境に`playwright`パッケージがないため未実施とし、既存E2Eの版数期待値と画像・失敗用フィクスチャは更新した。

## 2026-08-14 エディタ下部ステータスバー

* エディタ下部へ、既存の文字数チップ、作成・更新日時、ブラウザ／ローカル保存状態を統合したコンパクトなステータスバーを置いた。文字数は従来どおり`TextStatsUtils.calculateTextStats`を唯一の集計元とし、日時表示も`renderNoteMeta`へ戻した。表示上の作成日時は、ローカル初回保存後だけ`localCreatedAt`を優先し、それ以前は既存`createdAt`を使う。元の`createdAt`は変更せず、OS上のファイル作成日時も採用しない。
* ブラウザ保存は既存の保存遷移（編集あり、保存中、保存済み、保存失敗）をそのままチップと詳細へ反映する。ローカル保存は`未設定`、`保存中`、`保存済み`、`要保存`、`再接続が必要`、`競合あり`、`保存失敗`、`未対応`を独立した状態モデルで扱い、保存先、権限、最終保存日時、理由と次の操作を表示する。狭幅では保存先別チップを「保存状態」へ集約し、詳細内ではローカルを先、ブラウザを後に表示する。
* File System Access APIを使うローカルフォルダ保存を追加した。利用者が選んだフォルダを保存先としてIndexedDBへディレクトリハンドルを直接保持し、`Memo-Nexus/manifest.json`、`collections.json`、`sync-state.json`、`notes/`、`assets/`、`inbox/`を作る。すでに`Memo-Nexus`フォルダ自身を選んだ場合は同名フォルダを重ねない。メモは安定したファイル名とYAML front matterを持つMarkdown、添付画像は`assets/`へ保存し、本文参照を書き換える。
* 保存順序は従来のIndexedDB保存を先に確定し、その後ローカル保存キューへ積む。ローカル失敗時もブラウザ保存済みデータはロールバックしない。`localCreatedAt`と`localSavedAt`は全ローカル書き込み成功後だけIndexedDBへ反映する。連続編集はdebounceし、保存処理は直列化して競合を避ける。DBはversion 4へ上げ、既存storeを削除せず`local-config`だけを追加した。
* 起動・フォーカス・手動操作では、選択フォルダ直下、`inbox/`、`notes/`のMarkdownを走査する。未登録ファイルは「追加／除外」、ブラウザ消失後の既知メモは「復元／除外」、同時変更は「ローカル優先／アプリ優先／両方残す／保留」を明示選択する。競合・復元候補が残る間は自動上書きせず、既存コレクションは不足分を追加するマージに限定した。フォルダ切断は接続情報だけを削除し、ローカルファイルや孤立画像は自動削除しない。
* 未対応ブラウザではフォルダ選択を実行可能に見せず、ZIPバックアップを案内する。権限要求はフォルダ選択・再接続など利用者操作時だけ行い、起動時は状態確認に留める。各チップはbutton、`aria-expanded`、dialog、外側クリック、Escape、フォーカス復帰に対応し、状態色だけに依存しない文言を併記する。
* 自動テストでは、作成日時優先順位、状態遷移、Markdown／画像参照、manifest／collection、差分分類、競合、直列保存キュー、模擬File System Access API、ディレクトリハンドルのIndexedDB保持、権限、IndexedDB先行保存、DB移行、モバイル集約を確認した。`node --test`は447件成功し、全JavaScriptの`node --check`と`git diff --check`も成功した。実Edgeではデスクトップ／390px、ライト／ダーク、文字統計・ローカル詳細、Escapeとフォーカス復帰、日時表示、保存状態集約、コンソール警告なしを確認した。OSのフォルダ選択ダイアログを伴う実フォルダ書き込みは自動操作できないため未実施とし、模擬APIテストとPR記載の手動確認手順を残す。

## 2026-08-14 ローカル保存の誤競合修正

* `notes/`内の管理対象Markdownを、front matterやファイル名の推測ではなく`sync-state.json`のnote ID・`fileName`対応で特定する。走査時は前回書込みhash、現在のローカルファイルhash、現在のIndexedDB内容から生成したMarkdown hashを比較し、ローカルが前回書込みのまま、またはすでにアプリ最新版と同じ場合は候補へ追加しない。両方と異なる場合だけ外部変更による真の競合として扱う。
* フォーカス復帰時は、保留中のローカル保存を先にflushしてから外部Markdownを走査する。保存直前の`performLocalWorkspaceSave()`でもローカルhashを再確認する二重防御は維持した。真の競合でキューが一度失敗しても、利用者の競合解決後は次の保存を実行できるよう直列キューを回復可能にした。
* 回帰テストでは、初回保存後にIndexedDBだけを編集した状態が競合にならず、キュー実行後にMarkdown本文、`sync-state.json`のhash・保存日時、保存状態が更新されることを確認した。外部編集だけが競合になること、ローカルとアプリが同じ場合、未管理Markdownの新規・復元分類、真の競合の自動上書き防止、作成・更新日時表示、`createdAt`・`localCreatedAt`維持も確認し、`node --test`は453件成功した。
* Edgeではローカル版の設定画面とフォルダ選択操作まで確認した。OSフォルダ選択画面の自動操作は、Windows操作側が現在のブラウザURLを安全に判定できず停止したため、初回実ファイル保存から外部編集検出までの実機確認は未実施とする。

## 2026-08-14 ローカル再スキャンと保存キューの連携修正

* `ecf2263`の3ハッシュ判定と`sync-state.json`のnote ID・`fileName`対応を維持したまま、再スキャン結果をローカル保存状態へ即時反映するようにした。真の外部変更は`external-conflict`、復元候補は`restore-candidate`として区別して「確認が必要」にし、警告領域へ理由を表示する。新規・重複候補だけでは保存全体を競合にせず、外部ファイルを前回保存内容へ戻して候補が消えた場合は、未保存変更の有無に応じて`pending`または`saved`へ戻す。権限・一般エラーは走査結果で上書きしない。
* フォーカス復帰と手動再スキャンは、`flushSave()`でIndexedDBを先に確定し、ローカル保存キューの現在のtailを無条件に待ってから走査する。実行中の書込みが失敗した場合も走査は続け、競合候補を確認できるようにした。保留した真の競合は候補がある間維持し、保存直前のハッシュ再確認による二重防御も変更していない。
* 本番の候補生成・状態照合・保存後走査の共通関数を使う動作テストを追加した。模擬File System Access API上で通常編集後のMarkdownと`sync-state.json`更新、外部編集の即時競合化と自動上書き防止、外部内容を戻した後の競合解除、実行中保存完了後の1回だけの走査、保存失敗後の走査継続、新規・重複・復元候補、作成・更新日時と`createdAt`・`localCreatedAt`維持を確認した。`node --test`は458件成功した。
* Edgeの`http://127.0.0.1:5500`では、`app.js?v=0.4.0-76`、`local-save-state.js?v=0.4.0-2`、`local-save-queue.js?v=0.4.0-2`、`local-sync-utils.js?v=0.4.0-3`の読込みを確認した。既存の外部競合がある接続済みフォルダで、起動時と手動再スキャン直後に下部チップが「ローカル 確認が必要」となり、具体的な外部変更理由が警告領域へ表示され、作成・更新日時が下部に残ることを確認した。OSフォルダ選択以降の初回実保存、Memo-Nexus編集後の実ファイル本文・更新日時追随、外部ファイルを元へ戻した実機での競合解除は安全に自動操作できないため未実施とし、上記の実処理共通関数を通る動作テストで補完した。

## 2026-08-14 競合解除後のローカル保存再開

* 再スキャンで競合候補が消えて`saved`へ戻す場合は、既存の`lastSuccessAt`を明示的に引き継ぐ。再スキャン、候補除外、競合解除は実ファイル保存ではないため最終保存成功日時を更新せず、`lastSuccessAt`がない場合は`pending`にする。`localSavedAt`も変更せず、実際の`performLocalWorkspaceSave()`が全ファイル書込みに成功した時だけ`lastSuccessAt`と`localSavedAt`を新しい保存時刻へ更新する。
* 候補一覧の再描画と保存状態照合を`reconcileLocalScanCandidates()`へ集約した。再スキャン、最後の復元候補の除外、ローカル版の取込、Memo-Nexus版での上書き、両方を残す、復元の各経路で共通処理を通し、ブロック候補がなく、ローカル保存が有効で、未保存変更が残る場合だけ保存キューを`scan-unblocked`等の理由で1回再開する。保存直前の3ハッシュ再確認を維持し、権限・書込みエラー・非対応・保留中の競合では自動再開しない。
* 回帰テストでは、固定した`lastSuccessAt`と`localSavedAt`が競合検出・競合解除だけでは変わらないこと、外部変更とMemo-Nexus編集が重なった後に外部ファイルを戻すと、模擬File System Access APIと実保存キューを通ってMarkdown本文、`sync-state.json`のhash・`savedAt`、保存状態が1回だけ更新されることを確認した。最後の復元候補を除外した際の確認状態解除とpending保存再開、permission-required・error・unsupported・conflict-heldの保護、新規・重複候補、作成・更新日時、`createdAt`・`localCreatedAt`維持も確認した。`node --test`は461件成功し、全JavaScript 96ファイルの`node --check`と`git diff --check`も成功した。
* Edgeの`http://127.0.0.1:5500`では、`app.js?v=0.4.0-77`と`local-save-state.js?v=0.4.0-3`の読込み、作成・更新日時の継続表示、再接続が必要な状態で再スキャンしても状態が維持され、最終保存成功日時が`2026/08/14 10:02:51`のまま変わらないことを確認した。保存先権限が失われていたため、外部競合作成、Memo-Nexus側編集、外部ファイル復元後の自動保存、Markdown本文・更新日時追随、復元候補除外の実機操作は未実施とし、既存の外部変更を上書きしかねない再接続・保存操作も行っていない。

## 2026-08-14 権限回復前スキャンと起動時app-ahead検出

* ローカル保存の再接続を、権限要求、権限回復後の外部Markdown走査、安全確認後の保存、の順へ変更した。走査中はpending保存を再開せず、真の外部変更は`external-conflict`、復元候補は`restore-candidate`として利用者の判断を待つ。権限拒否・`NotAllowedError`・`SecurityError`は`permission-required`、外部競合は`conflict`、その他の書込み失敗は`error`に分類し、保存側で確定した競合や一般エラーを再接続の外側で権限エラーへ置き換えない。
* 管理対象Markdown走査を、候補一覧に加えて`appAheadNoteIds`と`needsLocalSave`を返す共通解析へ拡張した。`sync-state.json`の前回hash、現在のローカルMarkdown hash、現在のIndexedDB内容から生成したMarkdown hashを比較し、ローカルが前回書込みのままでアプリ側だけ新しい`app-ahead`は競合候補へ表示せず、起動時の保存再開へ使う。3者が同一なら起動時に保存キューを実行せず、Markdown更新日時、`lastSuccessAt`、`localSavedAt`を変更しない。真の外部変更の自動上書き防止、保存直前の再確認、実保存成功時だけの日時更新、`createdAt`・`localCreatedAt`維持は変更していない。
* 模擬File System Access API、共通走査解析、再接続順序関数、実保存キューを通る回帰テストを追加した。権限回復後の走査が保存より先であること、競合時の保存0回と外部内容維持、安全な`app-ahead`の保存1回、権限・競合・一般エラー分類、起動時app-aheadのMarkdown・`sync-state.json`更新、同一内容の書込み0回と日時維持、起動時の真の競合を確認した。`node --test`は465件成功し、全JavaScript 96ファイルの`node --check`も成功した。
* Edgeの`http://127.0.0.1:5500`では、`app.js?v=0.4.0-78`、`local-save-state.js?v=0.4.0-4`、`local-save-queue.js?v=0.4.0-3`、`local-sync-utils.js?v=0.4.0-4`の読込みを確認した。下部バーには作成日時`2026/08/14 10:02`、更新日時`2026/08/14 17:03`が残り、権限喪失中の表示は「ローカル 再接続が必要」、最終保存日時は`2026/08/14 10:02:51`のままだった。接続先実フォルダの原本を特定してバックアップできないため、再接続ボタン、外部競合時の実ファイル非上書き、安全時の保存再開、app-ahead再読込、同一内容再読込時のファイル更新日時維持は実機では未実施とし、原本へ保存操作を行っていない。

## 2026-08-14 ローカル保存途中失敗の再試行と初回保存重複の修正

* Markdown書込み後、添付や`sync-state.json`の更新前に保存が失敗した場合でも、安全に再試行できるようにした。比較用hashは管理対象Markdownのfront matterにある`localSavedAt`行だけを除外し、本文、タイトル、その他のメタデータ、添付参照はすべて比較対象に残す。走査と`performLocalWorkspaceSave()`直前の二重防御は同じ比較関数を使い、再試行時刻だけが異なるアプリ書込み済みMarkdownは`app-ahead`、本文等も異なる場合は真の外部競合として扱う。`sync-state.json.hash`は後方互換のため従来どおり完全なMarkdownのraw hashを保存する。
* 初回フォルダ選択では、権限取得後の走査でpending保存を自動再開しない。復元・競合候補があれば保存せず確認を求め、安全な場合だけ明示的な`folder-selected`保存を1回実行する。これにより初回保存のMarkdownと`sync-state.json`を二重に書かず、既存Markdownがあるフォルダを先に上書きしない。既存の再接続時scan-first、保存直前の外部変更確認、保存日時、`createdAt`・`localCreatedAt`の仕様は維持した。
* 模擬File System Access APIと実保存キューを通る回帰テストで、途中失敗後の再試行、走査側の`app-ahead`判定、raw hash更新、本文外部編集時の競合保護、初回フォルダ選択の保存1回、復元候補時の保存0回を確認した。`node --test`は470件成功し、全JavaScript 96ファイルの`node --check`と`git diff --check`も成功した。
* Edgeの`http://127.0.0.1:5500`では、`app.js?v=0.4.0-79`、`local-sync-utils.js?v=0.4.0-5`を読み込み、下部バーに作成・更新日時と「ローカル 再接続が必要」「ブラウザ 保存済み」が表示されることを確認した。既存保存先の原本を特定してバックアップできず権限も失われているため、専用テストフォルダを使う初回実保存、途中失敗の実機再現、再試行、外部編集保護は未実施とし、既存フォルダへの再接続・書込みは行っていない。

## 2026-08-14 GitHub Actionsによる最小CI

* `.github/workflows/ci.yml`を追加し、`main`対象のpull request、`main`へのpush、GitHub画面からの手動実行で`CI`を起動する。権限は`contents: read`だけとし、secrets、自動コミット、自動push、自動マージは使用しない。公式の`actions/checkout@v7`と`actions/setup-node@v7`だけを使い、`ubuntu-latest`、Node.js 22、10分上限、同一refの古い実行を中止するconcurrencyを設定した。既存のNode標準テストを利用するため、`package.json`や依存パッケージは追加していない。
* CIは`node --test`、`node_modules`を除く全JavaScriptの`node --check`、イベントごとの変更範囲に対する`git diff --check`を別ステップで実行する。JavaScriptはnull区切りで列挙して空白・日本語を含むファイル名を保護し、検出件数を表示して0件なら失敗する。pull requestはbase SHAからhead SHA、pushはbefore SHAから現在SHAを検査し、beforeが全ゼロの場合と手動実行は対象コミットを`git show --check`で確認する。
* ローカルでは`node --test`が470件成功し、全JavaScript 96ファイルの`node --check`、`origin/main`との共通祖先`20127bf1607dc6e30f86a28cc22db03650620f3e`からHEADまで、および未コミットCI差分の空白検査が成功した。既存の作成・更新日時、途中失敗後の再試行、外部競合保護、初回保存1回のテストを含む。ローカルのAction/YAML専用検証ツールは未導入のため追加依存なしでは実行できず、YAML構造とシェルを目視確認した。
* PR #96へのpushでGitHub Actions `CI`（run `31796520781`）がpull requestイベントとして起動し、Node.js 22で`node --test` 470件、全JavaScript 96ファイルの`node --check`、base `20127bf1607dc6e30f86a28cc22db03650620f3e`からhead `62611194d96e004b29c0899eab2ceaff782379be`までの`git diff --check`がすべて成功した。Checkoutは`persist-credentials: false`で、ワークフローは読み取り専用権限のままsecretsを使用していない。`main`へのpush実行はPRをマージしていないため、手動実行はワークフローがまだdefault branchにないため未実施であり、対応イベントと分岐はYAMLで確認した。

## 2026-08-14 IndexedDB安全起動と手動ローカル保存

* 通常作業の正本を従来どおりIndexedDBとし、本文・タイトル、メモ、コレクション、フラグ、添付の変更はブラウザへ自動保存する一方、ローカルフォルダは下部バーまたは設定の明示保存ボタンを押したときだけ更新する方針へ変更した。起動、フォーカス復帰、メモ切替、フォルダ選択・再接続、通常編集ではローカル書込みも外部Markdown走査も開始しない。変更後は状態だけを`ローカル 要保存`とし、明示保存時にIndexedDB確定、実行中キュー待機、読取り専用の外部変更走査、競合・復元候補で中止、安全な場合だけ`notes/`、`assets/`、`collections.json`、`manifest.json`、`sync-state.json`を書き出す。保存直前のhash再確認、復元・再スキャン、競合保護、作成・更新日時、`createdAt`・`localCreatedAt`、ZIP入出力は維持した。
* IndexedDB接続へ`versionchange`を設定し、新版の更新要求時は既存ドラフト退避を先に実行して接続を閉じ、再読み込みが必要であることを画面全体へ表示する。`indexedDB.open()`の`onblocked`では、別のMemo Nexusタブを閉じる案内を出したまま同じopen要求の完了を待つ。起動処理全体を例外捕捉し、失敗時は「メモを読み込めませんでした」、短い理由、サイトデータを削除しない注意、再読み込みボタンを表示する。起動完了までは初期HTMLの無題メモや0件表示を隠し、自動リロードやDB削除は行わない。
* 動作テストでは、`versionchange`のドラフト退避→close順、blocked後の起動再開、起動例外の捕捉、起動ガード、IndexedDB編集だけではローカルファイルを変更しないこと、明示保存の実行順と一式出力、復元・競合候補で書込みを中止すること、起動・フォーカス・コレクション・添付変更で自動走査・書込みしないことを確認した。既存の外部競合、途中失敗と再試行、日時、コレクション、添付、Markdown、ZIP入出力を含む`node --test`は475件、全JavaScript 99ファイルの`node --check`、`git diff --check`が成功した。
* Edgeでは既存データへ触れず、分離した`http://127.0.0.1:5501`で起動完了後に編集UIが表示されること、本文編集後に`ブラウザ 保存済み`となること、再読み込み後も本文がIndexedDBから復元されること、ローカル未設定を保存済みと表示しないこと、下部の「ローカルへ保存」からローカルフォルダ設定へ到達できること、確認時のconsole error／warningが0件であることを確認した。OSのフォルダ選択を伴う実ファイル一式の書込み、外部編集競合、別タブによる実DBアップグレード競合と起動失敗画面は、既存データを変更せず安全に再現できないため実機未実施とし、模擬File System Access APIとIndexedDBライフサイクルの自動テストで補完した。

## 2026-08-14 保存成功時刻とメモ日時の配置調整

* 下部ステータスバーのローカル・ブラウザ保存チップ直後へ、それぞれ独立した最終保存成功時刻を`M/D HH:mm`で表示する。ブラウザは現在のメモがIndexedDBへ保存成功した日時、ローカルは既存の`localSaveState.lastSuccessAt`を正本とし、時刻がなければ`—`を表示する。編集中、保存中、要保存、未設定、競合、権限不足、エラーへの遷移では成功時刻を現在時刻へ変更せず、実保存成功後だけ更新する。
* 作成日・更新日を下部バーからタイトル領域右側へ移し、`作成 2026/8/12 21:34`、`更新 2026/8/14 23:09`形式にした。作成日時は従来どおり`localCreatedAt`を優先して`createdAt`へフォールバックし、更新日時も既存の`bodyUpdatedAt`／`updatedAt`仕様を維持する。フラグ操作は下部バー左へ移し、その直後に既存の文字数チップと詳細ポップオーバーを配置した。IndexedDB構造、ローカル保存処理、Markdown・ZIP入出力形式は変更していない。
* ローカル日時の部品化と表示を`status-time-utils.js`へ共通化し、成功時刻の更新条件、未設定・要保存・保存中・競合・権限不足・エラー時の時刻維持、作成・更新日時、DOM順序、狭幅折り返しを回帰テストへ追加した。`node --test`は479件、全JavaScript 101ファイルの`node --check`、`git diff --check`が成功した。
* Chromeの分離環境`http://127.0.0.1:5501`で、1707pxのデスクトップ、390×844px相当、540×720pxのポップアウトを確認した。タイトルと日時、フラグと文字数、保存チップと成功時刻に重なり・横スクロールがなく、ライト／ダーク双方で日時色のコントラストが維持されること、ポップアウトでも作成・更新日時と下部バーが表示されることを確認した。ローカル保存の実行は今回の表示変更に不要なため行っていない。
## 2026-08-15 解説カード保存時のnote.body即時反映

### 変更内容

* `saveExplanationFromDialog()` の新規作成フローで `insertExplanationAnchorIntoBody()` の結果を取得後、
  `editor.value = insertion.body` に加えて `note.body = insertion.body` を同タイミングで代入するよう追加。
* これにより `putNote(note)` の第一引数に渡る `note` に、説明本文・メタ情報と同じ状態の本文（HTMLコメントアンカー入り）を含めるように統一。
* 既存の `scheduleSave()` は保存キューの既存フロー維持のため残し、`putNote` 呼び出し時点で整合する状態を保証。

### 確認結果

* `node --check app.js markdown-enhancements-utils.js`（成功）
* `node --test syntax-guide.test.js markdown-enhancements.test.js image-block-layout.test.js`（pass 70）
* `git diff --check`（問題なし）

## 2026-08-15 feat/image-and-explanation-block-ui

## 2026-08-15 解説カード本文アンカー永続化

* 解説作成時に `memo-nexus:explanation` HTMLコメントアンカー (`<!-- memo-nexus:explanation id=\"UUID\" -->`) を本文へ保存する構成を追加
* `memo-nexus:explanation id="<カードID>"` を優先解決キーとして採用し、同一語句の重複や選択範囲を超えた編集でも対象を特定しやすくした
* アンカー優先で位置が確定しない既存データは従来の `resolveExplanationTarget()` へフォールバックし、既存解説カードの互換を維持
* 本文側アンカーはプレビュー表示から除去し、既存画像ブロック仕様や既存編集/削除フローを変更しない
* 追加したテスト:
  * `markdown-enhancements.test.js`: アンカー挿入・同ID優先解決・重複語句識別・削除・編集・プレビュー除去
  * `backup-bundle-utils.test.js`: ZIP往復時の本文アンカー保持

### 確認結果

* `node --check app.js markdown-enhancements-utils.js`
* `node --test syntax-guide.test.js markdown-enhancements.test.js image-block-layout.test.js`（68件成功）
* `node --test backup-bundle-utils.test.js`（7件成功）
* `git diff --check`

## 2026-08-15 Memo-Nexus: 解説カードの選択基準と配色修正

### 変更内容

* 本文選択時の解説ボタン押下で、`pointerdown` 取得時点の選択範囲をスナップショットとして保持し、`focus` 移動で選択が消える問題を回避
* 解説カード表示対象解決を更新し、複数段落・複数リスト項目・Markdown装飾をまたいだ選択でも、選択範囲末尾側の表示ブロックで対象を確定するよう変更
* `visibleTargetForSourceRange()` の判定を「単一可視テキスト断片前提」から脱却し、末尾側 anchor に基づく解決を行うよう更新
* `resolveExplanationTarget()` 依存の既存挙動を崩さないよう、既存構造（保存・編集・削除・折りたたみ・対象再解決失敗時の維持）を維持
* 解説カードの外観を枠線ベースから無地背景ベースへ変更し、通常カード／対象不明カードを色調差分で視認しやすくした

### 確認結果

* `node --check app.js markdown-enhancements-utils.js`
* `node --test syntax-guide.test.js markdown-enhancements.test.js image-block-layout.test.js` → `pass 61`
* `git diff --check` → 問題なし

### 補足

* 画像ブロックの挿入・仕様自体は変更せず
* 本件は `app.js`、`markdown-enhancements-utils.js`、`markdown-enhancements.test.js`、`style.css` を編集

### 変更内容

* 画像・解説ブロックの記法ガイド文言を更新し、画像ブロック/画像キャプション/解説ブロック/画像削除時の添付保持を記載
* 本文編集欄上部に「画像」「解説」ボタンを追加し、アイコン付きで直下インラインUIに統合
* 画像を本文へ「画像ボタン」から挿入する仕様を、画像ブロック形式とカーソル/末尾フォールバックへ調整
* 添付一覧から参照中画像を削除する確認文を、添付画像も削除される実挙動に合わせて更新
* 解説は本文順で解析・挿入できるよう `hydrateExplanationCardsIntoDom` を修正し、本文外まとめ表示を廃止
* 解説挿入位置のためのゼロ幅マーカー管理を追加し、カーソル位置挿入時の位置保持を安定化
* 画像ブロックと解説カードのカード表示を更新（背景・内側余白・角丸・余白）し、後続本文・コードへのスタイル継承影響を抑制

### 修正理由

* 画像・解説ブロックを「本文順」かつ「本文中で直後」に見せるUI要件を満たし、カードの可読性と編集導線を改善するため
* 画像削除時でも添付データを残す意図と、既存メモ/Web Clipper添付との互換性を維持するため

### 確認方法

* 画像/解説の各テストを更新し、既存仕様に沿った記法定義・DOM挿入仕様を確認
* 画像ブロックと解説カードのレイアウトスタイルを確認
* `git status` で対象差分を確認し、mainから分岐した新規ブランチで作業

### 確認結果

* `node --check app.js markdown-enhancements-utils.js`
* `node --test syntax-guide.test.js markdown-enhancements.test.js image-block-layout.test.js` は実行時に `spawn EPERM` で失敗（この環境のNode test実行制約）
* `node --test *.test.js` も同じ `spawn EPERM` で全件失敗
* `markdown-enhancements.test.js` / `syntax-guide.test.js` / `image-block-layout.test.js` の追加分は、変更内容レビューにより意図どおりに追記済み

## 2026-08-15 可搬バックアップ v1

### 変更内容

* ZIPバックアップを `manifest.json`、`collections.json`、`notes/`、`assets/` からなる完全バックアップ形式へ変更
* ローカルフォルダ保存とZIP出力で、manifest・コレクション・Markdown front matter・assetsの共通シリアライズを使用
* コレクション階層、日時、フラグ、ゴミ箱、既存メタデータ、画像・PDF添付を復元できるようにした
* Import時にIDごとの `updatedAt` を比較し、厳密に新しいImportデータだけを適用
* 不明なコレクションは未分類へ移動し、個別Markdown・添付・collections.jsonの破損は可能な範囲でスキップして結果表示
* manifestの欠損・破損・形式不一致・未対応の将来versionは、IndexedDBを書き換えず中止

### 確認方法

* `node --test backup-bundle-utils.test.js` で形式・破損救済・PDF添付・ID競合を確認
* `node --test *.test.js` で既存の回帰テストを確認

## 2026-08-16 メモタグ機能

* メモの`tags`を小文字・前後空白除去・空文字除外・重複除外した配列として統一し、新規メモ、IndexedDB読込・保存、下書き退避・復元、ローカルMarkdown復元、ZIPバックアップ復元で保持するようにした。タグなしの旧メモ、旧Markdown、旧ZIPは空配列として扱う。
* タイトル周辺へタグの追加・削除UIを追加し、右コンテキストパネルへ使用中タグとメモ件数を表示する「タグ」タブを追加した。タグ選択は通常一覧、コレクションとの組み合わせ、ゴミ箱の既存条件へ追加で適用し、明示的な絞り込み解除を用意した。検索、フラグ、コレクションツリー、画像・解説ブロックの既存経路は維持した。
* Markdown front matterと可搬ZIPでタグあり・タグなしを往復するテスト、タグ正規化、タグ絞り込み、コレクションとの組み合わせ、タグ編集UI接続、外部Markdownのタグだけの変更検出を追加した。`node --test`は514件成功し、全JavaScript 103ファイルの`node --check`も成功した。

## 2026-08-16 PR #107 タグ表示修正と記法ガイド拡張

* `memo-list-utils.js`と`local-markdown.js`の配信キャッシュ番号を更新し、`normalizeMemoTags()`とMarkdown・ローカル同期のタグ正規化で`null`・`undefined`を除外した。空文字、前後空白、大文字小文字をまたぐ重複の既存正規化は維持した。
* エディタ操作列へ`# タグ`ボタンを追加した。クリック時はタイトル下の既存タグ入力欄へ移動してフォーカスするだけとし、入力内容の消去、本文への`#`挿入、タグ保存処理の複製は行わない。タグ追加は既存のフォーム送信、正規化、チップ・右側タグ一覧の再描画、IndexedDB保存経路を使用する。
* 記法ガイドへ本文とは別の「タグ」セクションを追加し、タイトル下の入力欄と`# タグ`ボタン、追加・Enter操作、重複・空白・大文字小文字の正規化、右側タグ絞り込みを説明した。本文の`# 見出し`はMarkdown見出しでありタグではなく、将来の`#タグ`記法とは`#`直後の空白で区別する方針を明記した。
* `node --test`は517件成功し、`node --check app.js`と`git diff --check`も成功した。Edgeで既存メモを開き、`# タグ`から入力欄へフォーカスし、「ブラウザ確認」をEnterで追加後、タイトル下のタグチップと右側「タグ」タブの「1件」表示へ直ちに反映されることを確認した。

## 2026-08-16 PR #107 登録制タグとtags.js分離

* IndexedDBをv5へ更新して`tags`ストアを追加し、`id`、`name`、`createdAt`、`updatedAt`を持つタグ定義を保存する。起動時は既存メモの`note.tags`だけを走査して不足定義を同じIDへ冪等に補完し、定義未作成を理由に旧タグを削除しない。本文の`#タグ`は移行・付与対象として解析しない。
* `tags.js`を新設し、タグID・表示名・配列・定義一覧の正規化、登録判定、登録済みタグへの付与制限、重複防止、紐付け解除、旧メモからの定義作成、削除済みメモを除く使用件数、タグ・コレクションのAND絞り込み、候補検索を`window.MemoNexusTags`へ集約した。`memo-list-utils.js`は削除状態と一覧見出しを担当し、タグ絞り込みを`tags.js`へ委譲する。
* 右側「タグ」タブへ「タグを作成」ダイアログを追加し、空名・正規化ID重複を防止する。作成済み定義は0件でも一覧表示し、作成時に現在メモへ自動付与しない。タイトル下は登録済み候補だけを表示する選択式へ変更し、未登録入力にはタグタブから作成する案内を出す。チップの×はメモとの紐付けだけを外し、タグ定義は残す。`# タグ`ボタンは選択欄へのフォーカスだけを行う。
* 記法ガイドを登録制に合わせ、新規タグの作成場所、登録済みタグの付与、本文の`#資料`はタグにならないこと、`# 見出し`はMarkdown見出しであること、本文検索とタグ絞り込みが別機能であることを明記した。
* `node --test`は527件成功した。Edgeの既存保存領域では、旧「ブラウザ確認」タグの保持、「調査」の作成直後の0件表示と候補反映、候補選択後のチップ・1件表示・該当メモだけの絞り込み、別メモ本文へ`#調査`を書いてもタグ付与・件数増加が起きないこと、再読み込み後の定義・付与保持を確認した。

## 2026-08-16 PR #107 タグ定義のバックアップ・取り込み同期

* 可搬バックアップ形式をv2へ更新し、ZIPとローカルフォルダのルートへ`tags.json`を追加した。IndexedDBの`tags`ストアにある全定義を`normalizeTagDefinitions()`で正規化し、使用件数0件を含む`id`、`name`、`createdAt`、`updatedAt`を保存する。メモとの紐付けは従来どおり各Markdown front matterの`tags`へタグID配列として保存する。
* `tags.json`がないv1 ZIP・旧ローカルフォルダは引き続き読み込み、復元後の`note.tags`から不足定義を冪等に補完する。v2では保存された表示名と日時を保持し、既存定義との衝突は両方の`updatedAt`を比較でき、取り込み側が新しい場合だけ更新する。不正IDと重複定義はスキップし、可搬復元ではメモ・コレクション・添付・タグ定義を同じIndexedDBトランザクションへまとめた。
* 外部Markdown、通常Markdown ZIP、可搬ZIP、ローカルフォルダ、起動時の下書き復元後に共通のタグ同期処理を通し、再読み込み前にタグ一覧・件数・候補へ反映する。タグ定義の作成・補完・復元は`markLocalWorkspacePending()`でローカル保存を要保存にし、ローカル書込み中はタグ更新を行わないため保存キューを再予約しない。
* `node --test`は535件成功し、`node --check app.js`、`node --check tags.js`、`node --check backup-bundle-utils.js`、`git diff --check`を確認した。ブラウザでは「未使用」を0件で作成し、「AI」を現在メモへ付与して1件表示、ZIP生成成功ログ、再読み込み後も「未使用」0件と表示名「AI」が保持されることを確認した。ブラウザ自動操作環境では生成ファイルの再アップロードとローカルフォルダ選択が許可されないため、ZIP復元・旧形式補完・ローカル要保存化は実データ往復テストと接続テストで確認した。

## 2026-08-16 PR #107 タグバックアップ正規化ガード

* `buildPortableBackupFiles()`で`normalizeTagDefinitions`を必須とし、正規化処理の渡し忘れ時に空の`tags.json`を生成せず、安全なバックアップを生成できないことを示す明示的なエラーへ変更した。正規化関数があり、登録済みタグが0件の場合は、関数を通した空配列を従来どおり`tags.json`へ保存する。
* 正規化関数なしの失敗、正常な空タグ、未使用タグ、`AI`のID`ai`・表示名`AI`の正規化保存を返り値の`tags.json`解析で検証した。通常ZIPとローカルフォルダ保存の両呼び出し元が正規化関数を渡す接続テストも追加した。
* `node --test`は538件成功し、`node --check app.js`、`node --check tags.js`、`node --check backup-bundle-utils.js`を確認した。ブラウザでは「未使用」0件と付与済み「AI」を使って通常ZIPを生成し、実ZIPの`tags.json`に「未使用」と、ID`ai`・表示名`AI`が保存され、新しいエラーが出ないことを確認した。

## 2026-08-17 Codexチャット試作

* ローカルブリッジ経由でCodex App Serverへ会話し、メモ本文・選択範囲は明示添付時だけ送る試作を追加した。App Serverは一時空ディレクトリ、`read-only` sandbox、`never` approval policy、ネットワーク禁止で起動し、承認・ツール要求を拒否する。

## 2026-08-17 Codexチャット試作レビュー修正

* textareaの選択範囲をスナップショットし、送信要求時のメモIDへthreadIdを保存するよう修正した。新しい会話は保存済みの紐付けも解除する。
* ブリッジは起動・RPCタイムアウト、子プロセス終了、SSEエラーを扱い、説明はローカル接続とCodexサービス通信の範囲に合わせた。

## 2026-08-17 Codexブリッジruntime分離

* App Serverのchild、tempCwd、初期化Promise、stdout buffer、pending RPC、SSE、turn errorをプロセス単位のruntimeへ分離した。古いruntimeのexit・error・stdout・stderr・タイムアウト・遅延write callbackは新しいruntimeへ作用せず、破棄はpending、SSE、kill、exit待ち、所有temp削除を一度だけ行う。
* Codex CLI 0.147.0の生成スキーマに合わせ、`error.params.error.message`をturnId単位に記録し、`turn/completed`のcompletedだけをdone、failed・interrupted・未知状態をerrorにした。ブラウザ側SSE読取りを独立関数へ移し、分割chunk、UTF-8境界、明示done、error、途中EOF、不正JSONを実データで検証した。
* 初期表示メモを開く`openNote()`からCodexへメモ変更を通知し、初期メモでも添付・送信できるようにした。fake child・stdin・timer・SSEを含む全564テストが成功した。
* ローカル実機ではhealth connected、APIのthread・delta・done、本文／選択範囲の添付と取消、UI実回答とコピー、回答中のメモ切替分離、ブリッジ停止時のbusy解除、再起動後の再接続・再送信を確認した。再読み込み後のthreadId復元、「新しい会話」の永続解除、正常shutdown後の一時ディレクトリ確認は未実施。

## 2026-08-17 Codex turn開始直後の通知競合修正

* `turn/start`のJSON-RPC応答とdelta・`turn/completed`が同じstdout chunkへ入った場合、HTTP側がSSEを登録するまでの通知をturn単位で一時保持し、登録後に`thread`、delta、完了の順で再生するようにした。競合を実際に再現する失敗テストを先に追加し、修正後に回答と終端が欠落しないことを確認した。
* SSEの通常書き込み・終端書き込みが例外になっても該当ストリームを一度だけ解放し、responseの終了を試みる。ブラウザのabort・closeでは該当responseとの紐付けだけを解除し、切断後の通知を書き込まず、別turnのストリームへ影響しないようにした。
* `node --test`は567件成功し、`node --check app.js`、`node --check codex-bridge.js`、`node --check codex-bridge-runtime.js`、`node --check codex-chat.js`、`node --check codex-chat-utils.js`、`git diff --check`が成功した。実会話E2Eでは`/health`のconnected、HTTP 200、`thread`、delta「確認」、doneを確認し、正常shutdown後に今回のApp Serverプロセスと一時ディレクトリが残らないことを確認した。

## 2026-08-17 Codex高速turn・SSE切断の最終修正

* `turn/start`の成功応答を処理する同じ同期処理内で`onResult`を一度だけ呼び、Promiseをresolveする前にSSEを登録して`thread`を送るようにした。公式App Server資料とCodex CLI 0.147.0の生成スキーマではレスポンス先行の明示保証を確認できなかったため、進行中の`turn/start`に対応する`turn/started`以後だけを早期通知として保持する。上限はruntimeごとに4 turn、turnごとに64イベント・64 KiBとし、timeout、RPC失敗、登録完了、ブラウザ切断、runtime破棄で削除する。上限超過時は部分回答を再生せず、`thread`後に診断可能な`error`で終了する。
* SSEの書込み・終了を共通処理へ集約し、`write`失敗時も該当streamとturn関連状態を先に解除して`end`、必要なら`destroy`を一度だけ試す。`end`・`destroy`の例外はruntime IDとturn IDを含めて診断し、健康なruntimeや別turnは終了しない。`attachStream()`自身がServerResponseの`close`を監視し、正常終了・ブラウザ更新・fetch中断のいずれでも対象stream、turn error、早期通知、close listenerだけを解放する。
* FakeChildとFakeSseResponseを使い、同一stdout chunkの応答→delta→completed、応答前のstarted→delta→completed、`onResult`の例外・非同期拒否・旧runtime分離、早期通知の件数・容量・turn数上限と破棄、SSEのwrite・end・destroy例外、実closeイベント、正常／異常statusの回帰を追加した。`node --test`は572件成功し、指定した全JavaScriptの`node --check`と`git diff --check`が成功した。
* Chromeの`http://127.0.0.1:8765/`と`npm run codex:bridge`で、`/health`のconnected、短い実回答の完了と送信ボタン再有効化、回答中のメモA→B切替で履歴が混ざらないこと、ページ再読み込みによる通信中断後もブリッジが生存して再送信できること、送信中のブリッジ停止でbusyが解除されること、再起動後の再接続・実回答を確認した。本文添付は操作せず、通常メッセージだけを送信した。終了後は8765・8787のlistener、App Serverプロセス、今回作成した一時ディレクトリが残っていないことを確認した。同一stdout chunk競合そのものは実機発生に依存するため、確定的な動作テストで検証した。

## 2026-08-17 Codex公開Origin・接続token対応

* GitHub PagesのOrigin`https://tetsujisugimori-coder.github.io`と既存ローカルOriginを完全一致で許可し、類似ドメイン、path付き値、`null`、不正な追加Originを拒否する。OPTIONSはrouteごとのGET／POSTと`Authorization`／`Content-Type`だけを許可し、要求された場合だけPrivate Network Access応答を返す。待受は`127.0.0.1`のまま維持した。
* `CODEX_BRIDGE_TOKEN`を32文字以上・空白なし・サンプル値以外に限定し、Bearer tokenをSHA-256固定長digestと`timingSafeEqual`で比較する。未認証・不正認証は同じ401とし、未認証healthは`running`とtoken必須だけを返す。Codexタブのpassword入力はtokenを`sessionStorage`だけへ保存して入力欄へ再表示せず、401時に消去する。メモ、`codexChat`、バックアップ、URL、ログ、レスポンスへtokenを含めない。
* `npm run codex:token`を追加し、ファイルへ保存せずランダムtokenを生成できるようにした。READMEと実験文書へ公開版／ローカル版の起動手順、Origin、ブラウザ権限、`127.0.0.1`の意味、iPhone制約、外部公開禁止、PowerShell終了時の環境変数消失、Windows自動起動未対応を記録した。
* Node標準HTTPとFake runtimeを使う統合テストでCORS、プリフライト、health最小化、tokenなし・形式不正・不一致・一致、thread開始／再開、SSE、実ソケット切断、token非漏洩を確認した。既存AI・明示添付・メモ別thread・高速turn・SSE切断を含む`node --test`は583件成功した。
* 実Codex App Serverは、既存8787を変更せず検証専用8790で起動した。公開Origin相当と生成tokenで未認証health`running`、認証health`connected`、新規threadのthread→複数delta→done、同じthreadの再開、通信切断後のconnectedを確認し、検証用listener、App Server、一時ディレクトリを後始末した。変更はまだ公開URLへデプロイされていないため、GitHub Pages上のブラウザE2Eは未実施であり、デプロイ後に確認が必要。

## 2026-08-17 Codex接続token表示手順の修正

* READMEと実験文書のPowerShell例へ、生成・設定した`CODEX_BRIDGE_TOKEN`を表示する行を追加した。表示値をMemo Nexusへ完全一致で入力すること、Git管理ファイル・メモ・スクリーンショットへ保存・公開しないこと、PowerShell終了時とtoken再生成時の再設定を明記した。
* 実装ロジックは変更せず、`node --test`は583件成功した。`node --check codex-bridge.js`、`node --check codex-bridge-token.js`、`node --check codex-chat.js`、`node --check codex-chat-utils.js`、`git diff --check`も成功した。

## 2026-08-19 フォント選択プルダウンの文字圏・書体表示改善

* フォント設定の標準`select`が使う表示名を、既存の正式名称用`label`とは分離して追加した。日本語向けは`Yu Gothic UI（日本語フォント）`、`Meiryo（メイリオ）`、`MS Mincho（ＭＳ 明朝）`、Noto JP/Serif JP、Zen Kaku Gothic New、Shippori Mincho（しっぽり明朝）を明示し、中国語向けは`Noto Sans SC（简体中文）`、`Noto Sans TC（繁體中文）`、`Source Han Sans（思源黑体／简体中文）`を表示する。欧文フォントは正式名称を維持した。Noto SC/TCへ思源黑体・思源黑體を流用していない。
* 各`option`へ対象フォントの`font-family`を設定し、閉じたselectにも現在選択中フォントを反映した。`id`、正式名称用`label`、CSS family、保存済み設定の正規化、Font Comparison連携は変更していない。
* `font-settings.js`と`app.js`の配信識別子をそれぞれ`0.5.0-5`、`0.5.0-92`へ更新した。既存のキャッシュ番号テストも対応する期待値だけを更新した。
* Edgeのローカル配信で設定画面を開き、全18件の選択肢、value不変、optionと選択中selectの計算済み`font-family`を確認した。一時的にConsolasを選んでも本文エディタは保存済みYu Gothic UIのままで、再読み込み後に選択値と本文表示が元へ戻ることを確認した。標準selectの展開リストはブラウザネイティブ描画のため自動スクリーンショットには含まれなかった。
* `npm test`（605件、失敗0件）、`node --check app.js`、`node --check font-settings.js`、`git diff --check`が成功した。

## 2026-08-19 Memo Nexus v0.5.0 Bridge Update リリース準備

* アプリバージョンを`0.5.0`、表示名を`Bridge Update`、ビルド日を`2026-08-19`へ更新した。リリース名は「Memo Nexus v0.5.0 — Bridge Update」。`app.js`の共通定数を起動ログ、PC／スマホ表示、設定内の保存状態、バックアップmanifestが引き続き参照し、READMEの現在版2箇所も同じ表記へ更新した。
* アプリ本体にService Worker、Cache API、独自キャッシュ名はない。静的配信の切替は`index.html`のURLクエリで行うため、ローカルCSS／JavaScript 39件を既存の末尾リビジョンを保った`v=0.5.0-*`へ更新した。Web ClipperのService Workerとmanifestは本体とは別管理のため変更していない。
* `DB_VERSION = 5`、ZIPの`BACKUP_VERSION = 2`とformat version、表ブロック版1、Codexクライアント通信版`0.1.1`、Web ClipperのManifest V3／拡張版`0.3.5`を維持した。`package.json`にはアプリ版とロックファイルがないため変更していない。過去のLOGにある0.4.0記録も維持した。
* `version.test.js`を追加し、現行版・表示名・ビルド日、全39資産のキャッシュ識別子、別用途バージョン不変を確認した。全121 JavaScriptファイルの`node --check`、`npm test`（605件、失敗0件）、`git diff --check`が成功した。
* Edgeのローカル配信で主要画面、コレクション、PC／スマホ用の`v0.5.0 "Bridge Update"`、39件の0.5.0資産URLを確認した。通常再読み込み後も同じ版数、主要画面、既存メモ一覧3件を維持し、起動失敗表示はなかった。コンソールerror／warningの取得、公開版・PWAで0.4.0から更新する実機キャッシュ切替、ZIP生成ファイル内のappVersion確認は未実施。

## 2026-08-19 PR #114 フォント条件検索・Weight単位読込・再試行

### 変更内容

* 既存アンケートを削除せず、既定で開く「条件からフォントを探す」として整理した。使用言語、文章の雰囲気、主な用途の3問と、アンケートは条件検索、選択欄は直接選択、Font Comparisonは詳細比較という関係を表示する。初期値は日本語／中立・読みやすさ重視／長文を書くで、設定画面を開いた時点からYu Gothic UI、Meiryo、Noto Sans JPの3候補を表示し、回答変更時に自動更新する。候補表示と条件変更だけでは保存もWebフォント読込も行わない。
* 候補ボタンを本文／見出し／コードの適用先が分かる文言へ変更した。選択時は全体設定または有効なメモ個別設定の該当欄とプレビューだけを更新し、既存の「フォント設定を保存」で確定する流れ、直接選択、Font Comparison連携を維持する。
* Webフォントローダーを`requestFont(fontId, weights)`へ変更し、フォントごとに`loadedWeights`、`loadingWeights`、`failedWeights`を管理する。題名・見出しは700、本文・コードは400をフォント単位で集約し、読込済みWeightを再取得せず、不足Weightだけを追加取得する。Google Fontsの`document.fonts.load()`も必要Weightだけを要求し、Source Han Sans CNはRegular（400）またはBold（700）の必要なOTFだけを`FontFace`で読み込む。
* 読込失敗したフォントとWeightごとに代替表示案内と「再試行」を追加した。再試行は現在の全体設定と有効なメモ個別設定で必要な失敗Weightだけを要求し、読込中はボタンを無効化し、成功後はエラーとボタンを消す。stylesheet失敗時は失敗したlinkとPromiseを破棄し、Source Han Sans CNの一部失敗もWeight単位で再試行する。選択ID、保存済み設定、CSSフォールバックは変更しない。

### 確認結果

* `node --check app.js`、`font-settings.js`、`font-recommendation.js`、`web-font-loader.js`は成功した。
* `node --test font-settings.test.js font-recommendation.test.js web-font-loader.test.js`は29件成功し、`npm test`は既存回帰を含む602件すべて成功した。
* `git diff --check`は問題なしだった。
* Edgeのローカル配信で、設定を開いた時点の3問・初期候補3件と動的Webフォントlink 0件、回答変更時の自動更新、日本語フォーマルの明朝系、簡体字のNoto Sans SC／Source Han Sans、繁体字のNoto Sans TC、英数字コードの等幅候補を確認した。条件変更後もlinkは0件だった。
* 同ブラウザで、Noto Sans JPを本文へ反映すると400だけ、同じフォントを見出しへ追加すると700が追加され、Source Han Sans CNの本文利用では400だけが読込済みになることを状態表示で確認した。候補は全体設定とメモ個別設定の該当欄へ保存前プレビューとして反映された。
* Google Fonts通信を一時遮断し、IBM Plex Sans 400の失敗時に代替表示と「再試行」が出ること、再試行中は無効、成功後はエラーとボタンが消えることを確認した。別タブの通常起動ではコンソールエラー0件だった。
* 隔離した別Originで、全体設定のNoto Sans JPとメモ個別設定のNoto Serif JPを保存し、再読み込み後も個別設定有効状態と各IDが復元されることを確認した。390px、タブレット幅、PC幅でページの`scrollWidth`と`clientWidth`が一致し、ライト／ダーク双方で検索パネルの文字と背景が切り替わり、検索UIが表示されることを確認した。
* Font ComparisonからMemo Nexusへ戻る実ブラウザ往復は今回再実施していない。送信元、用途、範囲、登録ID、`font-family`完全一致、URLパラメータ除去、受信直後に保存しない契約は既存自動テストで確認した。

## 2026-08-19 フォント推薦UIとWebフォント遅延読込

### 変更内容

* 既存8システムフォントを維持し、Font Comparison PR #16を参照専用として、契約どおりの10 WebフォントID・表示名・`font-family`・配信URLをMemo Nexus側の単一カタログへ追加した。`font-comparison`リポジトリおよびPR #16には変更していない。
* 全体／メモ個別の題名・本文・見出し・コードの全8フォント欄を、システムフォントとWebフォントの`optgroup`に分け、18種を同じ順序で選択できるようにした。未知IDは従来どおり既定値へ正規化する。
* 通常のフォント設定内へ、使用言語、文章の雰囲気、主な用途から最大3件を安定順位で返す折りたたみ式の推薦UIを追加した。対応済み／一部対応を優先し、対応不明は候補不足時だけ、非対応はさらに不足する場合だけ使う。推薦表示だけでは保存もWebフォント読込も行わない。
* Webフォントローダーを独立モジュールとして追加し、`idle`、`loading`、`loaded`、`error`を管理するようにした。現在の表示、設定プレビュー、推薦選択、Font Comparison受取確定で実際に使うフォントだけを読み込み、読込中／読込済みの重複要求を抑止する。失敗時も選択IDとCSSフォールバックを維持し、設定画面へ代替表示を案内する。
* Font Comparisonの戻り値検証を18種へ拡張し、送信元、用途、範囲、カタログID、正確な`font-family`を照合する。URLの`fontLabel`は表示に使わず、Memo Nexus側の正式名へ置き換え、処理済みパラメータは既存どおり除去する。
* 全体設定は端末localStorage、メモ個別設定は既存メモオブジェクトの`fontSettings`へIDだけを保存する構成を維持した。個別Webフォント設定がローカルMarkdown／ZIPバックアップを往復するテストを追加し、フォントファイルや配信CSSは保存対象にしていない。

### 確認結果

* `node --check app.js font-settings.js font-recommendation.js web-font-loader.js`（成功）
* `node --test font-settings.test.js font-recommendation.test.js web-font-loader.test.js backup-bundle-utils.test.js`（42件成功）
* `npm test`（600件成功）
* `git diff --check`（問題なし）
* Edgeのローカル配信で、起動時の動的Webフォントlinkが0件、推薦表示後も0件、Noto Sans JP選択後だけ1件になることを確認した。Noto Sans JPの全体設定保存・再読込、Interのメモ個別保存・別メモ切替・復帰、Shippori MinchoのFont Comparison戻り値受取とURLパラメータ除去も確認した。
* 同ブラウザで全8フォント欄が各18選択肢／2分類であること、Google Fontsの実読込成功、ライト／ダーク、390pxで推薦UI展開時もページとフォント設定領域に横はみ出しがないこと、コンソール警告・エラー0件を確認した。外部配信の失敗経路とSource Han Sansの2ウェイト読込は自動テストで確認した。

## 2026-08-20 ローカルMarkdown競合解決後の明示保存修正

* 原因は、競合解決時にメモIDだけを`forcedLocalSaveNoteIds`へ保持していた一方、`runManualLocalSave()`の保存前`scanExternalLocalMarkdown()`がその情報を考慮せず、古い`sync-state.json`ハッシュを基準に同じ競合を再登録していたことだった。PR #120の追加修正では、`sync-state.json`に`hash`がない旧形式、対応情報がない`notes/`内Markdownなど、`classifyMarkdownCandidate()`へフォールバックする競合に生ハッシュがなく、解決記録を作れないことも確認した。
* 解決情報を、メモID・選択操作・確認元Markdown相対パス・実際の書込み先相対パス・利用者が確認した生Markdownハッシュを持つ一時Mapへ変更した。`buildLocalScanAnalysis()`は通常・フォールバックの競合へ必要な生ハッシュを付け、5項目が一致する対象だけを保存前スキャンで一度だけ抑止する。ファイルが再変更されれば記録を無効化して新しい競合として表示し、別メモ、保留中の競合、未確認の外部変更には適用しない。
* `sync-state.json`に`fileName`だけがある旧形式、および対応情報のない`notes/`直下の同一ID Markdownは、確認した既存`notes/`ファイルをそのまま書込み先と`sync-state.json`の`fileName`へ採用する。保存後は実ファイルのハッシュを登録し通常管理へ移行する。選択フォルダ直下と`inbox/`は通常保存先と混同しないため、上書き・ローカル版読込・両方を残すを表示せず、元ファイルを削除しない除外または保留だけを提供する。
* 確認元／書込み先パスの決定、解決記録の生成、書込み直前の照合、成功後の記録破棄を`local-sync-utils.js`の共通関数へ切り出した。`performLocalWorkspaceSave()`は通常の競合保護を維持し、確認済み対象だけ書込み直前に生ハッシュ・両パス・メモIDを再照合する。Markdown、`sync-state.json`、IndexedDB更新が成功した後だけ解決情報を削除するため、書込み失敗時に同期済み扱いにはしない。
* 模擬File System Access APIで、Memo Nexus版上書き→今すぐ保存→Markdown書込み→`sync-state.json`更新→再スキャン、ローカル版読込、確認後の再外部編集、別メモの未解決競合、`sync-state.json`更新失敗を回帰テストした。旧形式と未管理`notes/`直下は上書き／読込の両方で同一ファイルへ保存し、`fileName`と実ハッシュが同期情報へ入ること、直下・`inbox/`は候補と元ファイルを残すことを確認した。画像付きメモでは`attachment://...`と`../assets/...`の往復と、同一メモへの読込では既存asset ID・画像ファイルを不必要に複製しないことを確認した。
* 画像付き競合の「両方を残す」では、画像ファイル名だけを基準に既存添付IDを再利用すると、IndexedDBの同じ主キーを新メモ所属で`put()`して元メモから添付所属を奪う問題を再現した。既知IDの添付レコードを主キーで読み、所有`memoId`が取込先メモと一致する場合だけIDを再利用する。別メモまたは所有者不明では衝突しない新IDを発行し、Blob・MIME・kind・sizeを維持した独立レコードを作成して本文参照も新IDへ置換する。
* 本番と同じ`getAttachmentRecord()`・`candidateAttachments()`・`putAttachments()`を呼ぶ模擬IndexedDBと模擬File System Access APIで「両方を残す」を実行し、元・新規の添付レコードと`memoId`、Blob内容、両本文の`attachment://...`、両Markdownの`../assets/...`、衝突しない新旧画像ファイル、`sync-state.json.notes/assets`の新旧対応、元画像ファイルの保持、新画像ファイルの作成、保存後の競合候補0件を確認した。元メモの本文・添付レコード・画像は変更または削除しない。
* `local-save.test.js`は56件、添付ユーティリティテストは25件、全自動テストは618件成功した。変更済み全JavaScriptの`node --check`、`git diff --check`も成功した。`attachment-utils.js`と`app.js`の配信識別子をそれぞれ`0.5.0-11`、`0.5.0-95`へ更新し、`local-sync-utils.js`は変更していないため`0.5.0-10`を維持した。
* Edge／Chromiumで実フォルダを選ぶ手動確認は、この実行環境ではネイティブのフォルダ選択と外部ファイル変更を安全に自動化できないため未実施。実機では通常・旧形式・未管理`notes/`直下・画像付きメモで各競合解決操作後に「ローカルへ保存」を押し、保存後再スキャン、保存前の再外部編集時の再競合表示を確認する。画像付き「両方を残す」では保存後と再読み込み後に元・新規メモの両方で画像が表示され、Markdownと画像ファイルが新旧両方存在することを確認する。直下・`inbox/`の同一ID競合では上書き／読込が表示されず、除外／保留だけで元ファイルが残ることも確認する必要がある。

## 2026-08-20 UTC保存日時とローカル日付の統一

* 原因は、UTC ISO・オフセット付きISO・数値タイムスタンプを保存している一方、画面ごとの`new Date()`、日付文字列、`Number(createdAt/updatedAt)`による比較が混在していたことだった。加えて初回ローカル保存時に設定される`localCreatedAt`を表示用作成日時で`createdAt`より優先していたため、正しいタイムゾーン変換後もメモ本来の作成時刻がローカル保存時刻へ置き換わって見えていた。UTCの瞬間は既存形式のまま保存し、表示、日付キー、「今日メモ」、バックアップファイル名だけを利用者のブラウザのローカルタイムゾーンへ統一した。`2026-08-20`のような日付だけの値はローカル年月日として解析し、UTCより西側でも前日へずらさない。タイムゾーンなしの旧日時は従来互換としてブラウザのローカル日時とみなし、不正・欠損値は安全な空表示または末尾ソートへフォールバックする。
* `status-time-utils.js`へローカル日時表示、ローカル日付キー、同日判定、日付だけの表示、実時刻変換・比較、`time`要素属性の処理を集約した。メモ本体、一覧ツールチップ、コレクション一覧、Web Clipperの既存メモ候補、保存時刻の表示を同じ処理へ接続し、作成日・更新日・関連メモ・添付・別ウィンドウ同期・ドラフト復元の比較を表示文字列や単純な数値変換ではなく実時刻で行う。表示上の正式な作成日時は日時として有効な`createdAt`を優先し、欠損・不正時だけ有効な`localCreatedAt`へフォールバックし、両方が無効なら空表示・末尾ソートとする。ローカルMarkdown候補も同じresolverを利用し、初回保存後、再読み込み後、再スキャン後も本来の`createdAt`を維持する。`localCreatedAt`と`localSavedAt`は互換用・保存時刻として残し、削除や移行は行わない。
* 新規メモは従来どおり`Date.now()`を保存する。ローカルMarkdown ZIP取込ではfront matterの`createdAt`、`localCreatedAt`、`updatedAt`、`bodyUpdatedAt`、`localSavedAt`を変更せず引き継ぎ、Web Clipper新規取込では`capturedAt`の瞬間を`createdAt`へ保持する。可搬ZIPの出力・復元もUTC ISOの瞬間が変わらないことを確認した。IndexedDB、Markdown、ZIPの既存日時を一括更新する移行処理は追加せず、本文中の日時文字列、DB・ZIP形式、PR #120の競合・画像保存処理は変更していない。
* 回帰テストでは、UTC境界をAsia/Tokyoで`2026/8/20 08:19`と表示して日付キーを`2026-08-20`にすること、America/Los_Angelesの前日判定、日付だけ、オフセット付きISO、数値時刻、正午、不正値、実時刻ソート、今日メモ、Markdown・ZIP・Web Clipper日時保持、別ウィンドウ同期を確認した。さらに`createdAt=2026-08-19T23:19:22.291Z`と初回保存時刻`2026-08-19T23:25:00.000Z`を再現し、保存前後の`createdAt`不変、表示の08:19維持、欠損・不正時のフォールバック、作成日順不変、Markdown再取込を実値で検証した。日時個別テスト12件、`local-save.test.js` 58件、全自動テスト631件が成功した。`local-save-state.test.js`は存在しないため、同モジュールを直接読み込む`local-save.test.js`へ追加した。今回変更したJavaScript 5ファイルの`node --check`と`git diff --check`も成功した。配信識別子は既存の`status-time-utils.js=0.5.0-2`、`memo-popout-utils.js=0.5.0-4`、`app.js=0.5.0-96`に加え、`local-save-state.js=0.5.0-5`、`local-markdown.js=0.5.0-4`へ更新した。

## 2026-08-23 IndexedDB通常保存の共通基盤

* 通常メモ保存を`noteId`・`revision`・固定`snapshot`・`saveRequestId`を持つ要求へ変更し、同一メモのIndexedDB書込みを直列化した。開始前の古い要求は最新revisionへ集約し、保存完了時は`currentRevision`と`lastSavedRevision`が一致した場合だけ保存済みとする。失敗時はrevisionとメモリ上のdraftを保持し、エラー状態を記録したまま同じrevisionまたは再編集後の新revisionを再保存できる。
* `scheduleSave()`は入力時にメモリ上のsnapshotとrevisionを確定し、`saveCurrentNote()`は共通基盤へ渡す薄い入口になった。`openNote()`は切替前メモの予約をそのメモIDのままキューへ渡し、保存完了処理から`currentId`の書換えとDB全件再読込を除去した。タグ、フラグ、メモ別フォント、解説カードの通常note更新も同じキューへ接続した。ローカルMarkdown手動保存、ZIP、クラウド、添付ストアは今回統合していない。
* 保存中再編集、保存中メモ切替、debounce中切替、古い要求、保存失敗・再試行、失敗後再編集、複数メモ、高頻度200 revisionの8テストを追加した。`node --test note-save-foundation.test.js`は8件成功し、同テストを20回反復して失敗0件、`node --test`は全639件成功した。`app.js`の配信識別子を`0.5.0-97`へ更新し、`note-save-foundation.js`を`0.5.0-1`として追加した。

## 2026-08-23 IndexedDB通常保存基盤のレビュー修正

* 入力時のdraft管理を固定snapshotのMapから`notes`配列と同じオブジェクトを共有するlive draftへ変更した。本文・題名・タグ・フラグ・フォント・解説カードの同期更新ではdeep cloneせず、debounce発火、メモ切替、明示保存、batch transaction開始時に`createSaveRequest()`を作る時だけdeep cloneして再帰的にfreezeする。古いrevisionの保存成功時は、より新しいlive draftを削除・上書きしない。
* タグ更新は関数開始時の`noteId`を固定し、タグ定義のIndexedDB待機後も`noteForSave(noteId)`から対象draftを再取得して`enqueueNoteSave(noteId)`へ渡す。表示メモが変わった場合はタグUIを再描画しない。保存基盤の`onStateChange`・`onSaveSuccess`・`onSaveError`は安全な通知としてDB書込み判定から分離し、通知例外時もactive、batch waiter、idle waiter、後続revisionを解放する。
* 通常保存と直接transactionの競合を防ぐため、ID昇順のメモロック、`enqueueBatchSave()`、`mutateNotesAtomically()`、`runExclusiveNoteOperation()`を追加した。メモ別フォント、全フォント初期化、コレクション移動、単体・複数ゴミ箱移動、復元、削除・削除取消、コレクション削除、既存IDへのローカルdraft・Markdown・可搬ZIP取込、Web Clipper既存メモ更新、ローカル手動保存後のnoteメタデータ反映を調停対象にした。複数ストアtransactionの原子性は維持する。起動時だけの旧メモmigrationと、衝突しない新規IDの初回`putNote()`はキュー開始前または未登録IDのため低レベル書込みを維持する。ZIP/Markdownのファイル書出し、クラウド同期、添付ストア単独更新は対象外だが、noteストアへ副作用がある可搬ZIP取込・ローカル保存メタデータ・Web Clipper更新は上記のとおり統合した。
* 実際の`app.js`から`openNote()`、`scheduleSave()`、タグ更新、live draft、batch mutation関数を抽出実行する統合テストを追加した。debounce切替、保存中再編集、保存中切替後の両メモ編集、タグ待機中切替と失敗保持、UI通知例外、入力200回のclone回数、本文保存とフォント変更・全初期化・複数コレクション移動、ゴミ箱batch失敗を検証した。保存競合テストは19件成功し20回反復で失敗0件、全自動テストは650件中650件成功した。配信識別子は`note-save-foundation.js=0.5.0-2`、`app.js=0.5.0-98`へ更新した。
* Edgeの分離ローカルURL`http://127.0.0.1:8765/`、PCタイムゾーン`Etc/GMT-9`で、初回ローカル保存相当の処理後も`createdAt`と表示値が08:19のまま、`localCreatedAt`と`localSavedAt`が08:25として別に維持されることを確認した。既存メモの本体上部、一覧ツールチップ、コレクション一覧の日付が一致し、通常再読み込み後も08:19選択と新しいJavaScript配信が維持され、console error／warningは0件だった。ネイティブの実フォルダ選択を伴うMarkdown保存・再取込、実ZIPの再アップロード、画像付き競合解決は自動操作していないため、模擬File System Access API、ZIP往復、PR #120回帰テストで補完した。実機では作成08:19の専用メモを08:25にローカル保存し、本文・一覧・コレクション・再読み込み・再スキャン後も08:19であること、画像付き保存と競合解決後保存で画像が維持され再競合しないことを確認する必要がある。

## 2026-08-23 IndexedDB原子バッチ失敗隔離・永久削除終端化

* 原子バッチの変更をlive draftへ先行反映せず、バッチ開始時の固定snapshotだけへ適用するよう変更した。transaction成功後はバッチが変更したフィールドだけをlive draftへ合流し、待機中に別フィールドへ行われた通常編集と新revisionを維持する。失敗時はバッチrevisionだけを解除するため、ゴミ箱移動、復元、コレクション変更、ローカル取込、Web Clipperなどのバッチ固有値が後続の単体保存へ混入せず、バッチ前・待機中の本文編集だけを通常保存できる。
* 保存基盤へ原子バッチtokenと永久削除専用の終端処理を追加した。バッチ中の新しい単体保存を遮断し、永久削除は対象IDを操作開始時から終端状態にして、変更、遅延保存、別バッチ、別ウィンドウ同期による再導入を防ぐ。既存保存をID順ロックで完了させてから削除し、成功後は同一セッションで復活不可、失敗時は終端状態を解除して編集・保存・再削除を再開できる。複数IDも同じ削除操作で扱う。
* 実アプリから本番`handleNoteSaveSuccess()`を抽出して保存基盤のcallbackへ接続する統合テストへ変更した。stale revision、背景メモAの完了中に表示中メモBを維持する経路、最新revision、バッチ失敗後のA/B単体保存、custom writer失敗、成功・失敗待機中の追加入力、複数IDの削除成功・失敗・再試行を回帰対象に追加した。配信識別子は`note-save-foundation.js=0.5.0-3`、`app.js=0.5.0-99`へ更新した。
* `node --test note-save-foundation.test.js`は16件、`node --test note-save-app-integration.test.js`は9件、`node --test`は全656件が成功した。保存競合25件の組合せを20回反復して500件すべて成功し、変更JavaScriptの`node --check`と`git diff --check`も成功した。ローカルHTTP配信は開始できたが、この実行環境ではブラウザー制御セッションの初期化に必要な資産パスを作成できず、実ブラウザー手動確認は実施できなかった。

## 2026-08-23 完全削除tombstoneによる複数ウィンドウ復活防止

* IndexedDBをversion 6へ更新し、本文や題名を含まない`note-tombstones`ストアを追加した。完全削除時は対象メモ、対象添付、全対象IDの`deletionId`・`deletedAt`を持つtombstoneを同じreadwrite transactionで確定する。ゴミ箱への移動ではtombstoneを作らない。
* 単件保存、原子バッチ、可搬バックアップ復元、Web Clipperのメモ＋添付更新、コレクション削除時のメモ移動を、`notes`書込みと同じtransaction内でtombstone確認してから書き込む経路へ統一した。1件でも完全削除済みなら`NOTE_PERMANENTLY_DELETED`でtransaction全体を中止し、古いウィンドウや休止タブ、draft mirror、通知欠落からの復活を防ぐ。
* BroadcastChannelでは完全削除のstarted／completed／abortedを`memoIds`・`deletionId`・`deletedAt`付きで通知する。通知は早期停止と表示更新に使い、正しさは永続tombstoneで保証する。専用エラー時は保存基盤の終端状態、遅延保存、live draft、localStorage draft、一覧・選択状態を整理し、メイン削除ボタンの非同期失敗も既存の共通エラー表示へ1回だけ渡す。
* 2ウィンドウが同じIndexedDB相当ストレージを共有するテストで、保存→削除、削除→保存、削除中編集、BroadcastChannel欠落、削除失敗rollbackと再open、複数ID、batch全体中止、メイン削除ボタン拒否を確認した。新規／旧DB移行とupgrade abort、同一transaction guard、20回のsave/delete競合も回帰対象に追加した。
* `node --test --test-isolation=none`は全667件成功した。変更した全19 JavaScriptの`node --check`と`git diff --check`も成功した。

## 2026-08-23 添付単独保存のtombstone guardと実競合テスト

* 添付単独保存の共通writerを`note-tombstone.js`へ追加し、`attachments`と`note-tombstones`を含む同一readwrite transaction内で対象`memoId`を重複排除して確認した後だけ全添付を保存するようにした。1件でも完全削除済みなら`NOTE_PERMANENTLY_DELETED`と原因の`noteId`・tombstoneを保持して全件abortし、`memoId`がない、または空白だけの新規recordは`ATTACHMENT_MEMO_ID_REQUIRED`で保存前に拒否する。
* 通常追加、画像ブロック、ドラッグ＆ドロップ、貼り付け、ローカルMarkdown、Markdown ZIP、Web Clipper新規保存は共通writerを通す。可搬ZIP復元と既存Web Clipper更新は、note・attachment・tombstoneを含む既存の同一transaction guardを維持した。attachment rollbackと通常削除はデータを復活させない削除専用処理のためtombstone guardを追加していない。現行の全作成経路は`memoId`を設定する。既存の所有者不明recordは読取り・削除互換を維持し、新しい書込みだけを拒否する。
* 添付処理は開始時、同一メモqueueの実行直前、各画像圧縮・Blob準備後、容量取得後、保存直前・保存後、`currentAttachments`更新前、editor参照挿入前、成功表示前にterminal状態を確認する。完全削除済みは既存の画面クリーンアップへ接続し、保存後にterminal化した場合は今回の添付だけをrollbackする。
* deferred gate付きIndexedDB transactionモデルで、note保存先行、完全削除先行、attachment追加先行、完全削除後のattachment追加、attachment準備中削除、複数memoId batchのA〜Fを実際に時間的に重ねた。productionの共通attachment writerを直接実行し、6種類（note保存先行、削除先行、添付先行、準備先行、Broadcast通知あり、通知なし）を4周、計24回反復してnote復活・孤立attachmentとも0件を確認した。
* `node --test --test-isolation=none`は全672件成功した。変更したJavaScriptの`node --check`と`git diff --check`も成功した。DB version 6、既存tombstone schema、完全削除transaction、note通常保存・atomic batch・BroadcastChannelの仕様は変更していない。実ブラウザーの2ウィンドウ手動確認は、この実行環境でブラウザー制御セッションの初期化に必要な資産パスを作成できないため未実施とした。

## 2026-08-24 ローカル保存batchのUI更新集約

* 原因は、ローカルファイルと`sync-state.json`を書き終えた後の全メモmetadata更新で、`enqueueBatchSave()`の成功通知がメモごとに`handleNoteSaveSuccess()`を呼び、語句リンク索引の無効化と`renderList()`または`renderAll()`を全件分同期実行していたことだった。保存状態を`保存済み`へ進める処理はこの連続描画より後にあり、メモ件数Nに対して全件一覧再構築をN回行うO(N²)相当の処理中は`保存中`のまま固まって見えていた。通常保存とローカル保存のキュー、メモ単位ロック、`whenIdle()`を確認し、循環待機や永続的に未解決となるPromiseは見つからなかった。
* atomic batchの各成功通知へbatch文脈を付け、メモリ反映をまとめて1回の線形走査で行った後、`completeAtomicBatch()`によるロック・idle解放後にUIを同期する。一般batchは索引無効化と一覧または全体描画を各最大1回、`localCreatedAt`・`localSavedAt`だけのローカル保存metadata batchは索引を無効化せず一覧だけを1回更新する。通常1件保存の通知経路、固定snapshot、revision、保存中再編集のmerge、transaction原子性、tombstone guardは維持した。配信識別子は`note-save-foundation.js?v=0.5.0-6`、`app.js?v=0.5.0-102`へ更新した。
* 200件の模擬メモで、ローカルmetadata batchの一覧描画1回・索引無効化0回、本文を含む一般batchの全体描画1回・索引無効化1回、全メモの`revision=lastSavedRevision=1`・dirty解除、現在メモと背景メモの内容一致を確認した。通常1件保存の`保存中`から`保存済み`、失敗batchのエラー状態・dirty・描画0回、保存中再編集、transaction rollback、通常保存との直列化、tombstone拒否、Markdown・添付・manifest・`sync-state.json`・ローカル日時仕様の既存テストを含め、`node --test`は全677件成功した。変更JavaScriptの`node --check`と`git diff --check`も成功した。
* ローカルHTTP配信は起動できたが、ブラウザー制御環境の初期化に必要な資産パスを作成できず、ネイティブのフォルダ選択、実フォルダへのMarkdown・添付・manifest・`sync-state.json`書込み、画面上の`保存中`から`保存済み`への遷移は実ブラウザーでは未確認とした。模擬File System Access APIによる統合テストで補完している。

## 2026-08-24 ローカル保存中再編集のsnapshot境界修正

* 追加調査で、ローカルファイル書込み待ち中に現在メモを再編集すると、書込み後のmetadata batch入口が現在DOMを再取込みし、280msの通常保存予約を解除していたことを確認した。古い本文AをMarkdownへ書いた後に新しい本文Bがmetadata snapshotへ混入し、内部metadata更新中はローカル変更versionも抑止されるため、Bがブラウザだけに残ったままローカル状態を保存済みにできる競合だった。通常保存キューとローカル保存キューには循環待機や未解決Promiseはなかった。
* ローカル保存開始時に全メモを1回だけcloneして`noteId`・`revision`・本文を含む固定snapshotを作り、Markdown、manifest、`sync-state.json`をその境界から生成する。書込み後は開始revisionと一致するメモだけをmetadata batchへ渡し、現在DOMの再取込みと遅延保存予約解除を行わない。batch中または完了後にrevisionが進んだメモはlive draftへローカル日時を合流せず、dirtyと予約を保持してローカル状態を`pending`へ戻す。
* `mutateNotesAtomically()`の一般batch既定値は維持し、ローカルmetadata経路だけに期待revision、DOM取込み抑止、予約解除抑止、dirty時の変更合流抑止、変更可能フィールド制限を指定した。`updateNotesTransaction()`は内部metadata更新だけをローカル変更versionへ数えず、同時のユーザー変更は抑止しない。`localCreatedAt`・`localSavedAt`以外が変わる場合は索引無効化を省略する前にtransactionを拒否する。固定snapshot、メモ単位ロック、通常保存との直列化、原子transaction、保存中再編集、tombstone guardは維持した。配信識別子は`app.js?v=0.5.0-103`へ更新した。
* 遅延ゲート付き統合テストで、AのMarkdown書込み中にBと`[[late-term]]`へ編集しても初回MarkdownはA、live draftと280ms予約はBのまま、metadata対象からAを除外し、状態を`pending`へ戻すことを確認した。後続通常保存ではBを保存して索引を1回だけ無効化し、次の明示ローカル保存でBをMarkdownへ出力する。200件中1件の再編集ではmetadata 199件、一覧描画1回、索引無効化0回、編集メモdirtyを確認し、無編集200件では全件metadata、一覧描画1回、索引無効化0回を確認した。metadata transaction失敗と後続通常保存失敗でもdraft・dirtyを維持する。
* `note-save-app-integration.test.js`の17件を20回反復して全340件成功した。`node --test`は全682件成功し、変更JavaScriptの`node --check`と`git diff --check`も成功した。実ブラウザーのネイティブフォルダ選択、実フォルダへのMarkdown・添付・manifest・`sync-state.json`書込み、画面上の`保存中`から`保存済み`／`未保存`への遷移はブラウザー制御環境の制約により未確認で、模擬File System Access APIと本番保存関数を抽出した統合テストで補完した。

## 2026-08-24 ローカル保存metadataの線形live索引化

* ローカル保存後metadataのrevision照合・atomic batch計画・成功反映・完了境界で、メモごとに`noteForSave()`から`notes.find()`を繰り返していた経路を、batch開始時に`notes`を1回走査して`noteLiveDrafts`を優先反映する一時`Map`へ置き換えた。Mapは当該metadata batch内だけで共有し、Markdown・manifest・`sync-state.json`用の固定snapshotとは分離した。既存のrevision境界、メモ単位ロック、通常保存との直列化、transaction原子性、保存中再編集、tombstone拒否は維持した。配信識別子は`app.js?v=0.5.0-104`へ更新した。
* 200件と400件の決定的な計数テストで、一時索引の構築が各1回、全件走査が200回／400回、参照が800回／1600回となり、メモ件数に比例することを確認した。本番`performLocalWorkspaceSave()`を模擬File System Access APIと実`getAllNotes()`で実行し、成功、ファイル書込み中再編集、metadata transaction失敗、tombstone化を検証した。統合テスト22件を20回反復して全440件、`node --test`は全687件が成功した。変更JavaScriptの`node --check`と`git diff --check`も成功した。実ブラウザーのネイティブフォルダ選択と実フォルダへの書込みは未確認である。

## 2026-08-25 Codexスレッド保存の共通基盤接続

* Codexチャットの`saveForNote()`が既存メモ全体を`putNote()`へ直接渡していたため、通常保存のrevision・固定snapshot・メモ単位ロックとは独立していた。既存の`noteSaveFoundation`を複製せず、従来の`noteId`契約を保ったまま名前空間付き`resourceKey`を受けられるようにし、Codexスレッドは`codex-thread:<threadId>`を保存単位として同じ`markChanged()`・`enqueueSave()`・追従保存へ接続した。
* Codex保存要求は作成時の`noteId`・`threadId`・revision・`codexChat`をdeep cloneしてfreezeする。writerは要求後に現在のスレッドやUIを読み直さず、実メモIDの既存ロックを取得してから、`notes`と`note-tombstones`の同一transaction内でDB上の最新メモへ`codexChat`だけを追加または除去する。これにより通常メモの本文・タグ等を古いCodex側snapshotで戻さず、同一threadの保存順、別thread切替、永久削除との競合を調停する。失敗時はdirtyと最新要求を保持して再試行でき、同じメモが新threadへ移った後は失敗した旧thread要求を再投入しない。
* 制御Promiseを使い、同一thread保存中の更新と古い完了、A保存中のB切替、異なるthreadのsnapshot分離、要求後の元オブジェクト変更、失敗後の最新revision再試行、旧thread再試行抑止、通常メモwriterとのロック順、既存形式の読込みを追加検証した。Codex対象24件、通常保存基盤41件、全695件が成功した。CI相当の全JavaScript構文検査と`git diff --check`も成功した。配信識別子は`note-save-foundation.js?v=0.5.0-7`、`codex-chat-utils.js?v=0.5.0-5`、`app.js?v=0.5.0-105`、`codex-chat.js?v=0.5.0-6`へ更新した。通常メモ、通常AI、ローカル保存、ZIP、添付、import/export、Bridge通信、DB schema、UIは変更していない。

## 2026-08-25 Codexスレッド保存の世代判定と未確定値分離

* 同じメモでthread AのA1が保存中、A2が共通基盤のpending、thread Bが別resource queueへ入った場合、`latestRequests`からAを除くだけではA2が残り、実メモIDロックの順序どおり`A1 → B → A2`と書いて旧threadを復活できた。coordinatorへメモ単位のthread世代を追加し、固定snapshotへ世代を含めた。Codex writerは実メモIDロックを取得した内側で、IndexedDB部分更新を呼ぶ直前にresource keyと世代を再確認する。staleなA2はwriterを呼ばずno-opで完了するため、共通基盤のrevision・`lastSavedRevision`・dirty・`whenIdle()`を解放しつつBを最終値にする。旧threadの再試行は引き続き`null`になる。
* Codex要求を`notes`と`noteLiveDrafts`へ保存前に反映していたため、Codex部分更新が失敗した後の通常本文保存が未確定`codexChat`を全メモsnapshotへ取り込み、別経路から確定できた。未確定値はcoordinatorのdeep clone・freeze済み最新要求だけに保持し、Codex transactionが実際に成功した要求を`WeakSet`で識別してからメモリへ反映する。成功済みの旧要求と新しい未確定要求も分離し、新要求の失敗で直前のDB確定値を失わない。明示再試行成功時も同じ反映処理でDB・`notes`・live draftを一致させる。
* 通常メモwriterは、保存実行時のIndexedDB最新レコードから確定済み`codexChat`だけを引き継ぎ、本文・題名・タグ等は従来の固定snapshotを保存する。通常保存が先なら後続Codex部分更新が本文を保持し、Codex保存が先なら後続の古い通常snapshotが最新threadを戻さない。通常保存成功時のメモリ反映も現在の確定済みCodex値を維持する。通常メモのrevision・キュー・tombstone、atomic batch、Codexの`codex-thread:<threadId>`、DB schema、UI、Bridge、通常AI、ローカル保存、ZIP、添付、import/exportは変更していない。
* 制御Promiseを使い、同一メモA1/A2/B、stale解除、writer不実行、旧retry、追加・解除失敗後の通常保存、成功済み旧要求と失敗する新要求、明示再試行後のDB・`notes`・live draft一致、通常保存／Codex保存の両順序、異なるメモの独立性を検証した。Codex utility 29件を24回反復して全696件、関連118件、`npm test`全705件が成功した。全126 JavaScriptの`node --check`と`git diff --check`も成功した。配信識別子は`codex-chat-utils.js?v=0.5.0-6`、`app.js?v=0.5.0-106`へ更新した。

## 2026-08-25 atomic batchとCodexスレッド保存の確定値統合

* atomic batchは実メモIDロック取得前に全メモの固定snapshotを作り、従来の`updateNotesTransaction()`と通常mutation用custom writerはそのsnapshotをそのまま`store.put()`していた。このため、先行Codex transactionが最新`codexChat`を確定しても、後続batchが古い値へ巻き戻していた。通常batchはロック取得後、tombstone guardと同じreadwrite transaction内で対象全件を読み、`mergeStoredCodexThread()`により`codexChat`だけをDB確定値へ合わせてから一括保存するようにした。DBに`codexChat`がなければ固定snapshotの旧値を削除し、本文・題名・タグ・フラグ・collection・metadataとtransaction原子性は維持する。
* 保護対象は既定の通常atomic mutation、ローカル保存metadata、Web Clip更新、コレクション削除に限定した。可搬バックアップは現行Markdown front matterで`codexChat`を出力・読込しておらず、入力メモ全体で置換する既存custom writerをそのまま使うため、復元・importの置換契約とバックアップ形式は変更していない。batch writerが返した実保存snapshotを`enqueueBatchSave()`の`savedSnapshot`へ対応付け、`applyNoteBatchSaveSuccess()`は要求時の古いsnapshotではなくその確定結果を`notes`とbatch用live索引へ反映する。保存中の追加編集とlive draftには従来どおりbatch固有の変更だけを合流する。
* 制御PromiseでCodex先行、atomic batch先行、スレッド解除、transaction失敗、複数メモと対象外メモの独立性、意図的な全体置換を決定的に再現した。本番`updateNotesTransaction()`抽出テストを含む保存統合30件、保存基盤20件、Codex utility 29件、関連回帰227件が成功し、3ファイル79件を20回反復して全1580件成功した。`npm test`は全714件、全126 JavaScriptの`node --check`、`git diff --check`が成功した。配信識別子は`note-save-foundation.js?v=0.5.0-8`、`app.js?v=0.5.0-107`へ更新した。実ブラウザーでCodex保存とIndexedDB batchを同時実行する手動確認、Web Clip実更新、ネイティブフォルダでのローカルmetadata更新、実ZIP復元は未実施で、transactionモデルと関連回帰テストで補完した。

## 2026-08-26 通常mutation保存経路の横断監査

* リポジトリ全体から`putNote`、noteストアの`put`・`delete`、`notes`・live draftの更新、`scheduleSave`・`enqueueNoteSave`・atomic batch、Codex、タグ、フラグ、フォント、解説、Web Clipper、import・restore、ローカル保存、添付、完全削除を横断調査した。本文・題名・タグ・フラグ・メモ別フォント・解説・Codexなどの通常mutationは、単件キュー、atomic batch、またはCodex部分更新writerを介して既に共通基盤へ接続されていた。通常mutationからnoteストアを直接更新する未統合経路は見つからなかったため、本番保存処理は変更していない。
* 可搬ZIP、Markdown・local draftの既存ID取込、Web Clipper既存メモ更新は入力データや複数ストアtransactionの置換契約を持つため全体置換・特殊経路として維持した。ローカルMarkdown・ZIP出力は外部保存、添付ストア、完全削除、起動時migration、新規IDの初回保存はそれぞれ別の意味論を持つため、通常mutationへ統合していない。既存の排他、tombstone guard、Codex確定値保護はそのまま維持した。
* `note-save-app-integration.test.js`のharnessを、本番通常writerと同じ`preserveStoredCodexThread: true`で動かすよう補正した。経路監査assertへ本文、タグ、フラグ、解説保存・削除、Codex coordinatorと実メモIDロックを追加し、本文保存中にCodex更新とタグ更新を重ねても、最終IndexedDB相当値とliveメモに本文・タグ・`codexChat`がすべて残りdirtyが解除される回帰テストを追加した。
* 保存関連80件と`npm test -- --test-isolation=none`の全715件が成功した。全JavaScriptの`node --check`と`git diff --check`も成功した。アプリ本体、保存基盤、DB schema、UI、配信識別子は変更していない。

## 2026-08-26 本文入力時の派生UI更新集約

* 本文入力経路を調査し、入力ごとにMarkdownカード、関連メモ、文章統計、表編集補助、AI参照プレビューを再構築していた処理と、保存完了時のメモ一覧・検索結果更新を確認した。フォント処理は入力経路外だったため変更していない。
* 編集欄のDOM更新、編集中メモへの本文・自動タイトル反映、revision・dirty更新、draft mirror、保存状態表示、Undo、既存280ms保存予約は入力ごとに維持した。`note-save-foundation.js`と保存要求、pending、error、lastErrorの遷移は変更していない。
* 派生UIは独立した180msのtrailing debounceへ移し、連続入力中の途中描画を止めて最後の内容を1回だけ反映する。メモ一覧・検索結果、Markdownカード、関連メモ、文章統計、表編集補助、AI参照プレビューを同じflushへ集約し、語句索引もその直前に1回だけ無効化する。
* schedulerはnoteIdと要求世代を照合し、メモ切替、通常・完全削除、別ウィンドウからの完全削除、ローカル保存先変更時に旧要求を無効化する。エディタのvalue、カーソル、選択範囲、IME、pasteの既存イベント処理は変更していない。
* 連続入力の1回集約、Aの連続編集直後にBへ切り替えた場合の旧描画棄却、連続貼り付けで最後の本文が一覧・カードへ反映される回帰テストを追加した。`node --test --test-isolation=none --test-reporter=dot`の全733件、`app.js`とschedulerの`node --check`、`git diff --check`が成功した。配信識別子は`app.js?v=0.5.0-108`へ更新した。

## 2026-08-26 派生UI集約のレビュー修正

* 原因はschedulerの全体`invalidate()`が別メモの削除やローカル保存先変更でも現在メモの予約を破棄したこと、IME composition状態を追跡していなかったこと、180ms描画後に同一revisionの保存成功が`renderAll()`で一覧・関連・統計を再描画したことだった。予約取消を`cancelNote(noteId)`へ限定し、保存先変更では取消さず、切替・削除・完全削除の対象IDと予約IDが一致するときだけ破棄する判断基準へ変更した。世代と要求オブジェクトによる旧callback棄却は維持した。
* `compositionstart`から`compositionend`までタイトル・本文の要求を保持し、確定後に180ms trailing debounceを再開するようにした。確定`input`が`compositionend`の前後どちらに届いても旧timerを世代で棄却し、最終`noteId + revision`だけを1回flushする。入力ごとのメモリ反映、revision、dirty、draft mirror、保存状態、Undo、280ms保存予約は変更せず、`note-save-foundation.js`にも変更を加えていない。
* schedulerが反映済み・予約中のrevisionを記録し、保存revision以上の表示要求がある場合は、保存成功時の一覧・関連・統計と語句索引再構築だけを省略するよう`renderAll()`を分離した。保存状態・日時・メタ・discovery・リンク統計・Codex通知、他メモ保存、popout、pending同期解除は維持した。`render:false`は当該メモの旧timerを取消し、呼び出し側が同期済みのMarkdownカード・表編集補助を再描画せず、一覧・関連・統計・AI補助を同期する。
* 主な変更は`typing-derived-ui-scheduler.js`の`createTypingDerivedUiScheduler`、`app.js`の`scheduleSave`、`renderTypingDerivedUi`、`renderTypingAuxiliaryUiAfterSynchronousPreview`、`renderAll`、保存成功・切替・削除・IME経路、`typing-derived-ui-scheduler.test.js`と`note-save-app-integration.test.js`の回帰テストである。別メモのゴミ箱・完全削除・保存先変更、現在メモ削除・切替、IME保留とイベント順序差、入力ごとの保存契約、同一revisionの二重描画、追加入力、`render:false`、連続入力・貼り付けを検証した。キャッシュ識別子は`typing-derived-ui-scheduler.js?v=0.5.0-2`、`app.js?v=0.5.0-109`へ更新し、参照する既存テストも揃えた。
* `node --test --test-isolation=none --test-reporter=dot`は全748件成功し、変更した全18 JavaScriptの`node --check`と`git diff --check`も成功した。残る性能課題は、trailing flush 1回の内部では一覧・Markdown・関連・統計・表・AIを各々全体描画することと、明示的なコレクション・削除操作では従来の`renderAll()`が残ることである。差分DOM描画、Worker化、フレームワーク移行は今回の対象外とした。
* 原因はschedulerの全体`invalidate()`が別メモの削除やローカル保存先変更でも現在メモの予約を破棄したこと、IME composition状態を追跡していなかったこと、180ms描画後に同一revisionの保存成功が`renderAll()`で一覧・関連・統計を再描画したことだった。予約取消を`cancelNote(noteId)`へ限定し、保存先変更では取消さず、切替・削除・完全削除の対象IDと予約IDが一致するときだけ破棄する判断基準へ変更した。世代と要求オブジェクトによる旧callback棄却は維持した。
* schedulerが反映済み・予約中のrevisionを記録し、保存revision以上の表示要求がある場合は、保存成功時の一覧・関連・統計と語句索引再構築だけを省略するよう`renderAll()`を分離した。保存状態・日時・メタ・discovery・リンク統計・Codex通知、他メモ保存、popout、pending同期解除は維持した。`render:false`は当該メモの旧timerを取消し、呼び出し側が同期済みのMarkdownカード・表編集補助を再描画せず、一覧・関連・統計・AI補助を同期する。
* 入力停止後の一覧とMarkdownカードは引き続き全件・全本文を再構築する。巨大データ向けの差分描画やWorker化は今回の対象外とした。

## 2026-08-26 表セル補助UIと保存成功描画の追加レビュー修正

* 表セル入力は`commitTableBlockChange()`から`scheduleSave({ render: false })`を呼び、語句索引、メモ一覧・検索結果、関連メモ、文章統計、AI参照プレビューまで1文字ごとに同期更新していた。`createTypingDerivedUiScheduler()`へ`full`（カード・表を含む全派生UI）と`auxiliary`（索引・一覧・関連・統計・AIだけ）の要求種別を追加し、どちらも180ms trailing debounceで最新の`noteId + revision + type`だけを残すようにした。表・画像などカードや表を同期更新済みの経路は`scheduleAuxiliary()`を使い、通常本文入力との前後関係に応じて古い要求を相互に置換する。
* `markRendered()`はカード・表相当の`primary`と`auxiliary`の反映範囲を別々に記録し、`requiredDerivedUiAfterSave()`が保存成功時に不足する`full`または`auxiliary`だけを返す。部分描画を全反映済みと誤認せず、保存中の追加入力を古い保存成功が取り消さない。メモ切替・現在メモ削除・完全削除は従来どおり対象IDだけを取消し、別メモ削除と保存先変更は現在メモの要求を維持する。
* `tableBlockEditors`へ`compositionstart`／`compositionend`の委譲リスナーを追加した。変換中もセルの文字、`editor.value`、revision、dirty、draft mirror、保存状態、Undo、既存280ms保存予約を入力ごとに更新する一方、全メモ一覧・関連・文章統計・AI補助は保留し、確定後の最終値を1回だけ反映する。`compositionend`と最終`input`の順序差は要求世代で旧callbackを棄却する。Markdownカードの同期更新はフォーカス・選択範囲を維持する既存表編集契約として残した。
* 通常単件保存の成功描画を`renderCurrentNoteSaveSuccessUi()`へ分離し、保存状態・保存日時・dirty解除・pending同期解除・メモメタ・discovery・リンク統計・Undo・Codex通知・popoutのタイトル／メタを維持した。本文だけの保存ではコレクションツリー／件数とタグパネル／件数を再構築せず、同一revisionで反映済みの一覧・関連・統計・カード・表・AIも描き直さない。タイトル変更は`noteSaveUiChanges`で保存完了まで追跡し、`updateCollectionMemoTitle()`でコレクション内の題名と操作ラベルだけを1回更新する。タグ明示変更は既存`updateCurrentNoteTags()`、コレクション明示変更とatomic batchは既存`renderAll()`を維持した。
* 主な変更は`typing-derived-ui-scheduler.js`の要求種別・反映範囲・不足描画判定、`app.js`の`scheduleSave()`、`renderTypingDerivedUi()`、`renderTypingAuxiliaryUiAfterSynchronousPreview()`、表IME handler、`handleNoteSaveSuccess()`、`renderCurrentNoteSaveSuccessUi()`、`updateCollectionMemoTitle()`である。`note-save-foundation.js`、IndexedDB schema、保存間隔280ms、revision・dirty・pending・error・lastError・atomic batch・Codex保存・保存先generation・tombstone guardは変更していない。
* `typing-derived-ui-scheduler.test.js`と`note-save-app-integration.test.js`へ、表セル3連続入力の1回集約、入力ごとの保存契約、full／auxiliaryの相互置換、切替・別メモ削除・保存先変更、表IMEとイベント順序、カード／表の非再描画、部分反映の保存補完、本文保存時のコレクション／タグ非描画、必須保存UI、タイトル軽量更新、タグ／コレクション件数、保存中の追加入力を追加した。既存の本文IME、連続入力・貼り付け、削除、保存raceも維持した。関連72件、表・画像・保存関連（再確認113件を含む）、`node --test --test-isolation=none --test-reporter=dot`の全760件が成功した。変更JavaScript 18ファイルの`node --check`と`git diff --check`も成功した。
* 配信識別子は`typing-derived-ui-scheduler.js?v=0.5.0-3`、`app.js?v=0.5.0-110`へ更新し、`index.html`と参照テストを揃えた。手動性能確認は、通常本文の連続入力、本文IME、表セルへの日本語連続入力、セル移動しながらの入力、大きなメモの入力停止後、多数メモ環境の表入力で、途中の一覧・関連・統計・AI更新が止まり最終値だけ反映されることを確認する。残る課題は、trailing flush自体が一覧・関連・統計・AIを全件再計算すること、表IME中も既存契約上Markdownカードを同期更新すること、明示的なタグ・コレクション・atomic操作では全体描画が残ることであり、差分DOM・Worker化・大規模分割は対象外とした。

## 2026-08-26 タイピング遅延の実装前横断調査（現行main再監査）

* 現行`main`はPR #136の派生UI debounceを既に含むため、通常本文の`input`から`renderAll()`を同期実行する経路はなかった。イベント順は、`beforeinput`のUndo snapshot判定、ブラウザによる`textarea.value`更新、`input`の`resetEditorCaretIdle()`、`scheduleSave()`である。`scheduleSave()`は`applyCurrentEditorDraft()`で本文・自動タイトルをlive noteへ反映し、`markLocalMemoDirty()`から共通保存基盤の`markChanged()`でrevision・dirtyを即時更新する。その後、draft mirror、保存状態、280ms保存予約、180ms派生UI予約、Undoボタンを処理する。
* 最も有力な入力同期コストは`saveCurrentDraftMirror()`である。入力1回ごとに本文全体を含むdraftを作り、`JSON.stringify()`して同期`localStorage.setItem()`し、ログも出す。`applyCurrentEditorDraft()`でもタイトル欄が空なら`titleFromBody()`が本文を改行分割し、最初にdirtyになる入力では保存前リンク件数用に`collectLinks(activeNotes())`が全メモを走査する。`markChanged()`自体は必要なrevision更新だが、state change通知から保存状態DOMを同期更新する。本文入力ではさらに`keydown`、`beforeinput`、`input`からキャレットアイドルtimerが同一打鍵で複数回解除・再予約される。
* 180ms停止後の`renderTypingDerivedUi()`は語句索引を無効化し、メモ一覧・検索結果、Markdownカード、関連メモ、文章統計、表編集補助、AI参照表示をまとめて1回更新する。ここでは`renderList()`が全メモの検索文字列生成・ソート・DOM全置換と語句関係索引再構築を行い、`renderPreview()`が本文全体のMarkdown／表／画像／数式／コード／Mermaidを再生成し、`findRelated()`が全メモ本文のリンク抽出・tokenize・sortを行う。連続入力は末尾1回へ集約されるが、入力間隔が180msを超える場合は打鍵ごとにこの一式が起こり得る。Webフォントの適用・読込は`openNote()`、popout、フォント設定変更経路だけで、本文`input`経路にはなかった。
* 即時維持が必要なのは`textarea`の値、live noteの本文・タイトル、revision・dirty、保存状態、280ms保存予約と保存要求作成時の最新snapshotである。保存中の追加入力は新revisionとしてdirtyを保ち、280ms後の要求は共通基盤のpendingへ最新revisionで集約される。IME中も本文・revision・dirty・draft mirror・保存予約は更新し、派生UIだけを`compositionend`後まで保留する。メモ切替時は旧メモの編集値を反映して保存をflushし、旧派生要求を`noteId`単位で取消して新メモを同期描画するため、旧callbackは新メモへ作用しない。
* 次の最小修正候補は、(1) draft mirrorの同期書込みを専用trailing予約へ集約し、メモ切替・`visibilitychange(hidden)`・`pagehide`・DB versionchangeでは対象`noteId`の最新値を同期flushすること、(2) 180ms flushを本文変更とタイトル変更で分け、タイトルだけなら本文不変のMarkdownカード・表編集補助・文章統計を再構築しないこと、(3) キャレットアイドル再予約を実入力1回につき1回へ整理すること、の順である。(1)でもlive note、revision、dirty、280ms保存要求は遅延させず、保存中追加入力とIME確定順は現行の共通保存基盤・schedulerから分離する。
* テスト不足は、実`saveCurrentDraftMirror()`を使った連続入力時の`localStorage.setItem()`回数・最終本文・切替／hidden／pagehide flush、タイトル専用入力で不要な本文派生描画を呼ばないこと、実DOMまたはブラウザで多数メモ・長文・Markdown／Mermaidを含む180ms flush時間とinput handler時間を計測する性能回帰である。現行schedulerの連続入力、IME、切替、保存中追加入力、同一revisionの二重描画抑止は既存テストにあるが、保存統合harnessではdraft mirrorがnoopであり同期localStorageコストを検証していない。`node --test`は全760件成功、失敗・skip・todoはいずれも0件、総時間約1.86秒だった。今回のアプリコード・テストコード変更はなく、追記対象は`LOG.md`だけである。

## 2026-08-26 draft mirror書込みの遅延・集約

* 入力ごとに同期実行していた`saveCurrentDraftMirror()`を、draft mirror専用の`createDraftMirrorScheduler()`へ分離した。連続入力は対象`noteId`とrevisionを固定した200ms trailing debounceへ集約し、callback実行時に予約対象が現在メモであることを再確認してから、実行時点のlive noteとエディタ値で最新draftを作る。200msは入力の短い停止で復元用コピーを残しつつ、既存280ms通常保存より先に実行できる値として選んだ。timer ID、要求世代、対象`noteId`、revisionをscheduler内部で明示管理し、取消済みの旧callbackは別メモの要求を書いたり取り消したりしない。
* メモ切替では旧メモのエディタ値をlive noteへ反映した直後かつ`currentId`変更前に、`visibilitychange(hidden)`、`pagehide`、IndexedDB `versionchange`では状態遷移処理の中で、対象メモの保留timerを取消して同期flushする。同一noteId・revisionの連続したhidden／pagehideは二重書込みしない。draft mirror削除時も同じメモの保留要求を取り消し、削除後に旧callbackがmirrorを復活させない。`localStorage.setItem()`失敗は従来どおり警告だけに留め、入力・通常保存・切替を止めず、失敗したrevisionは後続flushで再試行できる。
* エディタDOM、live noteの本文・タイトル、`markChanged()`によるrevision・dirty、保存状態表示、既存280ms通常保存予約、保存要求作成時の最新snapshot、保存中追加入力の新revision、IME中の既存入力・保存契約は即時のまま維持した。`note-save-foundation.js`、IndexedDB schema、通常保存間隔、保存race・generation・tombstone guard、派生UI schedulerは変更していない。
* `draft-mirror-scheduler.test.js`へ実`JSON.stringify()`と`localStorage.setItem()`相当を使うharnessを追加し、連続入力の1回集約と最終タイトル・本文、切替前flush、hidden／pagehide、Aの旧callbackがBを書かないこと、保存中追加入力、IME相当入力、書込み失敗、flush後の旧callbackと二重ライフサイクル、別メモ要求の非取消を検証した。アプリ統合・IndexedDB lifecycle・保存race・IME・メモ切替・派生UI schedulerを含む関連86件、全770件が成功し、失敗・skip・todoは0件だった。変更JavaScriptの`node --check`と`git diff --check`も成功した。
* ブラウザでは日本語を含む長文の5回連続更新が入力直後0回・200ms停止後1回のdraft mirror書込みとなること、入力直後の再読み込みで最新日本語本文が復元されること、通常保存開始後の追加入力でも最終本文が再読み込み後に残ることを確認した。メモ一覧経由の切替でも内容を保持し、確認に使った既存の空メモは元のタイトル・空本文へ戻して再読み込み後まで確認した。ブラウザ自動操作ではOSの日本語IME変換セッションと実タブのhidden遷移を完全再現できないため、composition中の即時保存契約とhidden flushはイベントharnessで補完した。手動確認としては実IME変換中の連続入力と直後のメモ切替・タブ切替を引き続き実機で確認する余地がある。
* 配信識別子は新規`draft-mirror-scheduler.js?v=0.5.0-1`と`app.js?v=0.5.0-111`へ更新し、`index.html`とキャッシュ参照テストを揃えた。残る課題は、200ms停止ごとには本文全体の`JSON.stringify()`と同期localStorage書込みが1回残ること、localStorage自体の容量・メインスレッド制約、実データ量とOS IMEを用いた性能計測であり、通常保存や派生UIの追加最適化は今回の対象外とした。

## 2026-08-26 draft mirror未編集切替のレビュー修正

* 原因は、schedulerの`flush(noteId, revision)`が対象noteIdの`pendingRequest`有無を確認せず`invoke()`へ進み、`openNote()`もメモ切替ごとに通常flushを呼んでいたことだった。このため未編集メモでも本文全体の`JSON.stringify()`と同期`localStorage.setItem()`が走り、現在時刻の`draftSavedAt`・`updatedAt`を持つ不要な復元候補を作り得た。
* 通常`flush()`は対象noteIdと一致する保留要求だけを消費し、要求がなければtimer・世代・revision・別メモ要求へ触れず`false`を返す契約へ変更した。クラッシュ対策用には明示的な`forceFlush(noteId, revision)`を追加し、現在メモだけを書き、対象外メモの要求を取り消さず、同一noteId・revisionの連続退避を重複排除する。書込み失敗は反映済みrevisionとして記録しないため、同じrevisionを後続の強制退避で再試行できる。
* メモ切替では、切替前に保留要求の有無を固定し、`applyCurrentEditorDraft()`の変更検出を取得する。保留要求があれば最新revisionで通常flushし、保留なしでもDOMとlive noteが異なる場合だけ強制退避し、未編集かつ保留なしならdraft mirrorを書かない。その後に既存`flushScheduledNoteSave(previousId)`を呼び、最後に`currentId`を切替先へ変更するため、旧callbackは切替先の内容を書かない。
* `visibilitychange(hidden)`、`pagehide`、IndexedDB `versionchange`は、最新DOMを`applyCurrentEditorDraft()`で反映してから`forceFlushDraftMirror()`を呼び、その後に通常保存timerの取消・IndexedDB保存または接続終了へ進む。hidden直後のpagehideは同じrevisionとして1回だけ書く。通常280ms保存、revision・dirty・`markChanged()`、保存中追加入力、IME順序、`note-save-foundation.js`、schema、generation、tombstone guard、派生UI、Codex・ローカル保存は変更していない。
* `draft-mirror-scheduler.test.js`へ未編集通常flush 0回、未編集切替0回、予約あり切替前1回、未予約DOM変更の強制退避、保留なしhidden強制退避、hidden＋pagehide重複排除、B保留中のA通常／強制flush非干渉、旧callback無効化、失敗後同revision再試行を追加した。`note-save-app-integration.test.js`の実`openNote()` harnessへ実schedulerとdraft書込み記録を接続し、書込み回数・最終内容・切替前の`currentId`を検証した。IndexedDB lifecycleの変更反映・強制退避・timer取消・接続終了順も更新した。
* draft mirror・メモ切替・IME・IndexedDB lifecycle・保存raceの関連114件、全777件が成功し、fail・cancelled・skip・todoはすべて0件だった。変更JavaScript 19ファイルの`node --check`と`git diff --check`も成功した。配信識別子は`draft-mirror-scheduler.js?v=0.5.0-2`、`app.js?v=0.5.0-112`へ更新した。残る制約は、編集がある場合の200ms停止後または強制退避では本文全体の同期localStorage書込みが1回必要なことと、OS実IME・実タブhiddenを含む実データ性能確認が手動確認領域として残ることである。

## 2026-08-26 タイピング時UI分離の現行main再確認

* `input`、`beforeinput`、composition、表セル編集、保存成功、メモ切替の各経路を再調査した。通常本文入力は`applyCurrentEditorDraft()`でlive note・revision・dirtyを即時更新し、draft mirrorは200ms、通常保存は既存の280ms予約を維持する。メモ一覧／検索結果、Markdownカード／プレビュー、関連メモ、文章統計、表編集補助、AI参照表示は`createTypingDerivedUiScheduler()`の180ms trailing debounceで最新`noteId + revision`の1回へ集約済みで、タグ一覧は本文入力経路に含まれない。
* メモ切替では旧メモの保留draftを切替前にflushし、通常保存を旧`noteId`へ固定したうえで、旧メモの派生UI予約だけを`cancelNote(previousId)`で破棄する。遅延callbackも要求世代、現在の`noteId`、revisionを再確認するため、旧メモの更新が切替先を描き替えない。保存成功では同一revisionの重い派生UIを再描画せず、保存状態・メタ・discovery・リンク統計等だけを更新し、タイトル変更はコレクション内の表示名と操作ラベルだけを軽量更新する。
* 保存共通基盤、revision・dirty、280ms保存予約、保存競合・tombstone・Codex・ローカル保存・添付表示は変更していない。現行コードと既存テストが依頼内容を満たすため、アプリコードとテストコードの追加変更は行わなかった。
* `node --test --test-isolation=none typing-derived-ui-scheduler.test.js draft-mirror-scheduler.test.js note-save-foundation.test.js note-save-app-integration.test.js note-tombstone.test.js`は関連125件すべて成功した。`node --test --test-isolation=none --test-reporter=dot`は全777件成功（終了コード0）だった。

## 2026-08-26 本文派生解析の入力経路分離

* 本文`input`から`applyCurrentEditorDraft()`へ入る同期経路を再調査し、最初のdirty化時に`collectLinks(activeNotes())`が全メモ本文を走査していた処理を除去した。入力時は変更前本文の文字列参照だけを`noteSaveBeforeBodies`へ保持し、保存成功時に変更前本文と保存snapshotの`[[語句]]`数だけを比較して既存の「新発見」表示を維持する。通常入力直後のMarkdown／リンク解析は0回となり、全メモ走査も不要になった。
* 表セル、表挿入／削除、画像ブロック、表貼り付けで本文更新直後に同期実行していた`renderPreview()`を、既存180ms trailing debounceの`auxiliary`要求へ移した。構造化編集DOM、本文、カーソル／フォーカス、revision・dirty、Undo snapshot、draft mirror予約、通常保存予約は即時のまま、Markdownカード、メモ一覧／検索用表示・抜粋、語句索引、関連メモ、文章統計、AI参照表示だけを最新`noteId + revision`の1回へ集約する。Undoの同期`renderList()`も同じ予約へ統合した。180msは既存値を`TYPING_DERIVED_UI_DEBOUNCE_MS`として定数化し、200ms draft mirrorと280ms通常保存より先に表示を揃える関係を明記した。
* schedulerへ`cancelAll()`を追加し、IndexedDB versionchangeと`pagehide`でtimer・要求世代・IME composition保留を破棄する。メモ切替・削除は従来の`cancelNote(noteId)`、callback実行時の現在noteId・revision・要求世代検証を維持し、古い本文や別メモの派生表示を反映しない。保存共通基盤、保存間隔、保存snapshot、競合・tombstone、Codex、ローカル保存、タグ／検索／関連判定ロジック自体は変更していない。
* 主な変更は`app.js`、`typing-derived-ui-scheduler.js`、`typing-derived-ui-scheduler.test.js`、`note-save-app-integration.test.js`、`image-block-layout.test.js`、`index.html`で、配信識別子を`typing-derived-ui-scheduler.js?v=0.5.0-4`、`app.js?v=0.5.0-113`へ更新し、参照する既存テストを揃えた。関連テスト`node --test --test-isolation=none typing-derived-ui-scheduler.test.js note-save-app-integration.test.js note-save-foundation.test.js term-link-utils.test.js memo-list-utils.test.js draft-mirror-scheduler.test.js note-tombstone.test.js`は150件成功した。`node --test --test-isolation=none --test-reporter=dot`は全779件成功（終了コード0）、`node --check app.js`、`node --check typing-derived-ui-scheduler.js`、`git diff --check`も成功した。
## 2026-08-27 タイピング性能の開発用計測基盤

* 体感遅延を推測で最適化せず、入力時の同期処理と180ms停止後の派生UI描画を実測して比較するため、独立した`typing-performance-monitor.js`を追加した。通常は完全に無効で、URLへ`?debugTypingPerf=1`（既存クエリがある場合は`&debugTypingPerf=1`）を明示した時だけ有効になる。URLは書き換えず、結果はlocalStorage・IndexedDBへ保存せず、入力ごとのconsole出力も行わない。最新200件だけをリングバッファへ保持する。
* 開発者コンソールでは`MemoNexusTypingPerformance.isEnabled()`で有効状態、`getEvents()`で記録、`getSummary()`で処理名ごとの`count`・`average`・`p50`・`p95`・`max`、`clear()`で消去を確認できる。記録属性は本文文字数、タイトル文字数、未削除メモ件数、入力種別、IME composition状態、現在メモか、full／auxiliary種別に限定した。本文、タイトル、タグ名、メモID、任意文字列は許可属性と処理名の境界で除外する。
* 同期経路は本文・タイトル・表セルを区別し、Undo snapshot、`applyCurrentEditorDraft()`、`scheduleDraftMirror()`、保存状態更新、280ms通常保存timer再予約、full／auxiliary scheduler要求、`updateUndoButton()`、各input handler全体を記録する。遅延描画はfull／auxiliaryを区別し、`renderMemoListPanel()`、`renderPreview()`、`renderRelated()`、`renderTextStats()`、full時の`renderTableBlockEditors()`、`updateAiTargetPreview()`、派生描画全体を個別記録する。
* 計測は既存呼び出しの直前直後だけに置き、処理順を変更していない。180ms派生UI debounce、200ms draft mirror、280ms通常保存、最新`noteId + revision`だけを描画する世代管理、IME中の描画保留、切替・削除・pagehide・versionchangeのcancel、保存共通基盤のrevision・dirty・snapshot・成功判定、Undo、自動タイトル候補、full／auxiliaryの描画順と通常UIを維持した。`typing-derived-ui-scheduler.js`、保存処理、Markdown変換、一覧描画ロジック自体は変更していない。
* 自動テストは計測モジュール9件、派生UI scheduler 28件、保存競合統合48件が成功した。`node --test --test-isolation=none --test-reporter=tap`は全788件成功し、fail・cancelled・skipped・todoは0件だった。固定ミリ秒閾値は設けず、端末ごとにp95と最大値を処理間で比較できることを検証対象とした。配信識別子は新規`typing-performance-monitor.js?v=0.5.0-1`と`app.js?v=0.5.0-114`へ更新した。
* 実機確認は、計測記録を`clear()`してから、短い本文の連続入力、長文の連続入力、タイトル欄を空にした本文入力、タイトル入力、日本語IMEの変換中と確定後、表セル入力、メモが少ない状態と多い状態、メモ切替直後の入力を順に行う。各シナリオ後に`getSummary()`で同期input全体と各内訳、full／auxiliary描画のp95・最大値を比較し、どの処理が支配的かを見る。通常URLでも起動し、`isEnabled()`が`false`、`getEvents()`が空で、UI・保存内容・保存順・描画順・URL・コンソール出力が従来どおりであることを確認する。OS実IME、実データ量、実メモ切替を伴うこれらの確認は手動実機項目として残る。

## 2026-08-27 PR #146 レビュー指摘対応：計測をサンプル単位へ変更

* 200件のリングバッファが「個別処理200件」ではなく「入力または派生描画200回」を表すよう、本文・タイトル・表セルの各inputを1同期サンプル、full・auxiliaryの各flushを1派生サンプルとして記録する形へ変更した。各サンプルの`durations`へ処理別時間をまとめ、`getSummary()`は従来の`body.sync.applyCurrentEditorDraft`等の処理名へ展開して`count`・`average`・`p50`・`p95`・`max`を集計する。`getSamples()`を主APIとして追加し、`getEvents()`は防御的コピーを返す互換エイリアスとして維持した。
* `beforeinput`で取得するUndo snapshot時間を、同じメモ・revision・入力種別・inputType・IME状態の`input`サンプルへ結合した。対応するinputが来ないpendingは次タスクで失効し、新しいbeforeinput、条件不一致、メモ切替・削除、pagehide、versionchange、`clear()`でも破棄する。派生描画も内部だけで保持するメモIDとrevisionが完全一致する場合に限って記録し、公開サンプルへIDを出さない。
* 属性は1サンプルにつき最大1回、`totalDuration`を確定した後に生成する。本文文字数、タイトル文字数、未削除メモ件数、現在メモかの真偽だけを属性として許可し、入力種別・IME状態・full／auxiliaryは固定列挙値だけを公開する。本文・タイトル・タグ名・メモID・任意文字列・未知の処理名は記録しない。auxiliaryには`renderTableBlockEditors`時間を含めず、取得結果は入れ子を含むコピーとして返す。通常起動では時計取得、属性生成、timer・配列・collector・リングバッファ操作を行わない。
* 有効化は従来どおりURLの`?debugTypingPerf=1`だけで、`MemoNexusTypingPerformance.isEnabled()`、`getSamples()`、`getSummary()`、`clear()`をコンソールから使用する。localStorage・IndexedDBへの計測保存、URL変更、入力ごとのconsole出力は追加していない。180ms派生UI、200ms draft mirror、280ms通常保存、保存のrevision・dirty・snapshot・成功判定、描画順、IME保留、Undo、空タイトル候補、schedulerの`noteId + revision`競合対策とcancel責務は変更していない。
* 計測モジュール22件と派生UI・draft mirror・保存競合・tombstoneを含む関連149件が成功し、全テストは801件すべて成功した。固定ミリ秒閾値は設けていない。配信識別子は`typing-performance-monitor.js?v=0.5.0-2`と`app.js?v=0.5.0-115`へ更新し、無関係な資産URLは変更していない。
* 実機では短文・長文の連続入力、空タイトルでの本文入力、タイトル入力、日本語IMEの変換中と確定後、表セル入力、メモ少数・多数、メモ切替直後を確認する。通常URLで無効かつUI・保存・描画・URL・consoleが不変であることも確認する。数値の絶対値ではなく、同期sampleとfull／auxiliary sampleの各処理でp95・最大値を占める箇所を比較し、Undo、draft mirror予約、一覧、Markdown preview、関連メモ、文章統計、表エディタ、AI参照表示のどれを次の最適化候補にするか判断する。OS実IMEと実データ量を用いた確認は未実施である。

## 2026-08-27 PR #146 残存レビュー指摘対応：同期totalDurationの統一

* 本文・タイトルは`beforeinput`の`captureUndoSnapshot()`時間をcollectorの`durations`へ記録する一方、公開サンプルの`totalDuration`には後続`input`ハンドラーの開始から終了までしか渡していなかった。このためUndo内訳を含む内訳合計がtotalを上回り、表セルとtotalの意味が異なっていた。表セルは`handleTableEditorInput()`の計測開始後に`commitTableBlockChange()`からUndoを取得するため、従来からinput区間内にUndo時間が1回含まれていた。
* collectorへ非公開の`beforeInputDuration`を追加し、実際の`beginInput()`由来でまだpending中のtokenへ`captureUndoSnapshot`時間を記録した場合だけ保持する。`completeInput()`では、呼び出し側が実測したinput区間へこのbeforeinput実処理区間を加算してから属性生成とサンプル保存へ進む。beforeinput開始時刻は保持せず、イベント間のブラウザ編集や待ち時間はtotalへ含めない。表セルは`consumeInput()`が直接作るpendingでないcollectorなので追加時間は0となり、外側input区間に既に含まれるUndoを二重加算しない。
* pendingが次タスクで失効した場合、新しいbeforeinput、note・revision・inputKind・inputType・IME状態の不一致、メモ切替・削除の`discardNote()`、pagehide・IndexedDB versionchangeの`discardTransient()`、`clear()`ではcollectorごと非公開時間を破棄する。古い情報を新しい入力へ流用せず、公開サンプルへnoteId、revision、token、開始時刻、非公開durationを追加していない。
* `totalDuration`は実測した2区間の合計であり、`durations`の単純合計にはしていない。このため`resetEditorCaretIdle()`など個別名のないinput同期処理も引き続き含む。属性生成、正規化、freeze、防御的コピー、リングバッファ追加はtotal確定後のままで、`typingPerformanceAttributes()`は1サンプル最大1回、通常URLは時計・timer・collector・配列・属性・サンプル・バッファ操作なしを維持した。
* 制御可能な時計でbeforeinput 1.5ms、イベント間待機100ms、input 4.0msからtotal 5.5msになることを追加した。本文・タイトル・表セルのtotalを同じ6.0msへ揃え、表セルのUndo内訳1.5msを維持しつつ7.5msへ二重加算しないこと、内訳合計以上かつ名前のない同期時間を含むこと、Undo間引き時は実消費0.2msだけを加えることを検証した。pending失効、新beforeinput、全照合条件不一致、`discardNote()`、`discardTransient()`、`clear()`で旧時間を流用しないこと、属性・privacy・防御的コピー・リングバッファ・派生描画の既存契約も確認した。
* 保存、Undo配線、本文・タイトル、表編集、IME、削除、メモ切替、full／auxiliary schedulerを含む関連テストは235件成功した。`node --test --test-isolation=none`の全64ファイル・806件はpass 806、fail・cancelled・skipped・todo 0だった。変更した`typing-performance-monitor.js`と同テストの`node --check`、`git diff --check`も成功した。配信識別子は`typing-performance-monitor.js?v=0.5.0-3`へ更新し、保存・Undo・描画の処理順、180ms／200ms／280msの予約、サンプル構造と最新200件、集計API、privacy境界は変更していない。
* 残る手動確認は、`?debugTypingPerf=1`を付けた実ブラウザで短文・長文、本文・タイトル・表セル、日本語OS IME、Undo間引き、メモ切替を操作し、同期サンプルのp50・p95・最大値と内訳を実端末で比較することである。通常URLで計測が無効かつUI・保存内容・URL・console出力が不変であることも実ブラウザでは未確認である。

## 2026-08-27 タグの視認性改善とメモ一覧検索連携

* タグ定義・本文タイトル下・メモ一覧・タグタブ・キーワード検索・コレクション併用・語句リンクの既存経路を確認した。タグ検索は既存の単一状態`selectedTagFilter`を維持し、本文・一覧・タグタブ・メモ一覧プルダウンから共通`applyTagFilter()`だけを更新する構成へ集約した。タグ操作は一覧再描画だけを行い、メモを開く・作成する・編集する処理へ接続していない。
* 本文タイトル下とメモ一覧は共通`createTagChip()`と`.tag-chip`を使う。チップは淡色背景・枠線・左アクセント・右上の折れ角を持つ付箋形状とし、表示名、キーボード操作、`aria-label`、`aria-pressed`を備える。タグ定義に有効な`color`があればCSS変数へ使い、未設定・不正・取得不能時は`#6f8372`へ戻す。DB・保存・バックアップ形式は変更していない。
* メモ一覧はタイトル直下、本文要約より前へタグを最大3件表示し、残りを操作を持たない`+N`で示す。タグなしメモには領域を作らない。メモ一覧の検索欄下へ単一選択のタグプルダウンを追加し、`すべて`または`×`で解除できる。選択中タグは検索UIと全タグチップへ同期し、既存キーワード検索・コレクション条件・作成日順をそのまま組み合わせる。
* タイトル下の登録済みタグ候補は、タグ名の前へCSS描画の色丸を追加した。既存の候補絞り込み、クリック選択、フォーム送信、新規タグ作成、タグ解除は維持した。`.wiki-link`、`.term-chip`、`renderWikiButton()`、`renderPreview()`、`openOrCreateLinkedNote()`は変更していない。
* `tags.test.js`と`memo-list-utils.test.js`へ、色フォールバック、タグなし、先頭3件と`+N`、本文・一覧の共通検索経路、解除、アクティブ状態、付箋CSS、語句リンク非変更を追加した。配信識別子更新に伴う既存キャッシュ契約テストも更新した。`node --test --test-isolation=none`は全64ファイル・811件がpassし、fail・cancelled・skipped・todoは0件だった。`node --check app.js`、`node --check tags.js`、`git diff --check`も成功した。`package.json`にlintスクリプトはない。実ブラウザでの視覚・操作確認は未実施である。
* 配信識別子は`style.css?v=0.5.0-58`、`tags.js?v=0.5.0-3`、`app.js?v=0.5.0-116`へ更新した。今回は単一タグ検索のみで、複数タグのAND／OR検索、色編集UI、タグ色の新規永続化は追加していない。

## 2026-08-27 PR #148 タグ色の永続化・付箋形状改善

* 原因は、描画がタグ定義の`color`を参照する一方で、タグ定義の正規化、既存タグ補完、新規作成、色編集の保存経路が`color`を生成・保持していなかったことだった。9色の固定パレットを`tags.js`へ定義し、保存値を小文字の6桁16進色へ統一した。パレット外、空、不正な値はFNV-1aによるタグIDの決定的ハッシュから自動色を選び、配列順、表示順、端末、再読み込みに依存しない。既存のパレット色は維持する。
* `normalizeTagDefinitions()`で全タグへ安全な色を補完し、起動時の`synchronizeRegisteredTagsForNotes()`はIndexedDBの生データと正規化結果の色だけを比較する。欠損・不正・未正規化色だけを`tags`ストアへ書き戻し、2回目以降は書き込まないため`updatedAt`を不要に更新しない。タグストアは`keyPath: "id"`のオブジェクトストアなのでDB_VERSIONは6のまま変更していない。タグ変更は従来の`putTagDefinitions()`を通り、ローカル保存のpending状態へ反映する。
* タグ作成ダイアログへ9色のパレットを追加し、タグ名由来の初期色、クリック、Tab、矢印キー、Enter／Space、`role="radio"`、`aria-checked`、枠線とチェックで選択できるようにした。`createTagDefinition()`は従来の第3引数`now`を維持し、第4引数で選択色を受け取る。重複名では既存定義を返し、既存色を変更しない。タグタブでは検索ボタンと独立した42pxの「色」ボタンから編集ダイアログを開き、実変更時だけ`color`と`updatedAt`を更新する。IndexedDB保存成功後にだけ`registeredTags`と本文、一覧、タグタブ、候補を再描画し、失敗時はメモリ表示を変更しない。メモ自体は保存し直さない。
* `mergeTagDefinitions()`は正規化前に色が有効だったかを非列挙情報で保持し、新しい色なし旧バックアップが既存の有効色を上書きしないようにした。完全バックアップZIPとローカル`tags.json`は既存の正規化・マージ経路で`color`を往復し、色なし旧形式と不正色混在データは自動色へ補完する。バックアップ形式version 2は変更していない。
* `.tag-chip`は淡色面、4pxの左アクセント、12pxの右上折り返し、角の少ない輪郭へ変更した。`clip-path`は折り返し疑似要素だけへ使い、ボタン本体のフォーカスリングや文字を切らない。`color-mix()`の前に基本色・枠線・折り返し色のフォールバックを置き、選択中も`--tag-color`を左アクセントと折り返しへ残す。長い名前は折り返し可能とし、一覧小型タグは狭幅確認後に高さ32pxへ調整した。語句リンク、メモリンク、`applyTagFilter()`、単一タグ検索、メモ保存仕様は変更していない。
* タグ・保存・バックアップ・一覧の関連テストは114件成功した。`node --test --test-isolation=none`は全819件がpassし、fail・cancelled・skipped・todoは0件だった。`node --check tags.js`、`node --check app.js`、`git diff --check`も成功した。`package.json`にlintスクリプトはない。配信識別子は`style.css?v=0.5.0-59`、`tags.js?v=0.5.0-4`、`app.js?v=0.5.0-117`へ更新し、依存する既存テストを揃えた。
* Windows Chromeで、ライト／ダーク、本文タイトル下、メモ一覧、タグタブ、候補の色丸、新規タグのキーボード色選択、既存色変更、本文／一覧タグ検索、検索解除、キーワード併用、再読み込み後の色維持、390×844の狭幅を確認した。本文・一覧・タグタブ・候補は同じ保存色を表示し、ブラウザconsoleのwarning／errorは0件だった。iPhone Safari実機、完全バックアップZIPの画面操作、ローカルフォルダへの実ファイル保存は未確認で、自動テストによる往復確認に留まる。複数タグAND／OR検索は今回も対象外である。

## 2026-08-27: 語句リンクとメモリンクを分離

* 既存の`[[語句]]`を登録語句・語句チップ・自動語句検出・語句統計のための語句リンクとして維持し、既存メモそのものを参照する正式記法`[[* メモ名]]`を追加した。`[[`直後の半角`*`と、その直後の1文字以上の半角スペース、空でないタイトルを必須とする。不正式な`*`／`＊`予約記法はメモリンクにも登録語句にもせず、通常文字として安全に表示する。
* 新規`memo-link-utils.js`へ、fenced code・Mermaid・インラインコードを除外する`parseMemoLinks()`、タイトル完全一致の`resolveMemoLinkTitle()`、導出索引の`buildMemoLinkRelationIndex()`、`createMemoLinkRelationCache()`を分離した。resolverは削除済みメモを除外し、`resolved`、`missing`、`ambiguous`を区別する。曖昧な場合は候補IDを返すだけで、先頭候補を選ばない。
* `term-link-utils.js`の`extractExplicitTerms()`はメモリンク予約記法を除外する。`collectLinkStats()`と新発見の増加判定も`extractExplicitTerms()`だけを使用し、メモリンクタイトルを登録語句、語句チップ、自動検出、統計、新発見へ混ぜない。
* `app.js`のインラインtokenを`term-link`、`memo-link`、`memo-link-invalid`へ分離し、メモリンク専用の`renderMemoLinkButton()`と`openResolvedMemoLink()`を追加した。resolvedは描画時に確定したnote IDで`openNote()`を呼び、missing／ambiguousは専用class・title・aria-labelで案内して、新規メモ作成や自動遷移を行わない。語句リンクの既存の開く／作る挙動は維持した。
* バックリンクはIndexedDBへ二重保存せず、有効なメモ本文から`sourceNoteId`、`targetTitle`、`targetNoteId`、`resolutionStatus`を導出する。同じ参照元から同じ対象への重複を整理し、resolvedだけを「このメモへのリンク」へ登録する。関連メモドロワーに独立欄を追加し、通常関連側との重複を除外したうえで、通常関連の最大8件仕様を維持した。
* 関連メモの本文リンク・逆リンク・共通リンクは、一意解決済みメモIDだけを使うように変更した。共通語句の既存スコアは維持した。グラフ辺は正式なメモリンクだけから作り、missingは未作成ノード、ambiguousは誤接続防止のため除外する。語句リンクはメモ間の辺にしない。
* メモ一覧snippetと本文由来の自動タイトルから正式記法の`[[ ]]`と`*`を除去した。記法ガイド、placeholder、READMEを語句リンクとメモリンクの別説明へ更新した。リンク先タイトル変更時に参照元本文を自動書き換えないため、旧タイトルのリンクはmissingになり得る制限も明記した。
* メモリンク索引は語句索引と同じ無効化境界へ接続し、本文・タイトル・作成・削除・復元・import・同期・atomic batchで必要時だけ無効化する。入力イベント内で全メモ解析せず、既存`typing-derived-ui-scheduler.js`のtrailing debounce後に一度構築し、プレビュー・関連メモ・グラフで共有する。同一note ID・revisionを描画済みなら保存成功時に再描画しない既存契約を維持した。
* `note-save-foundation.js`、IndexedDB schema、revision、dirty／pending／error、280ms保存、draft mirror、tombstone、atomic batch、Codexスレッド保存は変更していない。リンク情報は保存せず本文から再導出する。
* `memo-link-utils.test.js`と`memo-link-app.test.js`を追加し、構文、コード除外、解決3状態、重複、バックリンク、表示、クリック、グラフ、キャッシュ、scheduler接続を検証した。語句統計、関連ドロワー、記法ガイド、配信順、保存raceの既存テストも更新した。`npm test`は835件すべて成功し、fail・cancelled・skipped・todoは0件だった。変更JavaScript 26ファイルの`node --check`と`git diff --check`も成功した。
* 将来`[[* メモ名#位置]]`へ拡張する場合は、`parseMemoLinks()`が現在保持しているタイトル文字列をresolverへ渡す前に、メモ部分と位置指定部分へ分離する。初期実装では`メモ名#位置`全体をタイトルとして扱い、位置ジャンプは実装していない。
* ブラウザでの手動確認は未実施。語句リンク、メモリンク、missing／ambiguous、バックリンク、関連重複排除、コード領域、snippet、自動タイトル、グラフ、連続入力は自動テストとソース検査で確認した。

## 2026-08-27 PR #150 レビュー指摘対応：メモリンクの改名追従と索引線形化

* リンク先メモのタイトル変更前に、正式な`[[* 旧タイトル]]`が一意解決していた対象note IDを`buildMemoLinkRelationIndex()`から固定し、その関係だけを改名対象にした。語句リンク、missing、ambiguous、不正式記法、インラインコード、fenced code、Mermaid内は更新しない。タイトル部分の生文字列位置だけを後方から置換するため、記法内の前後空白、本文のCRLF、同一本文内の重複リンクを維持する。自己リンクと連続改名にも追従し、新タイトルが別メモと重複した後は通常どおりambiguousになる。
* `parseMemoLinks()`は改行を正規化したコピーではなく原文上のoffsetを返すようにし、`rewriteResolvedMemoLinks()`を追加した。`buildMemoLinkTitleIndex()`が未削除メモを一度だけ走査して`Map<title, noteId[]>`を構築し、全リンク解決で再利用する。これにより従来のリンクごとの全メモ`filter()`を除去し、索引構築を概ねO(N + L)へ変更した。公開`resolveMemoLinkTitle()`の単独利用契約は維持した。
* タイトルと全参照元本文は、既存の`mutateNotesAtomically()`、note IDロック、batch保存、revision、dirty／pending／error経路を使い、1回のIndexedDB transactionへまとめた。ロック取得後と`put()`直前に対象revisionを再検証し、待機中の追加入力やCodex thread mergeとの競合があれば全件を中止する。writer失敗でもDBへ部分反映せず、改名メモをdirty／errorのまま保持して既存再試行経路へ残す。`note-save-foundation.js`とIndexedDB schemaは変更していない。
* batch成功時は、表示中メモが同じrevisionでcleanな場合だけタイトル・本文・draft mirror・previewを保存結果へ同期する。別ウィンドウからタイトル変更・作成・削除を受信して解決状態が変わり得る場合は索引無効化後にpreviewを再描画し、受信側が編集中なら既存pending判定を維持して本文を上書きしない。resolvedリンクのクリック時にも現在のタイトル解決を再確認し、古いDOMに残る別note IDへは遷移しない。
* `memo-link-utils.test.js`へ空白・CRLF・除外領域・missing／ambiguous・自己リンク・連続改名・新タイトル重複・1,001件の索引構築回数を追加した。`note-save-app-integration.test.js`へ複数参照元・重複リンクを含むatomic保存、batch失敗時の無部分保存、ロック待機中revision変更の中止を追加し、`memo-link-app.test.js`でeditor／preview同期とstaleクリック防止を確認した。`npm test`は全844件がpassし、fail・cancelled・skipped・todoは0件だった。`node --check app.js`、`node --check memo-link-utils.js`、`git diff --check`も成功した。
* READMEと記法ガイドを改名追従仕様へ更新し、配信識別子を`memo-link-utils.js?v=0.5.0-2`、`app.js?v=0.5.0-119`へ更新した。実ブラウザでの手動確認は未実施で、Windows Chrome等を使った複数ウィンドウ操作と視覚確認は残る。

## 2026-08-27 PR #150 残存レビュー指摘対応：改名batchの保存競合と別画面draft保護

* 改名batchの直前に対象メモをIndexedDBから一括取得し、実transaction内で再取得した値とnote ID・revision・updatedAt・タイトル・本文・その他の永続化フィールドを比較するようにした。同一revisionでも内容が変わった場合は専用`MEMO_LINK_RENAME_STORED_CONFLICT`と対象note IDを返し、全put前にtransactionを中止する。Codex threadだけの更新は競合にせず、transaction内の最新保存値を基礎に改名側が担当するタイトル・正式メモリンク本文・時刻・revisionだけを重ねるため、タグ、コレクション、フラグ、添付、source、ローカル保存メタデータ等を保持する。
* batchのローカルrevision検証と保存snapshotの基準revisionを分離した。保存済み参照元は最新DB本文から開始し、編集中参照元はlive draftを維持する。batch成功時にcleanな現在メモは保存結果を`notes`、current、request、保存共通基盤のcurrent／lastSaved、editorへ統一し、古いlive draftを残さない。batch中に追加入力された場合はdirtyを保ち、正式メモリンク部分だけを新タイトルへmergeして次の通常保存へ渡す。
* BroadcastChannelへ一意なrename ID、旧名・新名、対象note ID、一意解決済み参照元ID、保存revisionを含む`memo-link-renamed`通知を追加した。別画面がdirty／savingなら通常文を上書きせず、正式な`[[* 旧タイトル]]`だけをoffsetベースで置換し、既存のdirty化・revision・280ms保存予約へ接続する。語句リンク、不正式記法、inline code、fenced code、Mermaidは変更しない。同じ通知の重複受信は無視し、日本語IME composition中は確定後まで適用を保留する。
* 保存順序、同一revision内容競合、Codex-only merge、各永続化フィールド保持、dirty受信、通知重複、IME、batch成功後のrevision統一、改名直後の1文字保存、batch中追加入力、実transactionの無部分保存を決定的テストへ追加した。`npm test`は全852件がpassし、fail・cancelled・skipped・todoは0件だった。配信識別子は`memo-link-utils.js?v=0.5.0-3`、`app.js?v=0.5.0-120`へ更新した。`note-save-foundation.js`とIndexedDB schemaは変更していない。
* Windows Chromeの2タブで、参照元を別タブから先に編集・保存した後に対象タイトルを変更し、通常文「外部編集」を保持したまま正式メモリンクだけが新タイトルへ変わることを確認した。さらに改名直後に対象本文へ1文字追記し、再読み込み後にも残ることを確認した。両タブのconsole warning／errorは0件だった。

## 2026-08-27 PR #150 遅延通常保存による改名巻き戻しの修正

* 残っていた競合は、参照元または改名対象自身の古い通常保存snapshotが開始した後に別タブの改名batchが先にcommitし、その古い通常保存が後からメモ全体を`put()`して旧リンクまたは旧タイトルへ戻したうえで、受信側がcleanになった後の`memo-link-renamed`通知を従来の`hasLiveWork === false`判定で捨てる順序だった。
* rename IDごとに対象note ID一覧と未完了note IDを持つ改名intentを追加し、同じnote IDのintentは通知順に直列化した。dirty／savingでは利用者のlive draftへ正式リンクと、旧タイトルに完全一致する対象タイトルだけを合流して既存保存キューへ渡す。通常保存が進行中なら既存note lockと`whenIdle()`で保存完了まで待ち、その後に必ずIndexedDBを再検証する。全noteの確認または修復が終わるまでrename ID全体を処理済みにしない。IME中は本文・タイトルとも確定後まで保留する。
* clean修復は専用の単一メモreadwrite transactionで最新レコードを`get()`し、そのレコードを基礎に正式な`[[* 旧タイトル]]`と、対象note自身のタイトルが旧タイトルと一致する場合だけ新タイトルへ変更する。通常文章とtags、collectionId、isFlagged、attachments、source、codexChat、localCreatedAt、localSavedAt、任意の追加メタデータは最新レコードから保持する。本文・タイトルがすでに整合済みなら書き込まず、通知revisionより保存revisionが低い場合だけ単調性を回復する。同じrename IDの重複通知は書込みとrevisionを増やさない。利用者が別タイトルへ変更済みなら上書きしない。
* clean修復後は保存snapshotを`notes`、現在画面、保存共通基盤のcurrentRevision／lastSavedRevisionへ同期する。修復transaction中に追加入力が発生した場合はlive draftを置換せず、修復済みrevisionより大きいrevisionへ進めて後続通常保存を完了した後、再びIndexedDBを確認する。`note-save-foundation.js`とIndexedDB schemaは変更していない。配信識別子は`app.js?v=0.5.0-121`へ更新した。
* deferred writerで、遅延通常保存→改名batch→遅延commit→clean通知、saving中通知、通常保存先行、対象メモ自身の遅延保存、対象外フィールド保持、明示的な別タイトル保持、重複通知、本文・タイトルIME、複数note完了管理、B→C→Dを決定的に検証した。`npm test`は全859件がpassし、fail・cancelled・skipped・todoは0件だった。変更JavaScriptの`node --check`と`git diff --check`も成功した。Windows Chromeの2タブでは参照元編集と対象改名をほぼ同時に開始し、両方の保存完了後と再読み込み後に通常文章、`[[* 遅延メモC]]`、対象タイトル「遅延メモC」が残ることを確認し、console warning／errorは0件だった。ブラウザ操作では内部transactionのcommit順を固定できないため、厳密な遅延順序は決定的自動テストで確認した。

## 2026-08-30 Memo Nexus 結節点ロゴと起動演出

* 左上タイトルを、淡く発光する中央核、周囲4ノード、2本の曲線で構成する小型SVGの結節点ロゴへ変更した。既存テーマ変数を基礎に落ち着いた青緑と薄紫を使い、通常時は文字を静止させ、核の弱い呼吸とノードの微かな漂いだけを継続する。狭幅ではロゴを156px、結節点を28pxへ縮小する。
* 演出は固定の約1.5秒へ統一し、0〜0.4秒で核とノード、0.42秒以降に左から一文字ずつの`Memo Nexus`、1.19秒以降に一度だけの細いカーソルと接続線グローを再生する。旧来の日替わり・タイプライター・Nexus接続・光の走査の設定と循環は廃止し、通常画面の起動中一度とロゴbuttonのクリック時だけ再生する。
* 再生制御を`logo-animation-utils.js`の専用controllerへまとめ、再実行時は古いRAFをrequest IDで無効化し、残存タイマーを解除して先頭から再開する。`prefers-reduced-motion: reduce`では再生クラスとタイマーを作らず、文字を一括表示し、カーソル・呼吸・漂いを静止する。buttonの標準キーボード操作、focus表示、控えめなhoverを維持し、保存・AI・メモ・画面遷移の処理には接続していない。
* ロゴの状態テストとDOM・CSS契約テストを更新し、初回一度、クリック再生、再生中再スタート、reduced motion、1.5秒の時間配分、通常状態、モバイル縮小を検証した。`node --test`は全858件がpassし、fail・cancelled・skipped・todoは0件だった。`node --check app.js`、`node --check logo-animation-utils.js`、`git diff --check`も成功した。自動ブラウザ確認はローカル環境に`agent-browser`コマンドがないため未実施とし、実ブラウザでの視覚確認を手動確認対象に残した。

## 2026-08-30 PR #152 生体Nexusロゴ・起動順序・旧演出設定の復元

* 起動演出を`init()`途中から外し、正常初期化後に`hideStartupGuard()`で起動ガードと`app-starting`を解除してから、次の描画フレーム以降に一度だけ開始する構成へ変更した。ポップアウト、DB更新待ち、初期化失敗では開始しない。
* 中央核と4ノードを4本のSVG曲線で接続し、イベント開始時に決めた長さ・速度・うねり・静止・引き戻しの計画をRAFで描画する生体Nexus演出を追加した。通常時は核の弱い呼吸だけとし、12〜35秒のランダム待機後に1〜2本だけを動かす。非表示時はタイマーと演出を中断し、再表示後に新しい待機を設定する。
* `memo-nexus-logo-animation`の`typewriter`、`nexus`、`scan`、`off`を維持し、`daily`を`living-nexus`へ移行する設定を復元した。プレビューは保存せず、適用時だけ保存する。タイトルクリックは選択中の演出を先頭から再生し、`off`とReduced MotionではRAF・待機・点滅・呼吸を作らない。
* ロゴ枠を角丸なしの外枠・内線・強調四隅による額縁へ変更し、起動・クリック時は四辺を時間差描画、触手イベント中は近い辺を淡く発光させる。ライト／ダークと390px幅で、ロゴが操作領域からはみ出さないことを確認した。
* 決定的なコントローラーテストで、起動後一度、4触手の不等間隔伸長、伸長→うねり→静止→引き戻し、乱数差分、古いRAF・タイマー無効化、非表示復帰、Reduced Motion、`off`、旧値移行、旧3演出とプレビューを確認した。`node --test`は全866件成功、変更JavaScript 5ファイルの`node --check`と`git diff --check`も成功した。
* 実Edgeでは、ガード表示中は未再生、解除後に`waiting`から`extending`・`waving`へ進みSVGの`d`が実際に変化すること、連続クリックで待機状態へ戻って再開すること、旧3演出のプレビューと未適用復帰、適用、`off`、Reduced Motion、ライト／ダーク、1163px／390px、console warning・error 0件を確認した。検証環境では実タブをhiddenへ切り替えられなかったため、バックグラウンド復帰は表示状態注入テストで補完した。

## 2026-08-30 PR #152 生体Nexus触手の波動改善

* 1本の三次ベジェ曲線だった各触手を、接続点の接線が一致する2区間の三次ベジェ曲線へ変更した。根元・中間・先端・先端ノードへ順に位相差を与え、緩やかなS字／逆S字が根元から先へ伝わる形にした。触手終点とノード座標は同じ純粋計算結果を使用し、伸長・うねり・保持・引き戻しの全段階で接続を維持する。
* 起動4本と通常イベントの計画へ、根元・中間・先端振幅、2〜4回の波数、毎秒約0.46〜0.78周期の伝播速度、位相差、先端追従量を追加した。値はイベント開始時だけ決定し、次のイベントでは触手・長さ・速度に加えて振幅・波数・開始位相も同一になりにくくした。伸長途中から弱く波打ち、保持中は余韻を残して落ち着き、引き戻し中は先端を少し遅らせながら振幅を0へ減衰し、最後は初期座標へ正確に戻す。
* 単一RAF、12〜35秒待機、非表示中断、古いRAF・タイマーのrequest ID無効化、旧3演出、プレビューと適用の分離、額縁、Reduced Motion、`off`の既存制御は変更していない。SVG `viewBox`だけを`-4 -4 52 48`へ数px広げ、初期HTMLも2区間曲線に揃えた。配信識別子は`logo-animation-utils.js?v=0.5.0-5`、`app.js?v=0.5.0-124`へ更新した。
* 純粋関数テストで2区間、接線連続、位相伝播、終点一致、伸長中の弱いうねり、保持・引き戻しの減衰、未選択触手の静止、完了時の初期座標、起動と乱数端点イベントのSVG境界内収容を検証した。ロゴ関連23件と`node --test`全871件が成功し、`node --check app.js`、`node --check logo-animation-utils.js`、`git diff --check`も成功した。
* 実Edgeのライト／ダークで起動・クリック再生を観察し、複数箇所の曲率が時間差で変わって先端ノードが遅れて追従すること、速度が痙攣的でないこと、文字・額縁との重なりやSVG見切れがないこと、完了後に静止形へ戻ることを確認した。モバイル幅は前段の390px実確認からレイアウトCSSを変更しておらず、今回広げたSVGについては全制御点・ノードの境界テストで補完した。

## 2026-08-30 PR #152 レビュー対応：乱数境界と循環位相

* 2本イベントの2本目を選ぶオフセットについて、共有`randomBetween()`と通常の`[0, 1)`での選択傾向は変更せず、整数ロールを`TENTACLE_COUNT - 1`で循環させて1〜3へ収めた。乱数が厳密に1でもオフセット4にならず、0、範囲外、`NaN`を含めて2本のindexが重複せず0〜3へ収まる。
* 開始位相を0以上2π未満へ正規化し、前回値との比較を`abs(atan2(sin(a - b), cos(a - b)))`による0〜πの最短円周距離へ変更した。既存の0.12ラジアンしきい値と0.73ラジアン補正は維持し、2π直前と0直後も近い位相として補正した後、再度0以上2π未満へ正規化する。
* 触手形状、長さ、速度、振幅、波数、9.6秒起動演出、額縁、設定、単一RAF・タイマー制御は変更していない。配信識別子は`logo-animation-utils.js?v=0.5.0-6`へ更新し、`app.js?v=0.5.0-124`は維持した。ロゴ関連28件と`node --test`全876件が成功し、`node --check logo-animation-utils.js`と`git diff --check`も成功した。
