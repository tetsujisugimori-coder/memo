# Codex 作業ルール

## 最重要ルール
- ユーザーが明示的に「実装して」「修正して」「変更して」と言うまで、コードを変更しない。
- 「解析して」「教えて」「確認して」「相談したい」という依頼では、ファイル編集を行わない。
- 変更前に、必ず変更対象ファイル・変更する関数・変更理由を箇条書きで提示する。
- 既存機能を勝手に作り替えない。
- 依頼範囲外のリファクタリング、命名変更、UI変更、機能追加をしない。
- 変更後は、必ず差分の要約を出す。
- 可能なら git diff で変更内容を確認できる状態にする。

## 作業方針
- 小さく変更する。
- 一度に複数機能を変更しない。
- 既存の見た目や挙動をできるだけ維持する。
- 不明点がある場合は、推測で大改造せず、最小限の仮定で進める。

## メモ帳アプリの方針
- [[語句]] は知識リンクとして扱う。
- 関連メモ機能は当面そのまま維持する。
- 新機能は既存関数をなるべく使い回す。
- UI追加は最小限にする。