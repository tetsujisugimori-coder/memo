# Memo Nexus

現在のバージョンは `v0.5.0 "Bridge Update"` です。

Memo Nexusは、Markdownメモ、語句リンク、メモリンク、関連メモ、コレクション整理、ZIPバックアップに対応したブラウザ用メモ帳です。メモとコレクションは端末上の実フォルダではなく、ブラウザのIndexedDBへ保存されます。

## 図形ブロックの等辺印

図形ブロックのツールバーで「等辺」を選び、線分または多角形の辺を2本以上選択して「等辺を完了」を押すと、同じ印の等辺グループを作成できます。選択済みの辺をもう一度押すと選択解除します。印を押して選択すると、プロパティ欄から印の本数を1〜10本で変更でき、Deleteで印だけを削除できます。

等辺印は図形の意味を記す注釈です。辺の長さや頂点座標を自動的に変更する幾何制約ではありません。

## ロゴアニメーション

ヘッダーの `Memo Nexus` ロゴは、ページを開いたときに一度だけ短い演出を再生します。`設定` → `表示` → `ロゴアニメーション` で、日替わり（初期値）、タイプライター、Nexus接続、光の走査、オフを選べます。日替わりは端末のローカル日付から決定するため、同じ日には同じ演出になります。

ロゴをクリック、またはフォーカスして Enter / Space を押すと、タイプライター → Nexus接続 → 光の走査の順に次の演出へ切り替えて再生します。この一時的な選択は設定へ保存されず、再読み込み後は設定値（日替わりなら当日の演出）に戻ります。`prefers-reduced-motion: reduce` の環境では種類だけ切り替え、動きは再生しません。オフ設定では静的ロゴを表示します。

## エディタ演出

`設定` → `表示` → `エディタ演出` では、本文入力を4秒間止めた時にカーソルが一度だけ回転する演出を設定できます。初期値はONで、設定は端末内に保存されます。操作がないままなら、回転完了から20秒後に再び一回転します。IME変換中、文字列を選択中、ダイアログやAIパネルが開いている時、タブが非表示の時、Reduced Motionを尊重する設定が有効な時には再生しません。安全に座標を取得できない環境（モバイルやポップアウトを含む）でも通常のブラウザ標準キャレットを維持します。

## 語句リンクと色分け

本文に `[[語句]]` と書いた語句は、登録済み語句になります。同じ登録済み語句を本文に含む別メモも自動的に関連メモとして認識し、メモ一覧カード下部へ同じ語句ラベルと色を表示します。明示した語句は内部的に `explicit`、本文から見つけた語句は `automatic` として区別しますが、色はリンク種別ではなく語句そのもので決まるため、同じ語句は常に同じ色です。

本文中のすべての単語を自動リンクにはしません。少なくとも1件で `[[語句]]` と明示された語句だけが自動検出の対象です。英数字だけの語句は単語境界で照合するため、`AI` が `FAIL` の一部として一致することはありません。登録済み語句が通常本文に見つかった場合は、プレビュー上でも同じ色のクリック可能な語句リンクとして表示します。この変換は表示専用であり、本文エディタ、保存するMarkdown、IndexedDBのデータは書き換えません。コード、Markdownリンクのラベル、画像の代替文は自動リンク化しません。自動関連は保存済みメモ一覧の更新時に再計算され、本文を一文字入力するたびに全メモを検索しません。

`C` は短く誤検出しやすいため、自動検出では `C言語`、文頭・文末、空白・日本語句読点・括弧や引用符で区切られた `C`、または `Cについて`・`Cを` など助詞に続く `C` だけを対象にします。`CSS`、`ABC`、`Cドライブ`、`Cクラス`、`C++`、`C#` は対象外です。明示的な `[[C]]` は従来どおり常に明示語句として扱います。

登録済み語句が部分的に重複する場合、本文表示とカードの語句チップは長い語句を優先します。たとえば `GPT5.6` と `GPT5.6 sol` が登録されているとき、本文の `GPT5.6 sol` は表示上 `GPT5.6 sol` だけをリンク化します。一方、語句ランキングでは短い登録語句も関連として数えるため、同じ出現は `GPT5.6 sol` と `GPT5.6` の両方の件数に含まれます。明示的な `[[語句]]` は自動リンクより優先されます。

色の規則は固定です。色そのものはメモへ保存しないため、再読み込みや別メモでも同じ語句は同じ色になります。

|先頭文字|色グループ|
|---|---|
|英字 A〜G|A〜G グループ|
|英字 H〜N|H〜N グループ|
|英字 O〜T|O〜T グループ|
|英字 U〜Z|U〜Z グループ|
|数字|すべて同じ数字グループ|
|ひらがな・カタカナ|あ行、か行、さ行、た行、な行、は行、ま行、や行、ら行、わ行の先頭文字の行|
|漢字・その他|語句全体の安定した文字列ハッシュ|

英字は大文字・小文字を区別しません。ひらがなとカタカナは対応する五十音行で同じ色になります。たとえば `か / カ / が / ガ` はか行、`は / ハ / ば / バ / ぱ / パ` はは行です。小書きの `ぁぃぅぇぉ` はあ行、`ゃゅょ` はや行、`っ` はた行、`ヴ` はう・ウと同じあ行として扱います。長音符 `ー` など、表の規則に当てはまらない語頭文字は安定ハッシュへフォールバックします。

## メモリンクとバックリンク

既存メモそのものを参照する場合は `[[* メモ名]]` と書きます。`[[` の直後を半角 `*`、その直後を1文字以上の半角スペースにし、空でないメモ名を書くのが正式記法です。前後の余分な空白は除いて解決しますが、大文字・小文字や全角・半角は正規化せず、現在の有効なメモタイトルと完全一致で照合します。`[[*メモ名]]`、`[[ * メモ名]]`、`[[* ]]`、`[[＊ メモ名]]` はメモリンクにも登録語句にもなりません。

一致が1件なら、そのメモIDを使って既存メモを開きます。一致がなければmissing、同名タイトルが複数あればambiguousとして表示し、どちらも新規メモを作成せず自動遷移しません。fenced code block、Mermaidコードブロック、インラインコード内の記法はリンク化・索引化しません。`[[* メモ名#見出し]]` は現時点では `メモ名#見出し` 全体をタイトルとして扱い、位置ジャンプは行いません。

バックリンクは本文と有効なメモ一覧から導出し、IndexedDBへ別テーブルを保存しません。関連メモドロワーの「このメモへのリンク」に参照元を独立表示し、同じ参照元から同じ対象への重複記述は1件へまとめます。通常の関連メモ欄ではバックリンクとの重複を除き、従来どおり最大8件を表示します。グラフの辺も正式なメモリンクだけから作り、語句リンクはメモ間の辺にしません。missingは未作成ノード、ambiguousは誤接続を避けるため辺・ノードへ加えません。

メモリンク索引は語句索引と同じくキャッシュし、本文・タイトル・メモ集合・削除状態が変わったときに無効化します。索引構築時は有効なタイトルからメモID一覧を引くMapを1回作り、各リンクで全メモを再走査しません。入力イベントでは再構築せず、既存の入力停止後schedulerによる派生UI更新で共有します。

リンク先タイトルを変更すると、改名前にそのメモIDへ一意解決されていた正式なメモリンクは、参照元Markdown本文のタイトル部分も同じatomic batchで新タイトルへ自動更新します。同じ本文の複数リンク、複数の参照元、自己リンクも対象です。改名前からmissing／ambiguousだったリンク、語句リンク、通常文章、不正式な予約記法、インラインコード・fenced code block・Mermaid内の記法は更新しません。新タイトルと同名の有効なメモがある場合も本文は新タイトルへ更新し、その後はambiguousとして自動遷移しません。

## AIアシスタント（Ollama／Gemini、任意機能）

### Codexチャット試作（公開版＋ローカルブリッジ）

Codexタブは実験的なローカル連携です。GitHub Pagesの公開版からも、同じPCで起動した `http://127.0.0.1:8787` のブリッジへ接続できます。公開URLを開くだけでは動作せず、各利用者のPCにNode.js、Codex CLI、Codexへのログイン、ブリッジ起動が必要です。`127.0.0.1`は閲覧者自身のPCを指すため、他人が公開版を開いてもあなたのPCのCodexへ接続されません。iPhoneからWindows PCの`127.0.0.1`へは接続できません。

最初に十分に長いtokenを生成し、同じPowerShellで環境変数を設定してブリッジを起動します。tokenはGit管理ファイルへ保存しません。

```powershell
$env:CODEX_BRIDGE_TOKEN = npm run --silent codex:token
$env:CODEX_BRIDGE_TOKEN
$env:CODEX_BRIDGE_ALLOWED_ORIGINS = "http://127.0.0.1:8765,http://localhost:8765,https://tetsujisugimori-coder.github.io"
npm run codex:bridge
```

2行目で表示されたtokenを、Memo Nexusの「ブリッジ接続トークン」欄へコピーしてください。PowerShellへ設定した値とMemo Nexusへ入力する値は完全に同じである必要があります。tokenはGit管理ファイル、メモ、スクリーンショットなどへ保存・公開しないでください。

PowerShellを閉じると環境変数は失われるため、次回起動時はtokenを再生成するか、別途安全に保管している値を再設定する必要があります。tokenを再生成した場合は、Memo Nexus側にも新しいtokenを入力し直してください。

公開版ではPythonのHTTPサーバーは不要です。`https://tetsujisugimori-coder.github.io/memo/`を開き、AIパネルの「Codex」タブで同じtokenを入力します。tokenはそのタブの`sessionStorage`だけに保存され、タブを閉じると消えます。メモ、IndexedDBのメモ本文、ZIPバックアップへは保存されません。ブラウザがローカルネットワークアクセスを求めた場合は、接続先が`127.0.0.1`であることを確認して許可してください。

ローカル開発版では、別ターミナルで `python -m http.server 8765 --bind 127.0.0.1` を起動し、`http://127.0.0.1:8765/`を開きます。既定のローカルOriginと公開Originは完全一致で許可されます。追加Originは`CODEX_BRIDGE_ALLOWED_ORIGINS`へカンマ区切りで指定できます。パス、空要素、`null`、http/https以外は拒否されます。ブリッジを`0.0.0.0`、LAN内IP、インターネットへ公開しないでください。

環境変数の恒久設定とWindows自動起動は今回未対応です。接続できない場合は、Codex CLIのログイン、ブリッジ起動、token一致、ブラウザのローカルネットワーク権限、公開Origin設定を順に確認してください。CORS拒否、ブリッジ停止、ブラウザ権限拒否は画面から完全に区別できない場合があります。

入力と明示添付した内容はログイン済みCodexを通じてOpenAIのサービスへ送信されます。本文・選択範囲は添付ボタンを押した時だけ送信されます。APIキーは保存せず、既存のCodex CLIログインを使います。空の一時作業ディレクトリ、`read-only`、`approvalPolicy: "never"`、コマンド側ネットワーク禁止と承認要求拒否を維持しますが、会話専用のdeveloper instructionsだけで全ツール利用を完全保証するものではありません。

ブリッジは App Server ごとに状態を分離し、`initialize` 応答後にIDなしの `initialized` 通知を送ってからスレッド操作を開始します。異常終了、RPCタイムアウト、送信失敗では該当プロセスだけを破棄し、次回の接続確認で再起動します。子プロセスの終了を待ってから、そのプロセスが所有する一時ディレクトリだけを削除します。Bearer tokenが正しく、Originが完全一致した`/chat`だけがthread開始・再開とストリーミングへ進みます。`/health`の未認証応答はブリッジ起動中かとtokenが必要であることだけを返します。

画面右端のロボットから、メモを送らない自由チャットと、明示的に選んだメモ／文章を参照するチャットを同じ画面で利用できます。AI機能は初期状態で無効であり、Ollamaが未導入・未起動でも通常のメモ機能はそのまま利用できます。

利用準備:

1. `設定` → `AIアシスタント` でプロバイダを選び、機能を有効にします。
2. Ollamaを使う場合は、[Ollama](https://ollama.com/)を別途インストール・起動し、利用するモデルを導入して接続確認、モデル選択、設定保存を行います。
3. Geminiを使う場合は、利用者自身のGemini APIキーを入力し、接続確認で取得したモデルから選択して設定保存を行います。APIキーはこの端末のブラウザのlocalStorageに保存され、Memo Nexusのソースコード・出力・チャット履歴には保存しません。PCとiPhoneなど別端末のlocalStorageは共有されないため、それぞれの端末で入力してください。共有端末では保存しないでください。

- Ollamaの標準接続先は `http://127.0.0.1:11434` です。接続できない場合は、Ollamaが起動していること、接続先、モデルが1件以上導入済みであること、ブラウザから接続が許可されていることを確認してください。GeminiはGoogle Gemini APIへHTTPSで接続し、モデル取得と生成に入力したAPIキーをHTTPヘッダーとして使用します。
- 初期の動作確認対象は、モデル名に `qwen`、`phi`、`granite` を含むQwen系・Phi系・Granite系です。大文字・小文字やタグの違いを問わず分類します。
- その他のインストール済みモデルも選択できますが、Memo Nexusでは動作未確認として表示します。
- 通常のロボットクリックでは過去の参照会話を持ち越さず、新しい `自由チャット` として `参照なし` で開きます。現在のメモ、タイトル、選択文章、過去のチャット履歴は送信しません。参照範囲は `参照なし`、`現在のメモ`、`選択した文章`、`指定したメモ`、`すべてのメモ`、`選択したコレクション` から選べます。
- AIパネルの用途は参照範囲とは別に `自由質問`、`要約`、`整理`、`翻訳` から選べます。自由質問は参照なしでも利用でき、要約・整理・翻訳は参照範囲の選択が必要です。翻訳では翻訳先を選択できます。
- 自由質問では質問・指示の入力が必須です。要約・整理・翻訳では参照内容があれば追加指示は任意で、用途プリセットだけでも実行できます。
- `このメモを要約` と `選択範囲をAIに送る` は同じAI画面を開き、起動時の本文または選択内容を固定します。AI画面を開いた後にメモや選択状態が変わっても、送信前の参照内容は意図せず切り替わりません。
- `すべてのメモ` はゴミ箱・削除済みを除く有効メモと未分類メモを対象にし、`選択したコレクション` は子・孫コレクションまで再帰的に対象にします。送信前に件数と合計本文文字数を表示します。
- 複数メモ参照の上限は本文合計100,000文字です。上限を超える場合や対象0件の場合は送信せず、メモを黙って切り捨てません。
- 各質問には送信時の参照ラベルを表示します。チャット履歴は現在のタブ内だけに保持し、参照本文全体を永続履歴へ重複保存しません。
- 参照するメモ内容は、利用者が設定して選択したAIプロバイダへ送信されます。Ollamaは設定したローカルサーバーへ送信します。GeminiはクラウドAIであり、質問・指示、選択したメモ本文・選択文章、AI用参照コンテキスト、会話履歴をGoogle Gemini APIへ送信します。Ollamaでlocalhost以外を指定した場合は外部送信の可能性を警告します。
- Geminiの生成は `streamGenerateContent` のSSE応答を順次表示します。接続・認証・権限・モデル不存在・レート制限・割り当て上限・サーバーエラーは共通の画面エラーへ分類し、停止ボタンはAbortControllerで通信を中断します。自動テストではGemini APIへ実接続せず、通信をモックしています。
- Geminiの接続確認は、モデル一覧だけで完了しません。選択モデルに対して短い非ストリーミング生成を実行してから送信可能にします。モデルを変更した場合は再確認が必要です。Geminiはテキスト生成対応モデルのみを一覧に表示し、安定版、Preview、Experimentalで分けます。Ollamaの「動作未確認」表示はGeminiへ適用しません。
- `config.js`、GitHub Pagesに配信するフロントエンド環境変数、リポジトリへ置く設定ファイルにはAPIキーを保存しません。静的サイトの配信ファイルに含めた値は秘密情報として扱えないためです。
- Geminiの料金・無料枠・レート制限・利用条件は固定記載せず、Google側の最新条件を確認してください。
- iPhone Safariでは、GitHub PagesからのCORS通信、localStorageの保存・復元、プライベートブラウズの保存制限、SSEストリーム、AbortController、再読み込み、ホーム画面追加の実機確認は未実施です。動作を保証するものではありません。
- 回答はストリーミング表示され、生成中にパネルを閉じても処理を継続します。停止すると受信済みの文章を残します。タブを閉じた場合、会話や回答は永続化しません。
- 接続確認・モデル取得・生成のタイムアウトは、開始後または最後のストリーム受信から一定時間通信がない場合に発生します。継続的にチャンクを受信している生成は、全体の経過時間だけでは打ち切りません。
- AI回答は本文へ自動反映・自動保存されません。内容を確認してコピー、現在のメモのカーソル位置／本文末尾への挿入、または新規メモ保存を明示的に選んでください。回答生成時にメモを参照していなくても保存できます。
- ロボットとAIパネルはキーボード操作、ダークテーマ、狭幅表示、`prefers-reduced-motion` に対応しています。右側コンテキストパネルではコレクション、AI、メモ一覧を切り替えます。PCでは本文と右パネルを並べ、中間幅では右ドロワー、モバイルでは全画面で表示します。狭幅ドロワーは背景クリックまたは `Escape` で閉じ、背後の本文操作を抑止します。AIタブを右上の×で閉じた場合も、会話と入力内容を保ったままロボットボタンで再開できます。AIからメモ一覧へ切り替えても、メモ一覧の表示・検索状態は維持されます。

## コレクション・エクスプローラ

ツールバーの `コレクション` ボタンから、メモを階層的に整理できます。コレクションとタグは別機能で、1件のメモは1つのコレクションへ所属します。

初期コレクション:

- 歴史
- プログラミング
- IT技術
- 技術史
- 雑学
- 未分類
- ゴミ箱

基本操作:

1. `コレクション` ボタンでエクスプローラを開きます。
2. コレクション行を選択すると、新規メモの保存先になります。
3. `＋` からルートまたは選択中コレクション内へ新しいコレクションを作成できます。
4. コレクションやメモの `…` メニューから、名前変更、移動、エクスポート、削除を実行できます。
5. PCではメモやコレクションをドラッグして移動・並び替えできます。

コレクションの仕様:

- 最大5階層です。
- コレクションの件数には、直下とすべての子孫コレクションにある通常メモを含みます。
- `未分類` は削除・改名・移動できないシステムコレクションです。
- コレクション未選択、未分類選択中、ゴミ箱表示中の新規メモは未分類へ入ります。
- 既存メモは初回起動時に内容やIDを変更せず、未分類へ割り当てられます。

複数選択と移動:

- `Ctrl + クリック` で個別選択を追加・解除します。
- `Shift + クリック` で表示中メモの範囲を選択します。
- エクスプローラにフォーカスがある場合、`Ctrl + A` で表示中の通常メモを全選択します。
- 選択バーまたはドラッグ操作から、複数メモをまとめて移動できます。
- スマートフォンでは各メモの `…` メニューから移動できます。

ゴミ箱:

- 通常の削除ではメモ本体を消さず、削除日時を保存してゴミ箱へ移します。
- 復元すると同じメモIDのまま元のコレクションへ戻ります。
- 元のコレクションが削除済みの場合は未分類へ戻ります。
- `完全に削除` を実行した場合だけIndexedDBからメモを削除します。

コレクションとZIP:

- 設定画面の「完全バックアップZIP」は、通常メモ・ゴミ箱・コレクション・添付を可搬バックアップとして出力します。
- コレクションの `…` メニューから、そのコレクションとすべての子孫を個別にZIP出力できます。
- ZIP内の禁止文字は安全な文字へ置換し、同名メモには連番を付けます。
- 添付があるメモは、メモ名のフォルダ内へMarkdownと `attachments` フォルダを出力します。添付がないメモは従来どおりMarkdownだけを出力します。

## 画像・PDF添付

- メモカード下部の添付エリアから、JPEG/JPG、PNG、WebP画像とPDFを追加できます。ファイル選択、複数選択、PCでのドラッグ＆ドロップに対応し、画像はクリップボードからも貼り付けられます。
- 本文エディタへ画像を貼り付けると、カーソル位置へ `![代替テキスト](attachment://画像ID)` 形式の永続参照を挿入し、カード側の同じ位置へ画像を表示します。本文エディタへの画像ドロップも同じ保存・参照方式を使用します。
- 本文へ一時的なblob URLや画像本体を保存しません。表示時だけIndexedDBの画像BlobからObject URLを作成し、メモ切り替えやページ終了時に解放します。
- 画像は追加時に長辺最大1800px、JPEG/WebPは品質0.86を目安にブラウザ上で圧縮します。PNGは透明度と文字・線を保つためPNGのまま処理し、縮小が不要で変換後の方が大きい場合は元画像を保存します。
- PDFは再圧縮せず元データをIndexedDBへ保存し、カード下部の「開く」からブラウザ標準PDFビューアで表示します。
- 添付容量の上限は1メモあたり合計20MBです。画像は圧縮後のBlob、PDFは元のBlobの容量で判定し、複数追加で超過する場合はその追加全体を保存しません。
- 画像はサムネイルから拡大表示でき、画像とPDFは個別に削除できます。本文で参照中の画像を削除する場合は警告し、参照が残った場合もカードには安全な欠損表示を出します。メモをゴミ箱へ移しても添付は保持され、メモを完全削除したときに関連添付も削除されます。
- メモ単体、コレクション、全件の各エクスポートと、対応ブラウザでのローカルフォルダ書き出しには添付ファイルも含まれます。内部の `attachment://画像ID` は出力時だけ実ファイルへの相対パスへ変換し、アプリ内の元本文は変更しません。
- 「バックアップ／Markdown ZIP取り込み」は、従来のMarkdown ZIPに加えてMemo Nexus完全バックアップを復元できます。
- PDFの本文内埋め込みには対応せず、添付エリアからブラウザ標準ビューアで開きます。カード表示内容全体のPDF保存は将来機能です。

## 可搬バックアップ（format version 1）

完全バックアップZIPとローカルフォルダ保存は、保存先・梱包方法だけが異なる同じ論理構造を使います。

```text
manifest.json      # format: memo-nexus-backup、version: 1、出力日時
collections.json   # ID、親子関係、順序など
notes/*.md         # YAML front matter付きの通常のMarkdown本文
assets/*           # 画像・PDF添付
```

Markdownのfront matterにはメモID、タイトル、コレクションID、作成・更新日時、フラグ、ゴミ箱状態、添付情報などを保存します。Importはまずmanifestを検証し、コレクション、メモ、添付の順に復元します。IDが重複する場合は`updatedAt`が厳密に新しい方だけを採用し、同日時では既存データを維持します。不明なコレクションは未分類へ移動します。

壊れたMarkdownや欠損した添付は個別にスキップし、残りを復元します。manifestがない・壊れている・形式が異なる・アプリが理解できない将来のversionの場合は、既存データを変更せず中止します。Import結果は設定画面と保存状態に件数・スキップ内容として表示されます。将来の形式変更は、ZIPを書き換えず読み込み時のmigrationとして追加します。

表示:

- 広いPC画面ではメモ一覧を画面の最も右側へ表示します。
- 半画面、縦長画面、スマートフォンでは右から重なるドロワーとして表示します。
- ドロワーは閉じるボタン、外側クリック、`Escape` キーで閉じられます。
- ライトテーマとダークテーマに対応しています。

## フォント設定とFont Comparison連携

- 設定画面の「フォント設定」で、編集画面上部の題名フォント・題名サイズ、本文フォント・本文サイズ、見出しフォント、コードフォント・コードサイズを変更できます。
- すべてのフォント欄は「システムフォント」と「Webフォント（選択時に読込）」に分かれています。システムフォントはSegoe UI、Yu Gothic UI、Meiryo、MS Mincho、Consolas、Cascadia Code、Courier New、Times New Romanの8種です。WebフォントはNoto Sans JP、Noto Serif JP、Noto Sans SC、Noto Sans TC、Source Han Sans、Inter、IBM Plex Sans、JetBrains Mono、Zen Kaku Gothic New、Shippori Minchoの10種です。
- 既定で開く「条件からフォントを探す」は、使用言語、文章の雰囲気、主な用途から最大3件の候補を検索するアンケートです。初期回答は「日本語／中立・読みやすさ重視／長文を書く」で、設定画面を開いた時点から3件を表示し、回答変更時に自動更新します。候補表示だけでは保存もWebフォント読込も行いません。「長文を書く／読む」は本文、「見出し・短文」は見出し、「プログラミングコード」はコードの選択欄とプレビューへ反映し、「フォント設定を保存」で確定します。フォント選択欄では一覧から直接選択でき、Font Comparisonでは実際の表示を詳しく比較できます。
- 全体設定に加え、「このメモでは個別のフォント設定を使用する」を有効にすると、現在のメモだけ別の設定を保存できます。
- 全体設定はこの端末のlocalStorageへ保存し、ZIPバックアップには含めません。メモ個別設定はメモの`fontSettings`としてIndexedDB、ローカルMarkdown、ZIPバックアップを往復します。いずれもフォントIDと既存のサイズ設定だけを保存し、フォントファイルや配信CSSは保存しません。
- 各フォントの「フォントを比較して選ぶ」から `https://tetsujisugimori-coder.github.io/font-comparison/` を新しいタブで開き、現在の設定と比較文章を渡せます。比較文章は任意入力、現在のメモ本文、標準サンプルの順に使用し、URLへ含めるため最大700文字です。
- Font Comparisonとの往復は上記18種に対応します。戻り値は送信元、用途、適用範囲、登録済みフォントID、Memo Nexus側の正確な`font-family`を検証し、URL側の表示名は信頼せずローカルの正式名を使います。戻った時点では保存されず、「この設定を使用」でプレビューへ反映し、「フォント設定を保存」で確定します。
- Webフォントは起動、選択肢、条件検索の候補表示、検索条件の変更だけでは読み込みません。保存済み設定が現在の画面で使われる時、選択をプレビューへ反映した時、Font Comparisonの受取を確定した時に、必要なフォントの必要なWeightだけをGoogle FontsまたはjsDelivrから読み込みます。題名と見出しは700、本文とコードは400を要求し、同一フォントの用途はWeightを集約します。読込済みWeightは再取得せず、後から必要になった不足Weightだけを追加します。Source Han Sans CNもRegular（400）とBold（700）の必要なOTFだけを取得します。
- 題名設定は編集画面上部の題名入力欄だけに適用され、メモ一覧の題名には適用しません。そのほかのフォント設定も操作ボタン、KaTeX数式、Mermaid図の表示には適用しません。
- オフライン時や配信元からの読込に失敗した場合も、選択したIDは保持し、CSSの後続フォントで表示します。設定画面には失敗したフォントとWeight、代替表示の案内、「再試行」ボタンを表示します。再試行は現在の設定またはプレビューで必要な失敗Weightだけを要求し、読込中は重複操作を防ぎ、成功後はエラーとボタンを消します。Memo Nexusは実際に描画されたフォントを断定しません。

## 記法ガイド

- タイトル欄と本文入力欄の間にある `記法ガイド` ボタンから、現在対応しているMarkdown、KaTeX数式、計算ブロック、コードブロック、表ブロック、Mermaidの書き方を確認できます。
- 各サンプルは開始・終了のバッククォートを含む完成形でコピーできます。本文への自動挿入は行いません。
- 本文中の計算式を選択して `計算` を押すと、Calculator Memoへ式を渡して新しいタブで開けます。選択していない場合は、空のCalculator Memoを開きます。
- Markdown欄には見出し、太字・斜体・打ち消し線、箇条書き・番号付きリスト・チェックリスト、水平線、引用、通常リンク、`[[語句]]` の語句リンク、`[[* メモ名]]` のメモリンク、注意書きを掲載しています。
- 注意書きはGitHub形式の `> [!NOTE]`、`TIP`、`IMPORTANT`、`WARNING` を本文へそのまま保存します。通常の引用とは別の色・アイコンで表示され、Markdown出力でも読めます。
- 本文内に `<!-- memo-nexus:image-block --> ... <!-- /memo-nexus:image-block -->` があると、画像を本文順に反映した画像ブロックカードとして表示されます。画像参照は `attachment://ID` のみを本文へ保存し、画像本体は添付として保持されます。
- 画像ボタン（本文編集欄の直上）で添付画像を選択すると、カーソル位置へ画像ブロックとして挿入します。カーソル位置が取得できない場合は本文末尾に追加します。

- Excelなどからコピーした内容に解析可能なHTML表、Markdown表、TSV（タブ区切り）とJPEG・PNG・WebP画像が共存する場合、表貼り付け確認で形式を選べます。通常は構造を保持する「表として貼り付け」（混在HTMLでは「文章と表を分けて貼り付け」）が主操作で、必要に応じて「画像として貼り付け」「テキストとして貼り付け」「キャンセル」を選べます。
- 表または文章と複数表だけの場合は、従来どおり表とテキストとキャンセルだけを表示します。純粋な画像コピー、表として解析できないHTML＋画像、通常文章＋画像、コードフェンス内の画像は、確認を増やさず従来どおり直接画像として貼り付けます。通常文章だけは通常の貼り付けです。
- 形式選択中の画像は貼り付けイベント中にFileとして取得してダイアログ内だけに保持します。表・テキスト・キャンセルでは画像を保存せず、「画像として貼り付け」を選んだ時だけ既存の圧縮、容量検査、添付保存、本文参照、Undo、自動保存、ロールバック経路を使います。対象メモ、本文、選択範囲が変わった場合は全形式を中止します。
- HTML内に文章と表、または複数の表がある場合、「文章と表を分けて貼り付け」を選べます。文章と各表は元の順序を保ち、各表が独立した編集可能な表ブロックになります。表ごとに「1行目を見出しにする」を指定できます。文章のない単一HTML表、Markdown表、TSVは従来の単一表ダイアログを使用します。
- 文章部分は安全なプレーンテキストとして取り込みます。段落・見出し・リスト項目の境界やbrは改行にし、太字やリンクは表示文字列だけを保持します。HTML全体のMarkdown化、HTML装飾やリンクURL、画像の添付、数式評価、複雑なレイアウトの再現は行いません。
- 結合セルは左上セルへ値を置き、残りを空セルにします。入れ子表は外側のセル内の文字列として扱い、別の表ブロックへ重複挿入しません。
- 上限は各表100行・30列、全体で10表・合計3000セルです。解析できない表や上限超過が一つでもあると全体の変換を中止し、テキスト貼り付けかキャンセルだけを選べます。表を切り捨てて挿入しません。
- テキスト貼り付けは元の `text/plain` 全文を使います。HTMLしかない場合は全文章・全表を文書順の安全なテキストへ変換します。確定した混在貼り付けは1回のUndoで戻せ、Redo・自動保存・再読み込みでも順序と表データを維持します。混在貼り付け後は本文の挿入末尾、単一表では左上セルへフォーカスします。

- エディターツールの `CSV/TSV` から、UTF-8の `.csv` または `.tsv` を1ファイルずつ選んで表ブロックとして読み込めます。ファイルは外部サーバーへ送信せず、選択後にファイル名、形式、行数、列数、セル数を確認してから確定します。1行目を見出しにするかも選べ、確定前は本文・Undo/Redo・保存状態を変更しません。
- `.csv` はカンマ、`.tsv` はタブで区切りを拡張子どおりに決めます。UTF-8 BOM、LF/CRLF/CR、引用符内の区切り・改行、`""` の引用符、空セルと明示された末尾空列を保持します。通常の文章貼り付けでCSVを自動判定しないため、カンマを含む文章を表へ誤変換しません。
- 上限は100行、30列、3000セル、5MiBです。Shift_JISなどUTF-8以外の文字コード、`.xlsx`/`.xls`/`.ods`、複数ファイル、区切りの自動判定、数式評価には対応しません。読み込んだ表は通常の表ブロックとして編集・コピーでき、既存の「グラフを作成」も利用できます。

- 表ブロックの「操作」→「CSVで保存」または「TSVで保存」から、表示中のセル値を1表ずつファイルへ保存できます。CSVとTSVはいずれもUTF-8 BOM付き・CRLF区切りで、Windows版Excelでも開きやすい形式です。空セル、末尾空列、区切り文字、引用符、セル内改行を保持します。
- 保存ファイルに入るのはセル行列だけです。表ID、見出し設定、列配置、選択状態、Undo/Redo、メモID、Markdownマーカー、グラフとの関係は含みません。再読込時は既存の確認画面で「1行目を見出しにする」を改めて選択してください。
- 空の表、NUL文字を含む表、UTF-8 BOMを含めて5MiBを超える表は保存しません。数値・日付・先頭ゼロ・数式のような文字列も書き換えず、Memo Nexusは数式を評価しません。ただし、`=SUM(...)` などは外部の表計算ソフトで数式として解釈される可能性があります。`.xlsx`/`.xls`/`.ods`の書出しには対応しません。モバイルの保存先や操作はブラウザにより異なります。

- 保存済み表ブロックの「操作」→「グラフを作成」で、表の内容を取り込んだグラフ編集画面を開けます。先頭行の2列目以降を系列名、先頭列の2行目以降を項目名、それ以外を数値として読み取り、左上セルは取り込みません。「入力を確定」した時だけ元表の直後へ新しいグラフを追加し、元の表は変更しません。取消または編集中のEscapeでは追加しません。作成後の表とグラフは独立しており、同期されません。確定した挿入はUndoで戻し、Undo隣のRedoで同じグラフを復元できます。Undo後に別の編集をするとRedoは無効になります。
  - 見出し行と1件以上の項目が必要です。最大50項目・3系列（表では51行・4列）。系列名・項目名は前後空白を除去し、空欄は拒否、同名は使用できます。見出し行の表示オン／オフに関係なく先頭行を系列名として扱います。
  - 有限な正数・0・負数・小数・指数表記を使用できます。空欄、NaN、Infinity、カンマ、%や通貨記号、数式は変換せず、行・列付きのエラーを表示します。不揃いな行は既存表の正規化で補われた空セルを検証します。末尾の空行・空列も自動的に捨てません。
  - 初期設定は通常の新規グラフと同じです。種類・タイトル・単位・軸・色・表示設定は確定前に変更でき、円の負数や複合の系列数など既存の制約が適用されます。
  - 編集中に同じID・内容の表が移動した場合は移動先へ追従します。表の編集・削除・置換や同じIDの重複で安全に特定できない場合は挿入せず、取消して変換し直すよう案内します。表のIDや参照情報はグラフに保存しません。
- `グラフ` ボタンで独立したグラフブロックを挿入できます。作成・再編集画面の「グラフの種類」で棒グラフ、折れ線グラフ、複合、円グラフを切り替えると、同じブロックIDのままプレビューを更新します。タイトル、単位、項目名、有限な数値、項目順、表示設定は維持され、`編集を取り消す` で編集開始時の状態へ戻せます。棒グラフと折れ線グラフは最大3系列を設定でき、各系列の名前・色・項目ごとの値を凡例とともに比較できます。棒グラフは「棒の表示方法」で集合／積み上げ／100%積み上げ、「向き」で縦／横を切り替えられ、横棒でも3種類の棒モードをすべて利用できます。通常の積み上げは正数と負数を0から別々の方向へ積む発散型です（例: +30、-10、+20 → 正側+50、負側-10）。系列の順・ID・色は維持し、「合計値を表示」で正側・負側の端へそれぞれの合計を表示します。片側0の不要な合計は省き、全0は0を一つ表示します。正数だけの合計の意味は従来どおりです。合計が有限数の表現範囲を超える場合は「上限超過」と示し、元の有限入力値を保持します。集合棒と100%積み上げには合計値ラベルを表示しません。100%積み上げも正負別の発散型です。各項目の正側合計と負側の絶対値合計を別々に100%へ正規化します（例: +30、-10、+20 → +60%、-100%、+40%）。正負を相殺せず、正数だけなら0〜100%、負数だけなら-100〜0%、全体に両方あれば-100〜100%の軸を使います。全0は従来どおり0〜100%軸の0位置に置き、棒と割合ラベルは省略します。項目内の符号ごとに最大絶対値で縮小してから割合を計算するため、巨大な有限値の合計もオーバーフローしません。割合・座標は表示時だけ導出し、元の有限値、系列ID・順序・色はそのまま保存します。負側の割合は符号付きで表示し、省略したラベルも項目名・系列名・元値・未丸め割合をtitleと読み上げ情報へ残します。横棒では長い項目名用の左領域を確保し、必要時は2行までに省略して全文を読み上げ情報とSVGのタイトルへ残します。集合棒・通常積み上げ棒・100%積み上げ棒（縦／横）と折れ線は有限な負数に対応し、表示中の全系列の最小値・最大値（通常積み上げでは負側合計の最小値・正側合計の最大値、100%積み上げでは存在する符号側の±100%）と0を含む共通軸で描画します。0基準線から、正数の棒は上／右、負数の棒は下／左へ伸びます。円グラフは「表示する系列」で1系列を選択します。選択は`appearance.pieSeriesId`へ安定した系列IDで保存するため、棒・折れ線との切替、保存・再読み込み、名称変更、並べ替え、選択外系列の削除後も維持されます。選択中の系列を削除した場合、残る先頭系列へ安全に切り替わります。古いデータで`pieSeriesId`がない・不正な場合は現在の先頭系列を選びます。本文には `<!-- memo-nexus:chart-block:<UTF-8 hex JSON> -->` として `schemaVersion: 1`、`chartType: "bar" | "line" | "pie" | "combo"`、安定ID付き項目・系列、表示設定を保存するため、通常の自動保存・再読み込み・Markdown/ZIPエクスポートで保持されます。旧来の「項目名＋数値」形式は読み込み時に1系列として扱い、読むだけでは本文を変更しません。編集して保存すると系列形式で保存します。`chartType` がない従来データや未知の種類は棒グラフとして読み込みます。円グラフは引き続き負数に未対応です。全系列のどこかに負数があるまま切り替えると日本語のエラーを表示し、編集内容を一時保持して本文への保存・確定を止めます。棒グラフまたは折れ線へ戻せば同じ値で描画でき、エラー中も取消できます。一時保持中の変更はページを閉じると失われますが、本文の直前の有効なブロックは保持します。数値入力の空欄・途中のマイナス記号・NaN・Infinityは確定できず、負の0は通常の0として保存します。極端に桁差がある値の棒や点は浮動小数点・画面解像度の範囲で0位置と重なることがあります。表ブロックとの自動同期・CSV取り込みは未対応です。
- 複合グラフは2〜3系列を用意し、「グラフの種類」で「複合」を選びます。「折れ線にする系列」で選んだ1系列を折れ線、残りを縦の集合棒として、同じ単位・単一Y軸・共通の0基準線で比較します。初回は最後の系列を選び、`appearance.comboLineSeriesId`に安定IDを保存します。名称・色・項目／系列順の変更や種類往復でも選択を維持し、選択系列を削除した場合は残存する最後の系列IDへ切り替えます。1系列になると日本語エラーで確定を止め、一時編集中の値を保持します。有限な負数・正負混在・全0・巨大値に対応し、棒は0から上下へ伸び、点はカテゴリ中央に置きます。凡例と読み上げ情報で棒／折れ線を区別し、重なる値ラベルは省略しても元値を保持します。複合中は向き・積み上げ設定を選べませんが、元の`barOrientation`／`barMode`を保持し、棒へ戻すと復元します。横棒・積み上げとの複合、1系列だけの複合は対象外です。旧データや不正な選択IDは読むだけでは本文を書き換えず、取消時は元のマーカーへ戻します。
- 複合グラフの「軸モード」は初期状態では「単一軸」です。同じ単位の全系列を共通の尺度で比較します。「左右2軸」を明示的に選ぶと、棒の1〜2系列は左軸（既存の単位欄）、選択した1系列の折れ線は右軸（追加される「右軸の単位」欄）を使います。左右2軸は見た目が似ていても左右で尺度が異なるため、高さ・傾きだけで値の大小や変化量を比較しないでください。凡例と読み上げ情報に「棒・左軸」「折れ線・右軸」を示します。左右に正負があると各軸をそれぞれの最大絶対値による対称範囲へ拡張し、0位置を中央へそろえます。正数のみは下端、負数のみは上端を共通0とし、基準線は1本です。全0の軸は相手の0位置に合わせる空き範囲を使います。入力値は変更せず、軸範囲と座標は描画時だけ計算します。
- 二軸設定は既存マーカーの`appearance.comboAxisMode: "single" | "dual"`と`appearance.comboSecondaryUnit`（前後空白・改行を既存単位と同様に正規化）へ保存します。`schemaVersion: 1`、`DB_VERSION: 6`は維持します。欠損・不正な軸モードは単一軸として表示し、読むだけでは旧本文を書き換えません。軸モード・グラフ種類の往復でも左右の単位、入力値、安定ID・系列順・色・折れ線選択を保持し、取消時は編集開始時の本文マーカーへ戻します。複合以外では二軸設定を隠します。狭い画面では既存のグラフ内スクロールを使い、目盛りの長い数値は指数表記、単位は短縮表示にして全文・元値をtitleと読み上げへ残します。任意の系列別軸指定、複数折れ線、3本目の軸、日付軸、ズーム・パン、CSV／表ブロックとの自動同期、SVG／PDF出力は未対応です。
- 複合グラフでは「左軸のタイトル」（例: 売上）と「左軸の単位」（例: 万円）を設定できます。左右2軸時は「右軸のタイトル」（例: 成長率）と「右軸の単位」（例: %）も表示します。軸の近くへ「売上（万円）」のようにまとめ、タイトルのみ／単位のみも使えます。空欄なら従来表示です。任意の`appearance.leftAxisTitle`／`appearance.rightAxisTitle`を追加し、単位は既存の`unit`／`appearance.comboSecondaryUnit`を再利用します。二軸解除・種類切替でも設定を保持し、複合以外ではタイトル設定と表示を隠します。SVGツールチップと読み上げには系列の割当軸に対応する単位を表示します。数値自体は数値のまま保存し、単位換算や書式変更は行いません。長い軸タイトルは既存の省略処理で短縮し、全文と単位はtitle／読み上げへ残します。軸線とタイトルは複数系列の尺度を特定系列の色と誤認しないよう既存の中立色を使います。保存・再読込・再編集・Markdown/ZIP入出力で設定を保持し、旧データの移行は不要です。
- グラフの棒・折れ線の点・円の区画をクリック／タップすると、項目名、系列名、元値、単位をカード内の吹き出しで確認できます。100%積み上げと円は割合、複合は棒／折れ線と割当軸も表示します。マウスオーバー時は従来のSVGタイトルを使います。Tabでグラフのデータへ入り、矢印キーで前後、Home／Endで先頭／末尾へ移動できます（データのTab入口は1グラフ1か所）。0値もキーボードで確認可能です。同じ図形の再クリック／再タップ、Enter／Space、Escape、グラフ外への操作で閉じます。編集・再描画・メモ切替でも閉じ、選択・位置・表示状態は本文や保存データへ記録しません。狭幅では従来のグラフ内スクロールを利用します。
- グラフ編集画面の項目データ付近にある「表データを貼り付け」で、Excel・GoogleスプレッドシートからコピーしたTSV（タブ区切り）を取り込めます。1行目の2列目以降が系列名、2行目以降の1列目が項目名、残りが数値です。左上セルは任意の文字列または空欄で、保存しません（例: `項目<TAB>売上<TAB>利益`、次の行に `1月<TAB>100<TAB>20`。`<TAB>`は実際のタブ文字）。最大50項目・3系列で、LF／CRLF、正負の有限値・小数・指数表記に対応します。プレビューで項目数・系列名・値を確認し、「貼り付け内容を反映」で表全体を置き換えた後、「入力を確定」で保存します。反映後の修正も確定までは一時編集中の状態です。「キャンセル」は貼り付け前の編集途中状態を維持し、「編集を取り消す」は編集開始時へ戻します。同じ位置のID・色とグラフの表示設定を維持します。CSV、部分貼り付け、セル内改行、数式評価、ファイル読込には未対応で、カンマ・%・通貨記号を含む数値を自動変換しません。空欄・列数不一致などは行・列付きエラーを表示します。円の負数、複合の1系列は反映後も既存の検証で確定を止めます。
- 保存済みグラフの閲覧カードの「画像をクリップボードへコピー」で、PNG保存と同じタイトル・グラフ全体・軸／単位・凡例・二軸の注意書きを、現在のテーマと不透明背景のPNG画像としてコピーできます。データ表・操作ボタン・通知・ツールチップは含まず、編集プレビューにはボタンを表示しません。Clipboard API、secure context（HTTPSまたはlocalhostなど）、ブラウザ権限、PNG画像形式対応に依存します。利用できない場合は「PNG画像として保存」を使ってください。失敗時の自動ダウンロードやテキスト代替はありません。コピーは本文・保存状態・Undo／Redo・IndexedDBを変更しません。同じカードではコピーとPNG保存・SVG保存を同時実行せず、完了または失敗後に再試行できます。iPhone Safari実機は未確認で、WebKit／タッチエミュレーションやClipboard API境界のテストとは区別しています。
- 保存済みグラフの「SVG画像として保存」で、PNG保存と同じタイトル・グラフ全体・軸／単位・表示中の凡例・左右2軸の注意書きを、現在のテーマと不透明背景で保存できます。拡大しても線や文字が劣化しにくいSVG形式です。データ表・操作UI・通知・ツールチップは含まず、編集プレビューにはボタンを表示しません。本文・保存状態・Undo／Redoは変わりません。使用中のフォント指定は保持しますが、閲覧環境に同じフォントがなければフォールバックフォントへ置換され、文字幅などが変わる場合があります。SVGコピー・PDF・一括出力・フォント埋め込みは未対応です。保存先や同名ファイルの連番はブラウザに依存します。失敗時は再試行するかPNG保存を選べます。
- 保存済みグラフの閲覧カード下にある「PNG画像として保存」で、タイトル・グラフ本体・軸・ラベル・表示中の凡例（二軸の注意書きを含む）をPNGとしてダウンロードできます。横スクロールで隠れた部分も全体を出力し、現在のライト／ダークテーマと不透明な背景を反映します。データ表・操作ボタン・ツールチップは含めません。通常は論理サイズの2倍、長辺4096px・1600万画素以内へ全体を縮小します。本文・グラフデータ・保存状態は変わりません。編集中プレビューにはボタンを表示しません。PDF、一括出力は未対応です。ダウンロード後の保存先や操作はブラウザによって異なります。
- 再編集画面の「表データを貼り付け」の隣にある「全データをTSVでコピー」で、現在入力中の項目名・全系列名・元の入力値をコピーできます。データ表を表示している閲覧画面では、表の上の同じボタンから確定済みデータをコピーできます。円グラフも選択系列に限らず全系列、100%積み上げも割合ではなく元値が対象です。コピーだけでは編集を確定・保存しません。1行目は「項目」と全系列名、残りは項目名と数値で、タブ／LF区切りです。Excel、Googleスプレッドシート、テキストエディタ、Memo-Nexusの「表データを貼り付け」へ貼り付けられます。色・単位・軸設定・グラフ種類・IDなどは含みません。名前にタブや改行がある場合、空欄の名前や有限でない値がある場合は、理由を表示してコピーを中止します。クリップボードを利用できない場合も失敗を表示します。
- グラフの作成・再編集で「データ表を表示」を有効にすると、編集プレビューと閲覧画面のグラフ下に読み取り専用の表を表示できます。項目・系列・元の入力値を保存データから表示し、100%積み上げでも計算後の割合にはしません。円グラフは現在選択中の系列（0の項目を含む）だけ、複合グラフは全系列の種類・左右軸・単位を見出しに表示します。狭い画面では表部分を横スクロールできます。チェック変更後とTSV反映後は「入力を確定」まで一時編集中の状態で、取消で編集開始時へ戻ります。旧グラフは既定で非表示です。表内での直接編集・計算・CSV/TSVファイル出力には対応していません。
- グラフの項目データでは各行の「上へ」「下へ」で項目順を変更できます。項目と全系列の同じ位置の値を一体で移動し、棒・折れ線・円グラフで共通の順序を使います。円グラフでは項目名の隣で色を指定でき、`appearance.pieItemColors`へ安定した項目IDごとの安全な`#rrggbb`値だけを保存します。項目名や表示順には対応付けないため、並べ替え、名前変更、同名項目、表示系列の切替後も指定した色を維持します。未指定・旧データ・不正値は従来の固定パレットを使い、「既定色へ戻す」で指定色を解除できます。
- グラフの系列設定欄でも各系列の「上へ」「下へ」で系列順を変更できます。系列ID、名前、色、全項目の値を一体で移動し、入力表、棒・折れ線グラフ、凡例、読み上げ順へ反映します。円グラフは`pieSeriesId`で選択した系列を表示するため、並べ替え後も同じ系列を保持します。
- 本文またはコードを選択して `解説` を押す、または対象選択なしの `解説` でカーソル位置へ追加すると、対象文字列・前後文脈・位置とともに解説を保存します。本文を編集し直しても、位置・完全一致・前後文脈の順に再特定し、対象不明のカードは保持して本文中の確認カードとして表示します。プレーンテキスト、太字・斜体・打ち消し線、通常リンク、インラインコードの単一階層へ対応します。`**[保存](URL)**` のような装飾の入れ子や複数の表示ノードにまたがる選択は誤った位置へ付けず孤立状態として表示します。
- 画像を本文から削除しても、添付ファイル本体は削除されません。画像の再利用が必要な場合は、画像ボタンで再度本文へ参照を追加してください。
- 通常リンクは `http`／`https` だけを新しいタブで開き、`noopener noreferrer` を付けます。チェックリストの操作は、現在開いているメモの該当項目だけを明示的に更新します。

## KaTeX数式表示

本文のMarkdownは変更せず、カード表示時だけKaTeXで数式を描画します。数式記法は表示専用であり、式の値は計算しません。

インライン数式:

```md
円の面積は $A = \pi r^2$ です。
```

ブロック数式:

```md
$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$
```

- 分数、平方根、累乗、添字、総和、積分、行列、複数行の式など、KaTeXが対応する記法を表示できます。
- 長いブロック数式は数式領域だけを横スクロールできます。
- コードブロック、インラインコード、Mermaid、計算ブロック内の数式記法はKaTeXへ渡しません。
- 不正な数式はその箇所だけ「数式を表示できません」と元の記法を表示し、他の本文やブロックの描画を続けます。

## 計算ブロック

1ブロックにつき1つの独立した式を、Calculator Memoの現在のmainと同じ規則で計算します。保存されているMarkdown本文は変更しません。

```md
:::calc
(10000 + 5000) * 1.1
:::
```

カードでは入力式を `(10,000 ＋ 5,000) × 1.1`、結果を `= 16,500` のように区別して表示します。表示用の演算記号だけを置き換え、計算には元の入力式を使用します。

対応:

- 加算、減算、乗算、除算
- 括弧、小数、負数
- `%`（Calculator Memoと同じ単項パーセント。`10%`は`10 ÷ 100`、`200 + 10%`は`200.1`）
- `＋`、`−`、`×`、`÷`、乗算としての`x`・`X`、空白の正規化

非対応:

- 累乗、`sqrt()`などの数学関数、三角関数、対数、関数電卓機能
- 変数、代入、名前付き値、複数式、複数行計算
- ブロック間参照、計算履歴参照、単位計算、数式変形、方程式、グラフ

平方根や累乗はKaTeX数式として表示できますが、現在のCalculator Memoでもこれらの高度な計算機能は未実装のため、Memo Nexusの計算ブロックでは計算できません。Memo Nexusはメモ内の簡単な計算確認に限定し、関数電卓などの高度な計算は将来的にCalculator Memo側で担当する予定です。今回はCalculator Memoリポジトリを変更していません。

## AI朝刊の取り込み

追加機能:

- 本文中の `[[語句]]` を検出して、プレビュー下部に一覧表示します。

使い方:

1. 上部ツールバーの `設定` ボタンを押します。
2. 設定モーダルの `取り込み` から `AIニュースファイル取り込み` ボタンを押します。
3. `C:\Users\tetsu\Documents\Codex\メモ帳\IMPORT` に保存された `json` を選びます。
4. `AI朝刊 YYYY-MM-DD` という新規メモが追加されます。

補足:

- `.md` と `.txt` も取り込めます。
- `.md` や `.txt` の中に ```json ... ``` のコードブロックがあれば、その JSON を優先して朝刊メモ化します。
- AI朝刊などの取込ファイル保存先は `C:\Users\tetsu\Documents\Codex\メモ帳\IMPORT` を想定しています。
- JSONの見本は [`ai-news-template.json`](C:\Users\tetsu\Documents\Codex\メモ帳\ai-news-template.json) に置いてあります。
- オートメーション向けの出力仕様は [`ai-news-automation-format.md`](C:\Users\tetsu\Documents\Codex\メモ帳\ai-news-automation-format.md) に置いてあります。

JSON生成コード:

- 朝刊本文から取込用JSONを作るには `node build-ai-news-json.js input.md output.json` を使います。
- 入力が `.json` の場合は正規化して出力します。
- 入力が `.md` や `.txt` で、その中に ```json ... ``` があれば、そのJSONを抽出して出力します。

自動保存の状態表示:

- メモタイトル付近に自動保存の状態を表示します。
- 編集中は `編集中...`、保存処理中は `保存中...`、保存失敗時は `保存エラー` と表示します。
- 保存完了後は `保存済み: YYYY-MM-DD HH:mm:ss` の形式で、最後に保存に成功したローカル時刻を表示します。
- スマホ表示では画面幅に合わせて `保存済み HH:mm:ss` のように短縮表示します。

スマホ向けUndo:

- 上部ツールバーの `Undo` ボタンで、現在開いているメモの直前状態へ戻せます。
- Undo対象はメモID、タイトル、本文、保存時刻を持つスナップショットです。IndexedDBの保存形式や既存メモデータ構造は変更していません。
- 本文欄とタイトル欄の変更前状態を `beforeinput` で保存します。
- 削除、切り取り、貼り付け、ドロップはすぐにUndo履歴へ保存します。
- 通常入力は履歴が増えすぎないように約800ms間隔で保存し、連続する同一内容は保存しません。
- Undo履歴は最大50件です。
- PCの `Ctrl+Z` はブラウザ標準Undoをそのまま使います。アプリ側では `Ctrl+Z` を奪いません。
- Redoは現時点では未実装です。

iPhone保存安定化・ZIPバックアップ改善:

- 現在のアプリバージョンは `v0.5.0 "Bridge Update"` で、PCでは関連メモ見出し直下、スマホでは左側ツールバー直下、共通で開発者コンソールに表示します。
- Safari/PWAの古い静的ファイルキャッシュを避けるため、CSSとJavaScriptの読み込みURLに更新識別子を付けています。
- 起動時に現在のURLとIndexedDBのメモ件数をコンソールへ出力し、別URL・別保存領域を確認しやすくしています。
- localStorageが利用できない場合やメモ件数が極端に少ない場合は、プライベートブラウズ、別タブグループ、Safari版とホーム画面版の保存領域の違いについて画面上に注意を表示します。
- IndexedDBへの保存と削除は、リクエスト受付時ではなくトランザクション完了後に成功扱いにします。
- 入力直後の保存漏れ対策として、現在編集中の1件だけをlocalStorageへドラフト退避します。全メモはlocalStorageへ保存しません。
- 起動時にドラフトがIndexedDBより新しい場合、または同じIDのメモがIndexedDBから消失している場合は、そのドラフトを復元します。
- 30日以上古いドラフトは復元せず削除します。メモを削除した場合は、同じIDのドラフトも削除します。
- 入力中にZIPバックアップを実行した場合は、遅延保存を先に完了させてからZIPを生成します。
- iPhone Safariやホーム画面追加版でページを閉じる、アプリを切り替える、画面をロックする場合に備え、`pagehide` と `visibilitychange` で未保存の入力を保存します。
- ZIP内のMarkdownファイルにはメモの更新日時を記録し、1979/11/30のような不正な日時表示を防ぎます。
- 同名タイトルのメモは `メモ.md`、`メモ-2.md`、`メモ-3.md` のように一意のファイル名で出力します。
- 開発者コンソールでは、保存したメモのID・タイトルと、ZIPのファイル数・ファイル名一覧を確認できます。

JSON貼り付け取り込み:

- 上部ツールバーの `設定` ボタンを押し、設定モーダルの `取り込み` から `JSON貼り付け取り込み` ボタンを押すと、ニュースJSONを貼り付けて取り込めます。
- 貼り付けたJSONは `JSON.parse()` で解析し、1つのJSONから1件のメモだけを作成します。`items` ごとの個別メモは作成しません。
- 作成するメモタイトルは `AI朝刊 2026-05-31` のように、JSONのタイトルと日付を並べた形式にします。
- 本文はAIニュースファイル取り込みに近い体裁で、`# タイトル 日付`、`日付: YYYY-MM-DD`、`1. 見出し`、箇条書き、`## 全体傾向` の形に整形します。
- `items` の見出しは `title`、`heading`、`headline`、`見出し` の順に扱い、日付や全体傾向、要約、重要性、情報源、時代、タグなどの項目名の揺れも一部吸収します。
- JSONが不正、または `items` が配列でない場合はエラーを表示し、保存しません。

設定モーダル:

- 上部ツールバーの `設定` ボタンから、保存状態、取り込み、バックアップ、表示、データ管理を確認できます。
- 保存状態には、保存方式、DB名、ストア名、メモ件数、保存容量、上限目安、使用率、アプリバージョン、現在のURLを表示します。
- `navigator.storage.estimate()` に対応しているブラウザでは、保存容量をMB表示で確認できます。
- `Markdown ZIPを書き出す` は設定モーダル内からも実行できます。従来の上部ツールバーの `ZIP` ボタンも残しています。
- 表示設定、JSON全体書き出し、全データ削除は現時点では未実装で、Coming soonまたは未実装表示にしています。
- 左カラムは検索とメモ一覧中心に整理し、AIニュースファイル取り込み、JSON貼り付け取り込み、知識レベルパネルは表示していません。

語句統計機能 (新機能):

- 本文中の `[[語句]]` を集計して、プレビュー下部に一覧表示します。
- 同じ語句が普通テキストで出現している場合もカウントします。
- 各語句について「使用回数（本文での出現回数）」と「使用メモ数（その語句を含むメモの数）」を表示します。
- 語句が未作成（同名のメモタイトルが存在しない）かどうも判定して表示します（未作成は「未作成」と表示）。
- ツールバーの `語句` ボタンで統計パネルを開閉できます。

右側コンテキストパネル:

- 右側は固定幅のコンテキストパネルに統合し、`コレクション`、`AI`、`メモ一覧`をタブで切り替えます。起動時はコレクションを表示します。メモ一覧はゴミ箱以外を作成日時の新しい順に表示し、件数バッジは表示対象の全件数です。
- AIを開いてもメモ一覧と本文の列幅は変わらず、AIだけが右側パネル内に表示されます。長いコレクションやAI回答はパネル内でスクロールします。
- 狭い画面では右側パネルがドロワーになり、閉じるボタンと同じ3つのタブで操作できます。
- PCではメモ一覧から選択しても右パネルを維持し、狭い画面では選択後にドロワーを閉じます。狭幅で自動的に閉じたパネルは、PC幅へ戻ると再表示されます。×ボタンで明示的に閉じた状態は維持します。

関連メモドロワー:

- 関連メモはコレクション／AI／メモ一覧とは独立した右端中央のボタンから開きます。本文の幅を変えないオーバーレイで、外側クリックまたはEscapeで閉じられます。
- 「このメモへのリンク」には、一意解決された `[[* メモ名]]` の参照元を独立表示します。通常の関連メモは、一意解決済みメモリンクによる本文リンク・逆リンク・共通リンクと既存の共通語句をスコア順で最大8件表示し、バックリンク欄との重複を除きます。ゴミ箱・削除済み・自分自身は対象外です。
- 選択中メモがない場合や関連メモがない場合は、ドロワー内に案内を表示します。右側コンテキストパネルより前面に配置しているため、パネル表示中も関連メモボタンを利用できます。
- z-indexはCSS変数で一元管理し、モバイルの右側コンテキストパネルより関連メモの背景・ドロワー・ボタンを前面に配置しています。ドロワー表示中は背景オーバーレイが背面操作を遮断します。
- 関連メモボタンは右端の上側（40vh）に、AIロボットボタンは右端の下側（セーフエリア込み）に常時固定し、どちらも関連ドロワーやAIパネルを開いても操作できます。

備考:

- 関連メモの判定やスコアリングは現時点では簡素化しています。将来的にメモが増えて煩雑になったら重み調整や表記ゆれ対応を行ってください。
- 実装ファイル: `app.js`, `index.html`, `style.css`, `LOG.md`

## Web Clipperの取得結果とフォールバック

Web Clipperは、表示中のHTTP/HTTPSページからタイトル、URL、サイト名、description、選択範囲、記事本文、画像候補、取得日時、利用者の追記メモを取得します。ページ全文は`article`、`main`、`role=main`と十分な本文量を持つ候補を優先し、通常抽出で本文が得られない場合は利用者の選択文、JSON-LDの`articleBody`、Open Graphまたは通常のdescriptionの順に確認します。それでも本文がなければ、タイトルやURLなどのメタデータだけを保存候補として確認画面へ出します。クリップ方式を「リンクのみ」へ黙って変更することはありません。

結果は「成功」「部分成功」「失敗」に分かれます。本文抽出失敗や画像の一部失敗でも保存可能な情報があれば部分成功となり、確認画面に理由を表示してから通常メモの保存経路を使います。入力URL、選択文、追記メモは再試行中や保存失敗後も保持します。ログイン必須、JavaScriptで後から生成される未表示内容、bot対策、アクセス拒否、タイムアウト、空応答、画像URLの失効などでは取得できない場合があります。ログイン状態やサイト側のアクセス制限を回避する処理は行いません。

診断情報は処理段階、分類コード、HTTP状態（取得元が提供できる場合）、タイムアウト、本文・メタデータの有無、画像成功・失敗数、フォールバックと最終結果を構造化して保持します。本文、選択文、Cookie、認証情報、URLのqueryやfragmentは診断ログへ保存しません。現在の本文入力元はブラウザ拡張の表示済みDOMです。将来別のブラウザ拡張や取得アダプターを追加する場合は、`extensions/web-clipper/clip-result.js`へ抽出済み本文・メタデータ・画像結果・分類済みissueを渡す接続を利用します。

ページ全文と画像付きクリップは、拡張ストレージへ10分TTLの一時レコードを置き、content scriptの起動通知に対して本体が同じ転送IDで受信準備完了を返してからpayloadを送ります。本体が項目別に検証してACKを返し、content scriptが一致するACKを受け取った時だけ対象レコードを削除します。ACK待ちや一時エラーでは削除せず、転送IDをタブの`sessionStorage`へ保持するため再読み込み後もTTL内なら再開します。0.3.8のpayloadは正本の`record`と旧本体用の互換`clip`を併記し、旧本体の`content-ready`は拡張自身が`attempt`付きで送る通知と区別してから送信を開始します。新本体は0.3.7の`clip`単体payloadも同じ項目基準で検証し、ACK送信時点で旧方式の受信成功として保存を許可しつつ更新を推奨します。未知形式は`extension_update_required`として案内します。失敗画面では原因コード、再受信、元ページからの再実行、リンクのみの代替、本文を含まない診断コピーを利用できます。空の受信失敗状態は通常の手動Webクリップと区別され、保存できません。

`web-clipper-config.js`ではWeb Clipper全体の最低互換版0.3.0と転送プロトコルv2／推奨拡張版0.3.8を別項目で管理します。旧来のextension-origin直送は明示的な許可一覧に限定します。現在の同一window転送はこの一覧ではなく、同一origin、タブが保持するUUID、検証済みpayloadの一致で制限します。実機の展開読み込み版0.3.8のIDは`aelacnladkiohkhbjhfbmekpbfgpcmlh`です。Memo-Nexus由来を履歴で確認できた旧IDだけを維持し、無関係なEdgeアドオン版Web ClipperのIDは削除しています。
